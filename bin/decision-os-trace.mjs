#!/usr/bin/env node
/**
 * WHAT: Provides the Decision OS configured alias for the generalized trace-evidence CLI.
 * WHY: Repository operators need a native name while other repositories reuse the core command.
 */
import './trace-evidence.mjs';
