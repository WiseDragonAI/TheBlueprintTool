{
  "name": "generator-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "generator-cli": "./dist/bin/generator-cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import tsx \"test/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "cli": "tsx bin/generator-cli.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
