Cards with structured `fields` expose an in-card tab strip with Description and Fields tabs. The description tab shows the existing comment or markdown body, and the fields tab visualizes each field name and type without replacing the card title or labels.