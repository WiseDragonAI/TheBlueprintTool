Inline markdown code spans rendered inside a card description use the card inherited zone hue by defaulting to cyan when no zone is inherited. Runtime rendering clamps the color brightness upward so dark zone colors remain readable while preserving the original hue and saturation.