Dragging inside a regular zone color input must update the visible zone color continuously without committing or re-rendering the ledger DOM on each input event. The final change event commits one persisted color update.