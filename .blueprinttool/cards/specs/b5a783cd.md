The UI shows voice capture, uploading, transcribing, retrying, success, and failed/unconfigured lifecycle status by patching the already-rendered note immediately. If upload succeeds but transcription fails or is unconfigured, status must say the voice was uploaded, keep the ledger note visible, and expose retry from the preserved `voiceFileRef`.