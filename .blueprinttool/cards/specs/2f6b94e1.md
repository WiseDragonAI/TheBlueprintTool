Voice transcription configuration remains workspace-local in `.blueprinttool/.settings.json` with documented key aliases. The waveform renders the accumulated recording envelope and uses 95% of the graph height for peak amplitude so live capture reflects the actual recording instead of a half-height graph.