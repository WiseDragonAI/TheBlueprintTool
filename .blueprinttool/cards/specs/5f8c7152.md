Threads preserve operator and agent history in their Markdown sidecar order. `# OPERATOR` covers typed operator notes and any voice transcription lifecycle note; voice is not a separate thread role. `# AGENT` covers assistant answers.

When a thread file is edited directly, the server parses the file back into runtime notes without requiring the agent to patch ledger JSON arrays.
