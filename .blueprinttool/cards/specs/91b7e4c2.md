Multiple standalone card images next to each other, including image-only lines separated by blank lines, are grouped into one carousel media block. Carousel navigation is index-based and wraps infinitely: next from the last image returns to the first image, and previous from the first image returns to the last image.