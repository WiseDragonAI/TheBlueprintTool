Dragging the naked canvas to pan must update only viewport x/y and the content transform on pointermove. Pan must not rerun scale-driven detail-mode work, card measuring, relationship routing, or full canvas rendering while the pointer is moving. Performance telemetry for pan records the first frame, slow frames, and periodic samples so diagnostics are available without logging every move.