Switching a card to its Fields tab keeps the tab frame height measured from the Description tab. Field content scrolls inside the card frame and wheel events over the tab frame are captured by the card content instead of zooming or panning the canvas.