Card tab frame height is derived from the rendered Description content children, not from the stretched absolute panel box. Switching to Fields must not feed field content or a previous frame height back into the card's outer height; Fields stays inside the preserved description-height scroll frame.