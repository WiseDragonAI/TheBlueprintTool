Normal zone selection must replace the current selection with the zone and intersecting cards. Intersection detection must run from active ledger coordinates and sizes, not DOM geometry, and selection must update selection chrome without a full ledger surface rerender.