External card and thread content refreshes must preserve live interaction state when possible. If a sidecar Markdown edit arrives while voice recording is active, the browser must defer the ledger content refresh until recording stops, then flush the pending refresh before transcription upload continues.

The refresh must not clear the selected thread, interrupt capture, cancel the voice note, or change the active ledger tab.
