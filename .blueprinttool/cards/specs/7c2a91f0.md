Card descriptions and field-tab markdown support standard image syntax. Standalone image-only lines render through the managed media component instead of raw text or broken image placeholders. Inline images also render through the shared inline parser while preserving markdown text around them.