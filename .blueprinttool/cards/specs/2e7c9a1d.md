Floating card tab controls must show visibly different base, hover, active, and active-hover states at card scale. Hover and active states use direct card zone hue for button borders, fills, and glows instead of pale readable-color mixes. The floating tab group backdrop shadow is dark only, never zone colored, so controls remain legible over card text.