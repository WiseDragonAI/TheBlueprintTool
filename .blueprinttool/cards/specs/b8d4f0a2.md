CoreV2 imports the DroidFleet voice composer control dock class contract for voice notes: `.agent-chat.phone`, `.control-dock`, terminal STOP/SEND buttons, waveform SVG, and vertical meter. The dock is wired to real voice state, renders no fake attachment chips, stays in the right-side terminal panel, and spans the panel width.