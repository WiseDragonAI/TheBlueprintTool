Runtime card rendering follows the CoreV2 one-file-per-function canon. `patchLedgerCard` composes imported component/helper functions instead of declaring tab, field, label, markdown, or title helper functions inside the same source file.