Low-detail card titles that use viewport counter-scaling must lay out in a pre-transform width equal to the card width multiplied by the current viewport scale. After the inverse transform is applied, the visible title wraps within the card instead of overflowing its card box. The title box uses border-box sizing and a small viewport-scaled inset so the visible title has stable padding at zoom-out.