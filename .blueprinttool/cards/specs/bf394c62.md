The dark honeycomb grid is canvas-world material. It must live inside the transformed canvas content layer so wheel zoom changes the visible tile scale and panning/zooming moves it with cards, zones, groups, and relationship overlays without a second viewport-locked coordinate system.