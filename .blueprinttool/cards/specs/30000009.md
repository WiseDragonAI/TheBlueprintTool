Cards, zones, and relationship overlays must not render high-detail text, controls, shadows, or labels when the viewport is zoomed far enough that those details are not legible. The low-detail state must be derived from viewport scale, not from a separate persisted mode.