Floating card tab base, hover, active, and active-hover colors must be driven by the card owning zone color resolved from ledger coordinates before DOM measurement. CSS using `--card-zone-color` is not sufficient unless the card receives a deterministic zone id and zone-color value before tab controls paint; the DOM overlap pass may refresh the current color of that owning zone and remains only a fallback for non-ledger cards.