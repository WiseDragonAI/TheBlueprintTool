diff --git a/.decision-os b/.decision-os
deleted file mode 160000
index d6add5cc..00000000
--- a/.decision-os
+++ /dev/null
@@ -1 +0,0 @@
-Subproject commit d6add5ccdd2ef9e3d6cf20214ce0614fe5711af4
* Unmerged path .decision-os/cards/tasks/card-ce629f8b-db81-4af6-9a7b-9fdf88615919.md
diff --git a/.decision-os/cards/tasks/card-codex-skill-1785490716749-55d3bc13.md b/.decision-os/cards/tasks/card-codex-skill-1785490716749-55d3bc13.md
new file mode 100644
index 00000000..f4237496
--- /dev/null
+++ b/.decision-os/cards/tasks/card-codex-skill-1785490716749-55d3bc13.md
@@ -0,0 +1,21 @@
+## A. Gate Assessment
+
+1. Both scoped **typechecks** `pass`.
+2. The complete **frontend suite** `passes` `620/620`, and the complete **backend suite** `passes` `667/667`.
+3. The complete **changed hunk inventory** `is reviewed`; generated vendors are unchanged, the exact history migration is admitted, and unrelated canary artifacts are excluded.
+4. One **proof cleanup defect** `is corrected`: the functional browser fixture now deletes its disposable Git branch after restoring authority.
+
+---
+
+## B. Controller Decision
+
+1. **Execution** `continues` with focused commit construction, merge into current `dev`, push, and canary restoration.
+2. **Main integration** `remains prohibited`, and the **master task** `remains` `todo`.
+
+---
+
+## C. Next Result Contract
+
+1. The next boundary must `commit` only reviewed remediation paths and the task decision record.
+2. It must `merge` onto the current `dev` lineage without overwriting unrelated dirty state, `push` the resulting merge, and `restore` the registered canary to the canonical Dev command.
+3. It must `leave` the operator a clean exact-route QA target and keep the master task active.
* Unmerged path .decision-os/threads/tasks/thread-card-ce629f8b-db81-4af6-9a7b-9fdf88615919.md
* Unmerged path .decision-os~HEAD
