{
  "name": "ledger-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "ledger-cli": "./dist/bin/ledger-cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "cli": "tsx bin/ledger-cli.ts",
    "test": "node --test --import tsx \"test/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
