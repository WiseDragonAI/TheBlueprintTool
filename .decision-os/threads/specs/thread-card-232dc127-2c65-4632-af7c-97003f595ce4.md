# OPERATOR
<!-- decision-os:note {"id":"note-1784181668689-d5e65dfeab1dc8","timestamp":"2026-07-16T06:01:09.015Z","voiceFileRef":"/data/data/com.termux/files/home/decision-os/.decision-os/voice-uploads/voice-1784181668998-3c184287-ecfd-4c2c-acaf-20476b88317b.wav","status":"transcribed","transcriptionStartedAt":"2026-07-16T06:01:09.024Z"} -->

Pour l'instant, d'après ce que je comprends, on a mis tous les statuts des cartes dans le Markdown, alors qu'en vrai, je me demande si ça n'avait pas du sens de plutôt le mettre dans le JSON et avoir ce qu'il faut sur le LedgerCli pour pouvoir changer les statuts des cartes. Puisque en fait, de toute façon, on est obligé de faire des calls à LedgerCli et ça permettrait d'avoir une fiabilité un peu plus importante. Donc fait une étude de comment les mastertasks fonctionnent aujourd'hui et comment les agents font les calls. Donc tu analyses les logs des cinq six dernières sessions pour voir comment est-ce que les agents travaillent, voir les instructions et voir ce qu'on pourrait faire pour simplifier le travail des agents pour qu'ils puissent avoir moins de commandes à émettre tout en ayant une fiabilité plus importante et sans modifier drastiquement le data model.

# AGENT
<!-- decision-os:note {"id":"note-agent-1784182223727-5a0a858c","timestamp":"2026-07-16T06:10:23.728Z"} -->

## A. Study result

1. **The proposed direction is correct, and most of the required JSON model already exists.** Subtask completion is already authoritative in `cards[].status`; the Control Room already derives master waiting, active, and complete state from card status plus live run state.
2. **The reliability defect is the remaining Markdown duplicate.** `master-task-apply` writes `— Status: pending`, `done` changes only JSON, and `master-task-gate` rejects the resulting mismatch even though the launch instruction exposes no projection-sync command.
3. **The smallest correction is to keep JSON status authoritative, use existing `subtask` relationships for membership, and stop generating or gating on Markdown status suffixes.** Keep `#master-task`, canonical links, timestamps, and queue rank as metadata during compatibility migration.

---

## B. Session evidence

1. **Six latest prior runs:** `94` completed shell calls, `65` `ledger-cli` invocations, and `5` failed shell calls.
2. **Representative seven-subtask study:** `9` content mutations, `7` status writes, `2` validations, `3` gates, and `1` answer produced `22` lifecycle/write calls.
3. **Selected CLI change:** add one atomic `master-task-progress --plan-stdin` transaction covering structured card updates, verified subtask statuses, the thread reply, rollback, and the returned post-write gate.
4. **Expected reduction:** the representative `22` lifecycle/write calls become `1`; total CLI calls in that run fall from `28` to `7` when unrelated reads remain unchanged.

---

## C. Ledger state

1. **Research subtasks:** all three are `done`.
2. **Gate:** `ready: true`; no discrepancies or stale projections.
3. **Master:** remains active; completion was not requested.

# OPERATOR
<!-- decision-os:note {"id":"note-1784183887750-edc732caa0bc18","timestamp":"2026-07-16T06:38:07.909Z","voiceFileRef":"/data/data/com.termux/files/home/decision-os/.decision-os/voice-uploads/voice-1784183887900-ead36f6e-331d-4eec-801a-8a2f5cce5e52.wav","status":"transcribed","transcriptionStartedAt":"2026-07-16T06:38:07.918Z"} -->

On a aussi un système de tag qui permettrait d'ajouter un tag master ou un tag subtask sur les cartes. C'est pas dans le markdown, de mémoire, normalement, c'est dans le JSON. Et donc en fait, on pourrait réutiliser ça. Je ne sais pas pourquoi t'en as pas parlé.

# AGENT
<!-- decision-os:note {"id":"note-agent-1784184125501-42544255","timestamp":"2026-07-16T06:42:05.503Z"} -->

## A. Correction

1. **You are right: I omitted an existing JSON primitive that materially improves the design.** Cards already persist `labels`, and `ledger-cli mutate --card-labels` already writes them.
2. **Why I missed it:** I scoped the first pass around status authority and parent-child membership, then incorrectly retained `#master-task` as harmless Markdown classification without auditing the card-label contract. The current implementation explains the split but does not justify the omission: master-task code parses Markdown, while the existing label specification currently describes labels as decorative metadata.
3. **Canonical field:** the JSON field is `labels`; the frontend accepts `tags` only as a compatibility fallback.

---

## B. Corrected model

1. **Classification:** `cards[].labels` contains `master-task` on the master and `subtask` on every child.
2. **Completion:** `cards[].status` is the only persisted status source.
3. **Membership:** existing `relationships[label=subtask]` remains authoritative because a `subtask` label does not identify which master owns the card.
4. **Markdown:** remove `#master-task`, `#task-*`, and `— Status:` from machine-readable authority; keep legacy parsing only during migration.
5. **CLI:** make `master-task-apply` write the labels and make the proposed atomic `master-task-progress` validate and update labels, statuses, relationships, content, and the thread in one transaction.

---

## C. Recorded evidence

1. **Study updated:** the master study and CLI-contract subtask now include the label model, invariants, migration sequence, and acceptance criteria.
2. **Ledger updated:** the current study card has `labels: ["master-task"]`; all three research cards have `labels: ["subtask"]`.
3. **Compatibility preserved:** the current Markdown markers remain temporarily because today's Control Room and gate still require them.
4. **Gate:** `ready: true`, with no discrepancies or stale projections.
5. **Master:** remains active; completion was not requested.
