# OPERATOR
<!-- decision-os:note {"id":"note-1783875966132-1d21e57dc8f35","timestamp":"2026-07-12T17:06:06.203Z","voiceFileRef":"/data/data/com.termux/files/home/decision-os/.decision-os/voice-uploads/voice-1783875966190-78128600-dc4e-4e8e-905c-011db89f0b0c.wav","status":"transcribed","transcriptionStartedAt":"2026-07-12T17:06:39.226Z"} -->

Il y a un problème sur la liste des tâches, il y a écrit le nombre de temps que c'est en attente, depuis combien de temps c'est en attente, sauf que c'est faux, puisque ça ne prend pas en compte la dernière réponse de l'agent. Ça prend en compte, j'ai l'impression, le temps de création de la tâche. Alors que si la tâche elle a été traitée depuis, mais c'est juste que ce n'est pas terminé parce qu'il y a encore une décision de l'opérateur qui est demandée, et bah ce temps du coup il est faux. Et donc il faut trouver la bonne formule et le bon parsing de data pour montrer le temps correct.

# AGENT
<!-- decision-os:note {"id":"note-agent-1783876175875-257cce59","timestamp":"2026-07-12T17:09:35.857Z"} -->

Implemented the waiting-time correction in the mobile Control Room.

The queue now derives `waitingSince` and FIFO order from the newest valid timestamp in the hydrated task thread. This includes the latest agent reply and any newer operator message. Tasks without a valid timestamped thread still use the card Markdown `Waiting since` value.

Verification: all `43` mobile tests pass, the master-task validator passes, and the running operator route returns HTTP `200`. The change is committed as `ca9b856` and merged into `main` as `64300b9`. The card remains `todo` with `#task-waiting`; it was not marked complete without operator authorization.
