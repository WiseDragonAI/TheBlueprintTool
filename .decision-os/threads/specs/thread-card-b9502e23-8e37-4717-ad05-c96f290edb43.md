# OPERATOR
<!-- decision-os:note {"id":"note-1784103531111-f4aab1a2af6238","timestamp":"2026-07-15T08:18:51.232Z","voiceFileRef":"/data/data/com.termux/files/home/decision-os/.decision-os/voice-uploads/voice-1784103531221-d61e67f4-3db7-4a85-bbb8-bafffdbd4a93.wav","status":"transcribed","transcriptionStartedAt":"2026-07-15T08:18:51.239Z"} -->

On a traité beaucoup de cartes pour recevoir les leçons. Ce que je veux, c'est que tu regardes la memory, SQLite, et que tu analyses toutes les leçons qu'on a pu retenir et que tu me dises les trends dès que tu me passes à la suite.

# AGENT
<!-- decision-os:note {"id":"note-agent-1784103733115-fd64ca41","timestamp":"2026-07-15T08:22:13.119Z"} -->

I analyzed all `30` lessons stored for project `ZGVjaXNpb24tb3M` in the catalog memory SQLite database. `PRAGMA quick_check` returned `ok`; the corpus contains `26` code lessons and `4` copywriting lessons, with no sampling limit applied.

The ranked trends are:

1. **State, lifecycle, and ownership consistency — 11 lessons (`36.7%`).** The largest recurring failure is incomplete propagation between authoritative workflow state and UI hydration, polling, routing context, generated cards, events, timers, and master-task projections.
2. **Verification at the real execution boundary — 7 lessons (`23.3%`).** False confidence repeatedly came from testing the wrong checkout, aliases, runtime layer, backing value, stale process state, or static DOM instead of the operator-visible gesture.
3. **Preserve authoritative boundaries and complete contracts — 7 lessons (`23.3%`).** Successful corrections preserve transaction ownership, file modes, persisted object state, semantic control colors, validated component boundaries, reference styling, and usable defaults.
4. **Operator intent and governance are executable requirements — 5 lessons (`16.7%`).** Approval timing, task hierarchy, concise instructions, skill dependencies, and closeout authorization materially determine whether work is valid and usable.

**Cross-cutting conclusion:** the dominant problem is incomplete end-to-end propagation, not isolated algorithm defects. For subsequent work, the strongest default is to map `authority → mutation → consumers → rendered state → persistence → terminal event`, then require evidence at every link before claiming success.

The percentages are a mutually exclusive primary-theme classification of this project-specific corpus collected from July 12–15, 2026. They measure representation in stored lessons, not production incident frequency or business impact.

The corpus inventory and full title-level synthesis are recorded in the two linked subtask cards. Both subtasks are `done`; the master gate is `ready` with no discrepancies. The master remains active because no master-task completion authorization was given.

# OPERATOR
<!-- decision-os:note {"id":"note-1784104941546-6cdc17d72be47","timestamp":"2026-07-15T08:42:21.767Z","voiceFileRef":"/data/data/com.termux/files/home/decision-os/.decision-os/voice-uploads/voice-1784104941750-fe161a4e-9f02-4e73-a203-185c55ed1e61.wav","status":"transcribed","transcriptionStartedAt":"2026-07-15T08:42:21.780Z"} -->

C'est de la merde ton résumé, je te demande pas des pourcentages, je te demande de m'expliquer quelles ont été les leçons principales dans leur essence, dans leur canton, dans leur contenu, pas... Je veux pas des stats, je veux une analyse.

# AGENT
<!-- decision-os:note {"id":"note-agent-1784105182579-3e0d679d","timestamp":"2026-07-15T08:46:22.580Z"} -->

You are right: the previous response classified the corpus instead of analyzing what the lessons mean.

The main lesson is that **correctness in Decision OS is continuity of meaning across boundaries**. Most failures began when one concept acquired different meanings in different places. A run belonged technically to a generated card while the operator watched the source card. A timer followed a restarted wrapper instead of the logical run. A direct card route rendered before its zone context existed. A backend settlement changed state without publishing the event that lets the canvas see it. These were not separate categories of bugs; they were breaks in the chain that carries one authoritative fact through the system.

A second lesson is that **the nearest value is not necessarily the authority**. The project color is a default for creating a zone, while the persisted zone color owns that existing zone. `delayed` is durable workflow state, while `processing` is derived from a live run. The logical run owns elapsed time, while a process segment can die and resume. The selected source card owns the operator-facing projection, even when pipeline mechanics use generated cards. The work repeatedly improves when we first decide what owns the concept, then make every other representation an explicit projection of it.

A third lesson is that **a change is larger than its payload**. A registry creation belongs inside the same rollback-safe transaction as the first write. A copied skill includes executable modes, not only file bytes. A Markdown write is not trusted until ledger-cli reads it back. An approved artifact is identified by the exact approved content, not by the implementer’s later interpretation. The recurring mistake was to change the obvious value while losing metadata, transactional guarantees, approval identity, or recovery behavior.

A fourth lesson is that **evidence must exist at the boundary the operator actually experiences**. A form held the correct color while Brave displayed broken handles. Static inspection looked correct while the real Android gesture had not been exercised. Tests ran from a feature worktree while aliases resolved modules from the primary checkout. Fresh frontend assets were paired with stale backend code. A process reported as alive had exited before the final answer. Evidence is valid only after proving the artifact, configuration, runtime layer, route, visible state, gesture, and observation time match the claim.

A fifth lesson is that **preserving product contracts matters as much as repairing logic**. A behavior fix must preserve the validated component until replacement is authorized. A visual reference includes surface, border, shadow, icon, casing, spacing, and states, not just its accent color. Defaults must be usable, and semantic exceptions such as the FILE control’s blue identity must survive theme inheritance. The smallest correct fix repairs the responsible boundary without silently redesigning the product.

Finally, **governance is executable system behavior**. Generated result cards affect the master gate. The exact literal `Status: complete` affects projection validity. A closeout invocation should authorize the canonical atomic closeout command without invented gates. A skill is not usable until its runtime dependencies exist. Approval before installation means installed state and Git remain unchanged until approval. Task structure, authorization, and wording are therefore part of correctness, not administrative decoration.

The synthesis now recorded in the master card is: identify the authoritative concept, define its permitted mutation, enumerate every consumer, preserve the existing contracts, and verify the rendered, persisted, and terminal outcomes on the actual target. That is the common engineering doctrine behind the stored lessons.

# OPERATOR
<!-- decision-os:note {"id":"note-1784106088376-377c34791d60e8","timestamp":"2026-07-15T09:01:28.701Z","voiceFileRef":"/data/data/com.termux/files/home/decision-os/.decision-os/voice-uploads/voice-1784106088685-ab9a9bc3-a598-48e6-baa6-b76638c567b3.wav","status":"transcribed","transcriptionStartedAt":"2026-07-15T09:01:28.712Z"} -->

Ton résumé, il est toujours aussi merdique, il y a beaucoup trop de contenu. Tu ne crois quand même pas que je vais relire tous les souvenirs et que tu me fasses de la prose comme ça pour m'expliquer autant les choses. Ça ne va pas, tu ne vas pas me faire des paragraphes, des pavés que je vais devoir lire. Tu me fais des bullet points, des résumés, tu m'extrais les informations les plus importantes, les plus pertinentes et tu réfléchis comme si tu devais présenter les informations à un humain qui doit comprendre vite l'essence et le plus important.

# AGENT
<!-- decision-os:note {"id":"note-agent-1784106207498-8dc2ff71","timestamp":"2026-07-15T09:03:27.499Z"} -->

You are right. Here is the short version:

1. **One concept must have one authority.** Most bugs came from state meaning different things in the backend, persistence, and UI.
2. **A shared-state fix must cover every consumer.** Rendering, hydration, polling, cancellation, resumption, deletion, and completion must all follow the same truth.
3. **A change includes its full contract.** Payload, metadata, transaction boundary, approval identity, rollback, and recovery must survive together.
4. **Only operator-visible evidence proves UI behavior.** Verify the actual served runtime, route, interaction, persistence after reload, and terminal state.
5. **Behavior fixes must preserve validated product boundaries.** Repair the responsible transition without replacing the component or redesigning the interaction.
6. **Governance is part of correctness.** Task links, exact statuses, approvals, and completion authorization affect whether work is genuinely complete.

**Rule to retain:** `authority → mutation → every consumer → visible state → persisted state → terminal state`.

I replaced the long master-card analysis with this concise synthesis. The two supporting subtasks remain complete; the master remains active because completion was not authorized.

# OPERATOR
<!-- decision-os:note {"id":"note-1784106893688-3a65e397cbb0b8","timestamp":"2026-07-15T09:14:53.834Z","voiceFileRef":"/data/data/com.termux/files/home/decision-os/.decision-os/voice-uploads/voice-1784106893821-3641d7e1-c18b-450e-a9c0-631167335821.wav","status":"transcribed","transcriptionStartedAt":"2026-07-15T09:14:53.847Z"} -->

Pour chaque point des leçons dans le summary, il faut que tu donnes un exemple précis d'un moment où il y a eu ce genre de problème et comment on l'a résolu.

# AGENT
<!-- decision-os:note {"id":"note-agent-1784107017790-97bf02ef","timestamp":"2026-07-15T09:16:57.791Z"} -->

Updated the summary so every lesson now includes one precise incident and its resolution:

1. **One authority:** a direct card route rendered cyan instead of its persisted violet zone color. **Resolved by:** hydrating the owning zone ID and color before render.
2. **Every consumer:** source-card run projection fixed Codex Log but missed Control Room classification. **Resolved by:** updating the ownership helper, classifier, and regression fixture together.
3. **Full contract:** a skill copy preserved bytes but lost executable modes. **Resolved by:** restoring source modes and testing create, update, and rollback.
4. **Operator-visible proof:** the form held `#5d5bcf` while Brave showed zeroed picker handles. **Resolved by:** implementing the authorized JavaScript picker and verifying Android touch-drag, Cancel, and Set on the served page.
5. **Validated boundary:** the first behavior fix silently replaced the picker with Coloris. **Resolved by:** reverting it and replacing the component only after explicit operator authorization.
6. **Executable governance:** `Status: completed` and `Status: done` kept a finished master gate stale. **Resolved by:** using the exact projection `Status: complete`, which made the gate ready.

The master summary retains the short working rule and supporting evidence. Both subtasks remain complete, the gate is ready with no discrepancies, and the master remains active because completion was not authorized.
