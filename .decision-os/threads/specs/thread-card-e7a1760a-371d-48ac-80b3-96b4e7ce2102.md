# OPERATOR
<!-- decision-os:note {"id":"note-1784016558626-3518ca23391ab8","timestamp":"2026-07-14T08:09:18.965Z","voiceFileRef":"/data/data/com.termux/files/home/decision-os/.decision-os/voice-uploads/voice-1784016558906-f00c0d14-5082-484e-a7fc-5042ba720098.wav","status":"transcribed","transcriptionStartedAt":"2026-07-14T08:09:18.978Z"} -->

J'ai déjà essayé d'implémenter ce genre de gate par le passé sur un projet, c'est toujours très complexe. Je verrais plus un système un peu plus simple où en fait, un process codex, il peut lancer juste une commande et qui donne l'état de la machine et l'état de ce qui est en train de tourner. Et que dans le agents.md de décision OS, on ait des instructions très courtes et très simples sur comment lancer cette commande, comment interpréter les résultats et comment se limiter et peut-être faire des sleeps de quelques secondes pour attendre et pour relancer plus tard. Ça ne serait pas un vrai système de lock, parce que c'est trop compliqué à mettre en place. Ce serait un système un peu de fake lock où ce serait possible de temps en temps d'avoir des collisions, mais dans la plupart du temps, ça marcherait. Et de ce que j'ai pu voir, c'est que deux tests suite qui tournent, ce n'est pas un problème. Donc si on limite à une seule avec cet outil, ça fonctionnerait parce que dans le pire des cas, il y a deux tests suite qui passent. Et par contre, il faut vraiment effectivement des instructions détaillées sur comment lancer le minimum possible. Par exemple, faire des type-check sans arrêt et faire quatre type-check, je n'ai pas l'impression que ça serve vraiment à grand-chose. On pourrait en avoir un seul. Donc il faut essayer de produire des instructions simples, le plus concises possible, avec le moins de mots possible. N'hésite pas à enlever les articles, les « the », les virgules, tous les tokens inutiles. Essaye de parler en apache et ce sera beaucoup plus efficace.

# AGENT
<!-- decision-os:note {"id":"note-agent-1784016879943-75fafc93","timestamp":"2026-07-14T08:14:39.944Z"} -->

## A. Delivered

1. **Command:** Added `node bin/decision-os-workload-status.mjs` and package binary `decision-os-workload-status`.
2. **Output:** One line reports `GO` or `WAIT` heavy-process count Codex worker count available memory and swap use. Active test and typecheck processes add `pid` and RSS lines.
3. **Fake lock:** Command is read-only. `WAIT` occurs for active test/typecheck low memory below `1 GiB` or swap use at least `75%`.

---

## B. Agent Rules

1. **Admission:** Run status command before typecheck or test suite.
2. **Wait:** On `WAIT` run `sleep 5` then check again.
3. **Budget:** Focused tests first `--test-concurrency=1` one typecheck maximum one full suite maximum failed tests only after full-suite failure.

---

## C. Verification

1. **Focused tests:** `3/3` pass with concurrency `1`.
2. **Runtime:** Live command returns `GO heavy=0 codex=3 mem=2.1GiB/7.3GiB swap=2.2GiB/4.0GiB`.
3. **Delivery:** Implementation `e93632e`; merge `76dee95`; worktree removed.
4. **Ledger:** Both subtasks `done`; projections synchronized; gate `ready: true`; master remains `#task-active` pending explicit completion authorization.

# OPERATOR
<!-- decision-os:note {"id":"note-1784045009206-8da7d32fa20db8","timestamp":"2026-07-14T16:03:30.201Z","voiceFileRef":"/data/data/com.termux/files/home/decision-os/.decision-os/voice-uploads/voice-1784045010114-52677abe-ce30-46c1-ae82-af8ede5c3427.wav","status":"transcribed","transcriptionStartedAt":"2026-07-14T16:03:30.213Z"} -->

Non, j'ai jamais dit qu'il faut le test concurrent C1 sur Node, c'est une erreur de penser ça. Il n'y a pas de problème à lancer les tests en concurrence sur le mobile. Le problème, c'est quand il y a plusieurs agents qui lancent les tests en concurrence sur le mobile. En plus de ça, ce qu'il faut, c'est qu'on ait des... on ait une... on ait un meilleur runbook, en fait, avec une meilleure hygiène de test et de typecheck. L'objectif n'est pas de créer un système qui est over-engineered, c'est très important de pas faire de l'over-engineering sur ce système et de pas essayer de faire quelque chose de trop complexe. On veut faire quelque chose de simple. C'est une commande qui donne. On n'a même pas besoin du hardware de savoir quelle est la pression sur la mémoire, etc. On veut juste savoir, par rapport à décision OS, quels sont les tests qui sont en train de tourner et en fonction des résultats, sur le modèle de savoir si oui ou non, il peut lancer plus de tests ou est-ce qu'il doit faire un slip avec un retry dans X secondes. Donc, il y a deux choses à faire, il y a non seulement une CLI pour pouvoir checker l'état de ce qui est en train de tourner, et deuxièmement, il y a un runbook sur comment utiliser la CLI. Et troisièmement, il y a un autre runbook pour être un petit peu économe sur ce qu'on lance comme test et essayer d'être plus malin et d'être plus chirurgical dans notre façon de lancer les tests. Et que quand on veut lancer des tests dans leur globalité, il faut en général attendre la fin de l'implémentation une fois qu'on a déjà créé et fixé tous nos tests spécifiques à nos développements à nous.

# AGENT
<!-- decision-os:note {"id":"note-agent-1784045218750-97132217","timestamp":"2026-07-14T16:06:58.750Z"} -->

## A. Correction

1. **Removed:** Memory swap RSS Codex count and `--test-concurrency=1` policy.
2. **CLI:** `node bin/decision-os-workload-status.mjs` now inspects only Decision OS tests and typechecks across main checkout and `.worktrees`.
3. **Decision:** `GO tests=0 typechecks=0` when idle. `WAIT` plus process details when verification runs.

---

## B. Runbooks

1. **Admission:** Check before test or typecheck. On `WAIT` run `sleep 5` then retry. No lock.
2. **Hygiene:** Run smallest relevant tests during implementation. Add and fix change-specific tests first.
3. **Final checks:** Typecheck changed package once after code stabilizes. Run full suite once after implementation and focused tests pass.
4. **Concurrency:** Keep project default test concurrency.

---

## C. Evidence

1. **Focused tests:** `3/3` pass with default Node concurrency.
2. **Live CLI:** `GO tests=0 typechecks=0`.
3. **Commits:** Correction `e647f09`; merge `56b52e0`.
4. **Ledger:** Both subtasks `done`; projections synchronized; gate `ready: true`; master remains `#task-active` pending explicit completion authorization.

# OPERATOR
<!-- decision-os:note {"id":"note-1784055983581-78608c1ecfb338","timestamp":"2026-07-14T19:06:23.848Z","voiceFileRef":"/data/data/com.termux/files/home/decision-os/.decision-os/voice-uploads/voice-1784055983812-d07a68e0-13c1-42fd-b16b-a02059305948.wav","status":"transcribed","transcriptionStartedAt":"2026-07-14T19:06:23.855Z"} -->

Je veux que tu fasses les dix prochaines minutes à observer comment les tests vont être lancés. Là on a trois process codex qui tournent en concurrence, donc je veux que tu observes les quinze prochaines minutes en faisant des sleeps de quinze secondes et en regardant les process avec le CLI.

# AGENT
<!-- decision-os:note {"id":"note-agent-1784057436254-cb0dab9f","timestamp":"2026-07-14T19:30:36.255Z"} -->

## A. Observation

1. **Sampling:** `63` CLI samples from `02:06:53` through `02:29:58`; normal interval `15` seconds.
2. **Coverage limitation:** Two session continuations interrupted continuous sampling. Added `4.5` minutes to compensate. Process launches shorter than `15` seconds can remain unseen.
3. **Concurrency:** No sample showed more than one Decision OS test or typecheck. No test/typecheck overlap observed.

---

## B. Launches Seen

1. **Total:** `14` distinct PIDs: `12` tests and `2` typechecks.
2. **Worktree `codex-run-settlement`:** `10` tests and `2` backend typechecks.
3. **Worktree `control-room-logical-run-clock`:** `2` focused backend tests.
4. **Other agent:** No verification process detected from another worktree.

---

## C. Hygiene Findings

1. **Good sequence:** Focused backend tests ran first. One focused frontend test followed. Full backend suite began later.
2. **Repeated typecheck:** Backend typecheck launched twice: PIDs `32269` and `32508`.
3. **Repeated full suite:** `test/**/*.test.ts` launched twice: PIDs `1209` and `8841`.
4. **Forced concurrency:** `control-room-logical-run-clock` launched the same focused test twice with explicit `--test-concurrency=1`: PIDs `19794` and `21326`. Its local `AGENTS.md` says `Keep project default test concurrency`.
5. **Result boundary:** CLI reports running processes only. It does not expose test pass/fail results or prove agents checked CLI before launch.

---

## D. Conclusion

1. **Fake-lock outcome:** Observed verification remained serialized despite concurrent Codex agents.
2. **Remaining problem:** Verification hygiene was not followed consistently: two typechecks two full suites and two forced-concurrency focused runs.
3. **Master:** Remains `#task-active`; no completion authorization received.

# AGENT
<!-- decision-os:note {"id":"note-agent-1784094790129-510c80c0","timestamp":"2026-07-15T05:53:10.133Z"} -->

The integration is only partially correct.

1. **The runbook is available to every card-launched agent.** Both initial and continued Codex processes use the selected project root as `cwd`. Codex therefore loads that project's `AGENTS.md`, which contains the exact `Test Admission` and `Verification Hygiene` sections. The admission command, `GO`/`WAIT` behavior, focused-test ordering, one stabilized typecheck, one final suite, and default-concurrency rule are present as repository developer instructions.
2. **The short Decision OS launch prompt correctly does not duplicate that runbook.** It contains only card lifecycle mechanics and points to `AGENTS.md` only for the Markdown formatting contract. Repeating the entire verification policy there would spend tokens twice.
3. **The execution helper is not linked to the agent's initial context.** `ledger-cli execution-profile` exists, but `buildCardLaunchContext().actions` exposes only `masterTaskApply`. Neither the generated launch instructions nor the normalized context tells the agent that `execution-profile` provides the verified test and typecheck commands. Agents can therefore still inspect package files and scripts to rediscover those commands.
4. **One generated command contradicts the runbook.** `execution-profile` emits focused Node tests with `--test-concurrency=1`, while `AGENTS.md` and this card require project-default concurrency. The helper also does not return the required admission command.

The structural correction is:

1. Add `actions.executionProfile.command` to the normalized initial Decision OS context: `ledger-cli execution-profile --ledger "$DECISION_OS_LEDGER_FILE" --json`.
2. Add `commands.admission: "node bin/decision-os-workload-status.mjs"` to the execution-profile response.
3. Remove `--test-concurrency=1` from its focused-test templates so Node uses project-default concurrency.
4. Keep the detailed admission and verification policy only in `AGENTS.md`; do not copy it into the short Decision OS prompt.

Expected behavior: the system instructions continue to own verification policy once, while the injected action contract gives the agent one direct route to the exact repository commands. No prompt duplication and no source/package-script discovery are required.

# OPERATOR
<!-- decision-os:note {"id":"note-1784099618314-99e5fb611d15a8","timestamp":"2026-07-15T07:13:38.330Z"} -->

You were correct: Termux’s native process environment
  collapsed around 13:15:23, taking the Decision OS
  backend down.

  Evidence chain:

  1. At 13:15:17, one Codex worker launched the mobile
     test suite and full CLI suite concurrently.

  2. At 13:15:19, another worker launched additional
     focused backend and CLI tests.

  3. Three Codex workers were active, following three
     parallel TypeScript checks moments earlier.

  4. The backend log ends abruptly at 13:15:22.978—no
     exception, shutdown message, or graceful exit.

  5. At 13:15:27, 13:15:30, and 13:15:34, the three
     orphaned Codex workers aborted with SIGABRT: failed
     printing to stdout: Broken pipe. Those crashes were
     consequences of the backend disappearing.

  6. Your restored service tree started at 13:56:03;
     Decision OS became healthy on port 50150 at
     13:56:05.

  The strongest supported cause is resource/process
  exhaustion from overlapping Codex verification
  workloads. Current conditions still show substantial
  pressure: load average around 22, only 136 MiB
  immediately free, and 2.4 GiB of swap occupied.

  The ledger-cli skills update command happened
  immediately before the collapse, but its
  implementation contains no process-killing or restart
  behavior, so it was another concurrent workload—not a
  deliberate server restart.

  The 13:55:57 Termux “Requesting to stop service” event
  I initially cited was later recovery cleanup and
  unrelated to the original crash. The precise Android
  kernel/OOM-killer record is no longer present in the
  volatile log buffers, so resource exhaustion is
  strongly evidenced but the exact kernel killing
  mechanism cannot now be proven.

# OPERATOR
<!-- decision-os:note {"id":"note-1784099657482-f6853d588bc948","timestamp":"2026-07-15T07:14:17.599Z","voiceFileRef":"/data/data/com.termux/files/home/decision-os/.decision-os/voice-uploads/voice-1784099657584-a833670b-ddc5-4906-988b-a83338f07117.wav","status":"transcribed","transcriptionStartedAt":"2026-07-15T07:14:17.606Z","uploadReceivedAt":"2026-07-15T07:14:17.584Z","audioPersistedAt":"2026-07-15T07:14:17.591Z","acceptedAt":"2026-07-15T07:14:17.591Z","providerStartedAt":"2026-07-15T07:14:17.606Z","providerSettledAt":"2026-07-15T07:14:22.678Z","completedAt":"2026-07-15T07:14:22.690Z","revision":4} -->

On a eu encore des crashs, parce qu'il y a eu trop de choses qui ont été lancées en même temps. Et ça, c'est une analyse de Codex d'un autre agent, en fait. Donc il faut utiliser cette analyse pour voir si on peut encore améliorer notre système, puisque ces crashs récurrents, là, c'est un vrai problème.
