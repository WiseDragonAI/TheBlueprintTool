# OPERATOR
<!-- decision-os:note {"id":"note-1783968750710-f0b89b62f37648","timestamp":"2026-07-13T18:52:30.907Z","voiceFileRef":"/data/data/com.termux/files/home/decision-os/.decision-os/voice-uploads/voice-1783968750874-3419c494-c922-4c7a-91c2-fce0eddd5c93.wav","status":"transcribed","transcriptionStartedAt":"2026-07-13T18:52:30.914Z"} -->

Quand on crée un projet dans DecisionOS, on a besoin de créer un ledger par défaut, qui est en fait le ledger où on met les mastertasks. Sinon on se retrouve avec un projet où on ne peut pas créer des tâches dedans. Et là, le projet en question, c'est Search. Donc tu regardes le projet Search et tu vois comment tu peux le réparer. Et en plus de ça, tu me fais la création du ledger master ou ledger task, tasks avec un S. Oui voilà, c'est ça, tu l'appelles tasks et quand on crée un projet depuis l'interface web mobile.

# AGENT
<!-- decision-os:note {"id":"note-agent-1783969179158-fc3c9aa5","timestamp":"2026-07-13T18:59:39.159Z"} -->

## A. Outcome

1. **New projects:** Web project creation now initializes the canonical `tasks` ledger through the existing linked-ledger lifecycle.
2. **Search repair:** The `Search` project now registers `tasks`, contains a valid empty `tasks.json`, and includes its ledger-overview card.
3. **Delivery:** Commit `f1fc3f8` was merged by `b26b10c`; the feature worktree and branch were removed.

---

## B. Verification

1. **Unit tests:** `6/6` project-catalog tests pass.
2. **Integration test:** The project catalog HTTP integration test passes.
3. **Typecheck:** Backend TypeScript validation passes.
4. **Live target:** `GET /decision-os/projects` returns `Search` with `tasks` at HTTP `200`.
5. **Live ledger:** `GET /p/30e99761-6971-4350-9dbf-9ebcfb057aec/decision-os/tasks` returns the valid empty ledger at HTTP `200`.

---

## C. Gate

1. **Subtasks:** Both linked subtasks are `done`.
2. **Master gate:** `ready: true` with no discrepancies.
3. **Operator authorization required:** Confirm master-task completion if you want the master card marked `done`.
