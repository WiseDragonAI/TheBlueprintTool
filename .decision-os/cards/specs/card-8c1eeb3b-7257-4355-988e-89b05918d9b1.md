## A. Objective

1. **Target:** On the existing operator-facing Decision OS route, start Process Card with a representative skill and inspect the same card's thread and Codex Log.
2. **Lifecycle:** Observe the running pipeline state, active skill output, terminal state, preserved thread content, and persisted session metadata after reload.
3. **Boundary:** Do not restart the server without explicit operator authorization.

---

## B. Completion Evidence

1. **Required:** Record the route, HTTP result, browser input sequence, observed log transitions, preserved thread state, and reload result.
2. **Current gate:** Merge `bd60556` is not loaded by the running server because no restart was authorized.