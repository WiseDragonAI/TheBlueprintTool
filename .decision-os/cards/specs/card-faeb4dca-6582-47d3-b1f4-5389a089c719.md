## A. Trace Result

1. **First incorrect transition:** the event-driven refresh path called `renderControlRoom()` during an active `forceFallback` gesture, destroyed Sortable, and replaced the list before its body-level fallback node had settled.
2. **Observed chain:** pointer start created three canonical list rows plus one body fallback; the server event reached the `80ms` refresh callback; queue replacement detached Sortable's owned node from the canonical list lifecycle.
3. **Additional persistence evidence:** concurrent rank mutations did not produce a durable complete rank sequence, and ETag-backed rejection reloads could retain optimistic local state.

---

## B. Exercised Paths

1. Normal pointer drop.
2. Pointer cancellation.
3. Forced persistence rejection.
4. Live ledger mutation and Control Room event during the active gesture.
5. Fresh reload after successful persistence.
