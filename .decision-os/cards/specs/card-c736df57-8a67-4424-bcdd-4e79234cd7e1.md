## A. Automated Coverage

1. Extend `frontend/test/unit/responsive/helper/project-filter-chip.test.ts` with equal-fingerprint, empty-fingerprint, local-canonical, membership, and ledger-deduplication cases.
2. Extend `frontend/test/runtime/control-room-project-filter-chip.integration.test.ts` to guard grouped chip rendering and group-aware selection.
3. Run focused checks through `node bin/decision-os-verify.mjs -- <command>`.

---

## B. Served Scenario

1. Open the served Control Room route `/` with workstation and mobile `decision-os` projects connected.
2. Confirm exactly one `decision-os` repository chip is visible.
3. Select it and confirm tasks owned by both terminals remain visible.
4. Confirm each task still names its terminal and opens its original project-scoped route.

---

## C. Completion

1. Focused automated checks pass.
2. The served interaction confirms one repository chip and multi-terminal task membership.
3. The verification record includes the route, HTTP result, visible chip count, and observed task owners.
