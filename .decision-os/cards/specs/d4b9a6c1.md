Wheel events over command chrome such as todo status buttons, edit buttons, delete buttons, color/action controls, and other non-scroll command affordances must fall through to canvas navigation. Scrolling over those controls must keep the same behavior as scrolling over the canvas: normal wheel zooms around the cursor, and Ctrl-wheel pans the viewport. Only true text/editing surfaces and scrollable content panels with real overflow may capture wheel input.
