## A. Delivered

1. **Skills page:** Loads the local server skill catalog once and exposes `Resynchronize`.
2. **Pipelines page:** Loads the local server pipeline catalog once and exposes `Resynchronize` beside pipeline creation.
3. **Process Card:** Desktop processing exposes the same recovery action across its Skills and Pipelines tabs.
4. **Feedback:** The control disables while active, states `Synchronizing skills, then pipelines…`, reloads local data after success, and surfaces server failures without claiming success.
5. **Routing:** The synchronization request remains global when the modal is opened from a project-scoped route.

---

## B. Evidence

1. **Responsive:** `frontend/index.html` and `frontend/src/app/responsive/codex.js`.
2. **Desktop:** `render-card-process-modal.ts`, `render-pipelines-modal.ts`, and `request-federated-library-synchronization.ts`.
3. **Tests:** `33/33` focused frontend tests pass.
