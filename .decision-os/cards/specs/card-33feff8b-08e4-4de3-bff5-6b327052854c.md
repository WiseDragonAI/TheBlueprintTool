## A. Scope

1. **Surface:** Open the affected thread modal on `http://127.0.0.1:50150/specs` in mobile Brave.
2. **Contradicted result:** `TEXT` opens the textarea, then the interface exposes no action that returns to the collapsed dock.
3. **Evidence capture:** Attach one expanded-state screenshot with the address bar, textarea, and full bottom dock visible before another implementation edit.
4. **Implemented repair:** Commit `f994272`, merged as `976de54`, replaces the jump action with a dedicated close-text action while the textarea is expanded.

---

## B. Acceptance Criteria

1. **Visible input:** The thread textarea appears above the dock immediately after the tap.
2. **Focus:** The textarea receives focus and the mobile keyboard opens.
3. **Send control:** `TEXT` is replaced by `SEND` while the text composer is expanded.
4. **Close control:** A visible close action collapses the textarea without clearing its draft, then restores focus to `TEXT`.
5. **Reopen:** Tapping `TEXT` after closing restores the same draft.
6. **Submission:** A short text note submitted with `SEND` appears in the thread and remains after a fresh reload.

---

## C. Verification State

1. **Automated:** The mobile and shared-thread regressions pass, including open, forced close visibility, close, focus restoration, and draft preservation.
2. **Served Chromium:** The open-to-close transition passed with representative pointer input on the served mobile frontend.
3. **Mobile Brave:** Final operator verification remains pending; ledger status remains `todo`.
