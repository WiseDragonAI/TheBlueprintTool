The backend serves image assets referenced by markdown from the active workspace `.decision-os` directory through matching `/.decision-os/...` URLs. The route is constrained to image file extensions and must reject traversal outside the active workspace root.