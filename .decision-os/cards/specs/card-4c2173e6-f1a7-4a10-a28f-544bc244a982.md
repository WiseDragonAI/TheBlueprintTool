## A. Scope

1. **Objective:** Simplify the mobile card-detail header in `frontend-mobile`.

---

## B. Implementation

1. **Controls:** Place `Back`, `Process card`, and `Thread` in one equal-width row.
2. **Back label:** Render the navigation label as `← Back` while preserving its existing destination behavior.
3. **Title:** Place the card title below the controls at full width with a smaller mobile font.

---

## C. Acceptance Criteria

1. **Layout:** The three controls remain on one row at mobile width.
2. **Title:** Long card titles use the full available width below the controls.
3. **Regression:** The targeted mobile header test passes.

---

## D. Verification

1. **Status:** Complete.
2. **Test:** `node --test frontend-mobile/test/mobile-codex.test.mjs` passes `8` of `8` tests.
3. **Commit:** Implementation commit `e4bc577`; merge commit is present on `main`.
