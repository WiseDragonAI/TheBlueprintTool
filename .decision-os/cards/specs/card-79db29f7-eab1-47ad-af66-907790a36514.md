## A. Automated Coverage

1. Added equal-project grouping across terminals.
2. Added separation for distinct logical projects sharing one Git origin.
3. Added compatibility for remote peers without origin metadata.
4. Added rejection of conflicting remote origins.
5. Added membership, canonical presentation, online-state, and ledger-deduplication assertions.
6. Added renderer assertions for group-aware task and ledger filtering.

---

## B. Results

1. Focused project-filter tests: **6 passed, 0 failed**.
2. Frontend typecheck: **passed**.
3. Initial complete repository run: **passed**, including 185 backend tests and 172 browser-spec tests.
4. Later full rerun: project-filter coverage passed; unrelated queue-drag browser scenario timed out twice at its existing ordering boundary.

---

## C. Served Scenario

1. `http://127.0.0.1:50151/` returned HTTP 200.
2. Exactly one `decision-os` chip was rendered.
3. Clicking the chip retained tasks owned by Workstation and Mobile.
4. `Search` remained separate.
5. The selected result contained 36 tasks.
