## A. Implemented restoration

1. `renderControlRoom()` captures visible lists before replacement and restores replacement lists on the next animation frame.
2. Restoration clamps each saved value to the replacement list's current maximum scroll range.
3. Same-session memory remains authoritative across task refresh, project and ledger filter rerenders, task-detail return, and browser back navigation.
4. A fresh page reload creates a new module state and resets the three positions to `0`.
5. SortableJS setup and queue drag persistence were not changed.

---

## B. Served behavior

1. The browser regression preserved `queue: 280`, `exec: 330`, and `backlog: 450` after a task mutation followed by the application refresh control.
2. The same values survived the in-app task Back control.
3. The same values survived browser history Back.
4. A full reload returned all three values to `0`.
