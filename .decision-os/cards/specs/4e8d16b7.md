Managed card image frames resize by width inside the card and derive height from the loaded image natural aspect ratio. The frame must not impose a fixed max-height that crops tall content. Persisted image dimensions are stored under `card.imageSizes[src]` and measured from layout pixels with `offsetWidth` and `offsetHeight` so canvas zoom does not shrink saved values.