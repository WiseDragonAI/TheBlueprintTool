## A. Automated evidence

1. Focused suite: **49 tests passed**.
2. Complete frontend-mobile suite: **83 tests passed**.
3. RGB↔HSV round-trip preserves persisted `#5d5bcf` exactly.
4. Regression coverage includes generated defaults, hydration, dependent gradients, untouched Set, changed Set, request payload, and rejected-save reconciliation.

---

## B. Served interaction evidence

1. Project route and pinned noUiSlider asset returned HTTP `200` without restarting the server.
2. Initial state: `#5d5bcf`, `H=241`, `S=56`, `V=81`.
3. Touch-drag: Saturation changed to `35`; the draft swatch changed before form mutation.
4. Cancel preserved `#5d5bcf`; Set committed the adjusted draft.

---

## C. Operator gate

1. Open the project Color picker in Brave Android and confirm the purple color opens with all three handles hydrated away from zero.
2. Drag one handle, tap Set, tap Save, reload, and confirm the adjusted color rehydrates.
3. The card remains pending until this device path is confirmed.