## A. Current Gate

1. Focused backend and mobile regressions pass, and backend typecheck passes.
2. The live `50150` process predates the fix and still serves projector v4.
3. This subtask was incorrectly marked `done` by unauthorized closeout commit `b6e9698` and is reopened as `todo`.
4. Operator-authorized restart and live v5 diagnostic inspection remain required.
