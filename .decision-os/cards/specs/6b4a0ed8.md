The public canvas stylesheet is an import entrypoint. Tokens, shell chrome, canvas layer mechanics, canvas objects, thread inspector, dialogs, and responsive overrides live in separate CSS modules so style ownership stays decoupled and inspectable.