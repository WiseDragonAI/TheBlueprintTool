## A. Implemented Result

1. Thread launches append unique IDs to the ordered `codexThreadRunIds` list and retain `codexThreadRunId` as the newest pointer.
2. Historical run IDs remain authorized for status and event reads.
3. Deletion removes only the requested run; deleting the current run promotes the newest remaining run.

---

## B. Changed Code

1. `backend/src/business/codex/helper/project-card-codex-run.ts`
2. `backend/src/business/codex/helper/resolve-card-skill-run-ownership.ts`
3. `backend/src/business/codex/controller/delete-thread-codex-session-controller.ts`

---

## C. Verification

1. Sequential-launch, historical-read, and selective-deletion regressions pass.
2. Backend typecheck passes.
3. Changes are committed and merged into `main`.
