## A. Scope

1. **Implementation:** Persist the mobile Control Room tab and nearest task-row scroll anchor in the URL.

---

## B. Changes

1. **Route state:** Added `frontend-mobile/src/mobile-control-room-route.js` for canonical URL parsing and formatting.
2. **Tab history:** Added `history.pushState()` updates for Control Room tab selections.
3. **Scroll history:** Added animation-frame-throttled `history.replaceState()` updates for the nearest rendered task row.
4. **Restoration:** Restored the selected tab and task anchor during mobile route loading and rendering.

---

## C. Result

1. **Status:** `complete`
2. **Commit:** `d49170e`
