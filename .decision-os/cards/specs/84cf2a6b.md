When the canvas enters low-detail or overview-detail zoom, card descriptions, buttons, and hashes must stay hidden, but each card title and non-empty label chip remains visible. Titles and labels use the same viewport counter-scale rule as zone labels so they stay readable while remaining anchored to the card geometry. Low-detail titles keep a small card-relative inset so the text does not touch the card border.