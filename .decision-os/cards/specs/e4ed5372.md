**Implementation direction**

- Use browser `fetch` for reads and mutations against server endpoints.
- Send JSON payloads for ledger updates.
- Keep network IO behind explicit runtime/server boundaries.