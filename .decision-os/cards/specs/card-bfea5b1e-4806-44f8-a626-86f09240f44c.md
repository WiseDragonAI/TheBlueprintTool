## A. Implemented

1. **Route ownership:** every `loadRoute()` receives a monotonic generation, frozen route snapshot, and abort signal.
2. **Commit guards:** catalog, project state, ledger navigation, card detail, and Control Room reads recheck ownership before shared-state and DOM commits.
