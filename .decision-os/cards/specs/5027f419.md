Relationship arrows must share the same canvas-world pixel coordinate system as cards. SVG overlays must not introduce an independent viewBox scale that can detach arrows from their source or target card borders.