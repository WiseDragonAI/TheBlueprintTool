## A. Contradicted Success RCA

1. **Route:** canonical master-task card route on `http://127.0.0.1:50151`, HTTP `200`.
2. **Surface:** desktop `1440x900`, Brave 147 Chromium.
3. **Operator evidence:** the pasted screenshot showed a purple keycap and a baseline-bound left arrow.
4. **First incorrect transition:** `.back-button .terminal-button__key` replaced the shared neutral keycap foreground, background, and border with `currentColor` mixes.

---

## B. Correction

1. Removed the Back-specific `.terminal-button__key` override.
2. Preserved the shared keycap values: gray text, black background, gray border.
3. Split the left arrow into `.back-button__icon`, a centered `14px` square.
4. Kept accent color only on the arrow and `Back` label.

---

## C. Verification

1. **Focused responsive tests:** `78/78` passed.
2. **Frontend typecheck:** passed.
3. **Complete frontend suite:** `445/445` passed.
4. **Served keycap:** text `rgb(158, 168, 181)`, background `rgb(15, 19, 24)`, border `rgb(57, 64, 71)`.
5. **Alignment:** arrow and keycap vertical center offsets are both `0px`.
6. **Commits:** `cdaf7d6d`, `9901e3f4`.
