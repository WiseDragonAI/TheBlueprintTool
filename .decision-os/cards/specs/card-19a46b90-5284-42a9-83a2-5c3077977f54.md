## A. Regression Coverage

1. Added desktop split-geometry, mobile full-screen, shortcut transition, editable-target suppression, and canvas shortcut retention coverage.
2. Browser geometry waits for the inspector entrance transform to settle before measuring.
3. Android Chromium retains `--no-sandbox`, `--no-zygote`, `--single-process`, `--disable-dev-shm-usage`, and `--disable-gpu`; Linux Chromium uses multi-process mode for the two-page scenario.

---

## B. Check Results

1. `node --test` focused scope: **11 passed, 0 failed**.
2. Frontend TypeScript check: **passed**.
3. Served Chromium interaction check against port `50151`: **passed** for desktop geometry, mobile geometry, focus, voice start, voice cancel, and thread close.
4. Full frontend suite: **398 passed, 11 failed**. The remaining failures are outside the changed runtime behavior and cover stale vendored-asset assertions, one refresh test, and moved CSS-path assertions.
