## A. Responsive persistence result

1. Master-card detail resize completion writes the complete `imageSizes` map with `patch-card`.
2. The responsive card state updates before the request completes.
3. A rejected request restores the previous map and rerenders the confirmed width.

---

## B. Browser evidence

1. Success persisted `521px` and a fresh reload rehydrated `521px`.
2. A forced HTTP `500` after a second drag reconciled the visible width to `521px`.
3. The browser session intercepted persistence responses, so operator ledger data was not changed.
