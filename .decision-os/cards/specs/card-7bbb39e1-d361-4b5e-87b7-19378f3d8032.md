## A. Type

1. **Type:** `code`

---

## B. Targets

1. `frontend/src/runtime/thread/effect/render-thread-panel.ts` — `setThreadPanelTab`.
2. `frontend/src/runtime/thread/effect/render-thread-codex-log.ts` — `renderThreadCodexLog`.
3. `frontend/src/runtime/thread/effect/persist-thread-scroll.ts` — log scroll accessors.
4. `frontend/src/runtime/thread/helper/thread-follow-bottom.ts` — per-surface follow accessors.
5. `frontend/src/runtime/codex/effect/bind-thread-codex-run-log.ts` — `bindThreadCodexRunLog`.

---

## C. Action

1. Track log follow mode independently.
2. Default first open of a populated log to its latest event.
3. Keep streamed events pinned while following.
4. Disable follow and persist the offset on intentional upward scrolling.
5. Restore state across tab changes, reopen, reload, and restart.

---

## D. Done When

1. Existing populated logs open at the latest event.
2. New events stay visible while following.
3. Manual log position survives navigation and restarts without changing conversation state.

---

## E. Dependencies

1. Persist and hydrate per-thread scroll state.
