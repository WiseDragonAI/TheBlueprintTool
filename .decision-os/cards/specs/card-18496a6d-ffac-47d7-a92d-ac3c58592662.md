## A. Scope

1. **Objective:** Remove the server and ledger CLI root coupling, delete the remaining legacy selector references, and restore the home-scoped project catalog after the live restart.
2. **Server policy:** Do not restart the live server without explicit operator authorization.

---

## B. Implemented Contract

1. **Renamed scope:** Project-scoped Codex children now receive `DECISION_OS_LEDGER_ROOT`; `ledger-cli` uses it for filesystem scope enforcement.
2. **Removed ambiguity:** The obsolete `DECISION_OS_ROOT` name has no remaining source, test, or documentation reference outside managed Decision OS content.
3. **Launcher boundary:** The server launcher removes inherited `DECISION_OS_* ` variables except `DECISION_OS_FRONTEND_ROOT` before spawning the backend.
4. **Backend boundary:** Backend root resolution no longer reads a root environment variable; the launch cwd owns server scope.
5. **Canonical routing:** The legacy project cookie and header literals were removed from tests; project-owned routing remains URL-scoped under `/p/:projectId/...`.
6. **Skill ownership:** Identical server and workspace roots are deduplicated so a single-project workspace keeps editable workspace skill ownership.
7. **Documentation:** `README.md`, `MIGRATE_RUNBOOK.md`, and `documentation/specs.json` explain the server-cwd and ledger-root separation.

---

## C. Verification Evidence

1. **Static audit:** No `DECISION_OS_ROOT`, legacy project-cookie literal, or legacy project-header literal remains in repository source, tests, or documentation outside managed Decision OS content.
2. **Backend boundary:** Focused backend root, catalog, Codex environment, skill discovery, skill route, pipeline route, and thread-launch tests pass.
3. **Ledger CLI:** Typecheck passes and all `51` tests pass.
4. **Mobile Control Room:** All `37` tests pass.
5. **Launcher:** Both launcher tests pass, including inherited-scope sanitation.
6. **Backend typecheck:** Passes.
7. **Concurrent suite note:** The full concurrent backend suite produced one pipeline-completion timeout; the same pipeline test file passes in isolation.
8. **Feature commit:** `53f33cb` (`fix: isolate server and ledger roots`).
9. **Merge commit:** `d94689a` (`merge: isolate server and ledger roots`).
10. **Cleanup:** The feature worktree and merged branch were removed.

---

## D. Remaining Live Gate

1. **Restart authorization:** Explicit operator approval is required before restarting the Termux service.
2. **Catalog check:** After restart, `GET http://127.0.0.1:50150/decision-os/projects` must include the project rooted at `/data/data/com.termux/files/home/health/pink`.
3. **Surface check:** `GET http://127.0.0.1:50150/` must return `200`, and Pink must appear in the Control Room.