## A. Authoritative metadata correction

1. Added a server-owned favorite and tag metadata boundary shared by global and project skill APIs.
2. Existing local project metadata migrates deterministically by newest `updatedAt`; server-owned run defaults are preserved.
3. Metadata-only project writes target the server owner and no longer mutate project replicas. Full editable-skill writes retain their project boundary.

---

## B. Backend evidence

1. The focused route file passed `4/4`.
2. The complete backend suite passed `198/198`.
3. Backend typecheck passed.
