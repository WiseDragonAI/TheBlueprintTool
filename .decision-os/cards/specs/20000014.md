The zone tool creates a zone at the exact canvas rectangle drawn by the operator, including negative canvas coordinates after panning or zooming. New zone geometry must not clamp x or y to zero, and the created zone must use the current selected zone color.