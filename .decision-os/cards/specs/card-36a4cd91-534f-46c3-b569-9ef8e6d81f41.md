## A. Scope

1. **Drawer destinations:** Replace per-ledger drawer entries with `Control room`, `Ledgers`, `Pipelines`, and `Skill library`.
2. **Ledger navigation:** Route `Ledgers` to the workspace ledger list at `/ledgers`.
3. **Library navigation:** Open the existing pipeline library and skill library from the drawer.

---

## B. Acceptance Criteria

1. **Global navigation:** The drawer contains exactly the four requested application destinations.
2. **Header:** The standalone `P` pipeline shortcut is absent from the top bar.
3. **Libraries:** Both library destinations load their existing workspace-backed interfaces.
