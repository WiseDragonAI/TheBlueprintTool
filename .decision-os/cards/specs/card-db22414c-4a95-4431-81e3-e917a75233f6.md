#master-task #task-complete

Ledger: Specs
Waiting since: 2026-07-12T07:38:44.651Z
Active since: 2026-07-12T07:40:56.521Z
Completed at: 2026-07-12T10:40:00.000Z

## A. Scope

1. **Objective:** Render Codex `file_change` lifecycle records as compact tool calls in the Codex Log.
2. **Evidence:** The attached mobile screenshot shows duplicate `IN_PROGRESS` and `COMPLETED` cards for the same file-change item, outside the collapsed tool-call disclosure.
3. **Path contract:** Display file paths relative to the decision-os server workspace when the producer supplies an absolute path inside that workspace.

---

## B. Implementation

1. **Normalization:** Map `file_change` records to `tool_call` events while retaining the `File changes` title and file-change metric.
2. **Deduplication:** Coalesce matching started and completed lifecycle records by their Codex `itemId`.
3. **Presentation:** Place file changes in the existing compact tool-call group with the action label `Files`.

---

## C. Acceptance Criteria

1. **Single lifecycle row:** One file-change `itemId` renders once at its latest lifecycle status.
2. **Compact grouping:** File changes render inside the tool-call disclosure instead of standalone event cards.
3. **Relative paths:** Workspace-contained absolute paths render without the workspace prefix.
4. **Verification:** Targeted frontend and backend tests and both TypeScript checks pass.

---

## D. Subtasks

1. **Direct treatment:** No subtask cards were required; the scoped implementation was completed directly in this card.
