Core opens the correct surface, restores durable truth, clears transient selection by default, and renders a usable canvas. The canvas world must behave as an effectively infinite surface for panning, zooming, grid rendering, and relationship overlays; internal content layers must not crop visible world material or routes at nominal SVG/content bounds.