Completed at: 2026-07-19T06:09:59.091Z

## A. Root cause and correction

1. **Original scroll-preservation commit:** `e79167b8`.
2. **Regression-introducing interaction:** `a762e823` added a hydrating render followed by the data render. The second render could run before the first restoration frame.
3. **First incorrect transition:** replacement column nodes at `scrollTop = 0` became authoritative before restoration; detached old nodes could then emit a late zero-valued scroll event.
4. **Correction:** pending restoration blocks DOM capture, detached lists cannot update remembered state, and the restoration-frame marker clears only after saved positions are applied.
5. **Merged commits:** implementation `52711fb8`; merge `08e924f9`.

---

## B. Verification

1. **Served browser regression:** passed with two same-tick Control Room renders, then a real `todo` to `backlog` card mutation, refresh, task navigation, browser back, and reload reset.
2. **Focused frontend suite:** `51/51` passed.
3. **Frontend typecheck:** passed.
4. **Full repository suite:** frontend passed; backend stopped with environment-sensitive settings failures. Evidence: `read-decision-os-settings reads workspace settings and normalizes aliases` received the workstation OpenAI key instead of fixture literal `settings-key`. This failure is outside the Control Room files changed by this task.

---

## C. Durable lesson

1. Scroll memory must be owned by the currently mounted scroll container.
2. A deferred restoration creates a protected transition: replacement nodes and detached-node events cannot write authoritative state until restoration settles.
3. Every remount-preservation regression must exercise consecutive renders inside one animation frame and late events from removed nodes.

---

## D. Subtasks

1. [Reproduce and guard the consecutive-render scroll race](card:card-dd9ee16f-25d4-4b02-84cd-5893297ca9f4)
2. [Fix and verify Control Room scroll restoration](card:card-411fa6e2-b8b3-40c6-843b-0e2e959888b9)
