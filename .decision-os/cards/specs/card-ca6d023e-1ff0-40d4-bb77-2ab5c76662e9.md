## A. Scope

1. Add `bin/decision-os-verify.mjs` with explicit `focused`, `typecheck`, and `full` modes.
2. Resolve the primary checkout and active worktree from the script location, run `decision-os-workload-status.mjs`, and wait in five-second intervals only when it returns `WAIT`.
3. Clear inherited `TSX_TSCONFIG_PATH`, use absolute configs from the active checkout, and cap Node test concurrency at `3`.
4. Prepare package-local dependencies automatically from lockfile identity, using primary dependency links for identical locks and `npm ci --ignore-scripts` for changed locks.

---

## B. Regression contract

1. Add tests for checkout discovery, poisoned environment cleanup, matching-lock dependency links, changed-lock installation planning, admission behavior, command construction, and nonzero exit propagation.
2. Emit a machine-readable summary containing setup duration, execution duration, total duration, executed commands, selected scope, and result.