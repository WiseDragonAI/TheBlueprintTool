The production inspector must not show telemetry as the thread tab. Selecting a card, zone, or group prepares that object's thread, but it must not open the right panel. The operator opens the animated terminal panel with `A`, the Thread tool, or the Notes button.