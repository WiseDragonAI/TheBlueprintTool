#task-complete

## A. Scope

1. **Automate:** Add focused backend and frontend regression coverage based on the linked session JSONL.
2. **Inspect:** Confirm the current operator-facing project route serves successfully.

---

## B. Acceptance Criteria

1. **Backend:** Focused normalization and run-read tests pass.
2. **Frontend:** Focused event coalescing and Codex Log rendering tests pass.
3. **Route:** `http://127.0.0.1:50150/p/ZGVjaXNpb24tb3M/ledgers/specs` returns `HTTP 200`.

---

## C. Evidence

1. **Backend:** Todo-list normalization passes `1/1`; adjacent run-read regressions pass `5/5`.
2. **Frontend:** Todo decoding, lifecycle coalescing, and dedicated row rendering pass `3/3`.
3. **Typechecks:** Backend and frontend TypeScript checks pass.
4. **API:** The restarted server returns `Todo list`, `in_progress`, `completed`, readable checklist text, and ordered structured items from run `codex-skill-1783955146517-72c5eb8c`.
5. **Served behavior:** Chromium `138.0.7204.168` opened the exact linked card route with `HTTP 200`, selected `Thread`, selected `Codex Log`, and observed one coalesced todo list with heading status `completed` and five completed `✓` rows.
6. **Delivery:** Feature commit `dc195e8` is merged into `main` as `56955e1`.