## A. Implemented Lifecycle Contract

1. **Structured input:** `master-task-apply` accepts title, zone title, task-specific sections, and ID-free structured subtasks.
2. **Infrastructure ownership:** the command preserves the master lifecycle header, timestamps, and geometry while generating card IDs, relationship IDs, links, and pending projections.
3. **Atomicity:** validation completes before persistence and write failures restore every touched file.
4. **Discovery:** launch context and CLI help expose the exact input schema.
5. **Existing helpers:** `master-task-gate` and `answer --message-stdin` remain the closeout and reply contracts.

---

## B. Verification

1. **Automated:** structured planning preserves lifecycle metadata and creates the complete linked projection in one call.
2. **Suite:** all `53/53` `ledger-cli` tests pass.
3. **Merge:** implementation is included in `5adc394`.

---

## C. Remaining Gate

1. **Production evidence:** keep this card `todo` until post-restart tasks plan without source/help crawls, target recovery reads, manual identities, or direct thread patching.
