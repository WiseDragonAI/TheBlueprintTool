## A. Verification Evidence

1. **Focused regressions:** `28/28` backend execution, projection, federation, queue recovery, pipeline, and admission tests pass after the merge.
2. **Frontend scope:** all `43` Control Room assertions pass, including pending execution without queue-position hydration and legacy route canonicalization.
3. **Static checks:** backend and frontend TypeScript checks pass.
4. **Full suites:** backend passes `164/165`; its single stale runtime-entry assertion fails identically on untouched `main`. Frontend passes `411/421`; the ten remaining failures are existing stale vendor/runtime asset expectations, one existing refresh timeout, and isolated-worktree asset absence.
