Completed at: 2026-07-17T04:53:19.753Z

## A. Implemented Fix

1. **Shared resolver:** `projectScopedRequestPath` now converts persisted `.decision-os/...` references to `/.decision-os/...` before applying `/p/<project-id>`.
2. **Upload contract:** `POST /api/thread-image-upload` now emits root-relative `/.decision-os/thread-images/...` references for `imageFileRef` and generated Markdown.
3. **Compatibility:** Existing relative thread Markdown is resolved without rewriting thread history; external and already project-scoped URLs remain unchanged.
4. **Commits:** Implementation commit `ce3aa92`; merge commit `77ee09c`.

---

## B. Verification Evidence

1. **Focused frontend:** `project-request-scope.test.ts` passed `2/2` cases.
2. **Focused backend:** `decision-os-zone-mutations.integration.test.ts` passed `2/2` cases with the worktree TypeScript config.
3. **Typechecks:** Frontend and backend TypeScript checks passed.
4. **Active route:** `http://127.0.0.1:50151/p/ZGV2L0VkaXRvckJQL2RlY2lzaW9uLW9z/ledgers/specs` returned `200`.
5. **Asset delivery:** Both the legacy image and the latest operator image returned `200 image/png` from canonical `/p/<project-id>/.decision-os/thread-images/...` URLs.
6. **Served source:** The active server delivered the updated `rootRelativePath` normalization.
7. **Repository suite:** The full run stopped in the frontend suite with `11` failures from existing stale SortableJS and markup assertions. The smallest reproduced `mobile-control-room.test.mjs` scope failed `3` assertions unrelated to this image URL change.

---

## C. Remaining Desktop Gate

1. **Unavailable evidence:** No browser client was available in this run, so desktop paste input, optimistic image display, and persistence after reload were not observed.
2. **Required validation:** On the active desktop project route, paste one PNG into this thread, confirm it appears immediately, reload once, and confirm the same image remains visible.
3. **Lifecycle:** The desktop verification subtask and this master task remain open.

---

## D. Subtasks

1. [Normalize project-scoped pasted-image references](card:card-a8ea673d-d88b-436b-9e40-78151572e814)
2. [Verify desktop pasted-image rendering and persistence](card:card-6201b159-7e11-4f72-b93d-eede7b61a79b)
