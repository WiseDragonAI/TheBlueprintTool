## A. Verified Regression

1. **Uploaded asset:** The operator's PNG exists at `.decision-os/thread-images/thread-card-1d94a0d1-c8d6-4250-806d-3c4dab49c11e/paste-1784208728836-97e7a4353476.png` and decodes successfully.
2. **Persisted reference:** `POST /api/thread-image` returns markdown with a relative `.decision-os/thread-images/...` source from `backend/src/business/server/helper/create-http-server.ts`.
3. **First incorrect transition:** `projectScopedRequestPath` concatenates `/p/<project-id>` directly with that relative source, producing `/p/<project-id>.decision-os/...`.
4. **HTTP evidence:** The malformed project URL returns `404`; `/p/<project-id>/.decision-os/thread-images/...` returns `200` from the active server.
5. **Regression boundary:** Project-scoped URL handling was introduced on 2026-07-13; the upload response contract still emits the older relative asset form.

---

## B. Required Outcome

1. **Desktop rendering:** Pasted thread images load on canonical `/p/:projectId/ledgers/:ledgerId` routes.
2. **Compatibility:** Existing thread notes containing `.decision-os/thread-images/...` and newly uploaded notes resolve to the same canonical project-scoped asset URL.
3. **Preservation:** Upload persistence, thread markdown rendering, image resizing, mobile behavior, and unrelated external URLs retain their current contracts.

---

## C. Delivery Sequence

1. **First:** Normalize the asset reference contract at the shared project-scope boundary and at image creation.
2. **Then:** Exercise desktop paste, optimistic display, persisted reload, and canonical asset delivery on the served project route.

---

## D. Subtasks

1. [Normalize project-scoped pasted-image references](card:card-a8ea673d-d88b-436b-9e40-78151572e814)
2. [Verify desktop pasted-image rendering and persistence](card:card-6201b159-7e11-4f72-b93d-eede7b61a79b)
