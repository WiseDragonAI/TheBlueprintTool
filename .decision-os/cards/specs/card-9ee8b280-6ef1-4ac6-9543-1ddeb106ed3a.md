Completed at: 2026-07-16T14:20:42.001Z

## A. Corrected Root Cause

1. **Major audit failure:** I audited ledger history without first verifying the running server version against the repository version, so I misidentified the operator-reported disappearance as a ledger-deletion problem.
2. **Operator-confirmed observed cause:** the running server was stale and did not execute the JSON-authoritative master-task version. The historical stale-snapshot deletions in `280c68bc` and `995a3625` were real, but they did not establish the runtime cause of the reported UI symptom.
3. **Post-restart cause:** `master-task-progress` correctly removes legacy Markdown task projection fields, but both Control Room projectors still required the removed `Ledger:` and `Waiting since:` lines.

---

## B. Implemented Correction

1. JSON-labeled masters now derive their ledger display value from the discovered ledger entry.
2. A latest timestamped thread note now satisfies the waiting timestamp contract without a legacy Markdown header.
3. Legacy masters still use their Markdown metadata.
4. Projector identity is `control-room-v5-json-master-context`, forcing persisted pre-fix slices to rebuild after restart.

---

## C. Verification Evidence

1. Focused backend projection integration: **2 passed**.
2. Mobile Control Room suite: **45 passed**.
3. Backend typecheck: **passed**.
4. The attempted full backend suite was invalidated when its isolated worktree was removed before the still-running process completed; its output is not claimed as verification.
5. Fixes are merged in `9981942` and `12f2f84`.

---

## D. Live Runtime Gate

1. `http://127.0.0.1:50150/` returns `200`.
2. The current process started at `2026-07-16 19:25:23 +0700`, before the final fix merge, and still reports `control-room-v4-json-master-tasks` with both screenshot diagnostics.
3. The server was not restarted because the workspace contract requires explicit operator authorization.
4. The unauthorized closeout commit `b6e9698` was reversed at the task-state level: the master and live verification subtask are `todo`.
5. Required validation: restart the server once, then verify `control-room-v5-json-master-context` and zero diagnostics for `card-fe388485-7e75-4392-9117-894936bdb998` and `card-9ee8b280-6ef1-4ac6-9543-1ddeb106ed3a`.

---

## E. Subtasks

1. [Audit completion transactions for lost task state](card:card-d3f5fbfb-3f68-42aa-b4f5-7267ce6ad4cb)
2. [Restore missing master cards and relationships](card:card-13e08430-4ca8-4365-be21-285f81e906b4)
3. [Reconstruct the stale-server failure chain](card:card-f28ac3de-6019-4001-8ba0-3b7da89c63e4)
4. [Make JSON master projections metadata-authoritative](card:card-0dbe52fc-a455-4ef5-8b03-af0a250ff78c)
5. [Verify post-restart projection diagnostics](card:card-5cd21c88-608e-484c-bd01-461745681248)
