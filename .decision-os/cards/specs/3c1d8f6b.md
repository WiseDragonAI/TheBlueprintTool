The local development server serves app HTML and frontend asset routes with no-store cache headers so iterative canvas styling and runtime changes are visible after reload without stale browser module cache ambiguity.