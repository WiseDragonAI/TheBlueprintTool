## A. Verified Scope

1. **Runtime directory:** `.decision-os/runtime/` contains generated launchers, transient plans, and the verification lease lock.
2. **Temporary skill source:** `.tmp-retrospect-approved/` is a local staging copy of an already-created skill.
3. **Process queue:** `**/.decision-os/codex-process-queue.json` stores live scheduler ownership, process IDs, and absolute run paths.
4. **Migration markers:** `**/.decision-os/migrations/` contains generated completion markers.
5. **Preserved:** cards, threads, ledgers, project identity/settings, and pipeline stores remain Git-visible.

---

## B. Commits and Verification

1. **Runtime commit:** `511a173` (`chore: ignore local Decision OS runtime artifacts`).
2. **Process-state commit:** `32d76ba` (`chore: ignore Decision OS process state`).
3. **Checks:** `git check-ignore -v` matched every local artifact and did not match `.decision-os/specs.json`, project JSON, cards, threads, or pipeline stores.