## A. Verified Scope

1. **Ignored:** `.decision-os/runtime/` contains the generated `ledger-cli` launcher, a stale plan payload, and the verification lease lock.
2. **Ignored:** `.tmp-retrospect-approved/` contains a temporary source copy of the already-created retrospective skill.
3. **Preserved:** cards, threads, ledger JSON, and `.decision-os/codex-pipelines.json` remain Git-visible.

---

## B. Verification

1. **Commit:** `511a173` (`chore: ignore local Decision OS runtime artifacts`).
2. **Checks:** `git diff --check` passed and `git check-ignore -v` matched only the two new local-artifact rules.