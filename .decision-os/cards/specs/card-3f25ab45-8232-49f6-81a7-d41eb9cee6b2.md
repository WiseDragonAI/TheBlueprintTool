## A. Implementation

1. Added replicated, synchronizing, stale, blocked, and offline task indicators plus an `aria-live` task-detail skeleton.
2. Replica events and retry polling reconcile the shell in place while retained content survives recoverable failures.

---

## B. Verification

1. Responsive tests pass for accessible states, shell rendering, Back lifecycle, and reconciliation.
