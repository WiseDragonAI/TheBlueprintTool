## A. Evidence

1. The referenced run emitted `turn.completed` at JSONL line `226`.
2. The status endpoint returned `status: running` and `active: true` because the in-memory run remained authoritative.
3. The persisted queue retained the continuation as `running`, the card retained `codexActiveRunId`, and PIDs `7283`, `7313`, and `7433` remained alive.

---

## B. Root transition

1. The child process did not emit `close` after the terminal Codex event.
2. Without `close`, Decision OS did not update runtime status, remove the queue item, or invoke `onCodexRunSettled`.