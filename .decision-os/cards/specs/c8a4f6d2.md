Card-to-zone attribution and derived readable zone colors must be cached from ledger/canvas geometry instead of recomputed during pan, zoom, selection rendering, or full surface refreshes that do not change geometry. The cache key is the active ledger tab plus card and regular-zone geometry/color revision. It stores each card id's owning regular zone id, raw zone color, readable/clamped color, and fallback/no-zone state. The cache is invalidated only when a card or regular zone is created, deleted, moved, resized, recolored, or when a new ledger tab is loaded; viewport pan/zoom, low-detail mode changes, relationship redraw, selection changes, thread panel changes, and telemetry rendering must not invalidate or recompute attribution.

Refactor shape: introduce a canvas runtime `zoneAttributionCache` owned outside the DOM renderer. Build it from `state.activeLedger.cards` and regular `annotations` using ledger coordinates on ledger load/render, and update dirty entries incrementally from geometry mutation paths. `patchLedgerCard()` applies cached `data-card-zone-id`, `data-card-zone-color`, `--card-zone-color`, `--card-code-color`, and `--card-readable-color` without measuring DOM. `renderCardZoneColors()` becomes a fallback/compatibility effect for static non-ledger cards and explicit dirty-zone recalculation only; it must not scan every card against every zone during pan or zoom. Acceptance: a synthetic 15-zone x 5-card-per-zone canvas must keep plain pan pointermove transform-only, pan pointerup under 16 ms when no geometry changed, and 10-15 wheel zoom events must not trigger card-zone attribution recomputation.