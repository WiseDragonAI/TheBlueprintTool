The CLI parses each control-flow entry pseudoCode block from the MasterLedger into the matching controller GeneratedFunction body so generation, dependency discovery, validation, and tests use the ledger-authored pseudocode.