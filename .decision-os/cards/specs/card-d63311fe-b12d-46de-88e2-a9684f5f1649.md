## A. Type

1. **Type:** `code`

---

## B. Targets

1. `frontend/src/runtime/thread/effect/render-thread-panel.ts` — `renderThreadPanel`.
2. `frontend/src/runtime/thread/effect/render-thread-notes.ts` — `renderThreadNotes`.
3. `frontend/src/runtime/thread/effect/load-active-thread-slice.ts` — `loadActiveThreadSlice`.
4. `frontend/src/runtime/thread/effect/open-thread-panel.ts` — `openThreadPanel`.
5. `frontend/src/runtime/thread/effect/select-thread.ts` — `selectThread`.
6. `frontend/src/runtime/thread/effect/close-thread-panel.ts` — `closeThreadPanel`.
7. `frontend/src/runtime/thread/effect/render-thread-jump-button.ts` — `syncThreadJumpButtonVisibility`.
8. `frontend/src/runtime/thread/effect/pin-thread-feed-to-last-message.ts` — `pinThreadFeedToLastMessage`.
9. `frontend/src/app/responsive/thread.js` — `openMobileThread` and `closeMobileThread`.

---

## C. Action

1. Default a thread without saved state to follow mode and bottom placement.
2. Preserve bottom across transcription, submission, and agent-response rerenders.
3. Disable and persist follow mode on intentional upward scrolling.
4. Persist offsets during scroll, close, and thread changes.
5. Apply one state contract to desktop and mobile entry.

---

## D. Done When

1. Completed transcription and new agent replies stay visible while following.
2. An upward-scrolled conversation retains its position across close, reopen, reload, and restart.
3. Jump to bottom re-enables persistent follow mode.

---

## E. Dependencies

1. Persist and hydrate per-thread scroll state.
