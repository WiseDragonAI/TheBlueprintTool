## A. Result

1. Added a deterministic browser regression that performs two Control Room renders before the pending animation frame.
2. Extended the same test through a real card status move from Queue to Backlog and verified scroll stability after refresh and navigation.

---

## B. Evidence

1. `tests/browser/control-room/the-column-scroll-position-survives-refresh-and-navigation.spec.ts` passed on a served isolated workspace.
