## A. Scope

1. **Objective:** Remove the secondary project-path line and render the mobile new-task project picker as compact centered buttons in two columns.

---

## B. Implementation

1. **Rendering:** Append only the `strong` element populated from `project.name` in `frontend-mobile/src/mobile.js`.
2. **Regression:** Assert that the picker function does not reference `project.relativePath` or the `Project workspace` fallback.
3. **Layout:** Define two equal-width columns and center each `52px`-minimum project button on both axes in `frontend-mobile/assets/mobile.css`.

---

## C. Verification

1. **Automated tests:** All `46` tests under `frontend-mobile/test/*.test.mjs` pass with the two-column layout regression.
2. **Served asset:** `http://127.0.0.1:50150/assets/mobile.css` serves the two-column grid and centered `52px` button rules; `http://127.0.0.1:50150/?tab=queue` returns `200`.
3. **Commits:** Layout commit `c255384` is merged into `main` through merge commit `3e982f3`.
4. **Device gate:** Corrected Brave rendering is not yet observed; keep this subtask ledger-`todo`.

---

## D. Contradicted-Success RCA

1. **Target evidence:** The operator reproduced the layout at `http://127.0.0.1:50150/?tab=queue` in Brave on Android by opening `New task`; screenshot note `note-1783922853129-bf6f8da09eecb` shows three full-width, left-aligned rows.
2. **DOM state:** The served renderer creates one `.new-task-project-option` per catalog project and appends only one `strong` containing `project.name`.
3. **Request state:** Initial route loading fetches `GET /decision-os/projects`; opening `New task` calls `openNewTaskProjectModal` without a mutation request.
4. **Persisted state:** `/decision-os/projects` still returns the catalog names `home`, `decision-os`, and `pink`; no persisted project field controls picker geometry.
5. **First incorrect transition:** The DOM-to-layout transition retained `.new-task-project-list { display: grid }` without `grid-template-columns`, while `.new-task-project-option` retained `min-height: 68px` and `text-align: left`.
6. **Regression boundary:** Require two equal-width grid columns, `52px` minimum height, and horizontal plus vertical centering in the mobile CSS test.
