Relationship routing must group every visible source and target endpoint by card id and border side before routing. Endpoints in the same card-side group receive deterministic evenly spaced slots along that side instead of all using the card center.