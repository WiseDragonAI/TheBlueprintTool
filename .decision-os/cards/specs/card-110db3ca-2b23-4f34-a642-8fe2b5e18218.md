## A. A. Reproduction evidence

1. Screenshot route project ID `aGVhbHRoL3Bpbms` resolves to `/data/data/com.termux/files/home/health/pink`.

---

2. Run `codex-skill-1784211925748-5e8f0f24` was persisted on master card `card-f0b10289-06fd-4605-bf0e-65b7b21c96e9`, recorded running in the queue, and returned running by the project-scoped status API.

---

3. The empty state came from stale `canvasState.activeLedger`; the fresh card was stored only in `currentCard`.

---

## B. B. Regression boundary

1. Exercise `createTaskIntake()` through launch and the awaited `refreshThreadLedger()` call.

---

2. Assert the shared active ledger contains the returned card run fields before `renderThreadPanel()`.

---

3. Assert `bindThreadCodexRunLog()` receives the persisted run ID and the empty message is absent.
