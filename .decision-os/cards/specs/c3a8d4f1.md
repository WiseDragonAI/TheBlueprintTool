Thread notes must classify message ownership in the renderer. Operator-origin notes, including voice notes, align right inside a compact neutral grey card matching the input surface with only the left border colored by the active card or zone accent. Agent and assistant replies align left and render as plain terminal conversation text without a surrounding card.