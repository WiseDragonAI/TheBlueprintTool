#master-task #task-complete

Ledger: Specs
Waiting since: 2026-07-13T09:49:39.285Z
Queue rank: 3
Active since: 2026-07-15T06:06:44.590Z
Completed at: 2026-07-15T12:19:08.565Z

## A. Operator Problem

1. **Observed delay:** Reloading the Control Room takes approximately `3–5 seconds`.
2. **Verified cause:** The measured reload transferred approximately `17.6 MB` of hydrated ledgers, thread histories, and Codex run histories to derive roughly `42` master-task summaries.
3. **Primary boundary:** The measured performance outcome and first four implementation steps concern the Control Room. The fifth subtask retains the operator-requested audit follow-through for the same over-hydration problem on other surfaces.
4. **Canvas invariant:** Desktop canvases continue receiving every card body required to render their ledger. The fifth subtask removes unrelated thread bodies and oversized mutation responses, not canvas card content.
5. **Storage finding:** The current ledger JSON totals approximately `361 KB`; the data format is not the bottleneck. SQLite is abandoned, and ledger JSON plus Markdown remain authoritative.

---

## B. Selected Outcome

1. **Server ownership:** The server derives stable Control Room task summaries from authoritative files and runtime state. The browser renders the summaries without parsing complete ledgers.
2. **One compact response:** Global `GET /api/control-room` replaces Control Room ledger fan-out and Control Room run-history requests.
3. **Compact runtime state:** Skill runs and pipeline runs expose only the lifecycle fields required to classify Control Room tasks. Existing explicit run-log surfaces remain unchanged.
4. **Reliable freshness:** Filesystem events rebuild the affected project slice immediately; a `30-second` fingerprint reconciliation repairs missed events.
5. **Multi-project isolation:** Every discovered project owns independent watchers, revisions, dependency indexes, and task projections; the global response aggregates validated project slices.
6. **Backlog vocabulary:** Persisted `delayed` state becomes `backlog`, and backlog membership comes from ledger status.

---

## C. Delivery Order

1. **Backlog foundation:** Rename and migrate `delayed` to `backlog` before freezing the Control Room schema.
2. **Control Room runtime summary:** Add compact skill-run and pipeline-run lifecycle summaries without changing run-log transport and run-log rendering.
3. **Control Room projection:** Build the multi-project task projection, watcher ownership indexes, fingerprint lifecycle, and `GET /api/control-room`.
4. **Mobile cutover:** Render the complete mobile Control Room from the projection, remove its broad requests, and verify the operator route.
5. **Audited transport follow-through:** Scope ledger, thread, navigation, search, mutation, server-controller, watcher, and CLI reads to their consumers while preserving required desktop canvas card bodies.

---

## D. Completion Gates

1. **Payload:** A fresh Control Room reload makes one data request below `250 KB` for the measured workspace.
2. **Latency:** A valid warm projection responds below `100 ms` at the server and materially improves first render on the served mobile route.
3. **No discarded bulk state:** Control Room traffic contains no unrelated card bodies, thread bodies, run events, and run diagnostics.
4. **Behavior parity:** Queue ordering and drag persistence, active lifecycle, backlog, completion, task creation, project and ledger filters, counts, direct links, scroll anchors, subtasks, task detail actions, deletion, and diagnostics retain current behavior.
5. **Freshness:** Watcher-observed Control Room changes appear within `1 second`; deliberately missed events recover within `30 seconds`.
6. **Multi-project lifecycle:** Startup, project addition, project removal, project movement, shutdown, and duplicate resource ids preserve task isolation and release watchers correctly.
7. **Failure safety:** Invalid task sources and failed rebuilds retain the last valid immutable revision with an explicit stale state.
8. **Control Room cleanup:** Repository search finds no Control Room consumer of universal hydrated ledger reads, full skill-run history, and full pipeline-run detail.
9. **Target evidence:** The served mobile Control Room is verified with request count, response bytes, first-render timing, task interactions, persisted results after reload, and rejection reconciliation.
10. **Canvas preservation:** Served desktop verification proves the ledger canvas still renders all cards and persists canvas operations after thread hydration is removed from its transport.

---

## E. Subtasks

1. [Rename delayed workflow to backlog](card:card-dd596665-9fda-4421-9152-10e676970e17) — Status: complete
2. [Add a compact Codex run-status read path](card:card-55fd1779-4267-4c63-914f-24ccb44ead8f) — Status: complete
3. [Build the stable server projection cache](card:card-21104e8d-ab8b-41dd-ac47-a8f475b128e3) — Status: complete
4. [Migrate and verify the mobile Control Room reload](card:card-c9c354ca-59a7-4086-81d2-ef61e85104fb) — Status: complete
5. [Scope hydrated ledger transport](card:card-e9143c38-c0e1-456d-8ae6-47015de3950d) — Status: complete