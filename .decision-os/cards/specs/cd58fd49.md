Card descriptions use the shared markdown parser and renderer for paragraphs, lists, tables, fenced code, horizontal rules, strong spans, inline code, and heading blocks. `##` and `###` headings must render with visible hierarchy instead of raw hash-prefixed text or flat paragraphs.