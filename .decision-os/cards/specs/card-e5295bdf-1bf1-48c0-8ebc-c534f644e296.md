## A. End-to-end verification

1. Create a short-interval test schedule for a deterministic fixture pipeline.
2. Observe the due occurrence, queued pipeline run, generated step cards, terminal run status, and persisted occurrence-to-run link.
3. Restart only the isolated test server and verify recovery without duplicate enqueue.
4. Run focused backend and frontend tests, scoped typechecks, then the repository full suite through `bin/decision-os-verify.mjs`.

---

## B. Operational contract

1. Document that schedules fire only while a Decision OS server is running for the owning project.
2. Document cron and time-zone examples, missed-occurrence recovery, idempotency, disablement, deletion, and launch-failure visibility.
3. Record the final test commands, results, served route, and remaining device-verification boundary.
