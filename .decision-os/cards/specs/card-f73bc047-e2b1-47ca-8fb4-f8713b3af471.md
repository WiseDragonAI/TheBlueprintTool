## A. Implemented Project-Only Control

1. **Control branch:** `syncCardControls` detects `data-card-type="project"` plus `data-target-project-id` before generic actions.
2. **Single action:** The project branch renders exactly one accessible `Settings` button with a gear SVG and the explicit project ID.
3. **Removed generic editing:** Project cards omit the inline title pencil and status chrome; generic card and linked-ledger control branches are unchanged.
4. **Styling:** `frontend/assets/canvas/canvas-layer.css` owns the compact gear-and-label control boundary.

---

## B. Verification

1. **Rendered DOM:** The served project card exposed one control button, one gear SVG, and zero `.ledger-card-title-edit-button` nodes.
2. **Regression checks:** Focused canvas routing and ledger-card rendering tests passed.
