When the group tool is used, pointerdown and pointerup coordinates are converted through the canvas bounding box and viewport transform, so the created group starts at the visible cursor origin regardless of shell offsets or zoom.