#task #task-done #frontend #ux

## A. Root cause corrected

1. **The contradicted screen modeled a server-wide capacity as project-scoped, which created the unnecessary project selector.**
2. **It used a native number input instead of the existing `codex-icon` up/down selector contract.**

---

## B. Implemented result

1. `/settings` contains no project selector and no editable input.
2. A read-only numeral sits between accessible `↑` and `↓` buttons and clamps to `1`–`32`.
3. Boundary buttons initialize correctly even when the settings request fails, and settings save one server-wide value.
4. The control reuses the exact `codex-icon` surface and disabled-state styling.

---

## C. Evidence

1. Focused Settings tests passed: `3` passed, `0` failed; the full mobile suite passed: `75` passed, `0` failed.
2. Android Chromium on `http://127.0.0.1:50150/` reached `/settings` with HTTP `200` and found zero selectors and zero inputs.
3. Touch checks observed `1 → 2 → 1`, the down button disabled at `1`, and the up button disabled at `32`.
4. Computed styles matched the reference `codex-icon`: `44 × 44`, `rgb(245, 240, 232)` foreground, `rgb(27, 31, 38)` background, `1px solid rgba(255, 255, 255, 0.16)` border, and unchanged casing.
5. Fixes are merged into `main` by `2a32780` and `4ecff1a`.

---

## D. Reopened queued-launch defect

1. **Operator sequence:** while all Codex slots are occupied, launch a thread Codex run; the control briefly starts, then returns to an apparently idle launch state.
2. **Persisted evidence:** continuation `codex-continuation-1784046392077-8448a12b` remained queued from `2026-07-14T16:26:32.077Z` until `2026-07-14T16:28:15.636Z`, proving backend acceptance and later promotion.
3. **First incorrect transition:** `request-card-skill-run-status.ts` discards backend `queuePosition`; `bind-thread-codex-run-log.ts` and `sync-thread-codex-run-controls.ts` reduce `pending` to `running: false`, which re-exposes the launch controls and renders `RESUME` instead of queued state.
4. **Required correction:** preserve queue position, treat `pending` as occupied, show `Queued · position N` without elapsed time, retain cancellation, and transition to running without re-enabling launch.

---

## E. Queued-launch correction

1. The normalized run summary now preserves `pending` and `queuePosition` from the backend.
2. Queued and running sessions both hide the launch row; queued Codex Log renders `Queued · position N`, no elapsed timer, and a `CANCEL` action.
3. Promotion reuses the same run identity and changes the surface from `pending` to `running` without exposing another launch.
4. Android Chromium verified the touch launch at the canonical card route with a controlled HTTP `202`: position `2` appeared immediately and survived reload.
5. Focused queue-state tests passed; frontend typecheck passed; the full mobile suite passed `78/78`.
6. Correction commit `4f44640` is merged into `main` by `6eb67bc`.
