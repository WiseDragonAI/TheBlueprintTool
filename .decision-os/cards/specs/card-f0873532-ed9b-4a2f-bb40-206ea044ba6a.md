## A. Corrected interface

1. Replaced the two-column desktop result grid with one vertical column.
2. Matched Control Room filtering: projects first, then project-scoped labels and `Clear`.
3. Added completion timestamps to task metadata and a date-order selector defaulting to `Newest first`.
4. Preserved search, canonical task navigation, history return, and responsive styling.

---

## B. Verification

1. Focused frontend regressions passed `55/55`; frontend typecheck and all `511` frontend tests passed.
2. The operator route renders vertical geometry, project-first disclosure, and `desc` by default without page errors.
3. The focused served-browser scenario verifies dates and both order directions with the v14 backend.
