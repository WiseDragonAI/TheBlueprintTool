Zone labels stay readable at a stable screen size while the zone remains large enough to inspect. In overview-detail zoom, labels are suppressed instead of inverse-scaling across neighboring cards or zones.