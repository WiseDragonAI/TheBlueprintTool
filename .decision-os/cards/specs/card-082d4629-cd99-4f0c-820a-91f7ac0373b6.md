## A. Delivered

1. **Skills:** Snapshot export includes `SKILL.md` and every regular package file. Whole-package hashes cover paths, modes, and bodies.
2. **Pipelines:** Snapshot export includes saved definitions and exactly their referenced steps. Runs and local preferences stay local.
3. **Validation:** Import validates identities, paths, encodings, modes, size, frontmatter, revision integrity, and complete step references before replacing state.

---

## B. Evidence

1. **Tests:** `federated-library-cache.test.ts` proves referenced-asset transfer, asset-only revision changes, idempotent imports, valid pipeline references, and rejection without cache corruption.
