## A. Verified result

1. Replaced `Complete master task` with `Complete manually` and `Complete with pipeline`.
2. Manual completion retains the atomic master-task mutation.
3. Pipeline completion uses the configured pipeline against the displayed master card and navigates to Exec after admission.
4. Unconfigured and rejected admissions do not complete the task.

---

## B. Evidence

1. Served browser verification confirms the distinct requests and navigation paths.
2. Desktop controls share one row; mobile controls stack without overflow.
3. Rejected admission surfaces `Pipeline admission rejected.` with no ledger completion mutation.
