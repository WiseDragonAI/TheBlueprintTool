When a ledger first loads, card tab frames resync after browser layout, after font readiness, and when the description panel resizes. The card border must grow to contain the full Description panel so description text never overflows below the card.