## A. Persisted Run Finding

1. **Card reference:** `card-fe388485-7e75-4392-9117-894936bdb998` retains `codexThreadRunId` `codex-skill-1784019156206-af4b348e`.
2. **Terminal evidence:** The run JSONL ends with `turn.completed`; the served project-scoped status endpoint returns HTTP `200`, `status: complete`, and `active: false`.
3. **Stale display artifact:** The Markdown run summary still begins with `Status: processing`, but the status controller derives the terminal state from JSONL and does not treat the run as active.
4. **Conclusion:** Persisted active state does not block relaunch. The old run ID intentionally selects the continuation path.
