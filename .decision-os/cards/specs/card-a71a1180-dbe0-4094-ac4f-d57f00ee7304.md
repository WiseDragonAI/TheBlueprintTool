## A. Implemented Result

1. Added the `card-thread-open` desktop shell state.
2. At widths from `760px`, the task shell reserves `clamp(420px, 33vw, 620px)` and the existing inspector is fixed to that right-column width.
3. Below `760px`, the inspector retains its full-viewport geometry and body scroll lock.

---

## B. Verification

1. Served route: `http://127.0.0.1:50151/p/ZGV2L0VkaXRvckJQL2RlY2lzaW9uLW9z/ledgers/specs/zones/ungrouped/cards/card-a4c81cea-0c42-4221-9891-fe57013a4149` returned the responsive card surface.
2. At `1440×1000`, the settled inspector measured `475.1875px`, ended at viewport edge `1440`, and the task remained visible at `654.8125px` wide.
3. At `390×844`, the inspector measured exactly `390×844` at coordinate `0,0`.
4. Opening and closing the thread added and removed `card-thread-open`.
