## A. Change

1. **Type:** `code`
2. **Targets:** `frontend/index.html`, `frontend/assets/application.css`, and `frontend/src/app/responsive/thread.js`.
3. **Symbols:** `#card-view`, `.mobile-thread-inspector`, `.thread-panel.agent-chat.phone`, `openMobileThread`, and `closeMobileThread`.
4. **Action:** Add a desktop card-detail shell state that keeps the task readable in the left column and presents the unchanged thread component in the right column at `clamp(420px, 33vw, 620px)`. Preserve the existing full-screen inspector below the mobile breakpoint.

---

## B. Completion

1. **Desktop:** At `1440px`, opening Thread leaves the task visible and the inspector matches the canvas reference width.
2. **Close transition:** Closing the inspector restores the card-only layout.
3. **Mobile:** At `390px`, the inspector remains full-screen.
