## A. Corrected Lifecycle

1. Removed rank persistence from the gesture settlement critical path.
2. Reinitialize the existing SortableJS Queue and Backlog instances immediately after cleanup.
3. Queue optimistic rank snapshots sequentially, omit unchanged ranks, defer live refresh until the tail drains, and reset the tail after rejection.
4. Preserve the validated task DOM, Kanban columns, styling, touch configuration, and cross-column placement behavior.

---

## B. Delivery

1. **Feature commit:** `87f033fa`.
2. **Merge commit:** `ac4e0450`.
3. **Changed runtime:** `frontend/src/app/responsive/application.js`.
