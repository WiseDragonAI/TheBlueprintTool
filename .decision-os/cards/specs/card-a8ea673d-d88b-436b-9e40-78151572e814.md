## A. Implementation Scope

1. **Shared resolver:** Update `frontend/src/runtime/project/helper/project-request-scope.ts` so recognized relative `.decision-os/...` inputs are normalized to `/.decision-os/...` before `/p/<project-id>` is applied.
2. **Upload contract:** Update `backend/src/business/server/helper/create-http-server.ts` so pasted-image responses emit `/.decision-os/thread-images/...`, matching the canonical asset route and the existing thread-file contract.
3. **Compatibility boundary:** Continue accepting already-persisted relative image sources without rewriting thread history.

---

## B. Regression Coverage

1. **Resolver cases:** Assert relative and root-relative Decision OS image sources both resolve to `/p/<project-id>/.decision-os/...`.
2. **Upload response:** Assert `imageFileRef` and generated markdown use the root-relative canonical source.
3. **Unaffected paths:** Assert cross-origin HTTP URLs and already scoped `/p/...` URLs remain unchanged.
4. **Checks:** Run the focused frontend resolver tests, the backend zone-mutation integration test, and scoped frontend and backend typechecks through `bin/decision-os-verify.mjs`.
