## A. Objective

1. **Run** the focused thread-panel and CSS contract tests and the permitted typecheck.
2. **Confirm** the live operator route serves the corrected frontend assets without restarting the server.
3. **Compare** the rendered `RUN` control with the Voice `REC` control in mobile Chromium and exercise the launch click path.

---

## B. Acceptance

1. **Automated:** focused tests and typecheck pass.
2. **Route:** the canonical card route responds with HTTP `200`.
3. **Surface:** `RUN` and `REC` compute to the same neutral background gradient.
4. **Foreground:** the SVG chevron and uppercase `RUN` label compute green.
5. **Gesture:** the touch click reaches the existing run path without an unintended mutation during verification.