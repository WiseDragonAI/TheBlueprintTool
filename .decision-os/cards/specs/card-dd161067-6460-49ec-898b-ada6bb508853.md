## A. Implemented component

1. Added the operator-authorized app-owned HSV picker using vendored noUiSlider `15.8.1` under the MIT license.
2. Reproduced the Brave slider hierarchy with three large dependent gradients, blue house-shaped handles, and a chosen-color swatch.
3. Hydrate the persisted hex into HSV before opening; retain generated saturation `0.7` and value `0.8` for unset colors.

---

## B. State contract

1. Slider movement updates draft state and the visible swatch only.
2. Cancel leaves the project-settings form unchanged.
3. Untouched Set preserves the exact persisted hex.
4. Changed Set converts the current HSV draft to hex and updates the project-settings form for the existing Save request.

---

## C. Verification state

1. Automated and served mobile Chromium interaction checks pass.
2. The subtask remains pending until the operator confirms the gesture on Brave Android.