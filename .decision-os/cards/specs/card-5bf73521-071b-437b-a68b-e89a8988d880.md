## A. Implemented agent contract

1. **Planning:** The injected prompt directs agents to `master-task-apply`, whose IDs and task labels are automatic.
2. **Progress:** Agents are directed to one `master-task-progress` call for card content, canonical labels, verified statuses, the thread reply, and the returned gate.
3. **Authority:** The prompt names JSON status and canonical relationships as ledger truth.
4. **Closure:** The prompt preserves the explicit authorization requirement for `master-task-complete`.

---

## B. Verification and delivery

1. **Regression:** Prompt assertions, legacy-token safety, relationship diagnostics, atomic rollback, and canonical link projection are covered.
2. **Commits:** Implementation is committed and merged in `1e88f2d` and `38e9808`.
3. **Qualification:** One broad backend run exposed an unrelated timing-sensitive duplicate-launch controller test; all lifecycle-related checks pass.
