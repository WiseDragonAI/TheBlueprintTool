## A. Scope

1. **Objective:** implement automatic recovery for capacity-interrupted headless card-thread Codex sessions.

---

## B. Implementation

1. **Classifier:** `transient-codex-capacity-failure.ts` reads only the current stdout and stderr segment.
2. **Controller:** `start-thread-codex-process-controller.ts` schedules `codex exec resume` after `5,000ms` with the captured session id and original selection.
3. **Regression:** the focused controller test verifies the delay, session id, model, effort, and final run status.

---

## C. Verification

1. **Backend typecheck:** passed.
2. **Focused regression:** passed in `5,979ms`.
3. **Baseline note:** the unchanged checkout independently reproduces the existing `toolCallCount` assertion mismatch in the broader controller test.
