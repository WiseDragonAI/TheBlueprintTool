The right terminal removes standalone Create note and Delete note buttons. Text notes are written in the dock composer and committed by keyboard so the input zone occupies the same terminal footprint as voice capture. The draft only clears after the server confirms the note exists in the ledger.