Opening a card description editor measures the existing rendered description area before replacing it and applies that size to the textarea. Wheel input over the textarea scrolls the textarea instead of zooming or panning the canvas.