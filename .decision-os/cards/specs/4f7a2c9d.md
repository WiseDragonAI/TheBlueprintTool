The server transcription module uses `gpt-4o-mini-transcribe` as the default OpenAI transcription model when `OPENAI_API_KEY` is configured. Frontend voice code posts captured audio to the backend and never calls OpenAI directly.