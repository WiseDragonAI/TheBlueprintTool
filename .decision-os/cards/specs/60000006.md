Selected cards expose the same corner resize handles as zones. Dragging a card resize handle mutates canvas-space x/y/w/h geometry, reroutes connected relationships live, and persists width and height to the ledger on pointer release.