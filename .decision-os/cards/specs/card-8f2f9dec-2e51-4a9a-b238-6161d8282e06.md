## A. Implemented

1. `renderControlRoom` now renders canonical project groups instead of raw project records.
2. `filteredControlTasks` and `controlTaskCount` match every project ID in the selected group.
3. Ledger chips use the selected group's deduplicated ledger inventory.
4. Task owner text, queue ordering, and terminal-qualified navigation remain unchanged.

---

## B. Served Evidence

1. Route: `http://127.0.0.1:50151/`.
2. HTTP result: `200`.
3. Visible labels contained exactly one `decision-os` chip and one separate `Search` chip.
4. Clicking `decision-os` hid the project row and rendered 36 matching tasks.
5. Filtered task metadata contained both `Workstation` and `Mobile` owners.

---

## C. Verification

1. Focused project-filter suite passed.
2. Frontend typecheck passed.
3. The complete served click path passed in disposable headless Chromium.
