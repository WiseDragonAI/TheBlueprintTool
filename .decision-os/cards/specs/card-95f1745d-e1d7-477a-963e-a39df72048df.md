## A. Corrected result

1. The responsive Control Room master-card detail now renders the canonical `28px` resize handle through `mediaSurface: 'detail'`.
2. Interact.js `1.10.27` owns desktop pointer normalization and dispatches the shared completion event.
3. Canvas carousel behavior remains unchanged.

---

## B. Browser evidence

1. The desktop pointer landed on `BUTTON.ledger-card-media-resize-handle`.
2. A real mouse drag changed the carousel from `616.81px` to `521px`.
3. Computed handle geometry and cursor match the canvas reference component.
