## A. Verified findings

1. Terminal `project-sync` records are projected from `failed` into `task-execution` even though they are locally unlocked.
2. The `Exec` column excludes drag handling, and `Retry synchronization` can return the same durable run to `failed`.
3. Repeated summary drift came from symptom-by-symptom correction, artifact conflation, incomplete backtick application, and missing rendered-output validation.

---

## B. Decision

1. The `master-task` and `thread reply` must be validated together against their distinct complete contracts.
2. Product recovery remains **unimplemented pending operator authorization**.
