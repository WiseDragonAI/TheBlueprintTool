## A. Verified result

1. Added the `Master-task completion pipeline` selector to the shared Settings screen.
2. The selector is populated from the returned server pipeline catalog and restores the saved selection.
3. Saving submits `masterTaskCompletionPipelineId`; `Not configured` persists an empty string.

---

## B. Evidence

1. Responsive settings regressions and the complete frontend suite pass.
2. The shared responsive application serves the same Settings contract at desktop and mobile widths.
