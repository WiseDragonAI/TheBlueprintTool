## A. Implemented Boundary

1. **Shared projection:** Backend and desktop Codex Log use the card's current run projection.
2. **Control Room correction:** Mobile Control Room now reads `codexActiveRunId` before the legacy thread and card run fields.
3. **Classification:** The resolved Process Card run status drives Queue and Active placement.

---

## B. Contradicted-Success Lesson

1. **Missed consumer:** The first regression verified card detail and Codex Log but did not include Control Room's independent run loader.
2. **Required review rule:** Any card run ownership change must enumerate and test every consumer that classifies, renders, cancels, resumes, deletes, or lists the current run.
3. **Regression:** A Process Card fixture now proves current-run resolution and Active classification.

---

## C. Evidence

1. **Checks:** `38` focused Control Room tests pass; both changed mobile modules pass syntax validation.
2. **Served result:** The running server serves the corrected resolver at HTTP `200`.
3. **Browser result:** Controlled running status on the persisted Process Card run places the source master in Active before and after reload.
4. **Delivery:** Commit `0206713` is merged by `c7683a9`.