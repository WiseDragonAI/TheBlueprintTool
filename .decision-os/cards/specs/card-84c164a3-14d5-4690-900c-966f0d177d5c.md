## A. Implementation

1. Navigation commits browser history and paints retained Control Room state, cached card data, or a task shell synchronously.
2. Background revalidation replaces retained content without a loading-view flash while preserving the existing card/thread teardown, tab, scroll, desktop thread, and View Transition contracts.

---

## B. Verification

1. Responsive source tests and the served browser scenario prove destination URL and paint before delayed fetch completion.
