## A. Implemented runtime authority

1. **Projection:** Control Room task state is derived from JSON labels, JSON statuses, canonical relationships, and live execution state.
2. **Mobile:** Detailed-card discovery and launch behavior use the same JSON model and stop patching lifecycle Markdown.
3. **Completion:** Canonical endpoints are validated, legacy tokens are stripped on touch, and the post-transaction gate is returned.
4. **Compatibility:** Markdown classification is used only for unlabeled legacy cards.

---

## B. Verification

1. **Backend:** Focused projection, lifecycle, prompt, and completion tests pass; the scoped typecheck passes.
2. **Mobile:** The full suite passes `84/84`.
