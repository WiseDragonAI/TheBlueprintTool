## A. Task

1. In `frontend/src/app/responsive/project-filter-chip.js`, derive deterministic Control Room filter groups from project catalog records.
2. Group records sharing the same non-empty `originFingerprint`.
3. Use the local project as the canonical label and color source, retain all member project IDs, and union ledgers by ledger ID.
4. Use the individual project ID as the grouping key when `originFingerprint` is empty.

---

## B. Targets

1. **File:** `frontend/src/app/responsive/project-filter-chip.js`
2. **Existing symbol:** `projectFilterChipPresentation`
3. **Input fields:** `id`, `name`, `color`, `remote`, `originFingerprint`, `ledgers`
4. **Output contract:** one presentation record with a stable group key, canonical chip presentation, member project IDs, and deduplicated ledgers.

---

## C. Completion

1. Equal non-empty fingerprints yield exactly one repository group.
2. The group contains every local and remote project ID for that origin.
3. Missing fingerprints do not merge unrelated projects.
4. Ledger entries with the same ID appear once.
