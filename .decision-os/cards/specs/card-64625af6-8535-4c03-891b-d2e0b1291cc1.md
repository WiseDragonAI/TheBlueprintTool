## A. Result

1. **Implemented:** `renderPipelineLibrary()` now wraps every pipeline button in `article.codex-list-card`.
2. **Accent:** `--skill-category-color` is set from `pipeline.projectColor`.
3. **Interaction:** The nested `.codex-list-item` retains the existing `openEditor(pipeline)` click behavior.
4. **Commit:** `a6a88c7e`.

---

## B. Verification

1. **Served evidence:** Every loaded pipeline on `/pipelines` had a `1px solid` border, a visible background, and the `#38d9e8` ownership accent.
2. **Status:** `verified`.
