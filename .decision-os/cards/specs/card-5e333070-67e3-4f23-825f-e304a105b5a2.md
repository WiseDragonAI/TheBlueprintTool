## A. Change

1. **Type:** `code`
2. **Targets:** `frontend/src/app/responsive/application.js`, `frontend/src/app/responsive/thread.js`, and `frontend/src/runtime/input/helper/is-card-editing-keyboard-target.ts`.
3. **Symbols:** responsive `window` keydown binding, `initializeMobileThread`, `openMobileThread`, `closeMobileThread`, and the editable-target guard.
4. **Action:** Route `A` to open the active card thread and focus its draft on a second press, `X` to open the thread and toggle voice capture, and `Esc` to cancel voice capture then close the thread. Suppress global commands while an editor owns input. Keep canvas selection shortcut ownership unchanged.

---

## B. Completion

1. **Responsive card route:** `A`, second `A`, `X`, and `Esc` perform the documented transitions.
2. **Editing safety:** Text entry does not trigger global commands.
3. **Canvas preservation:** `Del`, `Ctrl+C`, `Ctrl+V`, and `Ctrl+D` remain bound through `handleKeyboard`.
