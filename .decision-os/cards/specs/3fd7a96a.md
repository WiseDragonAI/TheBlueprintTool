Every regular zone and group, including static surface regions, newly drawn regions, and ledger-rendered regions, must expose a persistent edit icon button. Activating it edits the visible zone or group label inline; regular zones must also expose a color edit control that updates the zone color without requiring toolbox mode changes.