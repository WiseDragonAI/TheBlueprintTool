Voice recording must request real microphone input through browser media APIs and must surface permission failures to the operator instead of pretending that recording started. Voice capture writes operator thread notes; there is no separate `voice` thread role.
