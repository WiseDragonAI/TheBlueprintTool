decision-os must keep live-editable specs and data ledger JSON files in `./.decision-os` so the browser/server workbench can visualize current spec and data edits without reading application source files.