## A. Scope

1. **Runtime target:** Correct the carousel styles in `frontend-mobile/assets/mobile.css`, which is the stylesheet served by the configured mobile frontend.
2. **Overlay layout:** Keep arrows, filename, and direct image selectors inside the carousel image frame.

---

## B. Acceptance Criteria

1. **Side arrows:** Render `42px × 52px` pill controls centered at the left and right sides of the image.
2. **Filename:** Render the filename in a single-line rounded overlay above the direct selectors.
3. **Direct selectors:** Render `28px` circular selectors along the bottom inside the frame with a distinct active state.
4. **Verification:** Mobile tests pass and the live server returns the updated `mobile.css` with `cache-control: no-store`.

---

## C. Evidence

1. **Implementation commit:** `1139361`.
2. **Merge commit:** `aa5450d`.
3. **Tests:** `28` mobile tests passed.
4. **Live asset:** `http://127.0.0.1:50150/assets/mobile.css` returned the overlay rules with `HTTP 200`.
