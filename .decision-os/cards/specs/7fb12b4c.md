Each generation run reads the master ledger and creates a fresh git worktree under `./.worktrees/`. Files and directories inside that generated worktree are regenerated from scratch instead of being patched in place.