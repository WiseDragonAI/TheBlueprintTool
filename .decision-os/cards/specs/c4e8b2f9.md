Before low-detail or overview-detail zoom hides card descriptions, runtime measures each rendered card in full-detail mode and stores its width and height as a transient size cache on the card element. While zoomed out, the card keeps that cached footprint even though description content is hidden.