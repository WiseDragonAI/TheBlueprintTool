## A. Implemented

1. Added canonical filter grouping in `frontend/src/app/responsive/project-filter-chip.js`.
2. Groups use `originFingerprint + localProjectId`; a fingerprint-less remote joins the unique matching local logical project.
3. Conflicting origins and unrelated projects remain separate.
4. Groups expose member project IDs, local-first presentation, deduplicated ledgers, and combined online state.

---

## B. Evidence

1. Live catalog grouping produced one `decision-os` group with workstation and mobile IDs.
2. `Search` produced its own group with workstation and mobile IDs despite sharing the repository origin.
3. Unit cases cover missing remote fingerprints, conflicting fingerprints, missing local origins, ledger deduplication, and group membership.

---

## C. Verification

1. Focused helper and renderer suite passed.
2. Frontend typecheck passed.
3. Served catalog projection returned HTTP 200 and the expected group membership.
