## A. A. Delivery slices

1. Fix shared frontend ledger hydration and add the new-intake regression.

---

2. Add the manifest store and compatibility reader for current queue/pipeline records.

---

3. Add the detached direct-file launcher and migrate thread start/continuation.

---

4. Migrate pipeline skills to the same launcher.

---

5. Add the OS/file reconciler, durable retry admission, and process-group cancellation.

---

6. Remove pipe ingestion, adopted-child monitoring, and runtime-owned capacity state after restart tests pass.

---

## B. B. Verification matrix

1. Cover fresh launch, queued launch, completion, failure, cancellation, continuation, transient retry, server loss, server recovery, PID reuse defense, cross-project capacity, pipeline advancement, and UI reload consistency.

---

2. Run focused frontend and backend tests through `node bin/decision-os-verify.mjs`, then backend/frontend typechecks, then the full suite once.

---

3. Perform the served mobile Chromium check only after reading `BROWSER_RUNBOOK.md`; do not restart the server during browser verification.
