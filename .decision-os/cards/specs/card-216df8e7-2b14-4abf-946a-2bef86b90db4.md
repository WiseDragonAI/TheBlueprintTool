#master-task #task-complete

Ledger: Specs
Waiting since: 2026-07-14T14:09:31.318Z
Active since: 2026-07-14T14:10:55.474Z
Completed at: 2026-07-14T14:18:14.837Z

## A. Correction

1. **Finding:** `retrospect-and-close-task` manually completed cards and created a blanket `.decision-os` Git commit after the retrospective.
2. **Required path:** After retrospective evidence and thread reporting are persisted, complete the master through the canonical project-scoped ledger route with mutation `complete-master-task`.
3. **Commit boundary:** Let the server commit only the canonical ledger, master card, linked subtask cards, and master thread through the card commit system.

---

## B. Acceptance Criteria

1. **Skill contract:** The synchronized server skill requires `PATCH /p/<projectId>/decision-os/<ledgerId>` with `{ "action": "complete-master-task", "masterTaskId": "<master-card-id>" }`.
2. **Removed bypass:** The skill no longer instructs agents to manually rewrite every lifecycle, run `ledger-cli done` on the master, stage all `.decision-os`, and create a separate documentation commit.
3. **Verification:** The synchronized package is classified as `source: server`, its canonical card projection matches the package, and the focused skill update commit is recorded.
4. **Closeout:** This master is completed through the same canonical card commit route after its linked implementation task is verified.

---

## C. Subtasks

1. [Update retrospective completion workflow](card:card-25c345d0-1866-4dbf-8472-1321ff779863) — Status: complete
---

## D. Verified Evidence

1. **Server skill:** Live revision `dc8c9aa1b7861f3094bf394160ffafe1d9d2738642ad05dc36f1246e1a1f8d5d` is classified as `source: server`.
2. **Projection:** The installed `SKILL.md` and canonical Skills card projection are byte-identical.
3. **Focused commit:** `bbf808c18976d64e50b5333a8e00a5b76d6cb540` changes only the skill package, `agents/openai.yaml`, and canonical Skills card projection.
4. **Authorization:** The operator explicitly requested completion of this master card.