## A. Regression evidence

1. The regression seeds a project favorite and tag, starts the server, and proves both project and server catalogs hydrate the migrated values.
2. It writes new metadata through the project route, proves only the server owner changes, then reloads both catalogs and the detail endpoint.
3. Both catalogs return the same saved favorite and tag after reload.

---

## B. Remaining operator-route gate

1. The running server still serves the pre-merge backend and was not restarted under the server ownership rule.
2. Required final check after an authorized restart: hard reload Skills Library and confirm `implementation-orchestrator`, `retrospect-and-close-task`, and `task-dependency` appear first with favorite stars, matching Process Card.
3. Keep this subtask active until that served interaction is observed.
