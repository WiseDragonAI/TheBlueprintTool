Backend startup resolves the active `.decision-os` directory from the launch cwd or `DECISION_OS_ROOT`, reads optional `.decision-os/.settings.json`, and applies workspace settings for frontend root, host, port, and transcription configuration. External project workspaces are separate repos; decision-os must support launching from their cwd without embedding project-specific names or settings.