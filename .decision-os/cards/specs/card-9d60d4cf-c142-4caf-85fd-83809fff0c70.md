## A. Implemented Contract

1. **Single exit:** The duplicate `Back to library` action is removed from detail.
2. **Top-left control:** The existing `×` returns detail to the populated library; when the library is already visible, the same control closes the modal.

---

## B. Verification Evidence

1. **Focused tests:** The exclusive-detail navigation test passed.
2. **Served route:** Mobile Chromium found `0` duplicate Back actions.
3. **Close result:** After pressing the top-left `×`, detail was hidden, the library was visible with `33` rows, and the heading was `Skill library`.