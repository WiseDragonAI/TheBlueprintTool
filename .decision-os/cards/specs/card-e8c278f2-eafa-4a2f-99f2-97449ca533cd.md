## A. Implementation

1. **`buildThreadCodexPrompt` now receives the runtime `projectId` explicitly and renders the exact project value in the developer instructions.**
2. **The same prompt exposes the authorized `ledger-cli master-task-complete --card-id <id>` closeout command.**

---

## B. Verification

1. **Focused coverage:** `backend/test/codex/build-thread-codex-prompt.test.ts` passes and asserts both injected values.
2. **Typecheck:** backend TypeScript passes.
3. **Delivery:** implementation commit `f0d6c8d` is merged through `20a9724`.