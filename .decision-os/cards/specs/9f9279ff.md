Selecting the zone tool opens the zone color picker without resetting the selected color. The picker value mirrors `state.zoneColor`, and newly drawn zones use that value until the operator chooses another color.