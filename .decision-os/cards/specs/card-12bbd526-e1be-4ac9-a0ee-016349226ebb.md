## A. Type and Targets

1. **Type:** `code`
2. **Files:** `frontend/index.html`, `frontend/src/app/responsive/application.js`, `frontend/assets/application.css`
3. **Symbols:** `.new-task-project-sheet`, `.new-task-project-list`, `openNewTaskProjectModal`, `projectOwnerLabel`, `projectPresenceLabel`

---

## B. Implemented Change

1. Added an accessible tab list whose visible labels use the node label and whose secondary text reports `Online` or `Offline`.
2. Grouped `state.projects` by owning node and rendered only the active node's project names.
3. Selected the node containing `remote: false` projects whenever the dialog opens.
4. Removed project IDs from visible selector text and accessible project-choice names.
5. Kept offline-node projects visible while disabling their task-creation buttons.
6. Preserved `createTaskIntake(project.id)`, busy locking, error recovery, cancellation, focus handling, and added arrow, `Home`, and `End` tab navigation.

---

## C. Verification

1. **Focused selector regressions:** pass.
2. **Frontend typecheck:** pass.
3. **Served route:** HTTP `200` at `http://127.0.0.1:50151/`, with the new markup and runtime served.
4. **State:** implemented; automated checks pass; device interaction not yet verified.
