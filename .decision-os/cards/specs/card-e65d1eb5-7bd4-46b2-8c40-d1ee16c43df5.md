## A. Served verification

1. **Route:** `http://127.0.0.1:50151/pipelines`
2. **HTTP:** `200`
3. **Card count:** `3`
4. **Structure:** Each pipeline entry was an `ARTICLE.codex-list-card` containing the full-width `.codex-list-item` button.
5. **Visual states:** The card showed a border, background, ownership accent, `2px` keyboard-focus outline, and pointer-hover border change.
6. **Navigation:** Activating `Post-Implementation` opened the matching `Edit pipeline` dialog.
7. **Server boundary:** No restart was performed.

---

## B. Status

1. **Result:** `verified`.
