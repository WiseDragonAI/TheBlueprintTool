## A. Runtime Contract

1. **Project identity:** inject `DECISION_OS_PROJECT_ID` into every Decision OS Codex runtime.
2. **Completion command:** expose `ledger-cli master-task-complete --card-id <id>` using the injected ledger, server URL, and project ID to call the canonical completion transaction once.

---

## B. Implementation Evidence

1. **Project injection:** commit `1acbc98ce1457c2e0552b31f482081645a4fb882` adds `DECISION_OS_PROJECT_ID` to the shared Codex runtime.
2. **CLI command:** commit `f0d6c8d6590b877ee77aeab98d395843245be94d` adds `master-task-complete`, its canonical project-scoped request, help text, parsing, and tests.
3. **Focused verification:** the runtime test passes `1/1`; the CLI command suites pass `12/12`.
