## A. Retrospective

1. **Delivered result:** The first merge, `a89ce1f` through `fb72781`, fixed a lost scheduler wake-up for durable `pending` records but did not reproduce the operator's pre-crash claimed records.
2. **First contradiction:** The operator restarted the merged server and three jobs remained queued. The next correction, `a53d295` through `b0b6445`, requeued claimed records as continuations.
3. **Second contradiction:** The operator restarted again and reported two jobs still queued. This disproved the second success claim and showed that relaunching records alone did not model surviving process ownership and retained lifecycle history.
4. **Subsequent correction artifacts:** `e74bb7c` added PID-plus-start-time adoption and monitoring for surviving Codex children, `e768aed` recovered still-owned interrupted records and deduplicated logical runs, and `94d8607` derived recovery state from the latest JSONL lifecycle transition.

---

## B. Durable Lessons Saved

1. **Memory `40` — Reproduce restart recovery from the claimed state:** Crash recovery tests must start from the exact persisted lifecycle state produced by the operator scenario and cross the startup boundary.
2. **Memory `41` — Adopt surviving durable child processes:** Recovery must adopt a live child identified by PID and process start time, then recover only after that ownership is dead.
3. **Memory `42` — Use the latest lifecycle event for recovery:** Recovery must replay lifecycle order so a newer start transition supersedes an older terminal event in retained JSONL.

---

## C. Closure

1. **Master task:** `card-b85c6294-593d-4870-8c6e-ec559386df3c` completed through the canonical command.
2. **Canonical subtasks:** All three linked subtasks were already complete and passed the completion gate with no discrepancies.
3. **Completion commit:** `67c1c195f190ea9b1291711a8c15abdfc3bb0bd2`.
4. **Pipeline run:** `codex-skill-1784205397104-79897826`.
---

Codex run completed: exit code 0
