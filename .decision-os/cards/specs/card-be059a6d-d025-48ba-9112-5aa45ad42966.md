## A. Implemented state

1. Added module-local `controlRoomColumnScrollTop` values for `queue`, `exec`, and `backlog` in `frontend/src/app/responsive/application.js`.
2. Each column list updates only its own value through a passive `scroll` listener.
3. `captureControlRoomColumnScroll()` samples connected lists before visible rerenders and skips hidden Control Room lists so `loadRoute()` cannot overwrite remembered positions with hidden-layout zeros.
4. No ledger, server, URL, `localStorage`, or `sessionStorage` persistence was added.

---

## B. Verified result

1. Responsive Control Room tests passed: `50/50`.
2. The served browser regression changed `queue` to `280` while `exec` remained `330` and `backlog` remained `450`, proving independent memory.
