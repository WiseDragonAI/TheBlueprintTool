#master-task #task-complete

Ledger: Specs
Waiting since: 2026-07-13T08:41:55.520Z
Active since: 2026-07-13T09:56:27.238Z
Completed at: 2026-07-13T09:56:56.956Z

## A. Scope

1. **Objective:** Let the operator stop an active card-scoped Codex process from the `Codex Log` status widget at the top of the mobile thread.
2. **Control:** Replace the compact `Cancel` affordance with a prominent stop control that displays a solid square icon and the label `STOP`.
3. **Existing backend:** Reuse `requestCardSkillRunCancel` and the existing card-run cancellation route, which already terminate the active process and settle the run as `cancelled`.
4. **Boundary:** Do not change the voice recording dock in this task; the operator identified that control redesign as a separate problem.

---

## B. Interaction Contract

1. **Visibility:** Show the `STOP` control only while the current Codex run is active and cancellable.
2. **Activation:** A pointer activation must invoke cancellation once, disable repeated activation while the request is pending, and preserve propagation guards so the surrounding card does not handle the click.
3. **Pending state:** While cancellation is pending, keep the control disabled and show `STOPPING` without replacing the square stop meaning with an X icon.
4. **Settlement:** After the server confirms termination, the widget must render the terminal `CANCELLED` run status and hide the stop control.
5. **Failure:** When the cancellation request fails, restore the enabled `STOP` control and display the server error in the run status detail.

---

## C. Acceptance Criteria

1. **Visual control:** The active Codex widget presents a large stop button with a solid square icon and visible `STOP` label; it is not styled as `terminal-button--compact` and does not say `Cancel`.
2. **Process termination:** Activating the control sends exactly one card-run cancellation request and the active Codex child process terminates.
3. **State accuracy:** The widget transitions through `STOPPING` to `CANCELLED`; a rejected request returns the button to `STOP` without claiming termination.
4. **Regression coverage:** Frontend tests assert the rendered icon, label, non-compact sizing, pending state, request routing, terminal hiding, and rejected-request recovery; the existing backend cancellation test continues to pass.
5. **Target verification:** On the mobile project route `/p/ZGVjaXNpb24tb3M/ledgers/specs/zones/<zone-id>/cards/<card-id>`, open the card thread and `Codex Log`, verify the control with representative Brave pointer input, observe the cancellation request, confirm the process terminates, and confirm the terminal state remains after a fresh reload.

---

## D. Subtasks

1. [Implement the Codex session STOP control](card:card-a1b03a54-4315-4e20-8566-8f4c2dc8dc56) — Status: complete
2. [Verify Codex process stopping on the served thread](card:card-7af0bea9-eebb-42f1-942a-8e030c386cf3) — Status: complete
---

## E. Implementation Evidence

1. **Root cause:** `createHttpServer` created a fresh shallow runtime object for every HTTP request. The Codex start request registered its child process in one temporary object; the later stop request received another object and returned `404 Active run not found`.
2. **Backend correction:** Project-scoped runtime state is now retained across start, status, continue, and stop requests, while remaining isolated by `decisionOsRoot`.
3. **Frontend control:** The active run action is a dedicated `58px` square stop button with a solid `■` icon, visible `STOP` label, accessible `Stop Codex run` name, disabled `STOPPING` state, and rejected-request restoration.
4. **Commits:** Feature commit `9fad486` is merged into `main` by merge commit `de000dd`.
5. **Automated checks:** Frontend and backend TypeScript checks pass; the focused stop-control tests pass; the card-run cancellation integration test now proves the child process terminates and status becomes `cancelled`; the project-catalog isolation test passes from the repository root.
6. **Served assets:** The exact project-scoped card route returns `200 text/html`; the served widget module returns `200 text/javascript` with the merged `STOP` markup; the served stylesheet contains the merged `58px` stop-control rules.
7. **Deployment gate:** The currently running backend loaded the pre-merge runtime implementation. Server restart requires explicit operator authorization, so target pointer-to-process verification cannot run against the correction in this turn.

---

## F. Contradicted-Claim RCA and Mobile Correction

1. **Contradiction:** The operator screenshot at `2026-07-13T09:20:17.638Z` shows mobile Brave on a `/p/ZGVjaXNpb24tb3M/...` card route with `Codex Log` selected, run status `RUNNING`, elapsed `01:17`, three tools, and no stop control.
2. **First incorrect transition:** The running summary reached `renderThreadCodexLogStatus`, which rendered only the five `Status`, `Model`, `Effort`, `Elapsed`, and `Tools` cells. The earlier change targeted `renderCardSkillRunWidget`, a separate desktop/card-detail widget that is not mounted in the photographed mobile `Codex Log` surface.
3. **Request evidence:** No stop request could be initiated from the photographed state because the mobile DOM contained no stop action. The screenshot provides no network capture; this gap is now covered by the controller regression that proves one request while pending and restoration after a rejected request.
4. **Mobile correction:** When the shared `Codex Log` summary is `running`, append a full-width action row containing a `64px` square `STOP` button with solid `■` icon. Route `stop-thread-codex` through `mobile-thread.js` to the shared stop controller using the displayed `runId`, owning `cardId`, and active `ledgerId`.
5. **State contract:** The controller prevents duplicate requests, displays disabled `STOPPING`, leaves the poller to reconcile successful terminal status, and restores `STOP` with a visible server error after rejection.
6. **Regression:** `frontend-mobile/test/mobile-thread.test.mjs` locks the mobile renderer-to-controller path; `frontend/test/runtime/thread-codex-stop-controller.integration.test.ts` locks single-request and rejection recovery; the shared thread renderer tests continue to pass.
7. **Commits:** Mobile correction commit `8f05d5e` is merged into `main` by merge commit `80ae756`.
8. **Served target:** The mobile card route returns `200 text/html`; served `render-thread-codex-log-status.js`, `stop-thread-codex-run-controller.js`, `thread.css`, and `/src/mobile-thread.js` all contain the merged mobile stop implementation.
9. **Backend deployment:** The operator-facing backend PID changed after the earlier pre-merge deployment check, so the running process now loads the project-scoped process registry correction from `de000dd`.
10. **Open gate:** Reload the same mobile Brave card route from the screenshot, open `Codex Log` during a running session, tap `STOP`, and observe `STOPPING` followed by terminal `CANCELLED`. Keep both subtasks and the master `todo` until that sequence is observed.
11. **Visual verification:** The operator screenshot at `2026-07-13T09:36:42.610Z` confirms mobile Brave renders the corrected control on the real `Codex Log` surface: status `RUNNING`, elapsed `00:04`, tools `0`, and a large red square `STOP` button inside the status widget.
12. **Remaining behavioral evidence:** The screenshot does not show pointer activation, request emission, process termination, `STOPPING`, terminal `CANCELLED`, or post-reload persistence. Do not complete the linked cards from visual presence alone.

---

## G. Single-Row Action Redesign

1. **Operator finding:** The `2026-07-13T09:36:42.610Z` screenshot verifies the button but shows that the full-width action row makes the status widget two rows tall.
2. **Selected layout:** Remove the redundant `Status / RUNNING` cell. Use the first `76px` column for the current run action, followed by `Model`, `Effort`, `Elapsed`, and `Tools` on the same row.
3. **Running action:** Render red `STOP` with the solid `■` icon while status is `running`.
4. **Existing terminal action:** Render blue `RESUME` with a `▶` icon for `complete`, `failed`, and `cancelled` runs; route it through the existing thread continuation action with the displayed model and effort.
5. **No-run action:** Preserve the existing blue thread launch action when the thread owns no run.
6. **Regression:** Frontend TypeScript checking, five focused shared renderer/controller tests, and all six mobile-thread tests pass.
7. **Commits:** Redesign commit `5c3f8f4` is merged into `main` by merge commit `98d60f9`.
8. **Served target:** The served renderer contains red `STOP` and blue `RESUME`; the served stylesheet contains `grid-template-columns: 76px repeat(4, minmax(0, 1fr))`; the mobile card route returns `200 text/html`.
9. **Open gate:** Reload mobile Brave and confirm the action plus four information cells remain on one row, then exercise `STOPPING` to `CANCELLED` and blue `RESUME` on the terminal run.

---

## H. Failed-Tap RCA and Active-State Correction

1. **Contradiction:** At `2026-07-13T09:44:46.987Z`, the operator reported that tapping the displayed mobile `STOP` control did not stop the run.
2. **Captured target:** The affected surface is mobile Brave on the project-scoped card route with the thread `Codex Log` open. No browser network capture was supplied for the failed tap, so the request result is not inferred.
3. **First incorrect transition:** `renderThreadCodexLogStatus` converted a persisted `runId` with no hydrated summary into status `running` and rendered `STOP` without checking the backend `active` field. The DOM could therefore claim cancellability before the server confirmed that it owned a live child process.
4. **Invisible rejection:** `stopThreadCodexRunController` inserted a rejection message into the DOM, while the polling renderer called `root.replaceChildren()` on the next refresh. The rejection and pending state were not durable across that rerender.
5. **Correction:** Render `STOP` only when the hydrated summary is `ok`, `active`, and `running`. Preserve per-run pending and rejection state across log rerenders. Locally hydrated newly started runs now set `active: true` explicitly.
6. **Regression:** Frontend TypeScript checking, all six mobile-thread tests, both stop-controller tests, and three shared thread-renderer tests pass.
7. **Commits:** Correction commit `fc1d3f4` is merged into `main` by merge commit `d22d8c6`.
8. **Served evidence:** The card route returns `200`; the served renderer requires `summary.active === true`, preserves `STOPPING`, and restores the stop error. The current status endpoint reports this run as `status: running` and `active: true`.
9. **Restart decision:** No manual restart is required for this correction. The current backend already owns the active run, and the changed frontend modules are served directly. No server process was restarted or stopped during this work.
10. **Open gate:** Keep the master and both subtasks active until a new mobile Brave tap produces `STOPPING`, terminates the active process, reaches `CANCELLED`, and survives reload.
