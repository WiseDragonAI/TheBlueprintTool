## A. Reopened Failure Evidence

1. **Route and client:** `http://127.0.0.1:50150/skills`, Android Chromium mobile emulation at `412 × 915`.
2. **Gesture:** Open `retrospect-and-close-task`, select `Architecture`, then press `Save tags`.
3. **Request sequence:** The former client sent three `PUT` requests containing `{"tags":["Architecture"]}`. Each returned `403` with `Server skills are synchronized through ledger-cli.`
4. **Persisted state:** Fresh detail requests for the four owning projects returned no saved tag.
5. **Cause:** The tap controlled a local checkbox; persistence was deferred to a duplicate Save button, and the running backend process predates protected-skill metadata support.

---

## B. Corrected Tag Contract

1. **Vocabulary:** The server-owned categories remain `Architecture`, `Implementation`, `Interface`, `Writing`, `Marketing`, `Product`, `Research`, `Automation`, `Artifacts`, and `Platform`.
2. **Cardinality:** The store normalizer and save validator retain at most one supported tag.
3. **Interaction:** Mobile and desktop render category buttons. Selecting a different button immediately applies the tag optimistically and starts persistence; no Save Tags button is rendered.
4. **Failure behavior:** A rejected save restores server-confirmed state and exposes the backend error.
5. **Protected source behavior:** Tag metadata is project-local; synchronized server `SKILL.md` content remains unchanged.

---

## C. Evidence and Remaining Gate

1. **Backend regression:** `3/3` route and helper tests passed, including server-skill tag persistence with byte-identical synchronized Markdown.
2. **Client regressions:** Mobile passed `14/14`; focused desktop immediate-save and request tests passed `2/2`.
3. **Type safety:** Combined frontend/backend typecheck passed.
4. **Served client:** One tap selected exactly one tag before settlement, rendered zero Save Tags buttons, and sent only `["Architecture"]`.
5. **Pending:** The running server still serves the old backend code. Successful persistence and reload verification require an operator-authorized restart on port `50150`.