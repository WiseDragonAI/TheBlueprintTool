## A. Scope

1. **Objective:** Add the canonical `tasks` ledger to the existing `Search` project.

---

## B. Acceptance Criteria

1. **State:** `Search/.decision-os/state.json` registers the `tasks` ledger.
2. **Ledger file:** `Search/.decision-os/tasks.json` exists with a valid empty ledger.
3. **Catalog:** A fresh project discovery reports the `tasks` ledger for `Search`.
