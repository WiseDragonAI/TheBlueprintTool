## A. Served Lifecycle Verification

1. **Backlog exit:** Chromium at `1440 × 1000` loaded the master task with card and thread visible, then a mocked `Move to backlog` reached `/?tab=backlog` with Control Room visible, card hidden, and thread closed. The mutation was intercepted, so the ledger was not changed.
2. **Desktop open:** The served subtask detail opened with `card-thread-open`.
3. **Mobile boundary:** The same subtask at `390 × 844` rendered the card with its thread closed.
4. **HTTP:** The operator-facing server at `http://127.0.0.1:50151` returned `200` and served the merged files without a restart.
