Completed at: 2026-07-19T05:38:34.346Z

## A. Root Cause

1. **The failed Ardaria note persisted `error: transcription not configured`.**
2. The Ardaria project has no local `.decision-os/.settings.json`.
3. The server catalog settings contain no OpenAI transcription key, while the Decision OS repository settings contain the configured key and model.
4. Teamlink is not part of this request path; voice transcription calls the OpenAI audio transcription endpoint directly.
5. The frontend retained the server error in note state but rendered only `Transcription failed`.

---

## B. Merged Implementation

1. Commit `dd660e08` makes the launcher provide the repository settings file independently of launch cwd.
2. The settings helper merges repository defaults with active-workspace settings; explicit workspace values remain authoritative.
3. Failed thread notes render their persisted error with an accessible alert and include error changes in the render signature.
4. Existing retry behavior and voice-note structure are preserved.

---

## C. Verification

1. Backend and frontend typechecks pass.
2. Focused settings, launcher, and failed-note renderer regressions pass.
3. All `477` frontend tests pass.
4. The browser suite passes.
5. The backend suite passes `212` of `216` tests; four existing failures reproduce in isolated serial runs because their expectations omit the seeded `project-synchronization` pipeline and default `tasks` ledger. None touches voice settings or thread error rendering.

---

## D. Live Validation Gate

1. The running server still has the pre-change backend loaded because no restart was authorized.
2. After the next authorized server restart, retry the preserved Ardaria voice note and confirm transcription starts with repository settings.
3. Force one provider failure and confirm the thread displays the exact persisted error beneath `Transcription failed`.

---

## E. Subtasks

1. [Inherit repository transcription settings](card:card-d75ac8a3-6811-45d3-a2de-c0fc9343e6b2)
2. [Expose persisted voice transcription errors](card:card-547199d5-ab2f-4786-af0c-2e1b2f2d689c)
