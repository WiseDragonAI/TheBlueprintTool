## A. Implementation Result

1. **Implemented:** the operator-facing table is **Exec** and responsive state, route parsing, collection access, task status, timing helpers, and empty-state copy use execution terminology.
2. **Compatibility:** legacy `?tab=active` URLs canonicalize to `exec`.
3. **Presentation:** pending execution shows its one-based position when known and a waiting-for-execution label before hydration; running execution retains the stopwatch.
