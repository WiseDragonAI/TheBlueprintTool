Thread conversation text must render one pixel larger than the prior terminal note size. The scrollable conversation must keep bottom padding inside the chat surface and inside the note list so the last agent reply never touches the input footer border. Inline markdown code in thread notes must use the same hue-preserving readable color algorithm as card body code, with HSV value targeting 0.85 instead of neon 1.0.