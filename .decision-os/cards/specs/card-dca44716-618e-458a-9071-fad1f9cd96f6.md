## A. Implementation

1. Add a schedule section to each saved pipeline detail with cron expression, IANA time zone, enabled state, next run, last occurrence, and last launch result.
2. Provide create, edit, enable, disable, and delete actions against the project-scoped schedule endpoints.
3. Show validation errors beside the responsible field and show the computed next run before save.

---

## B. Acceptance evidence

1. Frontend tests cover schedule creation, editing, enablement, disablement, deletion, invalid input, and project scoping.
2. The served Pipelines surface displays the persisted schedule after a fresh reload.
3. Mobile Chromium verification records the route, HTTP result, computed next run, and successful enabled-state change without restarting the server.
