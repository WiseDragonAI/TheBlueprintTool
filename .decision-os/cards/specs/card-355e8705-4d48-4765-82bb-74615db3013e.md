#master-task #spec #task-complete

**Ledger**: `Specs`

**Waiting since**: `2026-07-13T18:20:09.322Z`

Active since: 2026-07-14T06:54:04.173Z
Completed at: 2026-07-14T07:39:40.988Z

## A. Scope

1. **Add project creation** to the mobile all-projects route at `/projects`.
2. **Collect** a required project name and an optional description in a modal that matches the existing creation controls.
3. **Create** a new directory directly below the server catalog root and initialize its `.decision-os/state.json` and stable project identity.

---

## B. Contract

1. **UI control**: render `+ Project` with the existing `.create-button` visual system in the Projects header.
2. **API**: accept `POST /decision-os/projects` with JSON fields `name` and `description`.
3. **Validation**: reject empty names, names longer than 120 characters, descriptions longer than 1000 characters, unsafe directory names, and collisions without modifying disk state.
4. **Persistence**: initialize the new project below the catalog root, persist its metadata, invalidate the catalog cache, and return the discovered project.
5. **Navigation**: after successful creation, refresh local project state and open the canonical project detail route.

---

## C. Acceptance Criteria

1. **Creation request tests** prove initialized state, metadata persistence, catalog discovery, validation rejection, and collision rejection.
2. **Mobile tests** prove the button, required name, optional description, submission request, error state, and post-success navigation.
3. **Served target**: verify `GET /projects` returns `200` from the existing server and exercise the complete creation flow in mobile Chromium without restarting the server.
4. **Repository integration**: commit the implementation in an isolated worktree, merge it into `main` with a merge commit, and remove the worktree and feature branch.

---

## D. Subtasks

1. [Implement safe project creation and initialization](card:card-94a30f4e-9473-4489-9165-7919db6010e4) — Status: complete
2. [Add the mobile create-project flow](card:card-a1c0de9b-4018-44e3-bebe-74d9c66b5960) — Status: complete
3. [Verify and integrate project creation](card:card-697f0d76-8e4e-4269-a20e-536d16157dff) — Status: complete