## A. Served-Route Reproduction

1. **Target:** `http://127.0.0.1:50150/skills` returned HTTP `200` from the unchanged server process.
2. **Client:** Termux Chromium used a `412 × 915` Android mobile viewport with touch input.
3. **Gesture:** Open `retrospect-and-close-task` and tap `Architecture` once.
4. **Optimistic observation:** `Architecture` changed from `aria-pressed="false"` to `aria-pressed="true"`, exactly one tag was selected, and the visible status became `Saving Architecture…` before request settlement.
5. **UI contract:** The detail surface contained zero Save Tags buttons.

---

## B. Request and Reconciliation Evidence

1. **Payload:** Each owning-project request contained exactly `{"tags":["Architecture"]}`.
2. **Running-backend response:** Three synchronized-source requests returned `403` with `Server skills are synchronized through ledger-cli.`
3. **Reconciliation:** The client restored `Architecture` to `aria-pressed="false"` and displayed the backend error.
4. **Persisted state:** Fresh detail requests showed no saved tag, matching the rejected response.

---

## C. Remaining Validation

1. **Implemented checks:** Backend regressions passed `3/3`, mobile passed `14/14`, focused desktop tag tests passed `2/2`, and combined typecheck passed.
2. **Server boundary:** The running backend process predates the committed metadata-save implementation.
3. **Pending operator action:** Authorize a restart on port `50150`; then verify optimistic selection, successful response, and persistence after a fresh reload on this same served route.