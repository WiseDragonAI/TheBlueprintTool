## A. Regression Evidence

1. **Automated:** Added project-card assertions for the unique Settings action, gear SVG contract, settings routing, omitted inline pencil, omitted status chrome, and unchanged non-project branches.
2. **Shared persistence:** Existing project settings tests confirmed complete name, description, and color payloads plus rejected-save reconciliation.
3. **Served browser:** Chromium exercised `http://127.0.0.1:50151/projects` with HTTP `200`; the modal opened from the gear control, a rejected PATCH preserved the modal and error, a successful PATCH reloaded server state, and a fresh reload preserved metadata and geometry.
4. **Full frontend run:** `408/418` tests passed. The ten failures are pre-existing responsive assertions that expect dynamically loaded vendor assets as static `index.html` tags; the change-specific checks and frontend typecheck pass.

---

## B. Commit

1. **Feature commit:** `2468f61 feat: add desktop project card settings`.
2. **Merge commit:** `9a59ed2 merge: add desktop project card settings`.
