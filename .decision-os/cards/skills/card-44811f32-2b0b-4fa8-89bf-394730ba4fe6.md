## A. Task Inventory

| id | type | title | target_files | target_symbols | action | done_when | depends_on |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `T01` | `code` | Split the thread launch contract from task context | `backend/src/business/codex/helper/build-thread-codex-prompt.ts` | `buildThreadCodexPrompt` | Change the builder to produce separate developer instructions and stdin task context. Build the developer instructions from the `Thread Codex Launch Prompt` contract in `card-93221cb2-549d-4549-aacc-4831a1f78441`, resolve every runtime placeholder, include `operatorNoteTimestamp`, and keep the current thread and card markdown in the stdin task context. | The builder exposes two distinct launch strings; the developer instructions contain Sections `A` through `D` with no unresolved `<...>` placeholders, and the stdin context contains the current thread and card markdown. | — |
| `T02` | `code` | Resolve the triggering operator timestamp | `backend/src/business/codex/controller/start-thread-codex-process-controller.ts` | `threadMarkdownForPrompt`, `startThreadCodexProcessController` | From the hydrated target thread, filter Codex artifact notes, select the latest remaining note whose role is `operator`, read its exact ISO `timestamp`, and pass that value to `buildThreadCodexPrompt` as `operatorNoteTimestamp`. Return a scoped request error when no timestamped operator note exists instead of launching with an invented time. | A thread run uses the latest real operator note timestamp from the target thread, and a missing timestamp prevents process spawn with a concrete error response. | — |
| `T03` | `code` | Inject launch-scoped Codex developer instructions | `backend/src/business/codex/helper/resolve-codex-command.ts`, `backend/src/business/codex/controller/start-thread-codex-process-controller.ts` | `resolveCodexCommand`, `startThreadCodexProcessController` | Add an optional `developerInstructions` input to `resolveCodexCommand`, encode it as a TOML string, and add `-c developer_instructions=<encoded-value>` to the initial `codex exec` arguments. Supply the developer-instruction string only from the thread launch controller so unrelated pipeline and skill launches retain their current command contract. | The thread controller spawns `codex exec` with one launch-scoped `developer_instructions` override containing the resolved contract; callers that omit the option produce the existing argument list. | `T01`, `T02` |
| `T04` | `test` | Cover developer-instruction and stdin prompt separation | `backend/test/codex/build-thread-codex-prompt.test.ts` | `thread Codex prompt uses a direct scoped contract without triggering open-note skills` | Update the prompt-builder fixture with an operator timestamp and assert the exact scoped contract fields, Sections `A` through `D`, timestamp injection, absence of unresolved placeholders, and exclusion of current card and thread bodies from developer instructions. Assert those bodies remain present in stdin task context. | The unit test fails if the launch contract, timestamp, placeholder resolution, or separation between developer instructions and stdin context regresses. | `T01` |
| `T05` | `test` | Cover Codex command encoding and caller isolation | `backend/test/codex/resolve-codex-command.test.ts` | `resolveCodexCommand honors an explicit executable setting` | Add assertions for a `developerInstructions` value containing newlines, quotes, and backslashes, verify its `-c developer_instructions=<encoded-value>` argument parses back to the original string, and verify omission leaves no developer-instruction override. | Command tests prove lossless TOML encoding and confirm the optional override does not affect existing callers. | `T03` |
| `T06` | `test` | Cover the complete thread launch payload | `backend/test/codex/start-card-skill-process-controller.test.ts` | `thread codex process route anchors the run widget on the source card and scopes the prompt` | Extend the fake Codex executable to capture `process.argv`, then assert the route places the latest operator note timestamp and resolved card, thread, ledger, workspace, and run-summary values in `developer_instructions` while stdin retains the operator request and card markdown. Add the missing-timestamp rejection case and assert no process is launched. | The controller integration test proves the spawned command receives the resolved developer instructions, stdin remains scoped task context, Codex artifact notes are excluded, and invalid timestamp input fails before spawn. | `T02`, `T03`, `T04`, `T05` |

---

## B. Open Questions

1. **Blocking questions:** None.

---

## C. Readiness

1. **Status:** `READY_FOR_TASK_DEPENDENCY`.
