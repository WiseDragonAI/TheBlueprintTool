## A. Source Wording

1. **Skill.** `executor-stack`.
2. **Source group.** `Local Codex`.
3. **Source wording.** Analyzes specs/data/runtime, identifies `Root Blocks`, validates stack choices, creates an agnostic directory scaffold, and asks blocking operator questions.

---

## B. Pipeline Fit

1. **Role.** Phase 1 Analysis.
2. **Best use.** It creates `executor-analysis-{YY-MM-DD-N}.md`, identifies `Root Blocks`, fills `Stack`, and produces an agnostic dir tree. It must not prepare `action`, `controller`, `effect`, `input`, `test`, or `state` files yet.

---

## C. Evidence Boundary

1. **Inventory source.** Reused from `.decision-os/cards/skills/card-9a1d7843-0bf4-47e8-b6df-cbc4e5571e39.md`.
2. **Terminology rule.** Keep the skill name, body structure names, labels, and pipeline stage names exactly as listed above when using this card.
