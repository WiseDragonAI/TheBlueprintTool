## A. Source Wording

1. **Skill.** `executor-precheck`.
2. **Source group.** `Local Codex`.
3. **Source wording.** Checks whether an operator goal has enough approved `Specs`, `Data Model`, `Runtime State`, repo facts, and scope boundary before stack planning.

---

## B. Pipeline Fit

1. **Role.** Preflight phase before `executor-stack`.
2. **Best use.** It must not invent `Specs`, infer missing `Data Model` or `Runtime State`, start stack analysis, create `Root Blocks`, or create domains, tests, inputs, actions, controllers, helpers, effects, state, screens, pages, or components.

---

## C. Evidence Boundary

1. **Inventory source.** Reused from `.decision-os/cards/skills/card-9a1d7843-0bf4-47e8-b6df-cbc4e5571e39.md`.
2. **Terminology rule.** Keep the skill name, body structure names, labels, and pipeline stage names exactly as listed above when using this card.
