## A. Scope

1. Add two-node tests for offline local capture, later convergence, preview-first demand, original-on-detail demand, hash rejection, retry backoff, and source-node unavailability.
2. Record current and optimized transferred bytes, decode work, demand count, cache hits, and visible-card render timing on the served canvas.
3. Confirm asset cleanup retains every object reachable from current original and derivative heads.

---

## B. Completion Gate

1. Keep the master task active until automated checks and served cross-node evidence cover the complete local-commit-to-demanded-render chain.
