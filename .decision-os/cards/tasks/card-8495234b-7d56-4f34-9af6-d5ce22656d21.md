## A. Scope

1. **Type:** `code`, `test`, `ops`.
2. **Objective:** Ensure execution-state reads for removed, optimistic, and non-task card identities return a stable empty projection without becoming internal runtime incidents.

---

## B. Code Boundary

1. **Target files:** `backend/src/business/codex/helper/task-execution-state-task-id.ts`, `backend/src/business/codex/http/task-execution-read-routes.ts`, `backend/src/business/federation/http/remote-execution-routes.ts`, and their focused unit plus integration tests.
2. **Target symbols:** `taskExecutionStateTaskId`, `handleTaskExecutionReadRoutes`, `handleRemoteExecutionRoutes`, and `resolveTaskLineage`.
3. **Action:** Keep strict lineage validation for launch admission, make both local and remote read paths tolerate identities absent from the current projection, and prevent expected stale reads from entering the incident ledger.

---

## C. Completion

1. **Done when:** Local and federated regressions prove missing cards return HTTP 200 with an empty execution projection, malformed route segments remain bounded client errors, valid subtasks still resolve to their master, and replaying the active request shapes creates no runtime incident.
2. **Depends on:** Make authored federated skill publication converge.
