#master-task

Ledger: Specs

## A. Operator decision

1. **Do not implement a separate Codex runner service.**
2. Server restart may lose ownership of active Codex processes.
3. **Startup must never relaunch a process that was recorded as running before the restart.**
4. Recover enough durable state to report that the run was interrupted and is no longer owned.
5. **Do not restart or stop the server during implementation.**

---

## B. Implemented behavior

1. Startup changes persisted running thread and continuation claims to `interrupted`, not `pending`.
2. Interrupted claims remain outside the scheduler and queue position calculations.
3. Status reads report `failed`, `active: false`, no queue position, the interruption timestamp, and the explicit no-relaunch reason.
4. Existing terminal event evidence remains authoritative.
5. Pipeline recovery already fails an unowned running skill without relaunching it.

---

## C. Verification and delivery

1. The focused suites passed with **8 tests and 0 failures**.
2. The regression proved startup did not invoke a configured fake Codex executable for a recovered running claim.
3. Backend typecheck and the full backend suite passed.
4. Feature commit `ef1fbcf` was merged to `main` in `cb839e0`.
5. The isolated worktree and merged feature branch were removed.
6. **No operator-facing server restart was performed.** The running server has not loaded the merge commit.

---
