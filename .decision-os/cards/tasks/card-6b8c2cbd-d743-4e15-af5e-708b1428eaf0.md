## A. Verified Result

1. **Pre-change reproduction:** On the canonical served card route at `390x844`, real wheel input moved the conversation to `900px` from bottom. Closing and reopening the same card retained that exact offset.
2. **First incorrect transition:** Responsive close retained `threadId`; same-card `selectThread()` returned early, while persisted follow state vetoed the open pin.
3. **Evidence boundary:** The reproduction used browser wheel, close, and Thread-button input without assigning `scrollTop`.
