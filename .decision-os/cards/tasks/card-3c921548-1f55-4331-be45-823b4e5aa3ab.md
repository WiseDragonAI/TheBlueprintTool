## A. Served Gate

1. **Current route evidence:** `http://127.0.0.1:50151/` returns `200`.
2. **Stale-process evidence:** `http://127.0.0.1:50151/api/codex/server-pipelines` returns `400` with `Project id is required in the URL.`, proving the active process predates the merged global endpoint.
3. **Required action:** Authorize one Decision OS server restart, then exercise create, save, two-project run, and fresh reload on the served surface.
