## A. Implemented Contract

1. **Legacy references:** Relative `.decision-os/thread-images/...` inputs now resolve to `/p/<project-id>/.decision-os/thread-images/...`.
2. **New uploads:** Image upload responses now return root-relative `/.decision-os/thread-images/...` references.
3. **Preserved paths:** External HTTP URLs and existing `/p/...` URLs remain unchanged.

---

## B. Verified Evidence

1. **Resolver tests:** Legacy relative, root-relative, cross-origin, and already scoped cases passed.
2. **Backend integration:** Upload response, Markdown, disk persistence, and asset delivery assertions passed.
3. **Checks:** Frontend and backend typechecks passed.
4. **Live delivery:** The active project route served the updated resolver, and both operator image assets returned `200 image/png` from canonical project-scoped URLs.
