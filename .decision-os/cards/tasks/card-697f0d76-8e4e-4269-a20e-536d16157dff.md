#task  #verification

## A. Scope

1. **Run** focused backend and mobile automated checks.
2. **Verify** the complete flow on the served `/projects` surface with mobile Chromium under the repository runbook.
3. **Commit and merge** the isolated feature work.

---

## B. Acceptance Criteria

1. **Record** the served route, HTTP result, creation behavior, persisted state after reload, and validation error behavior.
2. **Commit** focused implementation changes and merge them into `main` with a merge commit.
3. **Remove** the temporary worktree and merged feature branch.
