## A. Regression coverage

1. Added the runtime wiring regression to `frontend/test-responsive/mobile-control-room.test.mjs`.
2. Added `tests/browser/control-room/the-column-scroll-position-survives-refresh-and-navigation.spec.ts` with independent scroll, task refresh, in-app return, browser Back, and full-reload coverage.
3. Frontend typecheck passed.
4. Frontend suite passed `442/442` and backend suite passed `188/188` when the worktree TypeScript config was explicitly scoped.
5. The full browser run passed `173/174`; the unrelated queue-drag lifecycle test reproducibly fails its existing post-reload order assertion with `Beta, Alpha, Gamma` while its persisted rank check expects `Alpha, Beta, Gamma`. The new column-scroll browser test passes.

---

## B. Delivery evidence

1. Feature commit: `e79167b8`.
2. Main merge commit: `20470d4c`.
3. Existing operator route `http://127.0.0.1:50151/` returned HTTP `200` without a server restart.
4. The served `application.js` contains `controlRoomColumnScrollTop`, `captureControlRoomColumnScroll()`, and `restoreControlRoomColumnScroll()`.
