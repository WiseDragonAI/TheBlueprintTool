## A. Implemented

1. **Federation scope:** replica events reconcile only the active project card route and no longer reapply card-entry presentation.
2. **Thread scope:** refresh responses prove project, ledger, card, thread, panel, and generation ownership after every await.
3. **Lifecycle:** thread SSE opens with the panel, closes with the panel, and ignores events without the exact active `threadId`.
