#master-task #task-complete

Ledger: Specs
Waiting since: 2026-07-14T15:44:09.589Z
Active since: 2026-07-14T16:12:12.419Z
Completed at: 2026-07-14T16:31:34.042Z

## A. Requested outcome

1. **Replace the Control Room `Done` tab with `Delayed`.**
2. **Allow a master task to move into the backlog and return to Queue.**
3. **Reuse the persisted card `status` field and the existing ledger mutation endpoint.**

---

## B. Verified design

1. Persist `delayed` beside `todo` and `done` in the card status field.
2. Keep `processing` derived from the current Codex run and keep completed cards persisted as `done`.
3. Derive Queue from `todo`, Active from queued/running Codex state, and Delayed from `delayed`.
4. Expose `Move to backlog` on queued master tasks and `Restore to queue` on delayed master tasks.

---

## C. Acceptance evidence

1. Server mutation tests prove `todo → delayed → todo` persistence and rejection of unsupported statuses.
2. Control Room tests prove the third tab, route, count, empty state, task grouping, and backlog/restore mutation payloads.
3. The served mobile Control Room route returns HTTP 200 and loads the changed assets.

---

## D. Subtasks

1. [Extend persisted card status with delayed](card:card-f499fa82-da9d-4635-ab4b-b6c9b4ecf97f) — Status: complete
2. [Replace Done tab and add master-task backlog controls](card:card-96c89483-4652-45d4-942f-c1df28322cfa) — Status: complete
3. [Verify delayed workflow on the served target](card:card-ddfeb9bd-5150-4f0d-9ed7-0b858b2ec462) — Status: complete
---

## E. Retrospective

1. **Verified outcome:** commits `8068b37` and `c105660`, merged by `29b38c9` and `15fe805`, implement persisted `delayed` status, the Delayed Control Room view, and `Move to backlog` / `Restore to queue` controls through the existing mutation endpoint.
2. **Acceptance evidence:** 40 mobile tests, 8 desktop status tests, the focused server mutation test, and both TypeScript checks passed; Chromium observed the Delayed route and master-task route at HTTP `200` with the changed UI and no runtime error.
3. **Corrections:** isolated-worktree backend test launches initially failed from missing package resolution, then passed from the backend package context; the initial `Park task` label was corrected to the operator-required `Move to backlog`.
4. **Known limit:** the unrestarted server prevented a full served Move → reload → Restore persistence gesture; endpoint tests prove persistence and invalid-status rejection, satisfying the master acceptance criteria.
5. **Saved lessons:** centralized code memory records `13` and `14` cover authoritative status validation and separate frontend/backend runtime-freshness checks.