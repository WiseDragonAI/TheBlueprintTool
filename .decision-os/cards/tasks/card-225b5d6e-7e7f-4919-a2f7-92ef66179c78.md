## A. Implemented Result

1. **Renderer:** `renderControlRoom()` consumes the presentation helper and assigns only the project name as visible text.
2. **Marker:** Remote projects receive one aria-hidden inline SVG positioned at the chip top-right.
3. **Foreground:** The computed `--project-foreground` drives both label and icon through `currentColor`.
4. **Behavior:** Tooltip owner metadata, selection updates, `aria-pressed`, and offline disabling are unchanged.

---

## B. Verification

1. **Renderer contract:** The focused integration regression passes.
2. **Styles:** The chip uses the computed foreground variable, and the marker inherits it.
