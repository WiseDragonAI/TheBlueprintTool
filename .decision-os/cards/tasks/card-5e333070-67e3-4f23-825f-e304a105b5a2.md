## A. Implemented Result

1. Added responsive card-route routing for `A`, `X`, and `Escape`.
2. First `A` opens the selected task thread; second `A` focuses `.thread-draft`.
3. `X` opens the thread and starts voice capture; an active capture is stopped through the existing voice controller.
4. `Escape` cancels active voice capture before closing the thread.
5. The shared editable-target guard now suppresses global commands in inputs, textareas, selects, and contenteditable elements without changing the canvas selection-command router.

---

## B. Verification

1. Served keyboard sequence passed: `A` open, second `A` focus, `X` suppressed while the draft owned focus, `X` recording after blur, first `Escape` voice cancel, second `Escape` thread close.
2. Focused tests passed `11/11`, including editable-target and retained canvas `Delete`, `Ctrl+C`, `Ctrl+V`, and `Ctrl+D` routing assertions.
