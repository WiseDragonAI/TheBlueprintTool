## A. Corrected projection

1. Preserved normalized operator labels across local and federated tasks.
2. Added canonical `completedAt` and `completedTime` from each master card's `Completed at` Markdown field.
3. Published the contract as `control-room-v14-completion-time`.

---

## B. Verification

1. Local HTTP projection tests prove completion timestamps in `done`.
2. Federation tests prove labels and completion timestamps survive authority selection.
3. Focused backend tests passed `13/13`; backend typecheck and all `263` backend tests passed.
