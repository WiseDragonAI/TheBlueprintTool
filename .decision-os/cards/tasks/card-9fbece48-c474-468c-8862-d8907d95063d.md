## A. Implemented Shared Editor

1. **Canvas dialog:** The wide projects canvas now mounts the project settings and HSV color-picker dialogs in `frontend/index.html`.
2. **Shared contract:** `frontend/src/runtime/project/effect/project-settings-modal.ts` reuses `projectSettingsValues`, `saveProjectSettingsRequest`, and the responsive color-picker helpers.
3. **Persistence:** Saving uses `PATCH /decision-os/projects/:projectId`, keeps rejected edits inside the open dialog with the server error, and reloads the projects canvas after success.
4. **Geometry:** Reload reconciliation consumes the server projects-canvas document, whose existing project-card geometry remains authoritative.

---

## B. Verification

1. **Focused checks:** Project settings, canvas routing, and ledger-card rendering tests passed: `27/27`.
2. **Typecheck:** Frontend TypeScript completed with exit code `0`.
3. **Browser:** On `http://127.0.0.1:50151/projects`, a mocked rejected PATCH kept the dialog open with `Browser rejection proof.`; the subsequent real save closed the dialog and server-confirmed values survived reload.
