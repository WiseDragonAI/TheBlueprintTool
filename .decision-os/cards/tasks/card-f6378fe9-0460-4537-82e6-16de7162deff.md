#task

## A. Implementation

1. **Move CLI:** Add `tool/memory/memory.mjs` to the Decision OS repository and remove the home-level copy only after migration verification.
2. **Central database:** Resolve `<server-launch-root>/.decision-os/memories.sqlite3` from `--root`.
3. **Schema migration:** Add `project_id` and `type`, migrate the existing record as `ZGVjaXNpb24tb3M / code`, and update the composite upsert key.
4. **Commands:** Support `add`, `list`, `search`, and `migrate` with project and type filters.
5. **Provisioning:** Initialize the central database from the Decision OS server launch root.

---

## B. Verification

1. **CLI tests:** Cover add, upsert, list, search, project filtering, type filtering, and global records.
2. **Migration test:** Preserve row content and prove idempotent migration.
3. **Server test:** Launch against a temporary catalog root and confirm the central database is created once.
4. **Integrity:** Verify `PRAGMA quick_check=ok` and matching source and destination row counts before source deletion.

---

## C. Acceptance Criteria

1. **Paths:** The repository owns the tool at `tool/memory/memory.mjs`; the launch root owns data at `.decision-os/memories.sqlite3`.
2. **Safety:** `$HOME/.codex/memories_1.sqlite` is untouched.
3. **Handoff:** Update the retrospective skill draft to the new CLI contract and commit the implementation.

---

## D. Delivery Evidence

1. **Implementation:** Added the central schema, repository CLI, migration path, launch-root provisioning, project and type filters, global lessons, documentation, and server integration.
2. **Tests:** `5` memory/CLI tests and `5` backend/server tests pass; the latter includes the catalog integration and temporary launch-root provisioning. `tsc --noEmit` and `git diff --check` pass.
3. **Migration:** Migrated `1` legacy row twice idempotently to `$HOME/.decision-os/memories.sqlite3`; exact content and timestamps match and `PRAGMA quick_check=ok`.
4. **Cleanup:** Removed `$HOME/.codex/memories.sqlite3` and `$HOME/tool/memory/memory.mjs`; preserved `$HOME/.codex/memories_1.sqlite`.
5. **Commit:** Feature commit `3c78340`; merge commit `9e652c2` on `main`.
