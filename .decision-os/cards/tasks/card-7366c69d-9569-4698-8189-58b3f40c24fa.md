Ledger: Specs

## A. Implemented Result

1. **Queue authority:** `waitingTime` is now the only Queue ordering input; newest timestamps sort first.
2. **Queue ranks removed:** backend projection, frontend parsing, mutation types, rank writes, and rank persistence were removed.
3. **Legacy data:** existing `Queue rank` lines are ignored by projections and hidden from visible master-task content.
4. **Delivery:** commits `c142bf3b` and `d757f6f5` were merged into `main` by `5e460940` and `4c047dff`.

---

## B. Runtime Behavior

1. **Hydration:** the served frontend sorts every `/api/control-room` Queue response through `compareControlRoomQueueTasks()` before rendering, so the current server does not require an unauthorized restart for this correction.
2. **Interaction:** an in-Queue pointer or touch drop returns immediately to timestamp order and emits no Queue-order PATCH.
3. **Placement:** Queue and Backlog status movement retains the existing SortableJS `1.15.7` MIT interaction and status mutation path.

---

## C. Verification

1. **Focused backend:** `15/15` comparator, projection, and federation tests passed.
2. **Focused frontend:** `51/51` responsive Control Room tests passed.
3. **Served browser:** the Queue remained `Gamma`, `Beta`, `Alpha` from newest to oldest despite conflicting legacy ranks; pointer and touch reordering emitted no rank mutation; reload preserved the order.
4. **Typechecks:** backend and frontend TypeScript checks passed.
5. **Frontend suite:** `477/477` tests passed.
6. **Live route:** `http://127.0.0.1:50151/` returned `200`; the served application contains the hydration sort, and applying it to the live API placed the newest waiting timestamps first.
7. **Unrelated suite state:** broader backend and browser runs retained failures in pipeline catalog, reusable pipeline, column-scroll, project-label, and thread-timing tests; the focused Queue scopes passed.

---
