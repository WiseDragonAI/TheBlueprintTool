## A. D. Implemented flow

1. A 60×60 bottom-right floating microphone action now appears on cards labeled `master-task` and `subtask`.
2. The action opens the established thread panel and starts the established recorder immediately.
3. The shortcut stop path forces `queueCodex=true`; accepted uploads close the thread and navigate to `/?tab=queue`.
4. Failed uploads remain in the thread with the existing retained-audio retry state.

---

## B. E. Verification evidence

1. Commit: `db6fbbf` merged by `e0e5d58`.
2. Focused quick-voice tests: **2 passed**.
3. Voice-transcription runtime tests: **13 passed** in the focused run.
4. Frontend typecheck: **passed**.
5. Served route: HTTP **200** at the canonical master-task detail URL.
6. Mobile Chromium at 390×844 observed the visible 60×60 action, thread opening, recording state with `SEND`, upload field `queueCodex=true`, thread dismissal, and final URL `http://127.0.0.1:50150/?tab=queue` using mocked media and upload acceptance.
7. Full frontend baseline: **387 passed, 13 failed**. The failures are outside this change: missing legacy canvas compatibility assets, absent pinned vendor references in the isolated base revision, and one unrelated refresh timing test.

---

## C. F. Operator validation

1. On a phone, open one master task, tap the bottom-right microphone, record a short comment, then tap `SEND`.
2. Confirm the app returns to Control Room → Queue and Codex starts after transcription.

---
