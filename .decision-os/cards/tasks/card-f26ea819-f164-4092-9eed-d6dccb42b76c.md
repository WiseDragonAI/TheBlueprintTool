## A. Result

1. Canonical URLs stay `/p/P/...`; `?replica=N` selects a node.
2. Requests send `x-decision-os-replica-node`; synchronization sends separate project and node IDs.

---

## B. Proof

1. Request tests and HTTP integration prove remote mutation persists after reload.
