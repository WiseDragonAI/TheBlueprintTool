## A. Result

1. Cards group by `projectId + ledgerId + cardId`.
2. Authority is deterministic by node ID; persisted disagreement remains explicit.

---

## B. Proof

1. Tests prove ledger isolation and identical placement from reversed observers.
