## A. Regression coverage

1. Removed the assertion requiring quick capture to force `run`.
2. Added assertions that the voice-stop handler passes `button.dataset.launchMode` unchanged.
3. Added a negative assertion preventing reintroduction of the `quickVoiceCapture ? 'run' : launchMode` override.
4. Preserved checks for explicit `SEND`, `RUN`, and `PIPELINE` controls.

---

## B. Verification

1. Focused checks: **5 passed**.
2. Voice lifecycle checks: **17 passed**.
3. Full frontend suite: **366 passed, 0 failed**.
4. Live workstation `SEND` path verified the request fields and unchanged card route.
