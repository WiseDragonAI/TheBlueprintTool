## A. Audit Scope

1. **Audience** `is` the operator deciding whether to authorize the minimal frontend correction.
2. **Purpose** `is` to preserve the complete causal decision while removing repeated pipeline history, duplicated proof, and delivery detail from the master-task executive summary.
3. **Required output** `is` one retention map, one deletion map, the remaining decision-relevant uncertainty, and one concise executive-summary structure.
4. **Source boundary** `is` master task `card-93a99f92-5584-43f5-a933-2208303997ad`, its full thread, and substantive analysis cards `card-codex-skill-1785414681345-43f7406f`, `card-codex-skill-1785416949523-c96cdd1d`, `card-codex-skill-1785417406472-707150d4`, `card-codex-skill-1785417821940-9d3f5f27`, `card-codex-skill-1785418331446-317e06b8`, `card-codex-skill-1785419329615-03bc7700`, and `card-codex-skill-1785419833752-4ebbd8b0`.

---

## B. Exact Retention Map

1. **Observed failure and stable identity:** retain master `A.1-A.3`; these items establish that one optimistic same-ID voice note remains `uploading` after the backend has durably advanced and delivered its terminal transcription.
2. **First wrong transition and reload explanation:** retain master `A.4-A.5`; `{ ...incoming, ...localNote }` in `mergeLocalThreadNotes()` reinstalls the stale optimistic record, while reload succeeds because that in-memory overlay is absent.
3. **Constrained branch predicate:** retain `Constrain Same-ID Voice Merge`, `D.3`; admission requires an incoming thread slice, a same-ID optimistic local note, non-empty `localVoiceUploadId`, and acceptance by `shouldApplyVoiceServerNote()`.
4. **Accepted state rule:** retain master `B.3` and `Minimal Voice Merge Proof Package`, `D.1`; the incoming note remains lifecycle authority, only merged `imageSizes` plus `localVoiceUploadId` remain local, and `optimistic` becomes `false`.
5. **Rejected and non-voice rules:** retain master `B.4`; a backward voice snapshot keeps the current overlay, and non-voice optimistic notes keep existing precedence.
6. **Shared boundary:** retain master `B.5`; one branch in `mergeLocalThreadNotes()` corrects both `loadActiveThreadSlice()` and `reconcileActiveLedgerState()` without changing backend, transport, persistence, polling, rendering, component, schema, and dependency boundaries.
7. **Forward automated proof:** retain `Minimal Voice Merge Proof Package`, `D.2`; one active-thread regression proves same-row durable acceptance, representative durable fields, local cleanup ownership, measured image sizing, terminal presentation, and unchanged text-note precedence.
8. **Backward automated proof:** retain `Minimal Voice Merge Proof Package`, `D.3`; amend the existing full-ledger regression so a larger-revision incoming `uploading` snapshot cannot replace a newer local terminal voice state.
9. **Interaction gate:** retain master `C.4`; after implementation, one real microphone note must advance on the same row without reload and remain durable after reload.
10. **Proof and authorization boundary:** retain master `C.5` plus `D.6`, compressed to one sentence: analysis is complete, product state is unchanged, and implementation requires explicit authorization.

---

## C. Exact Deletion Map

1. **Master `D.1-D.5` — `process-noise`:** rewrite the section as the single authorization boundary retained in `B.10`; precheck status, skipped-skill reasoning, queue order, synthesis narration, and stop narration do not change the product decision.
2. **Master thread gate replies beginning `Gate Decision`, `Handoff Record`, and rolling-plan updates — `redundant`:** exclude them from the executive summary; they repeat sequencing history already preserved in the immutable thread and add no causal evidence.
3. **`Voice Note Live Merge Root Cause`, `A.1-A.3`, `C.2`, and `D.1-D.2` — `redundant`:** merge their repeated backend-to-refresh-to-merge chain into the retained five-item failure mechanism; keep file and symbol references in the analysis card as evidence, not in the executive summary.
4. **`Voice Lifecycle Merge Authority Gaps`, `B.1-B.6` and `C.1-C.6` — `misplaced`:** move the linked-spec inventory and missing-spec taxonomy out of the executive summary; the retained state rules already resolve the implementation decision.
5. **`Voice Lifecycle Merge Authority Gaps`, `A.6` — `over-specific`:** delete the three terminal variants and cleanup-failure permutations from the master summary; they do not exercise distinct merge decisions.
6. **`Voice lifecycle merge task inventory`, `T02` terminal variants and polling-deadline case — `over-specific`:** retain one `transcribed` incident regression; all three terminal labels share the same accepted lifecycle rank, and time is absent from the defective merge.
7. **`Voice lifecycle merge task inventory`, forward full-ledger proof in `T03` — `redundant`:** retain forward acceptance through the incident caller and use the full-ledger caller only for the distinct backward-transition proof.
8. **`Voice lifecycle merge task inventory`, exhaustive durable-field assertions — `over-specific`:** compress to representative durable fields because the accepted result keeps the incoming object and changes only `localVoiceUploadId`, `imageSizes`, and `optimistic`.
9. **`Voice state groups repaired and dispatch-ready`, `B.1-B.5`, `C.1-C.4`, and `D.1-D.5` — `misplaced`:** move worktree, verification, commit, merge, cleanup, push-range, grouping, and ownership controls to the future execution checklist; they do not belong in the causal executive summary.
10. **`Voice Merge Precheck Ready`, `B.1-B.6`, `E.1-E.7`, `F.1-F.3`, and `H.1-H.2` — `process-noise`:** delete available-truth serialization, repository inventory, temporary-record detail, readiness labels, and executor-stack handoff from the summary.
11. **Proof-limit lists repeated in `Voice Note Live Merge Root Cause E.4`, `Constrain Same-ID Voice Merge E.5`, `Voice state groups repaired and dispatch-ready F.3`, `Minimal Voice Merge Proof Package F.4`, and `Voice Merge Precheck Ready C.2` — `redundant`:** merge them into the single boundary sentence retained in `B.10`.
12. **Every repeated list of unchanged subsystems — `redundant`:** keep the boundary once in the selected correction; remove later repetitions naming backend, persistence, transport, polling, rendering, component, schema, dependency, fixture, config, migration, and data backfill.

---

## D. Remaining Decision-Relevant Uncertainty

1. **Causal uncertainty** `is` none: the first incorrect transition, minimal branch, accepted state, rejected state, and preserved non-voice behavior are established by the completed source analysis.
2. **Interaction uncertainty** `is` whether the implemented branch produces the required same-row real microphone transition on the served incident route; only post-implementation operator interaction can resolve it.
3. **Pending decision** `is` explicit operator authorization to implement the three-file change and run its proof package.
4. **Delivery push-range state** `is not` product uncertainty and belongs in the execution checklist at publication time.

---

## E. Proposed Executive-Summary Structure

1. **Failure mechanism:** state the observed stale row, stable note identity, exact local-over-incoming spread, and why reload removes the defect.
2. **Selected correction:** state the constrained voice predicate, incoming-state authority, two retained local fields, backward rejection, unchanged non-voice behavior, and shared merge boundary.
3. **Proof package:** state one forward live-slice regression, one backward full-ledger regression, and one post-implementation real microphone gate.
4. **Decision:** state that analysis is complete, no product state changed, and explicit implementation authorization is pending.

---

## F. Audit Result

1. **Retained decision** `fits` the operator’s requested logical explanation: what fails, why it fails, the smallest correction, the distinct proofs, and what authorization remains.
2. **Removed content** `is` pipeline narration, duplicated causal proof, repeated terminal cases, duplicate caller proof, exhaustive field enumeration, repeated delivery controls, and repeated proof-limit lists.
3. **Result** `preserves` every decision-changing fact without introducing a new framework, product scope, implementation step, and proof claim.
---

Codex run completed: exit code 0
