Completed at: 2026-07-17T13:05:04.651Z

## A. Verified Scope

1. **Observed defect:** The supplied desktop screenshot shows the Control Room ledger chips clipped at the right edge, and the operator reports that a vertical mouse wheel scrolls the page instead of the chip row.
2. **Shared surface:** `frontend/index.html` renders project and ledger selectors as `#control-project-filters.control-filters` and `#control-filters.control-filters`.
3. **Current ownership:** `frontend/assets/application.css` gives `.control-filters` `overflow-x: auto` and hides its scrollbar, while `frontend/src/app/responsive/application.js` creates both chip sets without a wheel listener.
4. **Required behavior:** On desktop, a vertical wheel gesture over an overflowed Control Room filter row must move that row horizontally and suppress page movement only while the row can move in the gesture direction.

---

## B. Task Inventory

| id | type | title | target_files | target_symbols | action | done_when | depends_on |
|---|---|---|---|---|---|---|---|
| T1 | code | Route desktop wheel input into Control Room filter overflow | `frontend/src/app/responsive/application.js`; `frontend/src/app/responsive/control-filter-wheel.js` | Control Room filter initialization; `handleControlFilterWheel`; `bindControlFilterWheelScrolling` | Bind both `.control-filters` elements once. At the desktop breakpoint, convert dominant vertical wheel input over a horizontally overflowed visible row into horizontal movement. Prevent the page default only when the row can move in that direction; preserve native horizontal trackpad input and allow vertical page scrolling for non-overflowing rows and reached boundaries. | With either a long project row or a long ledger row, mouse-wheel input advances hidden chips horizontally without changing page `scrollY`; when the row reaches the requested boundary, continued vertical input is available to the page. | — |
| T2 | test | Cover filter-row wheel ownership and served desktop behavior | `frontend/test-responsive/control-filter-wheel.test.mjs`; `frontend/test-responsive/mobile-control-room.test.mjs`; `tests/browser/application/the-application-is-one-responsive-frontend.spec.ts` | wheel routing geometry cases; Control Room binding assertions; desktop responsive application scenario | Add deterministic cases for overflow, both horizontal boundaries, non-overflow, mobile width, and horizontal trackpad input. Extend the served desktop scenario with enough project and ledger chips to overflow, then dispatch representative wheel input over each row and observe row movement and page position. | Focused checks pass; the served desktop route proves both rows move horizontally under vertical wheel input, page `scrollY` stays fixed while movement is possible, and boundary input is not trapped. | T1 |

---

## C. Open Questions

1. **None.** The screenshot, operator report, shared DOM boundary, current CSS overflow, and missing wheel binding provide enough evidence for a codebase-linked task list.

---

## D. Readiness

1. **READY_FOR_TASK_DEPENDENCY**

---

## E. Subtasks

1. [Route desktop wheel input into Control Room filter overflow](card:card-a8b63a30-7a07-465c-9708-1bd02f349f17)
2. [Cover filter-row wheel ownership and served desktop behavior](card:card-ba46939e-6aeb-4a86-918e-1583297a9bdc)
