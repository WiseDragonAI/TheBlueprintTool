## A. Task Inventory

| id | type | title | target_files | target_symbols | action | done_when | depends_on |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `T01` | `ops` | Protect the metadata implementation checkout | `AGENTS.md`<br>`.worktrees/` | Commit Hygiene<br>Staged Code Protection<br>metadata feature worktree | Create the metadata feature branch in an isolated worktree from the admitted `main` revision; preserve the primary checkout index plus every unrelated dirty path. | The isolated branch contains only the intended metadata iteration, the primary index remains unchanged, and later cleanup evidence identifies removal of the feature worktree plus branch. | None |
| `T02` | `code` | Keep responsive pipeline metadata in the live draft | `frontend/src/app/responsive/codex.js` | `initializeMobileCodex()`<br>`state.editor.name`<br>`state.editor.purpose`<br>`pipeline-name`<br>`pipeline-purpose`<br>`saveEditor()` | Bind each existing metadata control's `input` event to its raw `state.editor` field; retain `saveEditor()` as the sole trimming boundary; leave request, backend, persistence, structure, styling, and collection behavior unchanged. | Code review at `db083e8176728409efcaad9575d85653288f869a` shows both live-draft transitions and no other production-path change. | `T01` |
| `T04` | `test` | Prove metadata retention through step addition and removal | `tests/browser/codex/reusable-step-pipelines.spec.ts` | `Responsive pipeline editing preserves unsaved metadata across step addition and removal`<br>`startDecisionOsServer()`<br>`launchBrowser()` | Exercise `/pipelines` at `390 × 844`; enter unsaved Name plus Purpose; add one step; remove that step; assert both values after each `renderEditor()` transition and capture page-error state. | The named Linux Chromium case exits `0`, both values remain byte-identical after addition plus removal, the route returns HTTP `200`, and no page error is reported. | `T02` |
| `T05` | `ops` | Deliver the focused metadata correction | `frontend/src/app/responsive/codex.js`<br>`tests/browser/codex/reusable-step-pipelines.spec.ts`<br>`.worktrees/` | feature commit `db083e8176728409efcaad9575d85653288f869a`<br>merge `808a16a76f91802e8cab52b0816cc433d849dc45`<br>`main`<br>`origin/main` | Preserve the focused two-path feature commit; integrate it with the required merge message; publish `main`; remove its temporary worktree plus local feature branch without taking unrelated checkout state. | `main` plus `origin/main` resolve to `808a16a76f91802e8cab52b0816cc433d849dc45`, both complete commit messages contain `WHAT:` plus `WHY:`, and the initial temporary worktree plus branch are absent. | `T04` |
| `T06` | `code` | Keep detached lifecycle settlement observable | `backend/src/business/codex/helper/launch-codex-execution-process.ts`<br>`backend/src/business/federation/helper/execute-node-message.ts`<br>`backend/src/business/refresh/helper/watch-project-files.ts` | `launchCodexExecutionProcess()` execution and forced-kill deadlines<br>`executeNodeMessage()` execution and forced-settlement deadlines<br>`watchProjectFiles()` audit owner | Keep the finite Codex execution deadlines, node-message settlement deadlines, and project audit owner referenced until their explicit settlement plus close boundaries; retain existing cancellation, timer clearing, kill escalation, and failure containment. | The three production diffs are the only lifecycle source repairs; focused lifecycle coverage passes `21/21`; pending detached work settles without cancellation leakage. | `T05` |
| `T07` | `test` | Align executor fixtures with the runtime node identity | `backend/test/codex/codex-process-restart-recovery.test.ts`<br>`backend/test/codex/read-card-skill-run-controller.test.ts`<br>`backend/test/codex/resume-codex-pipeline-runs.test.ts`<br>`backend/test/codex/start-card-skill-process-controller.test.ts` | `EXECUTOR_NODE_ID`<br>`taskExecutionNodeId(runtime)`<br>`seedQueuedExecution()`<br>`seedExecution()`<br>restart, read-card, resume, and direct-skill cases | Configure `workstation` before server construction; seed durable executions under `taskExecutionNodeId(runtime)`; assert the same exact execution owner through startup recovery, run reads, pipeline resume, and direct card-skill launch. | All four fixture paths seed plus observe the server-resolved executor identity; the focused executor group passes `19/19`. | `T05` |
| `T08` | `test` | Assert stable pipeline and release fixture contracts | `backend/test/codex/start-codex-pipeline-run-controller.test.ts`<br>`backend/test/server/runtime-failsafe.integration.test.ts` | `saved pipeline is idempotent while active and runs five isolated skills strictly in order`<br>`normal health reports the active release identity` | Assert idempotency through stable run identity without sampling a transient scheduler phase; install delivery protocol plus release settings before server construction; restore environment, server, and temporary files in test cleanup. | The pipeline case passes on stable run identity; the release-health file passes `7/7` with the expected release SHA, protocol, active pointer, and cleanup. | `T05` |
| `T09` | `test` | Isolate authored publication observation | `backend/test/codex/codex-skill-library-routes.test.ts` | `server startup initializes a child repository and pipeline-prompt save never enters federation publication`<br>`federationRequestPaths`<br>`saved.publication.status` | Return valid empty peer snapshots; assert publication `not-applicable`; exclude only the force-refresh `skills-manifest?refresh=1` request that identifies authored publication while admitting automatic library synchronization traffic. | The skill-library file passes `13/13`; the case observes no authored force-refresh request and accepts valid background synchronization. | `T05` |
| `T10` | `test` | Isolate the reusable-pipeline browser fixture and save contract | `tests/browser/codex/reusable-step-pipelines.spec.ts` | `createFixture()`<br>`createPipelineAndSkillDefaults()`<br>`Reusable step pipelines preserve defaults and publish visible execution progression` | Give the temporary server empty relay plus federation settings, unique `browser-fixture` identity, and no production credential; assert save publication `not-applicable`, a `40`-character Git revision, and the visible new-revision success state. | The browser file passes `4/4` with `/snap/bin/chromium`; the fixture cannot attach to operator-facing peers; the reusable-pipeline case observes the current save contract. | `T05` |
| `T11` | `ops` | Preserve canonical green verification evidence | `bin/decision-os-verify.mjs`<br>`package.json` | repository verification lease<br>`test:front-back`<br>frontend and backend typechecks | Attribute the focused results to `T06` through `T10`; preserve the completed lease-wrapped canonical result without rerunning green verification. | Captured exit code is `0`: frontend `610/610`, backend `661/661`, browser `182/187` with `5` intentional skips, `0` failures, and `0` cancellations; `git diff --check` is recorded as passing. | `T06`, `T07`, `T08`, `T09`, `T10` |
| `T12` | `ops` | Commit the exact verified repair boundary | `.worktrees/pipeline-metadata-run-test-fix-1785426175338/` | `codex/pipeline-metadata-run-test-fix-1785426175338`<br>`T06` through `T10` tracked outputs<br>repair commit | Compare the worktree against `808a16a76f91802e8cab52b0816cc433d849dc45`; stage only the three production paths, seven backend test paths, and one browser path owned by `T06` through `T10`; exclude `.decision-os/runtime-incidents.json`; create one focused commit with verified `WHAT:` plus `WHY:` paragraphs. | The repair commit contains exactly `11` tracked paths with the verified `124` insertions plus `52` deletions; its full message satisfies the repository contract; runtime incident evidence plus unrelated files remain outside the index. | `T11` |
| `T13` | `ops` | Integrate the repair without taking checkout collisions | `AGENTS.md`<br>`.git/`<br>`.worktrees/pipeline-metadata-run-test-fix-1785426175338/` | primary `main` checkout<br>repair commit<br>merge commit<br>nine overlapping primary paths | Snapshot the primary staged plus unstaged state; merge the focused repair commit with the required merge message; resolve integration from the committed repair boundary while preserving all pre-existing primary bytes outside that boundary. | The merge tree contains the exact `T12` repair; the nine pre-existing overlapping primary changes plus every unrelated dirty path remain preserved; no protected staged hunk is modified; the merge message contains `WHAT:` plus `WHY:`. | `T12` |
| `T14` | `docs` | Record final repair and delivery evidence | `.decision-os/cards/tasks/card-27ec1923-e426-423e-af78-459051fc4ee2.md`<br>`.decision-os/threads/tasks/thread-card-27ec1923-e426-423e-af78-459051fc4ee2.md` | source-task progress<br>English agent note<br>Decision OS child-repository revision | Through the scoped ledger command boundary, record the five repair groups, exact `11` paths, focused results, canonical totals, repair commit, merge commit, preserved incident evidence, and unchanged live-server state; retain the master task as active. | The source card plus thread contain one concise evidence-backed closeout update; the child repository records the intended local content revision; no ledger JSON, runtime state, and incident file is edited manually. | `T13` |
| `T15` | `ops` | Publish main and clean the repair branch | `.worktrees/pipeline-metadata-run-test-fix-1785426175338/`<br>`AGENTS.md` | `origin/main`<br>`codex/pipeline-metadata-run-test-fix-1785426175338`<br>Wise SSH identity | Push the integrated `main` revision to `origin` with the Wise SSH key; prove the remote ref; remove the repair worktree; delete the merged local repair branch; preserve the local Decision OS child-repository history plus runtime incident evidence. | `origin/main` matches the verified merge; the repair worktree plus local branch are absent; the primary dirty state, child-repository history, `.decision-os/runtime-incidents.json`, and live server remain preserved. | `T14` |

---

## B. Open Questions

1. **Blocking questions:** None.

---

## C. Readiness

1. **Status:** `READY_FOR_TASK_DEPENDENCY`
---

Codex run completed: exit code 0
