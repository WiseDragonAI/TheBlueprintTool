## A. Retrospective result

1. **Requested outcome:** Keep independent runtime-memory scroll positions for `Queue`, `Exec`, and `Backlog` through Control Room rerenders, task refreshes, task-detail return, and browser Back; reset them only on full page reload.
2. **Delivered result:** Commit `e79167b8`, merged by `20470d4c`, captures each visible column before node replacement, restores it on the next animation frame, clamps it to the replacement range, and prevents hidden lists from overwriting remembered values with `0`.
3. **Verification:** The focused served browser regression preserved `queue: 280`, `exec: 330`, and `backlog: 450` through refresh and both return paths, then observed all three reset to `0` after full reload.
4. **Operator feedback:** The operator requested complete execution after intake. No later operator correction identified incorrect or incomplete behavior.

---

## B. Durable lesson saved

1. **Record `1` — Preserve scroll state across DOM replacement:** When preserving scroll across rerenders, capture only visible connected scroll owners before replacement, then restore each independent value after layout and clamp it to the new range. In commit `e79167b8`, hidden Control Room lists reported zero during `loadRoute()`, while next-frame restoration preserved three columns through refresh and navigation.
2. **Classification:** `code`; tag `frontend-state`; subtag `scroll-restoration`.
3. **Source:** `e79167b8 20470d4c codex-skill-1784287203094-029915af codex-skill-1784291353486-1830b14d`.
4. **Duplicate check:** Both memory searches returned no matching records before this lesson was added.

---

## C. Closure

1. **Gate:** Ready with no discrepancies; all three canonical subtasks were already `done`.
2. **Canonical completion:** `ledger-cli master-task-complete` completed master card `card-dc631595-1c61-4852-88d1-bf6ef845ea11` exactly once.
3. **Completion commit:** `27d8b5971c75dcc34fdbf64ec635ced72912974d`.
4. **Final state:** The master task and its canonical subtasks are `done`.
---

Codex run completed: exit code 0
