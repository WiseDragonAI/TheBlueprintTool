## A. Requirement

1. **Canonical parsing:** Recognize `#master-task` and task-status labels only when they occur on canonical label-only Markdown lines.
2. **Literal safety:** Ignore backticked tag examples and task-label text embedded in ordinary specification prose.
3. **Diagnostic safety:** Do not classify ordinary cards as malformed master tasks from literal examples.

---

## B. Completion Evidence

1. **Status:** `complete`.
2. **Commit:** `f1ef665` (`fix(mobile): wire active master task runs`).
3. **Verification:** The source thread records the corrected parser behavior and passing mobile tests.
