## A. Scope

Replace the inactive plain-text editing path with one reusable CodeMirror 6 adapter in a modal sized `80vw` by `95vh`. Pin and locally bundle the MIT-licensed editor packages. Include Markdown syntax, undo and redo, search, keyboard shortcuts, dirty state, save state, and accessible focus handling.

---

## B. Reuse Boundary

Keep file loading and persistence outside CodeMirror. The adapter accepts content, language, filename, revision, toolbar actions, and completion callbacks. Add a code comment that the same editor is intended for future thread-attachment click-to-edit behavior; that attachment feature is excluded now.

---

## C. Acceptance

1. Clicking a skill file opens the modal.
2. Cancel and Escape protect unsaved work.
3. Saving uses the application API and revision token.
4. Browser evidence verifies focus, typing, search, undo, save, close, and responsive dimensions on the served Skills route.
