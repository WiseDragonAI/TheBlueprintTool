## A. Dependency Graph

| from_task | to_task | edge_type | reason | evidence |
| --- | --- | --- | --- | --- |
| `T01` | `T02` | `hard-blocker` | The metadata source correction must be created in the protected feature worktree. | `T02.depends_on = T01`; `T01` owns worktree isolation and protection of the primary index. |
| `T02` | `T04` | `hard-blocker` | The responsive regression exercises the live-draft transitions introduced by the source correction. | `T04.depends_on = T02`; the named case asserts `state.editor.name` and `state.editor.purpose` retention after both `renderEditor()` transitions. |
| `T04` | `T05` | `hard-blocker` | Focused delivery requires the browser-proven source and regression pair. | `T05.depends_on = T04`; feature commit `db083e8176728409efcaad9575d85653288f869a` and merge `808a16a76f91802e8cab52b0816cc433d849dc45` contain that pair. |
| `T05` | `T06` | `hard-blocker` | Lifecycle repair was performed against the delivered metadata baseline. | `T06.depends_on = T05`; the repair worktree is based on `808a16a76f91802e8cab52b0816cc433d849dc45`. |
| `T05` | `T07` | `hard-blocker` | Executor fixture repair was performed against the delivered metadata baseline. | `T07.depends_on = T05`; its four fixture paths belong to the post-merge repair worktree. |
| `T05` | `T08` | `hard-blocker` | Stable pipeline and release fixture repair was performed against the delivered metadata baseline. | `T08.depends_on = T05`; its two fixture paths belong to the post-merge repair worktree. |
| `T05` | `T09` | `hard-blocker` | Authored publication observation repair was performed against the delivered metadata baseline. | `T09.depends_on = T05`; its skill-library fixture belongs to the post-merge repair worktree. |
| `T05` | `T10` | `hard-blocker` | Browser fixture isolation and save-contract repair were performed after focused metadata delivery. | `T10.depends_on = T05`; its browser fixture belongs to the post-merge repair worktree. |
| `T04` | `T10` | `shared-file-risk` | Both tasks edit the reusable-pipeline browser file, so the later fixture repair must retain the completed metadata regression. | Both tasks target `tests/browser/codex/reusable-step-pipelines.spec.ts`; the final browser file passes all `4/4` cases, including metadata retention. |
| `T06` | `T11` | `hard-blocker` | Canonical evidence must include the lifecycle source repairs and focused lifecycle result. | `T11.depends_on` includes `T06`; lifecycle coverage is `21/21`. |
| `T07` | `T11` | `hard-blocker` | Canonical evidence must include exact executor ownership across all four fixtures. | `T11.depends_on` includes `T07`; the executor group is `19/19`. |
| `T08` | `T11` | `hard-blocker` | Canonical evidence must include the stable pipeline and release fixture contracts. | `T11.depends_on` includes `T08`; the corrected release file is `7/7`. |
| `T09` | `T11` | `hard-blocker` | Canonical evidence must include publication-specific observation. | `T11.depends_on` includes `T09`; the skill-library file is `13/13`. |
| `T10` | `T11` | `hard-blocker` | Canonical evidence must include the isolated browser fixture and current save contract. | `T11.depends_on` includes `T10`; the Linux browser file is `4/4`. |
| `T11` | `T12` | `hard-blocker` | The repair commit may contain only the paths proven by the completed green verification. | `T12.depends_on = T11`; the worktree diff is exactly `11` tracked paths with `124` insertions and `52` deletions. |
| `T12` | `T13` | `hard-blocker` | Collision-safe integration requires an immutable focused repair commit. | `T13.depends_on = T12`; the primary checkout currently has nine overlapping modified paths and no staged paths. |
| `T13` | `T14` | `hard-blocker` | Final reporting requires the verified repair and merge identities plus preserved checkout evidence. | `T14.depends_on = T13`; its `done_when` requires repair commit, merge commit, incident preservation, and unchanged live-server state. |
| `T14` | `T15` | `hard-blocker` | Publication and cleanup follow the scoped Decision OS evidence update. | `T15.depends_on = T14`; remote-ref proof and branch cleanup require the reported integrated revision. |

---

## B. Independent Task Groups

| group_id | task_ids | target_files | target_symbols | independence_reason | dispatch_notes |
| --- | --- | --- | --- | --- | --- |
| `G01` | `T01`, `T02`, `T04`, `T05` | `AGENTS.md`<br>`.worktrees/`<br>`frontend/src/app/responsive/codex.js`<br>`tests/browser/codex/reusable-step-pipelines.spec.ts` | metadata feature worktree<br>`initializeMobileCodex()`<br>`state.editor.name`<br>`state.editor.purpose`<br>responsive retention case<br>feature commit and merge | These tasks form one completed source-test-delivery chain and share the feature worktree plus browser file. | Completed at merge `808a16a76f91802e8cab52b0816cc433d849dc45`; do not redispatch. |
| `G02` | `T06` | `backend/src/business/codex/helper/launch-codex-execution-process.ts`<br>`backend/src/business/federation/helper/execute-node-message.ts`<br>`backend/src/business/refresh/helper/watch-project-files.ts` | execution and forced-kill deadlines<br>node-message execution and forced-settlement deadlines<br>project audit owner | Its three production paths do not overlap the fixture-only paths in `G03` through `G06`. | Completed repair group; preserve these three paths as one lifecycle ownership change. |
| `G03` | `T07` | `backend/test/codex/codex-process-restart-recovery.test.ts`<br>`backend/test/codex/read-card-skill-run-controller.test.ts`<br>`backend/test/codex/resume-codex-pipeline-runs.test.ts`<br>`backend/test/codex/start-card-skill-process-controller.test.ts` | `EXECUTOR_NODE_ID`<br>`taskExecutionNodeId(runtime)`<br>`seedQueuedExecution()`<br>`seedExecution()` | Its four executor fixture paths are disjoint from `G02`, `G04`, `G05`, and `G06`. | Completed repair group; preserve exact runtime-node seeding and assertions. |
| `G04` | `T08` | `backend/test/codex/start-codex-pipeline-run-controller.test.ts`<br>`backend/test/server/runtime-failsafe.integration.test.ts` | stable saved-run identity case<br>active release identity case | Its pipeline and release fixture paths are disjoint from the other repair groups. | Completed repair group; preserve startup setting order, stable identity assertion, and cleanup. |
| `G05` | `T09` | `backend/test/codex/codex-skill-library-routes.test.ts` | pipeline-prompt save case<br>`federationRequestPaths`<br>`saved.publication.status` | Its authored-publication fixture is a single disjoint backend test path. | Completed repair group; preserve the force-refresh-specific publication observation. |
| `G06` | `T10` | `tests/browser/codex/reusable-step-pipelines.spec.ts` | `createFixture()`<br>`createPipelineAndSkillDefaults()`<br>reusable-pipeline progression case | Its repair target is disjoint from `G02` through `G05`; its earlier collision with `G01` is already ordered by `T05`. | Completed repair group; preserve the metadata regression while retaining isolated federation settings and the current save assertion. |
| `G07` | `T11` | `bin/decision-os-verify.mjs`<br>`package.json` | repository verification lease<br>`test:front-back`<br>frontend and backend typechecks | This group consumes the completed outputs of all five repair groups and changes no repair path. | Completed evidence group; retain the captured green result and do not rerun verification. |
| `G08` | `T12`, `T13`, `T14`, `T15` | `.worktrees/pipeline-metadata-run-test-fix-1785426175338/`<br>`.git/`<br>`AGENTS.md`<br>source task card and thread | repair commit<br>primary `main` merge<br>nine overlapping primary paths<br>Decision OS evidence revision<br>`origin/main`<br>repair worktree and branch | These tasks share one mutable Git, worktree, publication, and cleanup lifecycle; splitting them across concurrent workers would make preservation and cleanup evidence unsafe. | Remaining delivery group. Use one owner and execute `T12`, `T13`, `T14`, then `T15`; implementation-commit must consume the relationship-backed inventory, this graph, and the next implementation-orchestrator result. |

---

## C. Sequential Gates

1. **`G01` to `G02`–`G06`:** admit the repair groups only from delivered baseline `808a16a76f91802e8cab52b0816cc433d849dc45`, with the focused metadata source and browser regression preserved.
2. **`G02`–`G06` to `G07`:** the union must contain exactly the three lifecycle source paths, seven backend fixture paths, and one browser fixture path; focused results must be `21/21`, `19/19`, `7/7`, `13/13`, and `4/4`.
3. **`G07` to `G08`:** admit delivery only with captured canonical exit `0`, frontend `610/610`, backend `661/661`, browser `182/187` with five intentional skips, zero failures, zero cancellations, and passing `git diff --check`.
4. **Relationship gate before `G08`:** implementation-orchestrator must preserve the one-to-one mapping from `T01`, `T02`, `T04`, and `T05` through `T15`, retain the five repair groups, and hand the exact `11`-path boundary to implementation-commit.
5. **Internal `G08` gate:** create the exact `T12` repair commit before the `T13` merge; finish the collision-safe merge before the `T14` ledger report; record the integrated revision before the `T15` push, remote proof, worktree removal, and local branch deletion.

---

## D. Collision Risks

1. **Shared browser path:** `T04` and `T10` both own `tests/browser/codex/reusable-step-pipelines.spec.ts`; the completed metadata retention case and the later isolated save-contract fixture must remain in the same final file.
2. **Nine primary-checkout overlaps:** current read-only status confirms modifications in all three `T06` production paths, all four `T07` fixture paths, `backend/test/server/runtime-failsafe.integration.test.ts` from `T08`, and the `T10` browser path. `T13` must preserve their pre-existing primary bytes and must not alter a protected staged hunk.
3. **Exact repair boundary:** the repair worktree contains exactly `11` tracked modified paths: three from `T06`, four from `T07`, two from `T08`, one from `T09`, and one from `T10`. Concurrent staging would make `T12` ownership unverifiable.
4. **Shared Git lifecycle:** `T12`, `T13`, and `T15` mutate the repair branch, worktree, primary merge state, remote ref, and cleanup state; they require one sequential owner.
5. **Decision OS evidence:** `T14` updates the source task card and thread through the scoped ledger boundary and records a child-repository revision. Parallel ledger mutation would make the reported integration identity unstable.
6. **Preserved runtime state:** `.decision-os/runtime-incidents.json` is untracked in the repair worktree and excluded from the `11` paths; no delivery action may stage, rewrite, or delete it.
7. **Live service boundary:** the server remained unchanged throughout implementation and verification; delivery has no server restart ownership.

---

## E. Ambiguities

1. **Blocking dependency questions:** None. The replacement inventory has no open question, every active task is placed once, the repair worktree proves the exact `11`-path partition, and the primary checkout proves the nine overlapping paths.

---

## F. Readiness

1. **Status:** `READY_FOR_TASK_GROUP_COMPLETENESS`
---

Codex run completed: exit code 0
