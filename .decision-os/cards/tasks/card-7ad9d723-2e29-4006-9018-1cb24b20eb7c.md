## A. View-state correction

1. The existing client sorter already places records with `favorite: true` first.
2. Both Skills Library and Process Card now receive favorite and tag values from the same backend owner.
3. The existing Markdown renderer, reference cards, bounded scroll view, and bottom actions are unchanged.

---

## B. Presentation evidence

1. Favorite-first ordering and deterministic name ordering passed.
2. Stored-tag presentation and readable skill Markdown coverage passed.
3. Focused presentation result: `3/3`.
