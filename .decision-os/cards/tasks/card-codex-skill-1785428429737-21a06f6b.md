## A. Dependency Graph

1. **Edge basis:** every edge below comes from the populated `depends_on` field in task-list result `card-codex-skill-1785422557631-ffb87aa1`. No inferred edge is added because the later repair work has no task identity.

| from_task | to_task | edge_type | reason | evidence |
| --- | --- | --- | --- | --- |
| `T01` | `T02` | `hard-blocker` | Source editing starts only after the protected worktree exists. | `T01.done_when`; `T02.depends_on`; repository Commit Hygiene |
| `T02` | `T03` | `hard-blocker` | The source-contract assertions require the metadata listeners. | `T03.action`; `state.editor`; metadata inputs |
| `T02` | `T04` | `hard-blocker` | The browser regression requires the metadata transition in `state.editor`. | `T04.action`; `renderEditor()`; `/pipelines` |
| `T03` | `T05` | `test-order-risk` | The original focused-check task requires the source-contract test to pass first. | `T05.depends_on`; `frontend/test-responsive/mobile-codex.test.mjs` |
| `T04` | `T05` | `test-order-risk` | The original focused-check task requires the browser regression to pass first. | `T05.depends_on`; browser regression target |
| `T02` | `T06` | `hard-blocker` | Frontend typecheck follows the production edit. | `T06.depends_on`; `frontend/tsconfig.json` |
| `T05` | `T07` | `test-order-risk` | Repository verification follows the focused regression checks. | `T07.depends_on`; `test:front-back` |
| `T06` | `T07` | `test-order-risk` | Repository verification follows frontend typecheck. | `T07.depends_on`; `typecheck:frontend` |
| `T07` | `T08` | `hard-blocker` | The inventory permits integration only after repository verification passes. | `T08.depends_on`; feature commit and merge boundary |
| `T08` | `T09` | `hard-blocker` | Served-route proof requires the merged frontend. | `T09.depends_on`; `main`; `/pipelines` |
| `T08` | `T10` | `hard-blocker` | The task record requires the exact integrated commit evidence. | `T10.depends_on`; feature commit; merge commit |
| `T09` | `T10` | `hard-blocker` | The task record requires the served interaction observation. | `T10.depends_on`; `/pipelines` observation |
| `T10` | `T11` | `hard-blocker` | Documentation must exist before its focused commit and publication. | `T11.depends_on`; source card and thread paths |

---

## B. Independent Task Groups

1. **Placement:** every inventory task appears once. Groups containing superseded work remain non-dispatchable pending the decision in Section E.

| group_id | task_ids | target_files | target_symbols | independence_reason | dispatch_notes |
| --- | --- | --- | --- | --- | --- |
| `G01` | `T01`, `T02` | `.worktrees/`<br>`frontend/src/app/responsive/codex.js` | `state.editor`<br>`initializeMobileCodex()`<br>`pipeline-name`<br>`pipeline-purpose`<br>`saveEditor()` | Worktree isolation and the two listener changes form one production-edit boundary. | Completed in feature commit `db083e81`; merged as `808a16a7`; metadata code is published. |
| `G02` | `T03`, `T04` | `frontend/test-responsive/mobile-codex.test.mjs`<br>`tests/browser/codex/reusable-step-pipelines.spec.ts`<br>`tests/browser/application/the-application-is-one-responsive-frontend.spec.ts` | responsive source contract<br>`Responsive pipeline editing preserves unsaved metadata across step addition and removal`<br>`createFixture()` | These tasks own regression proof after `G01`; keeping them together prevents two workers from redefining the same acceptance boundary. | Do not dispatch. The scope review removed `T03`, replaced the original `T04` matrix with one focused responsive case, and named the application spec; the completed case instead landed in `reusable-step-pipelines.spec.ts`. |
| `G03` | `T05`, `T06`, `T07` | `bin/decision-os-verify.mjs`<br>`frontend/tsconfig.json`<br>`package.json`<br>`frontend/package.json`<br>the 11 repair paths listed in Section D | verification lease<br>`typecheck:frontend`<br>`test:front-back`<br>lifecycle settlement owners<br>exact executor fixture identity<br>publication-specific observation<br>isolated browser fixture | Focused checks, typecheck, repository verification, and the resulting repair boundary share one verification state and cannot be split safely after failures. | Verification is green. The 11-path repair remains uncommitted on `codex/pipeline-metadata-run-test-fix-1785426175338`; `T07` authorizes a suite run but does not own repair implementation. |
| `G04` | `T08`, `T09` | `frontend/src/app/responsive/codex.js`<br>`tests/browser/codex/reusable-step-pipelines.spec.ts` | feature commit<br>merge commit<br>`/pipelines`<br>`renderEditor()` | Integration and served-route proof consume the same metadata source and responsive regression. | Metadata integration, publication, HTTP `200`, and responsive Chromium proof are completed at `808a16a7`; this occurred before the later 11-path repair reached green verification. |
| `G05` | `T10`, `T11` | `.decision-os/cards/tasks/card-27ec1923-e426-423e-af78-459051fc4ee2.md`<br>`.decision-os/threads/tasks/thread-card-27ec1923-e426-423e-af78-459051fc4ee2.md`<br>`.worktrees/` | implementation evidence record<br>documentation commit<br>`origin/main`<br>repair worktree and branch | Both tasks edit the same task record and own the final publication and cleanup sequence. | Source evidence is recorded. Final repair commit, merge, push, worktree removal, and branch deletion remain blocked by the missing repair task identity. |

---

## C. Sequential Gates

1. **`G01` to `G02`:** both metadata controls update raw `state.editor` values before the focused add-step and remove-step regression executes; `saveEditor()` retains the sole trim boundary.
2. **`G02` to `G03`:** the authoritative inventory must identify the focused responsive regression task and remove the retired source-contract dependency before focused-check results can be mapped to task identities.
3. **`G03` to `G04`:** repository verification must exit `0`, and every repair path required for that result must have an authoritative task owner before integration is reconstructed.
4. **`G04` to `G05`:** the integrated revision and served `/pipelines` observation must be recorded before documentation publication and cleanup.

---

## D. Collision Risks

1. **Shared browser file:** `tests/browser/codex/reusable-step-pipelines.spec.ts` owns the completed responsive metadata regression and the repair to the reusable-pipeline save fixture. Splitting metadata proof from repair integration would create a same-file collision.
2. **Lifecycle source family:** `backend/src/business/codex/helper/launch-codex-execution-process.ts`, `backend/src/business/federation/helper/execute-node-message.ts`, and `backend/src/business/refresh/helper/watch-project-files.ts` jointly own referenced settlement deadlines and watcher lifetime. These three paths belong to one repair boundary.
3. **Exact executor fixture family:** `backend/test/codex/codex-process-restart-recovery.test.ts`, `backend/test/codex/read-card-skill-run-controller.test.ts`, `backend/test/codex/resume-codex-pipeline-runs.test.ts`, and `backend/test/codex/start-card-skill-process-controller.test.ts` share exact `workstation` executor identity through `taskExecutionNodeId(runtime)`.
4. **Stable contract fixtures:** `backend/test/codex/start-codex-pipeline-run-controller.test.ts` owns the stable run-identity assertion; `backend/test/server/runtime-failsafe.integration.test.ts` owns pre-construction delivery settings and environment cleanup.
5. **Publication fixture:** `backend/test/codex/codex-skill-library-routes.test.ts` distinguishes authored publication from automatic library synchronization.
6. **Verified repair boundary:** the three production files, seven backend test files, and one browser file above are the exact 11 modified tracked paths in the green repair worktree: `124` insertions and `52` deletions.
7. **Primary-checkout overlap:** nine repair paths already have unrelated or overlapping unstaged changes in the primary checkout: all three production files, `codex-process-restart-recovery.test.ts`, `read-card-skill-run-controller.test.ts`, `resume-codex-pipeline-runs.test.ts`, `start-card-skill-process-controller.test.ts`, `runtime-failsafe.integration.test.ts`, and `reusable-step-pipelines.spec.ts`. Direct parallel integration is unsafe.
8. **Runtime state:** `.decision-os/runtime-incidents.json` is untracked evidence excluded from the 11-path repair and must remain outside commit ownership.
9. **Migrations and data models:** none are changed by the metadata correction or the repair set.

---

## E. Ambiguities

1. **Authoritative task identity is unresolved:** `T03` remains in the inventory and blocks `T05`, while the reduced-scope review explicitly removes `T03` and the completed implementation contains no matching source-contract test.
2. **Regression ownership is unresolved:** the original `T04` requires seven collection transitions plus persistence, the scope review replaces that contract with one focused responsive case in the application spec, and the completed focused case is committed in `tests/browser/codex/reusable-step-pipelines.spec.ts`.
3. **Repair ownership is absent:** the verified 11-path repair changes production lifecycle ownership and test fixtures, while the inventory assigns `T07` only the action of running `test:front-back`. No task ID owns the repair implementation, its integration, or its collision-prone overlap with the primary checkout.
4. **Operator decision required:** authorize a replacement task-list that retires `T03`, narrows `T04` to the committed add-step and remove-step responsive regression, and assigns explicit task identities to the exact 11-path repair boundary before worker-result reconstruction.

---

## F. Readiness

1. **Status:** `BLOCKED_NEEDS_OPERATOR_ANSWER`
---

Codex run completed: exit code 0
