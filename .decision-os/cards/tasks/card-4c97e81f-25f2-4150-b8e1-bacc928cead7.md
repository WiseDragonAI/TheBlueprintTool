## A. Reopened RCA

1. The prior implementation measured the resumed Codex session, but the operator requires the latest Codex turn.
2. The persisted run proved the new turn started: Decision OS JSONL recorded `turn.started` after the `05:24:39.283Z` continuation, and Codex recorded `task_started` at `05:24:43.638Z`.
3. The Control Room's original-session projection was therefore the first incorrect state transition.

---

## B. Turn Timer Contract

1. Persist the observed `turn.started` timestamp beside the run log.
2. Before that event, expose the run as active without an elapsed timer.
3. After that event, project the latest persisted turn timestamp and invalidate the cached Control Room slice immediately.
4. On server restart, rebuild the same timestamp from durable run records.

---

## C. Implemented and Checked

1. Merge commit `787c062f` contains the corrected turn-boundary implementation.
2. Four focused timer regressions, the turn-publication integration, and backend typecheck pass.
3. The unrelated project-catalog baseline failure remains outside this subtask.

---

## D. Verification Still Required

1. Served restart verification has not been performed because the operator did not request another server restart after this implementation.
2. Keep this subtask open until a new turn starts from zero and its timer survives the next server restart.
