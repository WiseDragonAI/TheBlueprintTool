## A. Shortcut Placement Correction

1. **Desktop board:** `X` remains the only board-level task-creation shortcut and opens the existing Create task modal.
2. **Create task modal:** `1` through `9` choose projects from the active node table; `C` advances the modal to the next node table.
3. **Hints:** Modal project choices and the active node tab reuse `terminal-button__key`; these key hints remain desktop-only.
4. **Filter labels:** Project filter chips no longer contain task-creation shortcuts.

---

## B. Verification Boundary

1. **Automated:** The focused suite passed `50/50`; frontend typecheck passed.
2. **Served source:** The active route serves the modal key handlers and contains no board-level node shortcut control.
3. **Rendered interaction:** Browser interaction was unavailable, so keyboard transitions remain unverified.

---

## C. Repository Result

1. **Merge commit:** `951d900c`.
