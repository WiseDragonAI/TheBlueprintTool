## A. Change

1. **Type:** `code`
2. **Targets:** `frontend/src/app/responsive/application.js` and `frontend/src/app/responsive/control-filter-wheel.js`.
3. **Symbols:** Control Room filter initialization, `handleControlFilterWheel`, and `bindControlFilterWheelScrolling`.
4. **Action:** Bind both `.control-filters` elements once. At the desktop breakpoint, convert dominant vertical wheel input over a horizontally overflowed visible row into horizontal movement. Prevent the page default only when the row can move in that direction. Preserve native horizontal trackpad input and allow vertical page scrolling for non-overflowing rows and reached boundaries.

---

## B. Completion

1. **Project filters:** A long project row advances horizontally under vertical mouse-wheel input without changing page `scrollY`.
2. **Ledger filters:** A long ledger row has the same behavior after project selection.
3. **Boundary release:** Continued wheel input at the requested horizontal boundary is not trapped by the row.
4. **Input preservation:** Non-overflowing rows, mobile widths, and native horizontal trackpad input keep their existing ownership.
