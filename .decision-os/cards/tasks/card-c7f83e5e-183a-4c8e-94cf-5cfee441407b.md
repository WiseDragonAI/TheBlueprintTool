## A. Implemented

1. **Card entry is explicit:** `presentedCardIdentity` applies the desktop-open and mobile-closed default once per route identity.
2. **Reconciliation is presentation-neutral:** same-card renders update card data without opening or closing the thread and without rerendering the open thread composer.
