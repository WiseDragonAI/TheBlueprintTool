## A. Verified Result

1. The hardcoded canonical key is `tasks`.
2. Missing canonical ledgers are created with title `Tasks`.
3. Registered-project loading and selected-directory intake both enforce the invariant.
4. Synchronization task creation and execution resolve `tasks` semantically.
5. The implementation is merged in `e9f07d97`.
