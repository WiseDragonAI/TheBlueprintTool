## A. Implemented

1. Replaced the Termux-only runbook with one platform-aware runbook.
2. The Linux procedure uses the existing root `@playwright/test` dependency, `/snap/bin/chromium`, Linux launch flags, the current served route, and disposable screenshots under `/tmp`.
3. The Termux procedure retains `../tool/browser/browse.js`, Termux Chromium, Puppeteer Core, Android process flags, and `LD_PRELOAD` guidance.
4. Updated `AGENTS.md` to dispatch from the injected platform and forbid cross-platform command inference.
5. Preserved the existing server no-restart boundary.

---

## B. Verified

1. Node reported `linux/x64`; `/snap/bin/chromium` and root Playwright resolved successfully.
2. `http://127.0.0.1:50151/` returned HTTP `200`.
3. Playwright launched the documented Chromium executable, loaded the served route at `390x844`, and captured `/tmp/decision-os-linux-smoke.png`.
4. Commits: `6ef9472d` and `7265fe34`.
