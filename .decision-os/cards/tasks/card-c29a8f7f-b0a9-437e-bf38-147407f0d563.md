#master-task

Ledger: Specs
Queue rank: 6

## A. Scope

1. **Objective:** Ensure every project created from the web interface is immediately usable for master tasks by initializing a `tasks` ledger.
2. **Repair target:** Initialize the existing `Search` project with the same canonical `tasks` ledger structure.

---

## B. Evidence

1. **Creation defect:** `createDecisionOsProject` currently writes `state.json` with `ledgers: []`.
2. **Affected project:** `Search/.decision-os/state.json` contains `ledgers: []`.

---

## C. Acceptance Criteria

1. **New project contract:** Project creation persists one ledger named `tasks` and its valid ledger JSON.
2. **Regression coverage:** Automated tests verify the initialized state and ledger content.
3. **Repair:** The `Search` project exposes the `tasks` ledger after a fresh catalog read.
4. **Delivery:** The implementation is committed, merged into the primary checkout, and the feature worktree is removed.

---
