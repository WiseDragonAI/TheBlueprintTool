## A. Served-surface evidence

1. Linux Chromium `150.0.7871.114` loaded the canonical Specs route with **HTTP 200** from the existing server; no restart was performed.
2. Initial thread entry landed at the newest content with `bottomDistance: 0`.
3. Representative wheel input `-500` moved the conversation viewport from `scrollTop: 1566` to `1066` and set follow-bottom to false.
4. A `320px` delayed content growth triggered the observed layout path; the viewport remained at `scrollTop: 1066`.
5. Evidence image: `/tmp/thread-upward-scroll-verified.png`.
