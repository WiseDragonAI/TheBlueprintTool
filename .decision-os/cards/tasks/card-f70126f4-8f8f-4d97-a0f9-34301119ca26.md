## A. Operator Request Summary

1. **Card title:** Rename this card so it directly describes the Decision OS control room.
2. **Primary surface:** Make the control room the main Decision OS page, with a mobile-first interface organized into multiple tabs.
3. **Waiting-task stack:** Use the first tab for the stack of tasks waiting to be processed.
4. **Task hierarchy:** Every task is linked to a master task represented by a master card. Opening the master card shows its subtasks and each subtask status; opening a subtask opens its card directly.
5. **New-task action:** Put a `New task` button at the top of the control room.
6. **Task intake:** Pressing `New task` opens a new thread where the operator can record voice notes and attach files.
7. **Codex handoff:** From the intake thread, the operator launches Codex. Codex categorizes the task, selects the correct ledger, creates a new zone with a new master-task card, and then navigates the operator directly to that task card.
8. **Mandatory new zone:** Each new task creates a new zone. Codex must not reuse an existing zone.
9. **Queue order:** Show waiting master tasks in `FIFO` order so the oldest waiting task is immediately visible and can be handled first.
10. **Manual priority:** Allow drag-and-drop reordering of tasks in the waiting list.
11. **Ledger filters:** Generate dynamic filter buttons from ledger names available in the current Decision OS workspace.
12. **Current scope:** Keep the control room within the existing single-workspace server architecture.

---

## B. Control Room Information Architecture

1. **Default route:** The Decision OS root opens the control room rather than a specific ledger canvas.
2. **Mobile shell:** Use a sticky top action area, a scrollable content region, and a bottom tab bar that remains reachable with one hand.
3. **First tab — `Queue`:** Show master-task cards waiting for treatment. This is the default selected tab.
4. **Second tab — `Active`:** Show master tasks with at least one subtask currently being processed, including progress and elapsed state.
5. **Completed-task exclusion:** Exclude completed master tasks from every control-room tab.
6. **Top action:** Place the full-width primary `New task` button above the active tab content. Keep it visible while the queue scrolls.
7. **Filter row:** Place a horizontally scrollable row beneath `New task`, beginning with `All`, followed by one filter chip per ledger name available in the current workspace.

---

## C. Queue UI and Interaction

1. **Default ordering:** Order waiting master tasks by the time they entered the waiting state, oldest first. The first card is therefore the next task to treat.
2. **Master-card summary:** Each queue item shows the master-task title, ledger name, waiting age, aggregate progress such as `2/5 complete`, current overall status, and the next actionable subtask, all derived from the master card Markdown.
3. **Visual priority:** Give the oldest untouched item the strongest queue emphasis and show its waiting age without requiring expansion.
4. **Expand behavior:** Tapping the body of a master-task item expands it in place to reveal its subtasks while preserving the operator's queue position.
5. **Subtask row:** Each subtask row shows its title and exact status. Tapping the row opens the card referenced by that subtask's Markdown link.
6. **Master-card navigation:** A distinct `Open master task` action opens the master card itself. Expansion must not accidentally navigate away.
7. **Manual reordering:** A dedicated drag handle reorders master-task items. A completed drop rewrites the affected cards' `Queue rank` Markdown fields and immediately updates the visible list.
8. **FIFO after reordering:** `Queue rank` takes precedence when present; cards without it remain ordered by their `Waiting since` Markdown timestamps. This preserves deterministic ordering without separate queue storage.
9. **Touch accessibility:** Support long-press on the drag handle, provide a visible lifted state and insertion marker, and expose `Move up` and `Move down` actions for non-drag input.
10. **Filtering:** Selecting a ledger chip limits every task tab to master tasks from that ledger. `All` clears the ledger filter, and the selected filter persists while moving between tabs.
11. **Empty state:** When no waiting task matches the selected ledger, show `No waiting tasks` and retain the `New task` action and ledger filters.

---

## D. New Task User Journey

1. **Start:** The operator presses `New task`; Decision OS immediately creates a draft intake thread and opens it as a focused composer.
2. **Capture:** The composer provides `Record voice note`, file attachment, and text entry in the same chronological thread. Each successfully saved input appears as a thread message.
3. **Draft safety:** The intake remains a draft until Codex is launched. Closing the composer retains the draft and exposes it in the control room for later continuation.
4. **Launch readiness:** Enable `Launch Codex` after the thread contains at least one saved voice note, attachment, or text message.
5. **Processing state:** After launch, lock duplicate launches, keep the thread readable, show the active Codex state, and provide a return path to the control room.
6. **Categorization:** Codex reads the complete intake thread and attachments, determines the destination ledger from the ledgers available in the current workspace, and assigns the task to that ledger.
7. **Creation contract:** Codex creates exactly one new zone in the selected ledger and exactly one master-task card in that zone. The intake thread becomes the master card's thread so the captured context remains attached to the task.
8. **Subtask creation:** Codex creates the actionable subtask cards inside the new zone and writes their card links and statuses into the master card's `## Subtasks` Markdown section.
9. **Successful destination:** Once creation succeeds, Decision OS navigates directly to the new master-task card with its subtask list visible.
10. **Failure recovery:** If classification or creation fails, keep the intake thread intact, display the concrete failure, and allow the same launch to be retried without creating a second zone or master card.

---

## E. Markdown Reconstruction Contract

1. **Single source:** Reconstruct the complete control-room task view from existing card Markdown. Do not add a task record, task index, relationship table, queue table, or status store beside the current ledger and card model.
2. **Master label:** Identify a master-task card by the existing Markdown label `#master-task`.
3. **Control-room status labels:** Use exactly one Markdown label on each master card: `#task-waiting`, `#task-active`, or `#task-complete`. Display only `#task-waiting` and `#task-active` cards in the control room.
4. **Canonical fields:** Store `Ledger`, `Waiting since`, and optional `Queue rank` as labeled lines in the master card Markdown. Parse `Waiting since` as an ISO-8601 timestamp and `Queue rank` as a positive integer.
5. **Subtask list:** Store subtasks under `## Subtasks` as numbered items using the canonical form `[Subtask title](card:<card-id>) — Status: <status>`. The card link provides navigation; the status text provides row state and aggregate progress.
6. **Rendering input:** The control-room parser accepts one card Markdown string and returns its master-task marker, control-room status, ledger filter value, waiting timestamp, optional queue rank, and ordered subtask links with statuses.
7. **Invalid Markdown:** Exclude a card missing `#master-task`, a valid control-room status label, `Ledger`, or a valid `Waiting since` value. Surface a parse diagnostic that identifies the card and missing or invalid field.
8. **Write path:** Codex creation, subtask status changes, and drag reordering update the canonical Markdown fields in place through the existing card edit path.

---

## F. Markdown-Derived Behavior

1. **Queue eligibility:** Include a card in `Queue` only when its Markdown contains both `#master-task` and `#task-waiting`.
2. **Active eligibility:** Include a card in `Active` only when its Markdown contains both `#master-task` and `#task-active`.
3. **Completed exclusion:** A card labeled `#task-complete` never appears in the control room.
4. **Progress derivation:** Derive aggregate progress by parsing the statuses in the master card's `## Subtasks` items; do not store a separate progress value.
5. **FIFO ordering:** Order unranked queue cards by ascending `Waiting since`; the oldest valid timestamp appears first.
6. **Manual priority:** Order ranked cards by ascending `Queue rank` before unranked cards. Reordering normalizes the visible ranked sequence to consecutive positive integers in the affected master card Markdown.
7. **Status synchronization:** When a subtask status changes, update the corresponding status text in the master card Markdown through the existing card edit path before refreshing the control room.

---

## G. Acceptance Criteria

1. **Main page:** Opening Decision OS displays the control room and selects the `Queue` tab on mobile.
2. **Oldest first:** With three untouched waiting tasks, their visible order matches ascending waiting-entry timestamps.
3. **Persistent reorder:** Dragging the third waiting task to first place preserves that order after a reload and does not alter its creation or waiting timestamp.
4. **Master drill-down:** Expanding a master task displays every linked subtask with its current status.
5. **Direct subtask access:** Selecting a subtask opens the exact card referenced by its Markdown link.
6. **Intake tools:** `New task` opens a draft thread that accepts voice notes, file attachments, and text before Codex launches.
7. **Creation result:** A successful Codex launch creates one new zone, one master-task card containing the canonical labels and fields, linked subtask cards listed in `## Subtasks`, and navigates to the master card.
8. **No zone reuse:** Task creation never places the new master task in a pre-existing zone.
9. **Dynamic filters:** Ledger values parsed from eligible master card Markdown produce the available filter chips for the current workspace.
10. **Markdown-only rebuild:** Given only eligible master card Markdown strings, the parser reconstructs tab membership, filters, FIFO order, manual order, progress, subtask statuses, and subtask navigation targets.
11. **No parallel model:** Creating, updating, reordering, and rendering tasks introduces no task-specific persistence outside the existing ledger, card Markdown, and thread storage.
12. **Failure safety:** Retrying a failed launch does not duplicate the task's zone or master card and does not discard intake messages or attachments.

---

## H. Implementation Evidence

1. **Control-room runtime:** `frontend-mobile/src/mobile.js` makes `/` load the control room, reconstructs tasks from every current workspace ledger, renders `Queue` and `Active`, retains ledger filtering across tabs, opens master cards and linked subtasks, and persists manual order through existing `patch-card` Markdown edits.
2. **Markdown parser:** `frontend-mobile/src/mobile-control-room.js` validates the canonical labels and fields, derives FIFO and ranked order, calculates progress and the next actionable subtask, excludes completed tasks, and returns card-specific diagnostics without persisting a parallel task model.
3. **Mobile interaction:** `frontend-mobile/index.html` and `frontend-mobile/assets/mobile.css` provide the sticky `New task` action, horizontal ledger filters, expandable task stack, oldest-task emphasis, dedicated mouse and long-press touch reorder handle, keyboard-accessible move actions, and reachable bottom tabs.
4. **Task intake:** `New task` creates exactly one new zone and one Markdown-backed master card through existing ledger mutations, opens the card's existing thread, accepts text, voice, and file inputs, and enables the existing Codex launch only after saved intake exists.
5. **Verification:** `node --test frontend-mobile/test/*.test.mjs` passes `9` tests; frontend and backend TypeScript checks pass; frontend and backend project test commands exit successfully; the running workspace server returns `200` for `/` and `/assets/mobile.css` on port `50150`.
