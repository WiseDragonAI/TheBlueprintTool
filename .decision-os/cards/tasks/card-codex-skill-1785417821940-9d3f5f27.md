## A. Task Inventory

| id | type | title | target_files | target_symbols | action | done_when | depends_on |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `T01` | `code` | Admit forward durable voice state at the existing same-ID merge | `frontend/src/runtime/ledger/helper/merge-local-thread-notes.ts`<br>`frontend/src/runtime/voice/helper/voice-transcription-lifecycle.ts` | `mergeLocalThreadNotes()`<br>`imageSizesRecord()`<br>`shouldApplyVoiceServerNote()` | Import `shouldApplyVoiceServerNote()`. Before the generic optimistic spread, require an incoming thread slice, an existing same-ID note, `localNote.optimistic === true`, a non-empty `localVoiceUploadId`, and comparator acceptance. Keep the incoming note as lifecycle authority, retain the already merged local `imageSizes`, retain `localVoiceUploadId`, force `optimistic: false`, then continue. Let rejected voice snapshots and non-voice notes follow the current merge path. Do not change cleanup presentation, polling, transport, persistence, schemas, rendering, and component structure. | Code review shows one constrained branch. Accepted state retains server message, status, error, revision, voice metadata, lifecycle timestamps, and note ID; it retains only the established browser-owned cleanup ID plus merged image sizing from the local record. | None |
| `T02` | `test` | Prove live thread refresh installs terminal voice state | `frontend/test/runtime/ledger-content-refresh.integration.test.ts` | `loadActiveThreadSlice()`<br>`mergeLocalThreadNotes()`<br>`renderThreadNotes()` | Add caller-level cases with one same-ID optimistic voice note carrying `localVoiceUploadId`, local `imageSizes`, stale lifecycle fields, and an incoming forward durable note. Cover `transcribed`, `transcription failed`, and `execution launch failed`. Include a refresh whose lifecycle timestamps exceed the polling deadline to prove a later content event still installs durable state. Add a same-ID optimistic text case proving the voice branch is not entered. | Each voice case leaves exactly one note with the original ID, installs durable message, status, error, revision, metadata, and timestamps, sets `optimistic: false`, retains browser cleanup ownership plus image sizing, and renders terminal state without the stale busy presentation. The text case retains the current optimistic overlay behavior. No new cleanup-failure UI expectation is asserted. | `T01` |
| `T03` | `test` | Prove full-ledger reconciliation preserves voice lifecycle direction | `frontend/test/runtime/voice-transcription-runtime.integration.test.ts` | `loadActiveLedgerState()`<br>`reconcileActiveLedgerState()`<br>`mergeLocalThreadNotes()`<br>`shouldApplyVoiceServerNote()` | Extend the active-ledger refresh coverage with a forward same-ID voice snapshot that must install and a backward `uploading` snapshot with a larger replica revision that must be rejected. Give the local records a non-empty `localVoiceUploadId` so both cases exercise the new branch predicate through the real full-ledger caller. | The forward case installs the durable terminal note on the same ID while preserving local cleanup ownership. The backward case retains the newer local terminal lifecycle despite the larger incoming revision. Existing missing-note preservation remains unchanged. | `T01` |
| `T04` | `ops` | Run focused voice merge regressions | `bin/decision-os-verify.mjs`<br>`frontend/test/runtime/ledger-content-refresh.integration.test.ts`<br>`frontend/test/runtime/voice-transcription-runtime.integration.test.ts` | Repository verification lease<br>Node test runner | Run `node bin/decision-os-verify.mjs -- frontend/node_modules/.bin/tsx --test frontend/test/runtime/ledger-content-refresh.integration.test.ts frontend/test/runtime/voice-transcription-runtime.integration.test.ts`. | The lease-wrapped command exits `0` with both changed test files passing. | `T02`, `T03` |
| `T05` | `ops` | Typecheck the changed frontend boundary | `bin/decision-os-verify.mjs`<br>`frontend/tsconfig.json` | `typecheck:frontend` | Run `node bin/decision-os-verify.mjs -- npm run typecheck:frontend`. | The lease-wrapped frontend typecheck exits `0`. | `T01` |
| `T06` | `ops` | Run the repository verification suite | `bin/decision-os-verify.mjs`<br>`package.json`<br>`frontend/package.json` | `test:front-back` | After focused checks and typecheck pass, run `node bin/decision-os-verify.mjs -- npm run test:front-back`. | The lease-wrapped repository suite exits `0` with captured completion output. | `T04`, `T05` |
| `T07` | `scenario` | Verify the live same-row transition on the served incident route | `BROWSER_RUNBOOK.md`<br>`frontend/src/runtime/thread/effect/load-active-thread-slice.ts`<br>`frontend/src/runtime/thread/effect/render-thread-notes.ts` | `loadActiveThreadSlice()`<br>`renderThreadNotes()` | Follow the Linux Chromium procedure without restarting the Decision OS server. On `http://127.0.0.1:50150/p/927a2e68-e8e4-426f-9f77-fd05a2877bd9/ledgers/tasks/cards/card-b3918920-c2f3-4039-b046-51cf877863b1?replica=workstation`, record a voice note and observe its note ID, optimistic busy state, live terminal state, and post-reload state. | The route returns HTTP `200`; the same logical note ID changes from optimistic busy state to the durable transcript without reload, then remains terminal with the same transcript after a fresh reload. | `T06` |

---

## B. Open Questions

1. **Blocking questions:** None.

---

## C. Readiness

1. **Status:** `READY_FOR_TASK_DEPENDENCY`
---

Codex run completed: exit code 0
