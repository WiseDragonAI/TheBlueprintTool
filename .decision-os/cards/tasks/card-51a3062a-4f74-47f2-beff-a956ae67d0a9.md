## A. Scope

1. **Touch gesture:** Anchor each mobile swipe to its starting image and cap settlement to one adjacent slide.
2. **Visual controls:** Place larger chevrons over the image sides and refine the caption and direct selectors.

---

## B. Acceptance Criteria

1. **Swipe behavior:** Short gestures return to the starting image; qualifying gestures move exactly one image.
2. **Page scroll:** The carousel retains vertical page scrolling through `touch-action: pan-y`.
3. **Verification:** `frontend/test/runtime/card-markdown-images.integration.test.ts` passes and `npm run typecheck` succeeds.

---

## C. Evidence

1. **Implementation commit:** `c8d5120`.
2. **Focused test:** Passed on `2026-07-12`.
3. **TypeScript:** `npm run typecheck` passed on `2026-07-12`.
