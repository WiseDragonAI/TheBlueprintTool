## A. Implementation Boundary

1. Add one server-owned scheduler that periodically invokes the existing relay reconciliation for locally owned project stores.
2. Reconcile subscriptions for every currently known remote project so relay-root drift triggers existing missing-bucket repair.
3. Prevent overlapping cycles, bound asynchronous work, unref the timer, and clear it during server shutdown.
4. Route cycle failures through the existing contained federation background-error boundary.

---

## B. Verification

1. Confirm immediate `publishDelta()` delivery remains unchanged.
2. Confirm an unchanged cycle produces no task-state mutation and does not create unbounded network work.