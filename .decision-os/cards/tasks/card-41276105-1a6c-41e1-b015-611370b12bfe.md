## A. Implementation Delivery

1. **Native renderer:** `@pierre/diffs@1.2.12` is pinned and lazy-loaded from `frontend/assets/vendor/pierre-diffs-1.2.12.js`.
2. **Directive:** The parser and card renderer implement configurable `repo` and `path` values.
3. **Git boundary:** Project-scoped read and stage endpoints validate both paths inside the workspace and selected Git worktree.
4. **Delivery commits:** Feature `261d6614`; merge `cdbc7cbc`.
