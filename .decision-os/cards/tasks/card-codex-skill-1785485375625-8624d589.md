## A. Task Inventory

1. The inventory covers the clarified projection, markup, styling, automated checks, and served interaction proof.

| id | type | title | target_files | target_symbols | action | done_when | depends_on |
| --- | --- | --- | --- | --- | --- | --- | --- |
| T1 | code | Project active incidents into owner rows | `frontend/src/app/responsive/runtime-status.js` | `activeIncidents`, `interruptionScopes`, `projectPauseReasons`, `projectRuntimeRows`, `groupedActiveIncidents` | Add one owner-aware status projection that resolves each active incident first by exact catalog `context.projectId`, then by an exact catalog identifier in `/p/:projectId` and colon-delimited scopes; group equal code-and-message incidents only inside that owner; attach unowned groups to one conditional System row; preserve incident components, scopes, severity, occurrence totals, observation timestamps, and the existing project availability calculation. | Projection output contains every catalog project, contains a System row only for unowned active incidents, never merges incidents across owners, excludes resolved incidents, and keeps a locally available project available when only its federated cache is interrupted. | None |
| T2 | code | Render native expandable project status rows | `frontend/index.html`, `frontend/src/app/responsive/application.js` | `runtime-status-view`, `renderRuntimeStatus`, `formatObservedAt`, runtime status element registry | Remove the standalone Current interruptions and errors section and render each projected project and conditional System entry as a native `details` row with a `summary`; retain the project accent, availability badge, replica and pause detail, plus nested incident severity, interruption state, occurrence count, message, last observation time, and affected scopes; initialize rows with active incidents as expanded and rows without incidents as collapsed. | `/status` has one Projects hierarchy, every summary exposes native expanded state to assistive technology and keyboard input, incident-bearing rows start open, incident-free rows start closed, and no standalone incident container remains. | T1 |
| T3 | code | Style nested status rows responsively | `frontend/assets/runtime-status.css` | `.runtime-project-list`, `.runtime-project-row`, `.runtime-project-mark`, `.runtime-project-copy`, `.runtime-status-badge`, `.runtime-incident-card`, `.runtime-incident-heading`, `.runtime-incident-scopes`, mobile media query | Adapt the existing project-row and incident-card rules to the native summary and nested incident layout, including visible disclosure affordance, focus treatment, project accent, badge states, spacing, and narrow-screen flow. | Expanded and collapsed rows preserve readable hierarchy at desktop and 390-pixel mobile widths, keyboard focus is visible, incident content stays inside its owner row, and the page has no horizontal overflow. | T2 |
| T4 | test | Cover ownership and grouping projection | `frontend/test-responsive/runtime-status.test.mjs` | tests for `projectRuntimeRows`, `groupedActiveIncidents`, status route shell | Replace the independent grouping expectations with fixtures covering exact `context.projectId` priority, `/p/:projectId` scope ownership, colon-delimited scope ownership, identical incidents owned by different projects, unowned System incidents, resolved history exclusion, preserved incident fields, and federated-cache availability independence. Update the shell assertion to reject the removed standalone block. | Focused responsive tests prove every ownership boundary and retained field, including no cross-project merge, no false attribution, no resolved incident projection, and no Current interruptions and errors markup. | T1, T2 |
| T5 | test | Exercise expandable status rows in Chromium | `tests/browser/application/system-status-project-incidents.spec.ts` | `/status` responsive interaction scenario | Add a Linux Playwright scenario using the repository browser-test conventions and an isolated test server; exercise mouse and keyboard expansion at desktop and mobile widths, assert initial open states, verify the conditional System row, confirm incident placement and retained labels, and check horizontal overflow. | The focused leased browser test passes with `/snap/bin/chromium`, proves the complete disclosure interaction on `/status`, and observes no page errors at desktop and 390-pixel mobile widths. | T2, T3, T4 |
| T6 | scenario | Prove the served System status surface | `frontend/src/app/responsive/runtime-status.js`, `frontend/src/app/responsive/application.js`, `frontend/index.html`, `frontend/assets/runtime-status.css` | operator-facing `/status` route | After focused checks pass, run the scoped frontend typecheck, full repository suite, and a Linux Playwright inspection against the already served operator-facing `/status` route without restarting the server; record the route HTTP result and the observed expanded and collapsed behavior. | Scoped typecheck and full suite pass; the existing served route returns HTTP 200; project-owned incidents appear only in their rows; unowned incidents appear only in System; native disclosure works with representative pointer and keyboard input; the standalone incident block is absent. | T4, T5 |

---

## B. Open Questions

1. None.

---

## C. Readiness

1. `READY_FOR_TASK_DEPENDENCY`
---

Codex run completed: exit code 0
