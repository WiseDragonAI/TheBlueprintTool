## A. Task

1. **Type:** `code` and `data`.
2. Add a project-sync admission effect beside `backend/src/business/project-sync/controller/start-project-sync.ts` that resolves the target project ledger, creates a titled zone, creates the canonical master card, attaches JSON labels `master-task` and `synchronization`, and stores the source project plus origin fingerprint in card content.
3. Use `backend/src/business/ledger/helper/apply-ledger-mutation.ts` for zone, card, and relationship persistence.
4. Start the canonical synchronization pipeline through `startCodexPipelineRunController` with the created card as `sourceCardId`.
5. Make the existing idempotency key return the same master card and pipeline run when the same synchronization request is submitted again.

---

## B. Targets

1. `backend/src/business/project-sync/controller/start-project-sync.ts::createProjectSyncController`
2. `backend/src/business/server/helper/create-http-server.ts` `/api/project-sync` admission route
3. `backend/src/business/codex/controller/start-codex-pipeline-run-controller.ts::startCodexPipelineRunController`
4. New project-sync master-task admission helper and its persisted request contract

---

## C. Completion

1. The POST response contains `masterCardId`, `ledgerId`, `pipelineRunId`, and `duplicate`.
2. Ledger JSON contains one synchronization-labeled master card inside its own named zone.
3. Pipeline manifest state references that master card and survives a fresh server read.

---

## D. Dependencies

1. **Depends on:** `Define synchronization skills and the server pipeline`.
