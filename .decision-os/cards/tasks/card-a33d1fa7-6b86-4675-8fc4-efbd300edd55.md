## A. Scope

1. **Build:** Initialize app-server stdio, call `thread/read`, validate the thread id, capture `Thread.path`, and terminate cleanly.
2. **Constraint:** Leave the target thread unloaded.

---

## B. Acceptance

1. **Proof:** Integration coverage resolves a known native session and fails closed when `Thread.path` is absent.
