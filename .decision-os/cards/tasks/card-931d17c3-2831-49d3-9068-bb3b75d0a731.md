## A. Operator Validation and Memory

1. **Operator result:** The operator confirmed on `2026-07-21` that the corrected Git review recording and graph function on the target device.
2. **Root cause:** Separate JavaScript state was insufficient because waveform SVG resource IDs were document-global, microphone ownership was reentrant for the same logical owner string, and detached widget instances retained recorder resources without a disconnect lifecycle.
3. **Correction:** Unique SVG paint resources, opaque non-reentrant leases, scoped DOM queries, and disconnect cleanup made the Git widget an actual isolated instance while preserving the thread recorder.
4. **Reusable memory:** Project memory `77`, `Interactive widgets need instance-owned resources and lifecycle leases`, stores the engineering rule and cites this card plus commits `3da07b93` and `29857d6f`.
5. **Verification:** Memory search and latest-record listing both return memory `77`.
