#task

## A. Change

1. Store `projectColor` from `/decision-os/state` in runtime state.
2. Set the zone creation default from that verified project color.

---

## B. Verification

1. Add focused coverage for project-state loading and zone creation color selection.
