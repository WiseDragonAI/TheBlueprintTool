## A. Verified Result

1. **Controller:** `openThreadPanelController()` now owns requested selection, panel visibility, active surface normalization, entry request, and final render.
2. **Responsive parity:** `openMobileThread()` calls the same controller, so same-card reopen creates a new generation even when thread identity is unchanged.
3. **Entry parity:** Direct thread switches and tab activations create scoped requests and enable follow. Existing wrappers preserve shortcut, deep-link, master-task, and focus contracts.
4. **Served proof:** Mobile and desktop same-card reopen both returned to `0px` bottom distance with follow enabled.
