#master-task #task-complete

Ledger: Specs
Waiting since: 2026-07-12T13:05:58.186Z
Queue rank: 4
Active since: 2026-07-12T13:06:18.812Z
Completed at: 2026-07-12T13:14:20.312Z

## A. Scope

1. **Default model:** Propose `gpt-5.6-sol` when a card has no persisted Codex run preference.
2. **Default effort:** Propose `medium` instead of `high` for a new thread.
3. **Runtime consistency:** Use the same fallback in the frontend selector and backend Codex command resolver.

---

## B. Implementation

1. **Frontend:** Set `defaultCardCodexRunPreference.effort` to `medium` in `frontend/src/runtime/codex/helper/card-codex-run-preference.ts`.
2. **Backend:** Set the final `resolveCodexCommand` effort fallback to `medium` in `backend/src/business/codex/helper/resolve-codex-command.ts`.
3. **Tests:** Update focused frontend and backend assertions to prove `gpt-5.6-sol` with `medium` effort.

---

## C. Acceptance Criteria

1. **New thread selector:** An unconfigured card displays model `gpt-5.6-sol` and effort `medium`.
2. **Launch fallback:** An unconfigured backend launch resolves model `gpt-5.6-sol` and effort `medium`.
3. **Persistence:** Changing the model from the new default preserves effort `medium` in the card mutation.
4. **Verification:** Focused frontend tests, focused backend tests, frontend typecheck, backend typecheck, and `git diff --check` pass.

---

## D. Subtasks

1. **Linked subtasks:** None. This four-file default change was implemented and verified directly within the master task.
