## A. Scope

1. **Automated gate:** Run the focused backend and frontend integration suites for cancellation, run ownership, Codex Log rendering, input routing, and fresh thread launch.
2. **Repository gate:** Run the repository's configured typecheck and relevant test commands from `ledger-cli execution-profile --ledger .decision-os/specs.json --json`.
3. **Target surface:** Follow `BROWSER_RUNBOOK.md` before using mobile Chromium and exercise the served Decision OS project route without restarting the operator's server.

---

## B. Verification Scenarios

1. **Terminal run:** Open a completed session, scroll to the final control, cancel the modal once, then confirm; verify the log clears only after the successful response.
2. **Active run:** Delete a running session and verify the process stops before the server removes the artifacts and card ownership.
3. **Fresh relaunch:** Select `START` after deletion and verify a different run id appears with no resume session id in the launch path.
4. **Persistence:** Reload the project route and verify the deleted log remains absent while card Markdown, thread notes, and model/effort preferences remain present.
5. **Failure:** Exercise a rejected deletion and verify the existing log remains visible with an actionable inline error.

---

## C. Evidence Contract

1. **Record:** Capture the project route, HTTP status for deletion, prior and new run ids, active-process stop observation, artifact absence, reload result, and automated command results.
2. **Interaction claim:** Report `implemented; automated checks pass; device interaction not yet verified` unless the complete served-surface gesture is observed with representative Chromium input.
3. **Completion gate:** Keep this card `todo` until every required scenario has recorded evidence.

---

## D. Evidence and Gate

1. **Backend deletion:** `backend/test/codex/delete-thread-codex-session-controller.test.ts` passes `3/3` tests for terminal deletion, stale ownership, active settlement, runtime/artifact cleanup, and failure preservation.
2. **Backend regressions:** `backend/test/codex/read-card-skill-run-controller.test.ts` passes `5/5`; `backend/test/codex/start-card-skill-process-controller.test.ts` passes `6/6` for launch, cancellation, continuation, and capacity-resume behavior.
3. **Frontend deletion:** The exact `DELETE` request test passes `1/1`; the new Codex Log flow and existing STOP suites pass `4/4` for footer placement, confirmation routing, reconciliation contracts, and STOP regression coverage.
4. **Typechecks:** `npm run typecheck:backend` and `npm run typecheck:frontend` pass.
5. **Served route:** `http://127.0.0.1:50150/p/ZGVjaXNpb24tb3M/ledgers/specs` returns HTTP `200` and serves `frontend-mobile` from the restarted process.
6. **Mobile reproduction:** Chromium `138.0.7204.168` at `412 × 915` hit the visible deletion button for run `codex-skill-1783955146517-72c5eb8c`; the modal remained closed, no deletion request was sent, no page error occurred, and ledger ownership remained unchanged.
7. **Verified blocker:** `frontend-mobile/src/mobile-thread.js` does not route the shared renderer's `confirm-delete-thread-codex-session` action. Terminal deletion, active deletion, persistence, and fresh relaunch cannot be verified until the reopened UI subtask is corrected.

---

## E. Post-Fix Verification

1. **Corrected boundary:** The served `frontend-mobile` dispatcher now routes session confirmation and deletion through the shared controllers; touch confirmation, cancellation, exact request submission, and rejected-request reconciliation are observed on the canonical route.
2. **Checks:** Backend and frontend typechecks pass; terminal, active-settlement, and rollback backend tests pass `3/3`; focused request and renderer tests pass `3/3`; the mobile suite passes `60/60`.
3. **Non-destructive route evidence:** A deliberate ownership mismatch returned HTTP `404`, preserved run `codex-skill-1783955146517-72c5eb8c`, retained the visible button, restored note-modal semantics, and surfaced `Card not found.` inline.
4. **Remaining gate:** Successful served deletion, cleared-log reload, and fresh relaunch remain unverified because the selected owned run is the active process executing this task. This card remains `todo` pending one post-turn operator deletion and relaunch.
