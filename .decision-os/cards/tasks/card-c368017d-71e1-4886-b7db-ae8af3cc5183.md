## A. Scope

Test the complete canary authoring chain: content-kind creation, CodeMirror editing, atomic persistence, scoped Git commits, history navigation, Pierre adjacent-revision diffs, red/blue accessibility, pipeline-only injection, skill-discovery exclusion, federation publication, and production isolation.

---

## B. Failure Injection

Inject invalid input, unsafe paths, duplicate names, stale revisions, write failures, Git add and commit failures, corrupt history responses, export failures, unavailable dev relay, and rejected UI saves at their first owning boundary.

---

## C. Acceptance

1. Failures remain contained and durable bytes are preserved.
2. No unrelated file is staged or committed.
3. Diagnostics remain readable.
4. Browser proof covers optimistic editing, reload persistence, rejection reconciliation, revision navigation, accessible diff states, focus, and modal dimensions.
5. Production server and relay remain healthy and unchanged.
6. Focused checks, scoped typechecks, and the full admitted suite pass through the verification lease.

---

## Review Reconciliation — 2026-07-30

1. Current review checks: backend, frontend, and relay TypeScript pass; focused content, prompt, federation, delivery, and frontend checks pass; frontend full suite passes `609/609`; Termux relay passes `1/1`.
2. Backend full suite is red: `636` passed, `10` failed, `7` cancelled, `653` total. Cloudflare relay assertions report nine passes, then `workerd` segfaults and exits nonzero.
3. No current browser run was admitted because the mandatory platform instruction was absent. Historical G12 evidence does not prove the current post-merge tree.
4. Live production and canary application health both report protocol `0`, blank release SHA, and unbootstrapped release pointers.
5. Keep this verification card open.
