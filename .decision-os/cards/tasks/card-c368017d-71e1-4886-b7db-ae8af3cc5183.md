## A. Scope

Add focused API, persistence, federation, and browser tests for the complete authoring path. Inject invalid input, unsafe paths, duplicate creation, stale revisions, write failures, export failures, unavailable relay, and rejected requests at their first owning boundary.

---

## B. Acceptance

1. Failures remain contained to the owning skill operation.
2. Durable bytes remain unchanged after rejected writes.
3. Diagnostics remain available and actionable.
4. Browser proof covers optimistic UI, reload persistence, rejection reconciliation, focus trapping, and modal dimensions.
5. Scoped typechecks and the full admitted suite pass through the repository verification lease.
