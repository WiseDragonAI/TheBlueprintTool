#master-task

Ledger: Specs
Queue rank: 1

## A. Scope

1. **Objective:** Reduce the mobile thread header from three rows to two and expose the active Codex run through the `Codex Log` tab.
2. **Evidence:** The attached mobile screenshot shows the `Thread` and `Codex Log` tabs wrapping into the model selector row and obscuring its content.
3. **Verified root cause:** `.decision-os/.settings.json` serves `frontend-mobile`; its thread markup retained a separate `thread-title-row`, and its `44px` touch-target rules overflowed the shared desktop header's `28px` grid rows.
4. **Layout:** Keep `Thread`, `Codex Log`, and close controls in the first row; keep model, effort, and launch controls in the second row.
5. **Refined navigation:** Remove the thread target text from the header; the first row contains only `Thread`, `Codex Log`, and close.
6. **Master title:** Name the ledger card `Polish mobile thread Codex header`.

---

## B. Running Contract

1. **Launch controls:** Hide the complete model, effort, and `Codex` launch row immediately after launch and while the current run status is `running`.
2. **Run indicator:** Animate an inset multicolor edge and glow on `Codex Log` while the run status is `running`.
3. **Terminal transition:** Restore the launch row and remove the animated treatment when polling reports a non-running status.
4. **Accessibility:** Label the active run on `Codex Log` and replace motion with a static highlighted edge under `prefers-reduced-motion`.

---

## C. Acceptance Criteria

1. **Two-row header:** The navigation controls do not overlap the Codex model and effort selectors at mobile panel widths.
2. **Single launch:** The model, effort, and launch controls are absent while Codex is running.
3. **Visible activity:** `Codex Log` has a distinct animated running treatment without changing the selected-tab and focus contracts.
4. **Close control:** The close button is adjacent to the two thread view tabs and closes the thread panel.
5. **Verification:** Frontend TypeScript and focused thread tests pass.
6. **Served frontend:** The active `/specs` route serves the corrected `frontend-mobile` hierarchy and mobile-specific `44px` two-row geometry.
7. **Control geometry:** Model, effort, and `Codex` use three equal-width columns, identical `44px` heights, and `8px` gaps.
8. **Header spacing:** The navigation row and launch row have a `12px` gap; navigation controls have an `8px` gap.
9. **Target removal:** The redundant target label, including `Objective:`, is absent from the mobile header.
10. **Chromium evidence:** A live `360×800` Puppeteer/Chromium render confirms the running header, and a launch-row render measures model, effort, and `Codex` at `101×44px` each with `8px` gaps.

---
