## A. Implementation Scope

1. Update `backend/src/business/codex/helper/build-thread-codex-prompt.ts` so the launch contract identifies the master task as the canonical operator-facing decision surface.
2. Update `backend/src/business/codex/helper/build-card-launch-context.ts` so the action contract describes master content, subtask content, and thread reply by audience and purpose.
3. Keep technical investigation evidence in subtask cards unless it changes the operator's decision.

---

## B. Acceptance Evidence

1. `backend/test/codex/build-thread-codex-prompt.test.ts` proves the generated prompt states the master-task audience boundary.
2. `backend/test/codex/build-card-launch-context.test.ts` proves the injected action context distinguishes canonical operator synthesis, implementation detail, and concise reply.
3. A representative run fixture proves the master task remains understandable without reading the thread reply.
