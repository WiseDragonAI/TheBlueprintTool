## A. Scope

1. **Parser:** Preserve ordered Markdown list items and their starting number in the shared ledger-card block model.
2. **Renderer:** Emit semantic `ol` markup for ordered lists and keep `ul` markup for unordered lists.
3. **Thread style:** Use inherited typography for inline code and thread-accent color for inline code and list markers.

---

## B. Acceptance Criteria

1. **Shared behavior:** Desktop and mobile thread surfaces consume the same semantic renderer and thread styling.
2. **Regression:** Focused automated tests cover ordered-list parsing, ordered-list rendering, inline-code typography, marker color, and fenced code-block isolation.
3. **Verification:** The served mobile route returns the changed assets without requiring a server restart.

---

## C. Evidence

1. **Implementation commit:** `51baf95` merged into `main` by `bff6b7c`.
2. **Automated checks:** Frontend TypeScript passes; focused parser, renderer, and CSS contracts pass `26/26`.
3. **Served assets:** The mobile card route, `thread.css`, shared parser, and shared renderer return `200` with the merged behavior.
4. **Contradicted result:** The `2026-07-13T09:51:36Z` mobile Brave screenshot shows cyan numbered-list markers but no visible inline-code accent.
5. **RCA:** The later `.ledger-card-body code` rule in `frontend-mobile/assets/mobile.css` had equal specificity and overrode `.thread-note-message code` from `thread.css`.
6. **Correction:** Commit `a689b51`, merged by `817960c`, uses `.thread-note .thread-note-message code` and adds a regression with the exact backtick spans shown in the screenshot.
7. **Gate:** Automated checks pass; the subtask remains active until mobile Brave shows the corrected inline-code accent.
