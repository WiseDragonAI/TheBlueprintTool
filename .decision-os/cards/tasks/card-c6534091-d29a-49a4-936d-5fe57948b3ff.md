## A. Canonical Tasks Contract

1. **Hardcoded identity:** Project task routing resolves ledger ID `tasks`; array position is no longer used.
2. **Creation invariant:** Registered-project loading and selected-directory intake create `tasks` when it is absent.
3. **Ardaria:** `/media/jbb/57af6506-cd41-47dd-bcb1-5280ec4da1e7/Ardaria_57/.decision-os/state.json` now registers `tasks`, and `.decision-os/tasks.json` exists.
4. **Implementation:** The canonical routing change is merged at `e9f07d97`.

---

## B. Specs-to-Tasks Migration

1. **Moved graph:** **253 cards**, **51 zones**, and **197 relationships** moved from `specs` to `tasks`.
2. **Moved content:** **246 card files** and **69 thread files** moved.
3. **Verified destination:** `specs` contains **0** `master-task` cards; `tasks` contains all **49**.
4. **Integrity:** The target has **0** broken relationships and **0** cards with an incorrect domain. This card and its thread now resolve from `tasks`.
5. **Runtime ownership:** **3** active queue items and **21** pipeline runs were reassigned to `tasks`; no migrated runtime record retains `specs` ownership.
6. **Historical gaps:** The source already lacked **7 card sidecars** and **4 thread sidecars**. Their records and references were migrated and the missing files were reported; no content was fabricated.
7. **Migration command:** The reusable atomic migrator and missing-sidecar handling are merged at `30a516bd` and `7d6902bb`.

---

## C. MultiTerm Correction

1. Removed the legacy Decision OS registrations for MOH on `50149`, Rudy on `4175`, and Ardaria on `4174`.
2. I incorrectly removed two unrelated Ardaria services while interpreting the cleanup request. That was outside the requested scope.
3. I restored both services immediately and verified HTTP `200`.
4. MultiTerm now contains exactly: `jbb-home-decision-os` on `50151`, `Ardaria57-layout-builder` on `4187`, and `Ardaria57-crystal-slot-catalog` on `4188`.
5. All three registered services are running and return HTTP `200`.

---

## D. Verification

1. Migration helper regressions passed **3/3**.
2. Ledger CLI typecheck passed.
3. `master-task-gate` reports `ready: true` with no discrepancies and valid thread roles.
4. Both linked subtasks are verified `done`.
5. **Master status remains `todo`**; it was not closed.

---
