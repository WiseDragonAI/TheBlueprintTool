## A. Product Decision

1. **System status** must `present` one diagnostics hierarchy: catalog **project rows** plus a conditional **System row**; the separate incident block must `remain removed`.
2. Each **row** must `use` native accessible disclosure; rows with active incidents must `open` initially, while incident-free rows must `remain` collapsed.
3. **Project ownership** must `resolve` from exact catalog identity before exact scope identity; unverified ownership must `remain` under **System**.
4. **Implementation** must `remain` directly on `dev`, as ordered by the operator.

---

## B. Delivery State

1. **Owner-aware projection** now `groups` active incidents by verified owner, code, and message while retaining deterministic severity, counts, components, scopes, timestamps, interruption state, and unchanged project availability.
2. **System status rendering** now `uses` native project disclosures with nested incident cards and a conditional **System row**.
3. **Automated interaction proof** `covers` disclosure placement, pointer and keyboard input, conditional System behavior, desktop and `390`-pixel overflow, and page-error absence.

---

## C. Acceptance State

1. The complete **repository gate** now `passes` both typechecks plus the full frontend, backend, and browser suites after the isolated capacity-resume result.
2. Stable **skill identity selection** `restores` the reusable Explicit override lifecycle after canonical prompt expansion changed generic picker ordering.
3. Shared **canonical prompt fixtures** `restore` isolated refresh and reusable-pipeline scenarios without changing production prompt behavior.

---

## D. Served Proof

1. The existing **canary** `serves` `/status` with `HTTP 200` from the direct-`dev` frontend without a restart.
2. Live **decision-os** and **System** rows `contain` active incidents and `open` initially; the incident-free **Search** row `remains` collapsed.
3. Representative **pointer** and **keyboard** input `toggle` the native disclosures in both directions.
4. The **standalone incident shell** `is absent`; desktop and `390`-pixel views `show` no horizontal overflow and no page error.

---

## E. Commit Boundary

1. Exact **diff review** `admits` the owner projection, disclosure rendering, responsive rules, focused coverage, isolated Chromium scenario, and canonical prompt fixture repairs as one completed iteration.
2. Unrelated **Decision OS runtime and operator content** remain `unstaged` and outside the admitted commit boundary.
3. **Task closure** remains `unauthorized`; operator QA follows commit preparation.

---

## F. Next Execution

1. **Gate decision** `queues` `implementation-commit` at `medium` effort because every acceptance gate is green and the exact admitted diff is ready for scoped commit construction on direct `dev`.
