## A. Implementation Report

1. **Runtime correction:** Replaced the unscoped `threadPinOnRender` boolean with `ThreadViewportPinRequest { threadId, surface, openGeneration, reason }`, identity-matched consumption, and one canonical panel-entry controller.
2. **Transition precedence:** Panel open, thread switch, and tab activation enable follow and pin the activated surface. Ordinary rerenders preserve a paused viewport.
3. **Scroll ownership:** `renderThreadCodexLog()` is DOM-only. `applyThreadViewportState()` is the shared post-render writer. Live Codex polling updates only the log surface, so it cannot suppress conversation wheel tracking.
4. **Delayed layout:** One active `ResizeObserver` coalesces followed-content growth into one animation-frame bottom alignment and disconnects when ownership changes.
5. **Code quality:** Selection, controller entry, and rendering now share one active-tab state helper. New controller, effect, and helper files contain ownership-specific WHAT and WHY comments.
6. **Verification:** Focused tests pass, frontend TypeScript passes, and the full frontend suite passes `492/492`.
7. **Served evidence:** The canonical route returned HTTP `200` in `/snap/bin/chromium`. At `390x844` and `1280x900`, real wheel input created `follow=false` at `890px` from bottom; same-card reopen returned to `0px` with `follow=true`; an ordinary rerender retained `700px` distance; both tab activation and `1400px` delayed growth ended at `0px`.
8. **Commits:** `03ab9cdb`, `574e7a1f`, and `fff82f17` are merged into `main` through merge commits ending at `d208e773`.

---

## B. Subtasks

1. [Capture Contradicted Thread-Open Reproduction](card:card-6b8c2cbd-d743-4e15-af5e-708b1428eaf0)
2. [Define Typed Thread Viewport Transition Ownership](card:card-f49eceab-0e7e-4e6b-92c2-bb3989c749bc)
3. [Centralize Thread Entry Controller and Bottom Pinning](card:card-97762994-0ef1-4d00-851a-2b0190ba8fa1)
4. [Separate Continuation Scroll from Entry Pinning](card:card-d34605ff-6464-4c1a-ba7f-88398b4f3e86)
5. [Replace Scroll Regressions and Verify Served Interaction](card:card-9f1030e2-0df0-4461-ae93-5968a2f4a5e1)
