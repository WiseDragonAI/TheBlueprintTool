## A. Scope

Create a dedicated `dev` branch and isolated worktree from the current `main` tip. Register the worktree launcher on port `50151` in MultiTerm with separate state, settings, logs, process identity, and URL. Do not stop, restart, replace, or repoint production `50150`.

---

## B. Result

1. **Branch and worktree:** `dev` starts at `9583acf8` in `.worktrees/dev`.
2. **MultiTerm:** `decision-os-workstation-dev` owns port `50151` with automatic restart.
3. **Simultaneous availability:** production `50150` and canary `50151` both returned HTTP `200`.
4. **Catalog isolation:** production project discovery returned no `.worktrees/dev` entry.
5. **Versioned evidence:** canary configuration and runbook are committed on `dev` as `ee7f5cd7` and pushed to `origin/dev`.

---

## Review Reconciliation — 2026-07-30

1. Historical canary isolation is verified on the main-line commit `7ffd7841`; the earlier `ee7f5cd7` reference was not in the reviewed main ancestry.
2. The current canary on `50151` is HTTP-ready but reports a blank release SHA, delivery protocol `0`, and an unbootstrapped release pointer.
3. This card remains complete for environment creation. It does not prove release admission.
