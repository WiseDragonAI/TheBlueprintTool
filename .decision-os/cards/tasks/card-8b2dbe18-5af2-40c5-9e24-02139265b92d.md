## A. Scope

Create a dedicated `dev` branch and isolated worktree from the current `main` tip. Register the worktree launcher on port `50151` in MultiTerm with separate state, settings, logs, process identity, and URL. Do not stop, restart, replace, or repoint production `50150`.

---

## B. Acceptance

1. Git proves the `dev` branch starts at the selected `main` commit.
2. MultiTerm records the exact canary cwd, launcher, port, URL, and description.
3. Canary and production health routes return success simultaneously.
4. Canary project state and ignored settings resolve inside the canary worktree.
5. Startup, health, ownership, rollback, and cleanup evidence is documented before feature work begins.
