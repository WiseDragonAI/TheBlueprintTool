#master-task

Ledger: Specs

## A. Scope

1. **Running state:** When a thread Codex process is running, hide the model, effort, and launch controls.
2. **Layout collapse:** Remove the grid row and gap previously reserved for those hidden controls on desktop and mobile.
3. **Evidence:** The attached mobile screenshot shows a blank band below the `Thread` and `Codex Log` tabs during an active run.

---

## C. Acceptance Criteria

1. **Desktop:** `.thread-heading[data-codex-running="true"]` uses one `28px` row with a `0` gap.
2. **Mobile:** `.mobile-thread-inspector .thread-heading[data-codex-running="true"]` uses one `44px` row with a `0` gap.
3. **Regression:** Automated CSS contract tests cover both running-state layouts.
