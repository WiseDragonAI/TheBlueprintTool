## A. Scope

1. **Type:** `scenario`.
2. **Targets:** the existing `/api/control-room` and `/?tab=queue` routes.
3. **Constraint:** Use the running server and do not restart or stop it.

---

## B. Scenario

1. Inspect a Queue containing local and remote tasks with different waiting timestamps.
2. Record the API Queue identity, owner, rank, and `waitingTime` sequence.
3. Compare the rendered `.control-task` row sequence with the API sequence.

---

## C. Completion

1. Both routes return HTTP success.
2. Unranked tasks run from the latest `waitingTime` at the top to the earliest at the bottom.
3. Local and remote owners are interleaved by the ordering contract instead of grouped by node.
4. The DOM row order matches the API Queue order.

---

## D. Verification Status

1. **Automated server evidence:** temporary HTTP and two-node federation integration servers pass.
2. **Remaining gate:** the operator-facing server still runs the pre-merge backend because no restart was authorized.
3. After the next authorized server restart, verify that the rendered Queue matches `/api/control-room.queue` and interleaves owners by rank and waiting time.
