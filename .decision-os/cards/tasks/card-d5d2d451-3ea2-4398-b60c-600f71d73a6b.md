## A. Scope

1. **Build:** Append a versioned `decision_os.context_usage` record with session, timestamp, metrics, and source metadata.
2. **Constraint:** Deduplicate unchanged observations and keep Codex files read-only.

---

## B. Acceptance

1. **Proof:** Snapshots survive restart and repeated collection creates no duplicate for the same source event.
