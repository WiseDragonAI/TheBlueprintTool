## A. Retrospective finding

1. **Requested outcome:** `Shift+X` had to accept and durably persist voice audio, close the card without waiting for transcription, then represent pre-launch transcription as Active rather than Codex queued.
2. **First incorrect decision:** commit `0d758ea5` prevented upload interruption by keeping the card open, which removed the shortcut's required navigation behavior.
3. **Second incomplete decision:** commit `3bb012a4` restored navigation only after HTTP acceptance, but the backend acceptance path still waited for transcription and reused `executionStatus=pending`, so transcription appeared as queue waiting.
4. **Delivered correction:** feature commit `02653667`, merged as `2d407c69`, returns HTTP 202 after persistence, continues transcription asynchronously, and projects `transcribing-before-launch` into Active with `codexQueued=false`.

---

## B. Durable lessons saved

1. **Memory record `4` — Preserve the complete interaction contract:** behavior-only fixes must preserve and test every observable transition, including terminal navigation. Evidence: the operator rejected the first fix because it protected upload completion by removing the intended Shift+X exit.
2. **Memory record `5` — Separate durable acceptance from downstream processing:** asynchronous acceptance must follow durable persistence, while downstream processing uses a distinct lifecycle state. Evidence: the final correction returned HTTP 202 before transcription settled and stopped classifying transcription as ordinary Codex queue waiting.
3. **Deduplication:** both project-scoped `code` searches returned no represented lesson before these records were added; the subsequent list confirmed records `4` and `5` with run `codex-skill-1784291445490-701870f7` and their stable commit sources.

---

## C. Closeout

1. **Gate:** `ledger-cli master-task-gate` returned `ready=true` with no discrepancies.
2. **Authorization:** the intentional `$retrospect-and-close-task` invocation authorizes completion of master card `card-0fecda0d-a333-4116-99c5-9f7a604f22d2` and its canonical subtasks.
3. **Canonical action:** `ledger-cli master-task-complete --card-id card-0fecda0d-a333-4116-99c5-9f7a604f22d2` is issued exactly once after memory work.
---

Codex run completed: exit code 0
