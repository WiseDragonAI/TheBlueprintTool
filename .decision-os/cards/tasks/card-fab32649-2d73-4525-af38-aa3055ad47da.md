## A. Scope

1. **Automated checks:** add focused tests for aggregation, ownership, search, project filters, and tag filters.
2. **Target route:** verify `/skills` and `/pipelines` are served by the running Decision OS instance.
3. **Browser behavior:** exercise the complete filter interaction with mobile Chromium without restarting the server.

---

## B. Acceptance Criteria

1. **Focused tests:** the mobile Codex tests pass with `--test-concurrency=1`.
2. **HTTP:** both target routes return `200`.
3. **Behavior:** the initial list is visible, project filtering narrows results, tag filtering narrows results, and clearing restores the list.
