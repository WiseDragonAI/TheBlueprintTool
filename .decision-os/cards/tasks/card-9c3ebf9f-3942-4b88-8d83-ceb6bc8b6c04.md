#master-task

Ledger: Specs

## A. Scope

1. **Desktop behavior:** Preserve the restored desktop `A` shortcut and `focusThreadDraft()` behavior.
2. **Mobile behavior:** Open the mobile card thread without focusing `.thread-draft`.
3. **Layout:** Prevent the mobile software keyboard from opening automatically and shifting the thread layout.

---

## B. Implementation Contract

1. **Desktop restoration:** Keep revert commit `8b362cf`, which restores `focusThreadDraft()` in the desktop keyboard router.
2. **Mobile correction:** Remove `document.querySelector('.thread-draft')?.focus()` from `openMobileThread()` in `frontend-mobile/src/mobile-thread.js`.
3. **Regression coverage:** Scope the mobile test to `openMobileThread()` and assert that it renders the panel without calling `.focus()`.

---

## C. Acceptance Criteria

1. **Desktop behavior:** Desktop keyboard focus behavior remains unchanged after commit `8b362cf`.
2. **Mobile thread:** Tapping `Thread` renders the inspector without programmatically focusing `.thread-draft`.
3. **Keyboard:** Opening a mobile thread does not automatically raise the software keyboard.
4. **Verification:** All mobile thread tests and the frontend TypeScript check pass.

---
