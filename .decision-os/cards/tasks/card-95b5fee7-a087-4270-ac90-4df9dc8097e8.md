## A. Scope

1. **Type:** `code`, `test`, `ops`.
2. **Objective:** Trace the duplicate canary bind through launcher ownership and Node listener settlement, then make one runtime own the configured port while a competing start terminates deterministically after recording actionable evidence.

---

## B. Code Boundary

1. **Target files:** `backend/src/business/server/http/create-node-http-listener.ts`, `backend/src/server.ts`, the canary launcher boundary under `bin/`, and the focused server failure-containment tests.
2. **Target symbols:** `createNodeHttpListener`, `main`, listener `error` handling, and the launcher process-ownership path.
3. **Action:** Correct the first transition that permits an additional runtime to contend for the registered canary listener, ensure startup promises and process lifecycle settle, and retain health plus incident diagnostics for the affected startup scope.

---

## C. Completion

1. **Done when:** A regression injects a competing bind and proves the established listener stays available, the failed start settles without a restart loop, the incident contains owner and port context, and explicit revalidation resolves the scope only after exclusive ownership is restored.
2. **Depends on:** None.
