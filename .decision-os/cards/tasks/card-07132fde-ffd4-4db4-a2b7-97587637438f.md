## A. Operator request

1. **Remove the obsolete error-details disclosure from the Control Room immediately.**
2. Preserve current Control Room task cards and their inline validation indicators.

---

## B. Root cause

1. Commit `ea1892a2` removed the old task-detail disclosure styles but left the separate `control-diagnostics` element and its rendering paths.
2. `renderControlRoom()` replaced that `<details>` element's children, including its `<summary>`. Chromium then rendered its default `Details` summary, matching the operator screenshot.

---

## C. Implemented result

1. Removed the `control-diagnostics` HTML element, CSS rule, application element registration, diagnostics render block, and visible failure paths.
2. Adjusted the desktop Control Room grid from five rows to four.
3. Retained task-local diagnostic derivation and inline task indicators.
4. Committed the focused change as `a343b84c` and merged it into `main` as `76e308df`.

---

## D. Verification

1. Focused Control Room suite: **52 passed, 0 failed**.
2. Frontend TypeScript check: **passed**.
3. Full frontend suite: **461 passed, 0 failed**.
4. Running target `http://127.0.0.1:50151/`: **HTTP 200**; served HTML, CSS, and application JavaScript contain no `control-diagnostics` reference.
5. The existing server was not restarted.

---
