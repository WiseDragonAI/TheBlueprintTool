## A. Result

1. **State:** implemented and verified.
2. **Regression fixtures:** backend, frontend, federation, and browser cases contain stale ranks that conflict with timestamps.
3. **Assertions:** every Queue sorts newest-first, projections omit `queueRank`, the hydration boundary re-sorts API data, and gestures do not persist ranks.
4. **Checks:** focused backend `15/15`, focused frontend `51/51`, browser Queue `1/1`, frontend suite `477/477`, and both typechecks passed.
