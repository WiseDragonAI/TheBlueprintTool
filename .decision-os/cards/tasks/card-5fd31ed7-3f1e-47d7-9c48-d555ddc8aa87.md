## A. Scope

1. **Implementation:** Add the mobile master-task delete button, confirmation modal, existing `delete-card` mutation call, and return navigation.

---

## B. Acceptance Criteria

1. **Preservation:** Linked subtask cards remain after the master-task card is deleted.
2. **Verification:** `node --test frontend-mobile/test/mobile-control-room.test.mjs` passes.

---

## C. Evidence

1. **Commit:** Implemented in merge commit `31b3341` and focused commit `c3690b0`.
2. **Automated checks:** All `27` mobile Control Room tests pass; `node --check frontend-mobile/src/mobile.js` passes.
3. **Target route:** `http://127.0.0.1:50150/specs` returns HTTP `200` from the existing server process.
4. **Device interaction:** Not yet verified on the operator-facing mobile surface.
