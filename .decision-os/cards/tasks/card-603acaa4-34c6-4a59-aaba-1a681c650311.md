Completed at: 2026-07-19T05:37:26.227Z

## A. Requested Behavior

1. **Default follow:** Opening a task places both `Thread` and `Codex Log` at their newest content with follow-bottom enabled when no persisted pause exists.
2. **Conversation updates:** Optimistic voice-note insertion and completed transcription reconciliation remain visible while conversation follow is active.
3. **Codex updates:** Incoming Codex events remain visible while log follow is active.
4. **Exit condition:** A trusted upward operator scroll more than `72px` from bottom disables follow only for the active surface.
5. **Re-entry:** Clicking the existing down arrow pins the active surface to newest content and re-enables follow.
6. **Independent persistence:** Conversation and Codex Log retain separate offsets and follow flags through `decision-os.canvas.state`, task changes, reloads, and server restarts.
7. **Paused reading:** Renders and incoming content preserve the saved reading position while follow is disabled.

---

## B. Verified Regression Boundary

1. **Missing defaults:** A missing follow record resolved to disabled, preventing first-open bottom pinning.
2. **Conversation/log divergence:** Codex Log relied on an eight-pixel pre-render heuristic instead of explicit per-surface follow state.
3. **Duplicate omission:** The removed duplicate task contained a valid persistence requirement absent from this task: retain both offsets and follow flags across reloads and restarts.
4. **Reload edge found during served verification:** An early empty render saved `0` before notes arrived and overwrote a paused offset. The implementation now suppresses render-generated scroll tracking and does not take redundant pre-render snapshots.
5. **Existing update paths retained:** Optimistic voice insertion, transcription reconciliation, active-thread refresh, and Codex event rendering continue through their established render paths.

---

## C. Consolidation and Implementation

1. **Duplicate removed:** Deleted master `card-09e3fbd4-2aaf-4db2-9451-addf56f2cb04` and its four linked subtasks from the ledger after merging the persistence scope here.
2. **Conversation lifecycle:** Missing follow state now defaults to active; upward scroll disengages; the arrow resumes follow; live voice transitions pin only while following.
3. **Codex lifecycle:** Added `threadLogFollowBottomByThreadId`, active-surface routing, independent offset restore, live event pinning, disengagement, and arrow re-entry.
4. **Persistence:** Validated conversation/log offsets and follow records hydrate and persist through `decision-os.canvas.state`.
5. **Render safety:** DOM replacement cannot overwrite a paused offset with a transient empty-layout position.
6. **Commits:** `9564267c`, `3868f38c`, and `0b6d4d69`; merged by `403765a2`, `c2920da5`, and `d6787d75`.

---

## D. Verification

1. **Focused checks:** `28/28` thread selection and panel tests pass, including persisted independent follow state and render-time paused-offset protection.
2. **Typecheck:** Frontend TypeScript check passes.
3. **Full suite:** All `460` frontend tests pass through `node bin/decision-os-verify.mjs -- <command>`.
4. **Served route:** `http://127.0.0.1:50151/p/ZGV2L0VkaXRvckJQL2RlY2lzaW9uLW9z/ledgers/specs/zones/zone-0634e0db-9d1e-4a98-9c16-b9b0ae13ebf6/cards/card-603acaa4-34c6-4a59-aaba-1a681c650311` returned HTTP `200`; the server was not restarted.
5. **Conversation browser evidence:** Trusted wheel input changed `follow=true` at bottom to `follow=false` at offset `645`; incoming content held `645`; the arrow restored bottom and `follow=true`. Optimistic voice insertion moved to `955/955`, and completed transcription moved to `956/956`.
6. **Reload browser evidence:** A paused conversation restored offset `724/884`, `follow=false`, and the visible arrow after reload without being overwritten to `0`.
7. **Codex browser evidence:** Codex Log opened at `4918/4918`; trusted upward wheel input changed only log follow to disabled at `4318`; appended output held position; the arrow restored `4918/4918` and log follow without changing conversation follow.

---

## E. Subtasks

1. [Restore Conversation Follow-Bottom Lifecycle](card:card-74e6882a-94e0-44b4-bb0a-3f8f0d53f8c9)
2. [Apply Follow-Bottom Lifecycle to Codex Log](card:card-46ecd71e-7c74-4b48-90a7-805b9200a8b7)
3. [Add Follow-Bottom Regression Coverage](card:card-3f77f86a-c2da-4bbd-aa31-829607683fc7)
4. [Verify Served Follow-Bottom Interactions](card:card-92ec6b40-9bea-4c63-8273-d7987c151f7b)
