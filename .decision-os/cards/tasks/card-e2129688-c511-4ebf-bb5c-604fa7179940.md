## A. Result

1. **Label and icon:** the control renders uppercase `RUN` with the existing SVG chevron treatment.
2. **Reference fidelity:** the control reuses the Voice widget surface contract and limits its accent to the icon and label.
3. **Behavior:** the existing thread Codex action, model, and effort bindings are unchanged.

---

## B. Contradicted Success RCA

1. **Reported failure:** the first implementation colored the full control and used title-case text.
2. **First incorrect transition:** `.thread-codex-button` copied a card-status surface and omitted the existing `thread-action-button` mobile style hook.
3. **Persisted source:** merge `ddeaf92` contained the rejected surface treatment.
4. **Regression:** coverage rejects a Run-button background override and requires uppercase `RUN`, the SVG icon, and the shared mobile style hook.

---

## C. Verification

1. **Automated:** focused thread-panel and CSS contract tests passed; frontend typecheck passed.
2. **Target:** the canonical card route returned HTTP `200` without a server restart.
3. **Computed comparison:** the served `RUN` control and Voice `REC` control used the same neutral surface.
4. **Gesture:** a mobile Chromium touch click reached the existing run-status path; the verification interception prevented a Codex mutation.
5. **Commits:** `8382687`, `98bb0f3`, `47cb6b7`, and `7c2176b`.
