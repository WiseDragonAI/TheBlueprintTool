## A. Scope

1. Add global `GET`, arm `POST`, and cancel `DELETE` endpoints for deferred poweroff.
2. Compute drain status from every available local project pipeline store, every local durable process queue, runtime-owned processes, project-sync Codex ownership, and the global scheduler promise.
3. Re-evaluate after the one-second scheduler scan and after every Codex settlement callback.
4. Invoke the executor once after admission is closed and the authoritative gate is idle.
5. Keep remote federation-owned work outside this workstation gate.

---

## B. Targets

1. **Files:** `backend/src/business/server/helper/create-http-server.ts`; `backend/src/business/server/helper/project-url-scope.ts`.
2. **Symbols:** `projectContexts`; `scheduleGlobalCodexProcesses`; `globalCodexRunningProcessCount`; `projectSyncCodexRunningProcessCount`; `onCodexRunSettled`; global endpoint allowlist.

---

## C. Done When

1. API status reports durable request state plus exact pending and running counts.
2. Poweroff cannot fire while any local pending item, running store record, live process, project-sync process, or scheduler promise exists.
3. The eligible transition invokes the injected executor once.
