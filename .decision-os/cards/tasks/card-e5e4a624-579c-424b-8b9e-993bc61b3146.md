## A. Integration

1. Add root package scripts that invoke `bin/decision-os-verify.mjs` for focused, typecheck, and full verification.
2. Change `ledger-cli execution-profile` to return canonical verifier commands instead of raw TSX templates and manual bootstrap commands.
3. Replace the fragmented test instructions in `AGENTS.md` with the exact canonical command contract while preserving the existing admission order and verification scope rules.

---

## B. Compatibility

1. Update launcher and CLI tests that assert execution-profile output.
2. Keep existing package-local test scripts available for human use, but remove them from agent-facing instructions and generated Decision OS context.