## A. Implementation

1. **`ledger-cli master-task-complete --card-id <id>` reads the injected ledger, project, and server identities, then sends one PATCH to the existing atomic completion route.**
2. **The route returns its completion commit SHA in `x-decision-os-completion-commit`; the CLI includes it in a compact receipt.**
3. **The synchronized `retrospect-and-close-task` package uses `DECISION_OS_PROJECT_ID` directly and replaces project discovery plus curl construction with the single CLI command.**

---

## B. Verification

1. **Focused coverage:** prompt, parser, request-count, route-error, help, and commit-transaction tests pass.
2. **CLI suite:** `57` tests pass.
3. **Typechecks:** backend and ledger-cli TypeScript pass.
4. **Skill projection:** the installed package and Skills-card Markdown are byte-identical; the served skill route returns HTTP `200`, `source: server`, revision `d5b637e754dc7d793a990fbe77ed85b282856e5d95a12a7957ee73884b7f62f8`.
5. **Delivery:** code is merged through `20a9724`; synchronized skill commit is `5e26b225dc8449d3204ba501f14d35ca0a3d12e0`.
6. **Unrelated backend check:** all changed backend tests pass; the unchanged pipeline-capacity timing test remains red and reproduces alone with `['start','end']` instead of `['start','start']`.