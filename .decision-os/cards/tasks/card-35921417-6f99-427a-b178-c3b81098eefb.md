## A. Verified implementation

1. **Immediate handoff restored:** queued Shift+X submission is launched without awaiting network settlement.
2. **Navigation boundary:** `onPersisted` invokes `finishQueuedVoiceSubmission(true)` only after IndexedDB persistence succeeds.
3. **Served evidence:** Chromium reached `/?tab=exec` while the upload response was deliberately unresolved.
4. **Commits:** `18df448e` and merge `6890f88a`.
