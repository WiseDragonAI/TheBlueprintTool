## A. Change

1. **Type:** `test`
2. **Targets:** `frontend/test-responsive/control-filter-wheel.test.mjs`, `frontend/test-responsive/mobile-control-room.test.mjs`, and `tests/browser/application/the-application-is-one-responsive-frontend.spec.ts`.
3. **Action:** Add deterministic wheel-routing cases for horizontal overflow, both boundaries, non-overflow, mobile width, and horizontal trackpad input. Assert that the responsive application binds both Control Room filter rows, then extend the served desktop scenario with enough project and ledger chips to overflow and dispatch representative wheel input over each row.
4. **Dependency:** Complete the wheel-routing code subtask first.

---

## B. Completion

1. **Automated evidence:** Focused checks pass for every routing and boundary case.
2. **Served project row:** Browser input moves the project row horizontally while page `scrollY` stays fixed.
3. **Served ledger row:** Browser input moves the ledger row horizontally while page `scrollY` stays fixed.
4. **Boundary evidence:** Input at the reached horizontal boundary is available to page scrolling.
