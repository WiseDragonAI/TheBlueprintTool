## A. Scope

1. **Skill:** Update the server-owned `retrospect-and-close-task` package.
2. **Completion:** Replace manual lifecycle and Git closeout instructions with the canonical project-scoped `complete-master-task` mutation.
3. **Synchronization:** Run `ledger-cli skills update --source "$HOME/decision-os/.tmp-skill-source/retrospect-and-close-task" --json` from the server launch directory `$HOME`.

---

## B. Verification

1. **Package:** The Decision OS package validator accepted the revised source inside the update transaction.
2. **Projection:** The live API reports `source: server` and revision `dc8c9aa1b7861f3094bf394160ffafe1d9d2738642ad05dc36f1246e1a1f8d5d`; `cmp` confirms the package and canonical card projection are identical.
3. **Commit:** The focused server skill update commit is `bbf808c18976d64e50b5333a8e00a5b76d6cb540` and contains only the skill package, UI metadata, and canonical skill card projection.
4. **Contract:** The skill now requires `PATCH /p/<projectId>/decision-os/<ledgerId>` with action `complete-master-task` and forbids manual lifecycle rewrites, `ledger-cli done`, blanket `.decision-os` staging, and a second documentation commit.
5. **Environment:** The Python quick validator cannot start because the Termux environment has no `yaml` module; the transactional Decision OS validator passed.