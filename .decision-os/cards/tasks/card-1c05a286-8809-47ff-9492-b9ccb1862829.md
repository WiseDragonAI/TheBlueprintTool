## A. Scope

1. **Type:** `scenario`.
2. **Target:** `http://127.0.0.1:50150/`.
3. **Constraint:** Use the running Decision OS server and do not restart it.

---

## B. Scenario

1. Confirm the operator-facing route returns HTTP success.
2. At desktop width, inspect `Backlog | Queue | Exec` order and compare card DOM structure, computed foreground, background, spacing, and dimensions across lanes.
3. Drag one Queue task to Backlog and observe the UI before its request resolves.
4. Reload and confirm the task remains in Backlog.
5. Drag the task back to Queue, reload, and confirm its placement and rank remain.
6. Reorder Queue tasks within the lane.
7. Reject one status mutation and confirm the UI returns to server-confirmed state.
8. Confirm Exec rejects drops and mobile retains one selected tab.

---

## C. Completion

1. HTTP, computed-style, full pointer-gesture, optimistic-state, reload-persistence, and rejection-reconciliation evidence is recorded.
2. The task remains open when the served pointer workflow cannot be exercised.
