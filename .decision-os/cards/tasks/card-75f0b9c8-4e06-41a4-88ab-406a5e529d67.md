## A. Delivered Desktop Project Settings

1. **Project control:** Desktop project cards expose one accessible `Settings` control with a gear SVG and omit the generic pencil, content edit, status, processing, and delete controls.
2. **Shared editor:** `Settings` opens the existing project editor for title, description, and HSV color.
3. **Persistence:** Save uses `PATCH /decision-os/projects/:projectId`, refreshes from server-confirmed state, retains rejected edits in the open dialog, and preserves project-card geometry.

---

## B. Contradicted-Success RCA

1. **Operator evidence:** The thread screenshot showed `admin`, `rudy`, and `MOH` cards with the same cyan fallback border despite distinct saved project colors.
2. **First incorrect transition:** `ensureProjectsCanvasDocument` correctly copied `project.color` into `card.color`; `patchLedgerCard` then applied only zone attribution and cleared `--card-zone-color` for project cards without a zone.
3. **Correction:** `patchLedgerCard` now validates explicit `card.color`, applies it to the card color variables, and gives the explicit card color precedence over geometric zone inheritance.
4. **Regression:** The hydration test proves that `#a855f7` overrides an inherited `#eab308` zone color, while the existing zone-color test remains green.

---

## C. Verification Evidence

1. **Focused tests:** Ledger-card rendering passed `21/21`; project-canvas mirroring passed `1/1`.
2. **Typecheck:** Frontend TypeScript passed with exit code `0`.
3. **Frontend suite:** `410/420` passed. The ten failures are the previously recorded responsive assertions that expect dynamically loaded vendor assets as static `index.html` tags.
4. **Served browser:** Chromium loaded `http://127.0.0.1:50151/projects` with HTTP `200`. Project cards hydrated `#fadc2f`, `#60a5fa`, `#895cfa`, `#fbbf24`, and `#34d399`; computed borders differed by project and remained correct after a fresh reload.
5. **Geometry:** Browser-observed card positions and dimensions remained identical across the color render and fresh reload.
6. **Commits:** Feature commit `2f54fefe`; merge commit `0e2a7170`.

---

## D. Open Master State

1. **Subtasks:** All three relationship-backed subtasks remain verified and `done`.
2. **Master task:** This master card remains open for direct operator action.

---
