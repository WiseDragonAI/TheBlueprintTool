## A. Launch Boundary

1. **Process Card pipeline launch** must `close` the modal and `return` the operator to Control Room `Exec` before `POST /api/codex/pipelines/runs` settles.
2. **Pipeline-only handoff** must `retain` direct-skill timing while `separating` early navigation from late receipt settlement and the existing stale-action fence.
3. **Cold-route intent** must `persist` before Control Room hydration so the first authoritative projection can `materialize` the matching `preparing` task.

---

## B. Reconciliation Contract

1. **Successful admission** must `remove` the optimistic intent only through matching client receipt identity, then `refresh` the authoritative Control Room projection.
2. **Rejected admission** must `remove` only the matching request, `restore` confirmed placement, and `retain` the global mutation alert after the modal closes.
3. **Admission wait** must `expire` after `30_000ms` through a responsive-client constant and `reuse` the exact rejection lifecycle; no settings response, durable schema, or migration `changes`.
4. **Image upload** remains `excluded`; **direct-skill parity** remains `excluded`.

---

## C. Delivery Group

1. **Group `G1`** `contains` `T1 → T4 → T2 → T3` under one implementation owner.
2. **Task `T1`** `owns` early pipeline handoff and cold-route optimistic intent materialization.
3. **Task `T4`** `owns` the fixed `30_000ms` responsive admission deadline and exact timeout reconciliation.
4. **Tasks `T2` and `T3`** must `prove` pending optimism, matching success, exact rejection, timeout settlement, reload survival, and unchanged direct-skill behavior in focused contracts before the served Linux fixture.
5. **Collision control** `keeps` responsive launch, intent hydration, receipt identity, lifecycle listeners, focused tests, and browser routes within the same sequential group.

---

## D. Controller Decision

1. **Previous blocker** `is resolved`: the fixed `30_000ms` client deadline `bounds` the optimistic admission wait without `creating` a second server configuration source or `altering` the `30min` execution deadline.
2. **Next stage** `is` `implementation-orchestrator` on `gpt-5.6-sol` at `high` effort because `G1` is now dispatch-ready and one owner must implement the repaired sequential boundary.
3. **Implementation worker** must `use` an isolated feature worktree from current `dev`, `preserve` protected and unrelated state, `add` adjacent `WHAT:` and `WHY:` comments for modified branches, and `verify` through the repository lease.
4. **Master-task closure** remains `unauthorized`; the following gate must `assess` worker results, verification evidence, commit and integration state, then `select` the next quality stage.

---

## E. Implementation Evidence

1. **Tasks `T1` and `T4`** are implemented through pipeline-only pre-settlement handoff, cold-route intent materialization, the fixed `30_000ms` abort deadline, and shared exact-request rejection reconciliation.
2. **Task `T2`** is covered by focused contracts for pending timing, receipt identity, unrelated-request retention, cold-route hydration, timeout settlement, and unchanged direct-skill timing.
3. **Task `T3`** is covered by the served Linux fixture for pre-settlement `Preparing`, authoritative success after reload, rejection restoration with the global alert retained, and timeout restoration.
4. **Verification** passed the focused 26-test contract set, frontend typecheck, the focused Linux browser scenario, and the repository `test:front-back` suite.
5. **Task closure** remains unauthorized.
