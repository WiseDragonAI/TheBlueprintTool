## A. Verification

1. `node bin/decision-os-verify.mjs -- npm run test:front-back`: both typechecks passed; the front-end suite failed and stopped the workflow.
2. Focused front-end run: **ten failing assertions**.
3. Separate admitted back-end run: **one failing assertion**.
4. Separate admitted browser run: **167 passed and three failed**.

---

## B. Front-End Failures

1. Three responsive static tests expect SortableJS, noUiSlider, and Embla asset tags in `frontend/index.html`. Production now loads all three from `frontend/src/app/controller/boot-application.ts`.
2. Six thread and voice assertions read `frontend/assets/canvas/thread.css` or `frontend/assets/canvas/terminal-chat-controls.css`. These files moved to `frontend/assets/shared/`, and both `application-shared.css` and `canvas.css` import the shared files.
3. The in-flight ledger refresh test times out because its harness never installs the new canvas surface effects. `renderCanvasSurface()` therefore remains the inactive no-op, no cards are mounted for measurement, and the two expected geometry PATCH requests are never produced.

---

## C. Back-End and Browser Failures

1. The project-catalog integration test expects the served HTML to contain `src/runtime/canvas-runtime.ts`. The served HTML now correctly points to `src/runtime/surface-runtime.ts`, which selects the surface and dynamically imports `canvas-runtime.js` for wide canvas routes.
2. The no-bundler browser spec repeats that stale `canvas-runtime.ts` token expectation.
3. The thread-header browser spec reads the removed `frontend/assets/canvas/thread.css` path.
4. The responsive browser test records two 404 console errors. Direct reproduction identified the URL as `/p/ZGV2L0VkaXRvckJQL2RlY2lzaW9uLW9z/api/ledgers/specs/threads/thread-10000001`; the test opens the same missing thread once on desktop and once on mobile.

---

## D. Impact

1. The asset, CSS-path, runtime-token, and refresh-harness failures do not demonstrate broken operator behavior; they fail before or outside the current production boundary.
2. The thread 404 demonstrates that this browser test depends on live ledger data without requiring an existing thread fixture. The card and thread panels still render, but the zero-console-error assertion fails.
3. No product code was changed during this diagnostic run.
