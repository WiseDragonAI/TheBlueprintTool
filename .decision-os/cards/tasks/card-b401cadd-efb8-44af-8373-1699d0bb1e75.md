Create a durable node-message execution record before process spawn. Return a short-lived admission response with stable request and run IDs, explicit phase, timestamps, executor identity, and idempotency behavior. Add a bounded status lookup that survives requester disconnect and relay reconnect.