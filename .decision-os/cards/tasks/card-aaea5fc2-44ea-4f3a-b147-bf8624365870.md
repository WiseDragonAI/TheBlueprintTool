## A. Scope

1. **Objective:** Add the local mobile Chromium runbook and reference it from `AGENTS.md`.

---

## B. Implementation

1. **Runbook:** Create `BROWSER_RUNBOOK.md` from the verified parent-workspace guidance, adapted to the `decision-os` working directory.
2. **Agent contract:** Add mandatory mobile Chromium instructions to `AGENTS.md`.
3. **Safety:** Preserve the Termux launch flags that prevent Chromium child-process and network-service crashes.

---

## C. Acceptance Criteria

1. **Paths:** Every documented browser path exists on this device.
2. **Flags:** The runbook lists all five verified Android Chromium launch flags.
3. **Reference:** `AGENTS.md` links to `BROWSER_RUNBOOK.md`.
4. **Commit:** Only the requested documentation files are included in the feature commit.

---

## D. Evidence

1. **Chromium:** `/data/data/com.termux/files/usr/bin/chromium-browser` reports `Chromium 138.0.7204.168`.
2. **Helper:** `../tool/browser/browse.js` and `../tool/browser/node_modules/puppeteer-core` are present.
3. **Flag parity:** All five required flags appear in `BROWSER_RUNBOOK.md`, `AGENTS.md`, and `../tool/browser/browse.js`.
4. **Commit:** `336dd9c` contains only `AGENTS.md` and `BROWSER_RUNBOOK.md`.
5. **Server state:** No Chromium process was launched and the Decision OS server was not restarted or stopped.
