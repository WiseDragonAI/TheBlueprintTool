## A. Verification state

1. **Focused backend:** Authoritative admission tests pass `2/2`; stale pending ownership is replaced and a live child is rejected.
2. **Existing backend status suite:** Passes `8/8` with the live-process `active` projection.
3. **Focused mobile:** Passes `12/12`.
4. **Mobile suite:** `82/83` pass; the sole failure is the pre-existing unrelated `mobile-project-color` source-pattern assertion.
5. **Typechecks:** Backend and frontend scoped typechecks pass.
6. **Served mobile route:** HTTP `200`. Chromium displayed enabled visible `RUN`; the intercepted click issued a fresh project-scoped `POST /api/codex/threads/process` with the exact card identity and issued zero continuation requests.
7. **Runtime gate:** Port `50150` still runs the backend process loaded before merge `bc24088`. An operator-authorized restart is required before the real backend click can be exercised. The server was not restarted.