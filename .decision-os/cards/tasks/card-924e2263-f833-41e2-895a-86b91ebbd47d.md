## A. Required lys synchronization outcome

1. **Local creation:** Clicking `Synchronize project` for remote `lys` creates and registers `/home/jbb/lys` when no matching workstation checkout exists.
2. **Correct ownership:** The labeled synchronization master task is created in the workstation `lys` ledger; it never falls back to `Admin`.
3. **Immediate handoff:** Successful admission closes settings, opens Control Room `Exec`, and targets the executing `lys` task.
4. **Node visibility:** The same logical `lys` task is synchronized to the source checkout and projected under both node owners without competing canonical pipelines.

---

## B. Federated pipeline execution

1. **Canonical owner:** The master task and durable pipeline run live in workstation `lys`.
2. **Phone:** `project-sync-source-publisher` publishes the locked source state and returns its verified SHA.
3. **Workstation:** `project-sync-initiator-reconciler` reconciles `/home/jbb/lys` from that SHA.
4. **Phone:** `project-sync-source-finalizer` fast-forwards the source checkout to the verified workstation SHA.

---

## C. Git identity and safety

1. **Identity:** Admission verifies the origin fingerprint and preserves the origin lock across phases.
2. **SSH key:** Workstation synchronization uses `projectSyncGitSshIdentityFile` set to `/home/jbb/.ssh/id_jb_wise` for the clone and later repository Git operations.
3. **Verification:** Fixed Git commands validate cleanliness, SHA equality, and predecessor ancestry after every phase.
4. **Recovery:** Failed materialization marks the run `failed` and releases the origin lock.

---

## D. Implementation evidence

1. **Pipeline:** `4414f1c5` introduced the shared federated pipeline and Control Room admission path.
2. **Ownership:** `afc25500` materializes the matching project before task admission.
3. **SSH selection:** `e713d672` binds the configured workstation identity.
4. **Checks:** Focused ownership and SSH tests, the project-sync route integration, and backend typechecking pass.

---

## E. Remaining validation

1. **Served gate:** Port `50151` must load `e713d672` before exercising the click.
2. **Observation:** One click must create local `lys`, create its executing task, navigate to Control Room, dispatch phone/workstation/phone phases, and persist across reload.
3. **Lifecycle:** The master task remains open for operator closeout after served validation.

---
