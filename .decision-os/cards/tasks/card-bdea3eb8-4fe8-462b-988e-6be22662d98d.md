Persist voice acceptance before acknowledgment, route queued, transcribing, retrying, completed, and failed transitions through the local mutation repository, replicate transcription state and text, and exclude raw audio heads.