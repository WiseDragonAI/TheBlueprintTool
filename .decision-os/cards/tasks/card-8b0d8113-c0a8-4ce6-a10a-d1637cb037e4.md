## A. Scope

1. **Type:** `code`.
2. **Targets:** `backend/src/business/server/helper/control-room-projection-store.ts` and `backend/src/business/server/helper/federated-control-room-projection.ts`.
3. **Symbols:** `compareTasks()`, `aggregateProjection()`, and `federatedControlRoomProjection()`.

---

## B. Implementation

1. Reverse the unranked Queue timestamp fallback so larger `waitingTime` values appear first.
2. Sort the fully qualified federation Queue after local and remote projections are merged.
3. Preserve explicit `Queue rank` precedence and use project, ledger, and card identity as deterministic tie breakers.
4. Leave Exec, Backlog, Done, task qualification, and ownership metadata unchanged.

---

## C. Completion

1. `/api/control-room.queue` has one order across all projects and nodes.
2. Unranked tasks are newest-waiting first and longest-waiting last.
3. Explicit ranked order still survives a fresh projection.
4. Changing `ownerNodeId` alone cannot change a task's position.

---

## D. Implementation Results

1. Added `compareControlRoomQueueTasks()` as the shared rank-first, newest-waiting-first Queue comparator.
2. Applied it to the multi-project local projection and to the fully qualified federation Queue after merge.
3. Invalid timestamps sort last; equal values use project, ledger, and card identity for deterministic output.
4. Exec, Backlog, Done, ownership metadata, and drag persistence remain on their existing paths.
5. **Commit:** `21824d70`; merged by `c430f41e`.

---

## E. Reopened RCA

1. **Status:** Reopened after the operator showed a node-grouped served Queue despite the merged comparator.
2. **Comparator evidence:** the current Workstation API orders unranked tasks by descending `waitingTime`; every screenshot row checked in the current Queue has `queueRank: null`.
3. **Next boundary:** capture the actual client URL, node, DOM row sequence, and `/api/control-room` request status plus response before changing code.
4. **Regression requirement:** once the first incorrect boundary is verified, add a regression at that boundary before a second served-success claim.
