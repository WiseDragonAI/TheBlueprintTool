## A. Implemented correction

1. The responsive thread now publishes the selected `projectId`; upload, retry, transcription polling, watcher restoration, and pending-upload persistence retain that project for the full voice lifecycle.
2. Pending-upload restoration and watcher identity are project-scoped, preventing collisions between federation nodes with equal ledger and thread identifiers.
3. `publishCard` and `publishLedger` now invalidate the owning project's federation task-replica cache before publishing content changes, forcing the next replica read to include the new thread note.

---

## B. Commit

1. **Merged on `main`:** `61ec22cf` (`merge: preserve federated voice thread ownership`).
2. The isolated feature commit is `9e4b023d`.
