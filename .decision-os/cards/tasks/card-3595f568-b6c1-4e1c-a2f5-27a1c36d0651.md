## A. Execution report

1. **Implemented:** card-entry thread defaults now run only when the project, ledger, zone, and card identity changes. Same-card reconciliation preserves the operator-owned thread and composer presentation.
2. **Ownership boundaries:** route loads use a monotonic generation, immutable route snapshot, abort controller, and post-await ownership checks. Thread refreshes capture project, ledger, card, thread, panel state, and generation before applying responses.
3. **Scoped events:** federation refreshes match the active project card route; thread SSE is active only while the panel is open and accepts exact thread events. Accepted process runs navigate only while the initiating route and thread-presentation generation remain current.
4. **Mobile history:** opening a mobile thread adds a card-local history layer; browser Back closes that layer before card navigation.
5. **Verification:** frontend typecheck passed. The full frontend suite passed `465/465`. The exact served card route returned HTTP `200` and served commit `5d5820f0` code.
6. **Published:** `main` was pushed to `origin`; remote `refs/heads/main` resolves to `5d5820f0`.
7. **Remaining gate:** representative browser interaction is not verified because the in-app browser is unavailable and `chromium-browser` is not installed. The master task remains open.

---
