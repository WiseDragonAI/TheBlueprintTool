#master-task #task-complete

Ledger: `Specs`
Waiting since: `2026-07-13T15:41:52.540Z`
Queue rank: 7
Active since: 2026-07-14T08:12:40.600Z
Completed at: 2026-07-14T13:53:55.532Z

## A. Skill Draft

1. **Skill:** `retrospect-and-close-task`.
2. **Run:** Read the session, tool calls, cards, tests, and changes; save reusable lessons; summarize them in the thread; close verified cards; validate; commit `.decision-os`.
3. **Memory:** Use `$HOME/decision-os/tool/memory/memory.mjs` with the server launch root, project ID, and type.
4. **Safety:** Never write to `$HOME/.codex/memories_1.sqlite`.

---

## B. Commands

1. **Search:** `node "$HOME/decision-os/tool/memory/memory.mjs" search --root "$HOME" --project <project-id> --type <type> --query "<keywords>"`
2. **Save:** `node "$HOME/decision-os/tool/memory/memory.mjs" add --root "$HOME" --project <project-id> --type <type> --title "<title>" --body "<lesson>" --tag <tag> --subtag <subtag> --source "<source>"`
3. **Verify:** `node "$HOME/decision-os/tool/memory/memory.mjs" list --root "$HOME" --project <project-id> --type <type>`
4. **Close:** `ledger-cli done --ledger "$DECISION_OS_LEDGER_FILE" --card-id <card-id>`, then `ledger-cli validate-master-tasks --ledger "$DECISION_OS_LEDGER_FILE" --card-id <master-card-id>`, then `git add .decision-os && git commit -m "docs: close <task>"`.
5. **Create:** `ledger-cli skills create --root "$HOME" --source <skill-directory> --json`.

---

## C. Verified Database Choice

1. **Lesson store:** `$HOME/.decision-os/memories.sqlite3` contains the migrated lesson with `project_id=ZGVjaXNpb24tb3M`, `type=code`, and integrity `ok`.
2. **Codex internal store:** `$HOME/.codex/memories_1.sqlite` remains outside this contract.

---

## D. Created Skill

1. **Server package:** `$HOME/.skills/retrospect-and-close-task` contains the synchronized `SKILL.md` and `agents/openai.yaml` for every catalog project.
2. **Projection:** `$HOME/.decision-os/skills.json` maps Skills card `card-skill-retrospect-and-close-task-skill-md-0abb9a1300` to the exact `SKILL.md` mirror.
3. **Verification:** The live project-scoped Skills APIs for `ZGVjaXNpb24tb3M` and `aGVhbHRoL3Bpbms` return HTTP `200` and classify the skill as `source: server`.
4. **Commits:** Catalog ledger provisioning is `5e9dd21`; global skill creation is `c9fed44`; mistaken workspace creation was removed by `a54db68`.

---

## Z. Subtasks