## A. Objective and Context

1. Verify what Decision OS injects into a returning pipeline gate after that gate queues one selected skill.
2. Establish a runtime-backed acceptance boundary for successful re-entry while preserving the admitted prompt contract.

---

## B. Verified Current State

1. The successful dynamic path started a distinct returning-gate execution after the selected analysis completed.
2. The returning gate received the complete selected-skill Markdown through `PREVIOUS_SKILL_RESULT`, refreshed master-task and operator-thread content, and a new execution context.
3. The admitted prompt snapshot, revision, commit, model, and effort remained fixed across re-entry.
4. The generated execution input proves context injection; the operator-facing rendered prompt has not been independently captured.

---

## C. Strategic Constraints and Choice

1. Preserve immutable prompt admission while refreshing only runtime task, thread, predecessor-result, and execution variables.
2. Use one acceptance path: capture the complete returning prompt on the operator-facing execution surface, then harden the same boundary for unavailable selected-skill results and visible recovery states.
3. Preserve generated run outputs and their positioned relationships until the operator authorizes a graph correction.

---

## D. Current Decision and Blocker

1. Successful gate re-entry and predecessor-result injection are verified.
2. Keep the master task open until rendered-prompt evidence and failure-path behavior are established.
3. Master-task conversion is blocked because generated pipeline-result cards already occupy canonical subtask positions, while the scoped apply command accepts only a master with no canonical subtasks.
