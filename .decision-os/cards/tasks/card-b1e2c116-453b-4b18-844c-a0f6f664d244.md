## A. Implemented Result

1. **Helper:** Added side-effect-free `projectFilterForeground` and `projectFilterChipPresentation` exports in `frontend/src/app/responsive/project-filter-chip.js`.
2. **Contract:** The presentation contains exactly `label`, `showRemoteMarker`, and `foreground`.
3. **Locality:** The marker flag is true only for `remote === true`.

---

## B. Verification

1. **Color cases:** Yellow `#ffd732`, cyan `#59a8f8`, black, and white cases pass.
2. **Presentation cases:** Remote true, remote false, and absent remote values pass.
