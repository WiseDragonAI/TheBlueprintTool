## A. Scope

1. Add a global Control Room action named **Power off after queue** with explicit confirmation.
2. Render `armed` state with pending and running counts and a **Cancel shutdown** action.
3. Render `failed` with the exact backend diagnostic and require a new explicit arm.
4. Keep the action available on desktop and mobile, keyboard accessible, and outside project scoping.

---

## B. Targets

1. **Files:** `frontend/index.html`; `frontend/src/app/responsive/application.js`; `frontend/src/runtime/project/helper/project-request-scope.ts`; `frontend/assets/application.css`; `frontend/assets/shared/terminal-button.css`.
2. **Symbols:** Control Room footer; `renderControlRoom`; global request allowlist; arm/status/cancel handlers.

---

## C. Done When

1. Confirmation is required before arming.
2. Visible state and counts update from backend responses without navigation.
3. Cancel returns the surface to the ready state and restores focus.
4. The global endpoint is never rewritten beneath `/p/:projectId`.
