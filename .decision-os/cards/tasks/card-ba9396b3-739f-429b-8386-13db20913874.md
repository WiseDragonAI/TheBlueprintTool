## A. Implementation

1. **Added explicit voice launch modes: `send`, `run`, and `pipeline`.**
2. **`Shift+X` routes to direct Codex execution after transcription.**
3. **Direct voice Run start and continuation prompts explicitly state `Do not invoke or use any skill`.**
4. **`Ctrl+X` routes to the configured pipeline after transcription.**
5. **The legacy `queueCodex` upload field remains supported as direct Run.**

---

## B. Verification Evidence

1. **Backend orchestration tests passed for direct launch, existing-session continuation, configured pipeline launch, transcription failure cleanup, and the no-skill prompt.**
2. **The original implementation merged in commit `bbcaddd7`.**
3. **The desktop modifier preview merged in commit `03caebe8`.**

---

## C. Remaining Interaction Check

1. **After the next server restart, record a note and stop it with `Shift+X`; confirm the note transcribes and the direct run starts.**
2. **Record another note and stop it with `Ctrl+X`; confirm the configured pipeline enters execution.**

---

## D. Desktop Modifier Preview

1. **While desktop recording is active, holding `Shift` changes the visible Send action to the mobile Run icon and `RUN` label.**
2. **Holding `Control` changes it to the mobile Pipeline icon and `PIPELINE` label; Control has the same precedence as the actual `X` shortcut.**
3. **Releasing modifiers restores the Send icon and `SEND` label.**
4. **The visible shortcut badge remains `X`.**
5. **A real Chromium regression dispatched modifier keydown and keyup events and verified `SEND → RUN → PIPELINE → RUN → SEND`.**
