Remove head-of-line coupling between task-state replication and request/response control frames. Give state handlers and RPC delivery independent queues or bounded execution boundaries, and prove a stalled state merge cannot prevent node-message admission while the node remains advertised online.