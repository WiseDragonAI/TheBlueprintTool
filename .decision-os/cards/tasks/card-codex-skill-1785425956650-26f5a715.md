## A. Repository Intent

1. **Voice-note authority:** Decision OS creates one optimistic voice note, patches that same note through upload and transcription, then reconciles it with the durable ledger note without replacing its stable identity. The contract is implemented across `mergeLocalThreadNotes()` and `shouldApplyVoiceServerNote()` and documented in `README.md:281-295` plus `documentation/specs.json`.
2. **Replicated execution authority:** queued work belongs to the exact `executorNodeId`; startup recovery schedules only work owned by the hosted runtime. The contract is documented in `documentation/codex-task-assignment-and-replicated-execution-plan-2026-07-23.md:176-181` and `:302-312`.
3. **Failure containment:** detached processes, direct node-message executions, project watchers, and background publication must each have an explicit bounded settlement owner. A failure must remain inside its owning runtime scope and leave incident evidence.

---

## B. Current Iteration Intent

1. **Voice objective:** replace an optimistic same-ID `uploading` voice record with its forward durable transcript during live refresh while retaining `localVoiceUploadId`, local image sizing, and the stable note ID.
2. **Analysis objective:** reconstruct the backend verification failures hidden by two empty `run-test-and-fix` card results, identify each first incorrect transition, and establish one repair boundary before another mutation pass.
3. **Execution boundary:** this analysis made no product edit, test edit, server change, browser interaction, commit, push, subtask closure, and master-task closure.
4. **Repository state:** `HEAD` and `origin/main` are `808a16a7`. The voice correction is published through feature commit `5cc5837f` and merge `95efdc27`. The primary checkout contains an unrelated unstaged nine-file backend/browser delta; the index is empty.

---

## C. Findings

1. **Resolved gap — stale live transcription state.** The original first incorrect transition was the generic same-ID overlay in `mergeLocalThreadNotes()`: the incoming durable transcript entered the merged collection, then `{ ...incoming, ...localNote }` restored the older placeholder body, `uploading` lifecycle, old revision, and `optimistic: true`. The committed branch at `frontend/src/runtime/ledger/helper/merge-local-thread-notes.ts:68-84` accepts only an incoming thread slice, a same-ID optimistic local voice note, a non-empty `localVoiceUploadId`, and a forward transition admitted by `shouldApplyVoiceServerNote()`. The focused forward and backward regressions are in `frontend/test/runtime/ledger-content-refresh.integration.test.ts:458-538` and `frontend/test/runtime/voice-transcription-runtime.integration.test.ts:805-849`.
2. **Verification gap — real microphone behavior.** Automated evidence proves the merge transition. No evidence proves the same rendered row changes during a real microphone transcription and remains correct after a fresh reload. `fixed` and `complete` remain prohibited claims.
3. **Drift — restart-queue fixture ownership.** The captured failure was `Timed out waiting for .../b-queued/invoked.txt`. The fixture admitted the durable execution with `executorNodeId: local`, while process-wide repository settings could make `taskExecutionNodeId(runtime)` equal the configured workstation identity. `codex-process-scheduler.ts` correctly rejects work assigned to another node. The first incorrect transition was fixture admission with a different owner identity, before scheduler claim and child launch. The preserved causal repair gives the fixture and hosted runtime one explicit node identity; it does not change production scheduler behavior.
4. **Drift — publication tests observed the wrong boundary.** The full-suite pipeline-prompt test counted calls to the connector's generic `request()` method, so unrelated startup federation traffic produced `1 !== 0`. Pipeline-prompt publication is owned by `publishManifest()`. The preserved repair asserts that exact method remains unused. The isolated Git-recovery case completed local recovery with HTTP `200`, then failed because a `2,000 ms` incident poll expired under suite load. The preserved repair retains the detached publication contract and uses a bounded `10,000 ms` observation window.
5. **Omission — one awaited lifecycle lacked a referenced settlement owner.** `executeNodeMessage()` returned a promise while its execution deadline, cancellation escalation, and forced-settlement timers were unreferenced. With no other active handle, Node could end the test scope before the promise settled, producing `Promise resolution is still pending but the event loop has already resolved`. The causal production correction keeps those bounded timers referenced until settlement and cleanup.
6. **Rejected production changes — detached launcher and non-persistent watcher.** The Codex launcher intentionally survives replacement server shutdown, and the project watcher intentionally does not pin the process. Their production timers must remain unreferenced. Tests that explicitly await those detached boundaries need their own referenced test owner. The diagnosing run restored production `.unref()` behavior and added test-owned intervals. The current primary-checkout delta instead removes `.unref()` from `launchCodexExecutionProcess()` and `watchProjectFiles()`, so that unstaged delta does not match the causally reviewed repair.
7. **Omission — temporary servers inherited process-global runtime settings.** Several fixtures seeded `local` executions while their hosted runtime inherited a configured workstation node. The health fixture installed delivery release settings after server construction while startup had already admitted release identity. The reviewed repair supplies exact executor identity to each fixture and installs release settings before construction. Production identity enforcement and startup admission remain unchanged.
8. **Specification drift — authored publication response.** `documentation/working-documents/expanded-skill-authoring-authoritative-spec-2026-07-28.md:131-139` says post-commit relay failure returns `publication.status: failed`. Current source, tests, and commit `ad9c5248` return local success with `publication.status: not-applicable`, then persist publication failure asynchronously. The authoritative document is stale.
9. **Specification drift — transcription deadline.** `documentation/specs.json` specifies a maximum `30`-second transcribing timeout, while `voice-transcription-lifecycle.ts` uses `120,000 ms`. This drift did not cause the stale live-state defect and is outside the backend verification repair.
10. **Recovered verification evidence.** The original implementation run's backend result was `643/661` passed, `11` failed, and `7` cancelled; the earlier “two failures” summary described only the first visible scopes. The second debug run reproduced `9` failures and `7` cancellations, established the causes above, reached focused `39/39`, then reached focused `31/31`. Its final full gate passed both typechecks, frontend `610/610`, and backend `661/661`.
11. **Remaining repository failure.** The final full gate stopped at browser `181/187` passed with `5` skipped and one failure: `Reusable step pipelines preserve defaults and publish visible execution progression` waited for the obsolete message `Saved locally; publication failed: retry synchronization.` Current behavior returns `publication.status: not-applicable` and displays `Saved as a new Git revision.` Repository-green remains unestablished.
12. **Delivery omission — the causal repair is not integrated.** The preserved worktree `.worktrees/voice-note-run-test-fix-1785423910044` contains the reviewed eleven-file backend repair. It is unstaged and uncommitted. The primary checkout contains a different nine-file mixture that includes rejected launcher and watcher changes plus an unverified browser correction. No current commit contains the causal backend repair.

---

## D. Remediation Paths

1. **Preserve the voice correction:** keep the committed `mergeLocalThreadNotes()` voice branch and its forward/backward regressions unchanged.
2. **Use the causal worktree as the repair source:** continue `run-test-and-fix` in `.worktrees/voice-note-run-test-fix-1785423910044`; do not promote the divergent primary-checkout delta.
3. **Retain one production lifecycle correction:** keep referenced bounded timers in `executeNodeMessage()` through terminal settlement and cleanup.
4. **Preserve intentional detachment:** retain `.unref()` in `launchCodexExecutionProcess()` and `watchProjectFiles()`; keep the referenced owner inside tests that await those non-persistent lifecycles.
5. **Make execution fixtures deterministic:** assign each seeded execution to the exact hosted runtime node, isolate temporary server settings, and install release identity before server construction.
6. **Make publication assertions exact:** observe `publishManifest()` for pipeline-prompt exclusion, retain the bounded `10,000 ms` background-incident window, isolate the browser fixture from repository federation defaults, and assert the current `not-applicable` response plus successful Git-revision message.
7. **Reconcile documentation:** update the authored-publication specification to record local `not-applicable` response status followed by asynchronously persisted publication incidents.
8. **Keep timeout drift separate:** create a later focused correction that aligns `voice-transcription-lifecycle.ts` with the documented `30,000 ms` deadline and verifies timeout cleanup without changing the live reconciliation branch.
9. **Verify the integrated boundary:** run the causal focused backend files, backend typecheck, backend suite, browser pipeline regression, then `npm run test:front-back` once through `bin/decision-os-verify.mjs`. A terminal zero exit is required before repository-green.
10. **Continue the pipeline only after green:** run `code-quality-improver` against the admitted delta, run post-review verification, then use `implementation-commit`. Keep the master task active pending real-microphone evidence.

---

## E. Operator Decision Summary

1. **Voice decision:** the stale-state cause is corrected and published; no voice refactor and no rollback is warranted.
2. **Backend decision:** the restart failure was fixture identity drift, the publication failures were assertion-boundary drift, and one direct node-message settlement omission required production correction.
3. **Current gate:** historical backend `661/661` is established for the preserved causal worktree. Current repository-green is not established because the browser gate failed and the primary checkout contains a divergent unstaged repair mixture.
4. **Selected next action:** run `run-test-and-fix` from `.worktrees/voice-note-run-test-fix-1785423910044`, add the exact browser publication-fixture correction, and require a terminal full-suite pass before code-quality review.
5. **Proof boundary:** this analysis establishes causes and one remediation path from source plus captured run artifacts. It did not execute a new test.
---

Codex run completed: exit code 0
