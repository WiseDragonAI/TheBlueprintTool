## A. Implementation

1. **Composer control:** Add a mobile-only `SEND` button with `data-action="submit-thread-draft"` and route it through `submitThreadDraft()`.
2. **Responsive layout:** At `max-width: 760px`, reduce `.thread-draft` to `min-height: 72px`, hide `.terminal-command-hint`, and expose the send button.
3. **Desktop contract:** Keep the send button hidden and preserve the existing `Ctrl+Enter` workflow above the mobile breakpoint.

---

## B. Verification

1. **Focused test:** `node --test --import tsx test/runtime/mobile-thread-composer.integration.test.ts` passes from `frontend/`.
2. **Type safety:** `frontend/node_modules/.bin/tsc -p frontend/tsconfig.json --noEmit` passes.
3. **Commit:** Implementation is merged in commit `aaf00b5` through feature commit `437cf6d`.
