## A. Implementation

1. Remove the global `startupProjectTasks` listener barrier.
2. Open the HTTP listener after global settings, registry, and incident-ledger validation.
3. Preserve emergency diagnostics behavior for process-wide startup validation failures.

---

## B. Verification

1. Add a listener-deadline integration test with a production-sized task-state corpus.
2. Prove health, diagnostics, and static routes respond while project hydration is still pending.

---

## C. Delivered Boundary

1. The HTTP listener opens after synchronous local-state reconstruction without awaiting execution recovery.
2. Missing card and thread content returns local structural state immediately and hydrates through one observed background demand.
3. Remote replica reads return causal task state while content remains `synchronizing`.
4. Missing execution presentations return a schema-valid `hydrating` view while one deduplicated, bounded, server-cancellable relay request fills the local registry.
5. Backend typecheck, focused recovery and hydration checks, and the bounded complete backend suite pass.
