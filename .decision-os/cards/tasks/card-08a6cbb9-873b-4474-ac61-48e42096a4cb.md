## A. Scope

1. Add deterministic frontend integration coverage for arm, status, failure, and cancellation rendering.
2. Add served Chromium coverage for the confirmed interaction at desktop and mobile widths with mocked API responses.
3. Verify keyboard order, focus restoration, accessible names, live status text, responsive visibility, and global request URLs.

---

## B. Targets

1. **Files:** `frontend/test/runtime/deferred-poweroff-control.integration.test.ts`; `tests/browser/control-room/deferred-poweroff-control.spec.ts`.
2. **Symbols:** confirmation interaction; status polling; Control Room footer controls; mocked `/api/server/deferred-poweroff` routes.

---

## C. Done When

1. Desktop and mobile browser flows observe the complete arm and cancel lifecycle.
2. Failure state exposes the backend diagnostic and never claims shutdown is scheduled.
3. Browser fixtures make zero real systemd calls and preserve the running Decision OS server.
