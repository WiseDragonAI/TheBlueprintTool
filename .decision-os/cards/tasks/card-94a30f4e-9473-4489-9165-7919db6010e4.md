#task  #backend

## A. Scope

1. **Add** the server-side project creation contract and initialize a catalog child directory.

---

## B. Acceptance Criteria

1. **Validate** the required name, optional description, safe directory leaf, maximum lengths, and collisions before writes.
2. **Initialize** `.decision-os/state.json`, `.decision-os/project.json`, and catalog metadata.
3. **Expose** `POST /decision-os/projects` and return the discovered project.
4. **Cover** success and rejection paths with unit and integration tests.
