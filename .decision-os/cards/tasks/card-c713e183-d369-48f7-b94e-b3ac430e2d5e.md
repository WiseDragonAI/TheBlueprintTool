## A. Task

1. **Type:** `code`.
2. Replace direct `startProjectSyncCodex` execution in `createProjectSyncController` and the federated `/api/project-sync/role` handler with shared pipeline scheduling for the role skill selected by the synchronization phase.
3. Pass the sync identity, node identity, origin fingerprint, repository snapshot, required predecessor SHA, and master-task reference into each skill run context.
4. Continue validating each returned JSON result with `verifyProjectSyncPhase` before advancing the SHA handoff.
5. Project pipeline progress and terminal failures onto the synchronization master task so Control Room uses the canonical task execution state.

---

## B. Targets

1. `backend/src/business/project-sync/controller/start-project-sync.ts::runRole` and `advance`
2. `backend/src/business/server/helper/create-http-server.ts` `/api/project-sync/role` handler
3. `backend/src/business/codex/controller/start-codex-pipeline-run-controller.ts`
4. `backend/src/business/codex/helper/codex-pipeline-runner.ts` run context projection
5. `backend/src/business/project-sync/helper/verify-project-sync-phase.ts::verifyProjectSyncPhase`

---

## C. Completion

1. All three phases have durable pipeline skill-run IDs and verified SHA evidence.
2. Federated participant execution uses the same scheduler, cancellation, logging, and terminal status machinery as card-launched pipelines.
3. No call site invokes `startProjectSyncCodex`.

---

## D. Dependencies

1. **Depends on:** `Admit synchronization as a labeled master task`.
