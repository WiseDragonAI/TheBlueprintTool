## A. Implemented Result

1. **Unit coverage:** Added table-driven helper coverage under `frontend/test/unit/responsive/helper/project-filter-chip.test.ts`.
2. **Integration coverage:** Added renderer and CSS contract coverage under `frontend/test/runtime/control-room-project-filter-chip.integration.test.ts`.

---

## B. Verification

1. **Focused command:** Both files pass through the repository verification lease with `3` passing tests.
2. **Typecheck:** Frontend TypeScript validation passes.
3. **Sensitivity:** Assertions cover visible content, remote-only SVG creation, foreground wiring, selection semantics, tooltip metadata, and offline disabling.
