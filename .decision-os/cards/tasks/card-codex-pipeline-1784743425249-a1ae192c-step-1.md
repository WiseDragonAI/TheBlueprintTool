## A. Engineering Completeness Findings

1. The audit used source card `card-6ce8b11a-e641-4ac9-b124-76c3a23035b4` and its linked source cards: `card-ac30b58e-2fd5-4300-b4e2-016b3c71c8ee` for `BTN-01`, `card-dd7250a3-dee4-48b2-b095-b87a4b2049e5` for `BTN-02`, `card-aa3d0271-c9fa-424f-b903-731118f4e124` for `BTN-03`, `card-657e657f-73c9-4ef0-87c8-f352fc96f471` for `BTN-04` through `BTN-08`, and `card-893310ed-56bd-46fe-8445-0dae7e33ab23` for `BTN-09` and `BTN-10`.
2. The proposed architecture is sound after the repairs below. The responsive `/navigation` read model already supplies `cards`, `annotations`, and `relationships`; `application.js` already derives zone attribution in `ledgerZones()`. The relationship-backed parent destination can therefore be resolved in the frontend from the canonical `subtask` relationship and the parent card's existing zone attribution. **No backend API, durable data-model, migration, or configuration change is needed.**
3. The submitted inventory was stale. The current repository contains `91` static `<button>` tags in `frontend/index.html`, `66` production `createElement('button')` occurrences across the listed `25` source files, and `10` additional runtime-template `<button>` tags. The template sites are `frontend/src/runtime/voice/component/terminal-composer.ts` with `4`, `frontend/src/runtime/voice/component/control-dock.ts` with `4`, and `frontend/src/runtime/ledger/component/render-ledger-card-git-diff.ts` with `2`.
4. `GROUP-01` did not name the shared registry module and omitted `frontend/src/runtime/input/controller/handle-keyboard.ts`, even though `handle-action-click.ts` and `handle-keyboard.ts` currently duplicate command transitions for voice, thread, deletion, resize, and modal dismissal. A registry that excludes the keyboard dispatcher cannot prove click and keyboard parity.
5. `GROUP-06` covered the `createElement('button')` thread renderers but omitted the eight voice/template controls and the responsive action-identity transition in `frontend/src/app/responsive/thread-composer.js`. Those controls emit `thread-file-picker`, `toggle-thread-text`, `voice-toggle`, `submit-thread-draft`, `voice-cancel`, and `voice-stop`; they are part of the source card's explicit thread-and-voice family.
6. The existing voice persistence path already owns optimistic upload, durable pending-upload recovery, retry, server reconciliation, and thread identity. The repaired plan registers command ownership at the component/render boundary and preserves those domain controllers; it does not duplicate voice persistence in the command registry.
7. The named Control Room browser fixture currently creates only `master-task` cards and writes `relationships: []`. It cannot prove subtask Back behavior until the fixture includes one master card, one `subtask`-labeled child card, one positioned `subtask` relationship, and zone geometry that attributes both routes deterministically.
8. Initialization validation must remain **failsafe and scoped**: development diagnostics and automated checks reject an unowned, unknown, or multiply-owned command, while a renderer registration failure is contained to its owning surface and cannot terminate the server process.

---

## B. Fundamental Missing Tasks

1. `BTN-03` was missing the executable shared contract boundary. Add `frontend/src/runtime/input/command-ownership.ts` and include `handle-keyboard.ts` so static buttons, delegated clicks, form submissions, responsive listeners, and keyboard shortcuts resolve one command descriptor with one owner and one terminal settlement path.
2. `BTN-08` was missing the runtime voice-template family. Extend it to cover `terminal-composer.ts`, `control-dock.ts`, `render-voice-dock.ts`, `render-voice-status.ts`, and `responsive/thread-composer.js`, while retaining the five submitted runtime thread renderers.
3. `BTN-09` was missing an accurate completeness oracle. Its ownership test must enumerate static HTML, `createElement('button')` construction, and runtime-template construction; the current counts are audit evidence, not hard-coded proof of future completeness.
4. `BTN-10` was missing the relationship-backed browser fixture and final verification handoff. Extend the Control Room fixture with canonical parent-child state, then run the served click, `Escape`, history, pending, rejection, stale-completion, and reload scenarios through the repository verification lease.
5. No new task id is required. These corrections are direct implementation details of the existing source-backed `BTN-03`, `BTN-08`, `BTN-09`, and `BTN-10` tasks, and the authoritative group definitions in Section D include them.

---

## C. Input Card Edits Applied

1. Replaced the `90` static-button baseline with `91`, replaced the `62` dynamic-constructor baseline with `66`, and added the `10` runtime-template sites to the inventory evidence.
2. Amended `BTN-03` and `GROUP-01` with the new shared contract file and `handle-keyboard.ts`. The contract records `commandId`, state owner, transition owner, immutable resource identity, presentation generation, pending policy, reconciliation policy, keyboard binding, and terminal success/failure settlement.
3. Amended `BTN-08` and `GROUP-06` with the voice/template render boundary. The group owns thread presentation generation, voice surface identity, disabled/pending presentation, and the responsive `jump-thread-bottom` to `close-thread-text` command-identity transition.
4. Amended `BTN-09` and `GROUP-07` with new `frontend/test/runtime/button-command-ownership.integration.test.ts`, `frontend/test/runtime/control-room-parent-navigation.integration.test.ts`, and `frontend/test/unit/navigation/navigation-ownership.test.ts` targets. The tests cover reverse ancestry, missing source, invalid source label, ambiguous parents, descriptor validation, form ownership, constructor/template enumeration, uniqueness, and click/keyboard parity.
5. Amended `BTN-10` and `GROUP-08` so `tests/browser/control-room/the-column-scroll-position-survives-refresh-and-navigation.spec.ts` owns the canonical master/subtask fixture and verifies child Back to the parent route and title.
6. Preserved the submitted dependency gates: `GROUP-01` remains the contract gate; `GROUP-02` through `GROUP-06` remain parallel after it; `GROUP-07` remains the cross-family integration gate; `GROUP-08` remains the served-application and final-verification gate.
7. Added the implementation handoff constraint: workers use an isolated repository worktree because verification launches tests, run every focused and final check through `node bin/decision-os-verify.mjs -- <direct-command>`, preserve the Linux `/snap/bin/chromium` Playwright contract, and do not restart the operator server.

---

## D. Dispatch-Ready Groups

1. `GROUP-01` owns `BTN-01`, `BTN-02`, and `BTN-03` from source cards `card-ac30b58e-2fd5-4300-b4e2-016b3c71c8ee`, `card-dd7250a3-dee4-48b2-b095-b87a4b2049e5`, and `card-aa3d0271-c9fa-424f-b903-731118f4e124`. Target files are `frontend/index.html`, `frontend/src/app/responsive/control-room.js`, `frontend/src/app/responsive/navigation-ownership.js`, `frontend/src/app/responsive/application.js`, `frontend/src/app/responsive/codex.js`, `frontend/src/app/responsive/thread.js`, new `frontend/src/runtime/input/command-ownership.ts`, `frontend/src/runtime/input/controller/handle-action-click.ts`, `frontend/src/runtime/input/controller/handle-keyboard.ts`, and `frontend/src/runtime/input/effect/bind-inputs.ts`. Execute `BTN-01`, then `BTN-02`, then `BTN-03`. The worker must derive one canonical parent and zone-backed route, establish immutable Back/Close descriptors, route click and keyboard through one contract, contain registration failures to the owning surface, and expose stable registration APIs before downstream dispatch.
2. `GROUP-02` owns `BTN-04` from source card `card-657e657f-73c9-4ef0-87c8-f352fc96f471`. Target files are `frontend/src/app/responsive/application.js` and `frontend/src/app/responsive/codex.js`; target symbols are `taskRow`, `projectFilterButton`, `renderControlRoom`, `renderCards`, `renderCard`, `button`, `renderProcessList`, `renderPipelineLibrary`, and `renderEditor`. The worker consumes the finalized registry, captures route/component/request identity before asynchronous work, keeps responsive transaction rollback in the existing coordinator, and does not change the registry contract.
3. `GROUP-03` owns `BTN-05` from source card `card-657e657f-73c9-4ef0-87c8-f352fc96f471`. Target files are `frontend/src/runtime/canvas/effect/render-canvas-control-overlay.ts` and `frontend/src/runtime/canvas/effect/render-ledgers-indicator.ts`; target symbols are `renderCanvasControlOverlay` and `renderLedgersIndicator`. The worker preserves delegated `handleActionClick`, canvas selection, overlay teardown, and tool state while registering every overlay and ledger-indicator command once.
4. `GROUP-04` owns `BTN-06` from source card `card-657e657f-73c9-4ef0-87c8-f352fc96f471`. Target files are `frontend/src/runtime/codex/component/render-card-skill-run-widget.ts`, `frontend/src/runtime/codex/component/render-codex-library.ts`, `frontend/src/runtime/codex/effect/render-card-process-modal.ts`, `frontend/src/runtime/codex/effect/render-pipeline-editor-modal.ts`, `frontend/src/runtime/codex/effect/render-pipeline-skill-picker-modal.ts`, `frontend/src/runtime/codex/effect/render-pipelines-modal.ts`, and `frontend/src/runtime/codex/effect/render-skill-library-editor-modal.ts`. The worker binds run, modal, pipeline, skill, and request controls to stable resource identity and existing generation guards without moving Codex state into the registry.
5. `GROUP-05` owns `BTN-07` from source card `card-657e657f-73c9-4ef0-87c8-f352fc96f471`. Target files are `frontend/src/runtime/ledger/component/render-ledger-card-delete-button.ts`, `render-ledger-card-detail-layer.ts`, `render-ledger-card-git-diff.ts`, `render-ledger-card-html-embeds.ts`, `render-ledger-card-media.ts`, `render-ledger-card-questions.ts`, `render-ledger-card-status-button.ts`, `render-ledger-card-tabs.ts`, and `frontend/src/runtime/ledger/effect/open-ledger-card-image-viewer.ts`. The worker registers card mutation, Git-review voice, questionnaire, media, embed, tab, and viewer controls at their renderer/layer owners; persistence, optimistic state, rollback, and reconciliation remain in the existing domain controllers.
6. `GROUP-06` owns repaired `BTN-08` from source card `card-657e657f-73c9-4ef0-87c8-f352fc96f471`. Target files are `frontend/src/runtime/thread/component/render-thread-codex-log-status.ts`, `frontend/src/runtime/thread/effect/render-thread-codex-log.ts`, `render-thread-jump-button.ts`, `render-thread-notes.ts`, `render-thread-panel.ts`, `frontend/src/runtime/voice/component/terminal-composer.ts`, `frontend/src/runtime/voice/component/control-dock.ts`, `frontend/src/runtime/voice/effect/render-voice-dock.ts`, `frontend/src/runtime/voice/effect/render-voice-status.ts`, and `frontend/src/app/responsive/thread-composer.js`. The worker binds every command to the active thread presentation and voice capture owner, preserves existing upload/retry/reconciliation controllers, and makes pending, disabled, focus-restoration, rejection, and stale-presentation behavior explicit.
7. `GROUP-07` owns repaired `BTN-09` from source card `card-893310ed-56bd-46fe-8445-0dae7e33ab23`. Target files are `frontend/test/runtime/input-controller-routing.integration.test.ts`, `control-room-voice-transcribing-before-launch.integration.test.ts`, `mobile-thread-composer.integration.test.ts`, `codex-pipeline-modal.integration.test.ts`, `card-media-carousel-state.integration.test.ts`, `voice-action-entry-points.integration.test.ts`, new `button-command-ownership.integration.test.ts`, new `control-room-parent-navigation.integration.test.ts`, new `frontend/test/unit/navigation/navigation-ownership.test.ts`, `frontend/test/unit/voice/effect/render-voice-status.test.ts`, and `frontend/test/unit/ledger/helper/git-review-voice-capture.test.ts`. Start only after `GROUP-02` through `GROUP-06` settle. Verification must enumerate all three construction classes, prove single ownership and form ownership, exercise ancestry diagnostics and descriptor validation, and cover pending, success, rejection, stale completion, focus restoration, and duplicate-mutation suppression by family.
8. `GROUP-08` owns repaired `BTN-10` from source card `card-893310ed-56bd-46fe-8445-0dae7e33ab23`. Target files are `tests/browser/control-room/the-column-scroll-position-survives-refresh-and-navigation.spec.ts`, `tests/browser/navigation/navigation.spec.ts`, `tests/browser/codex/reusable-step-pipelines.spec.ts`, `tests/browser/thread/open-conversation-drawer.spec.ts`, `tests/browser/voice/voice-transcription-upload-is-optimistic.spec.ts`, `tests/browser/card/cards-have-a-discussion-thread.spec.ts`, `tests/browser/zone/a-zone-can-be-deleted-from-the-edit-panel.spec.ts`, and `tests/browser/toolbox/zone-confirmation-modal-can-process-keyboard-shortcuts.spec.ts`. The Control Room fixture must contain the canonical parent-child relationship and deterministic zone attribution. Run focused browser files with root `@playwright/test`, `/snap/bin/chromium`, and the Linux flags from `BROWSER_RUNBOOK.md`; then run one final leased `npm run test:front-back` and do not separately repeat its included typechecks and suites.
9. **Migration needs:** none. **Fixture needs:** the relationship-backed Control Room fixture in `GROUP-08`. **Configuration needs:** none. **API needs:** none. **Handoff data:** command declarations, owner identity, pending/reconciliation policy, test attribution, and any scoped diagnostics must remain discoverable by `GROUP-07` and `GROUP-08` without reopening the registry contract.

---

## E. Blocking Questions

1. None. The repository, linked source cards, operator constraints, current read models, and existing test harness determine the required repairs.

---

## F. Dispatch Readiness

1. **Status: `ready`.** The input grouping is dispatchable with the authoritative repairs in Sections C and D. No fundamental implementation task remains missing, and no card status was changed during this audit.
---

Codex run completed: exit code 0
