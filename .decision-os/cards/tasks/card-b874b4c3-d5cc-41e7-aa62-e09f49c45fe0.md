## A. Scope

1. **Control-room header:** Remove the repeated `Decision OS`, `Control room`, and `Live` presentation from the content header.
2. **Task creation:** Move `New task` into the fixed bottom action bar after `Queue`, `Active`, and `Done`.

---

## B. Acceptance Criteria

1. **Space recovery:** The content header contains only the ledger filters.
2. **Bottom actions:** `New task` is the fourth bottom action and preserves the existing task-intake behavior.
3. **Regression coverage:** Automated tests assert the four-column bottom layout and removed duplicate controls.
