## A. Scope

1. Type: `docs`.
2. Inspect the implemented button families and record the visual anatomy, typography, spacing, color, depth, icon treatment, and interactive states that define them.

---

## B. Codebase Targets

1. Target files: `frontend/assets/shared/terminal-button.css`, `frontend/assets/application.css`, `frontend/assets/canvas/objects.css`, and `frontend/index.html`.
2. Target symbols: `.terminal-button`, `.terminal-button--send`, `.terminal-button--stop`, `.terminal-button--neutral`, `.terminal-button--nav`, `.terminal-button--action`, `.primary-button`, `.create-button`, `.icon-button`, and their hover, active, focus, selected, disabled, and busy states.

---

## C. Completion

1. Done when this card contains a source-linked inventory that distinguishes the shared baseline from responsive and canvas-specific variants and accounts for every named interaction state.
