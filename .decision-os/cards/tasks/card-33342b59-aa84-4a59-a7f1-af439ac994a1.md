## A. Scope

1. Extend `federation-relay/` with a priority state lane for event batches, acknowledgements, bucket summaries, state-snapshot manifests and transfer, event inventories, missing-event requests, and convergence.
2. Route priority frames through the existing per-federation `Durable Object` while preserving outbound-only node connections, WebSocket hibernation, project scoping, and credential isolation.
3. Persist the complete shared event set, bounded bucket indexes, and latest verified checkpoint in Cloudflare while retaining node-local logs and snapshots for offline operation.
4. Publish after local durability, persist in the `Durable Object` before relay acknowledgement, and retransmit unacknowledged events after reconnect.
5. Bootstrap a blank node by advertising compatible snapshots, transferring and verifying the selected snapshot, then reconciling its event tail.
6. Reserve state-lane scheduling and flow control so content queues cannot delay state frames.

---

## B. Acceptance

1. Worker-runtime and two-node tests prove authenticated immediate delivery, durable acknowledgement, duplicate idempotence, disconnect catch-up, missing-old-event discovery, and periodic repair.
2. A blank node receives a verified state snapshot and tail while events emitted during bootstrap arrive before convergence.
3. Worker restart, Durable Object eviction, socket replacement, and offline destinations lose no accepted event.
4. A blocked content stream receives no additional credit while priority state work is pending, and state convergence completes first.
5. The public Worker health probe and authenticated two-node WSS exchange pass after deployment.

---

## C. Current Execution

1. **Repository implementation is merged.** The relay now durably stores project-scoped events, acknowledges only after storage, maintains bounded hourly reconciliation indexes, serves missing buckets, and broadcasts live batches only to authenticated project members.
2. Nodes publish all local history through anti-entropy, merge relay events into one project projection, clear obsolete peer-specific pending records, and upload verified checkpoints after convergence.
3. A fresh node receives the latest relay checkpoint before bucket reconciliation, verifies it locally, installs it atomically, and resumes with the uncovered tail.
4. Regression coverage proves convergence when writer and reader never overlap online, independent desktop/mobile genesis union, relay checkpoint transfer, duplicate-safe storage, and content-lane independence.
5. The subtask remains `todo` because public deployment and authenticated production `WSS` verification are blocked by the absent Cloudflare authentication on this workstation.
