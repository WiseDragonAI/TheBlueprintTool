## A. Scope

Trace the pinned @pierre/diffs patch parser, file model, line and hunk representation, unified and split renderers, shadow DOM, syntax behavior, expansion, overflow, lifecycle, cleanup, and supported customization hooks. Correlate upstream internals with Decision OS renderSkillRevisionDiff usage.

## B. Deliverable

Produce a source-backed internal model and identify which Pierre structures can be reused as data, which are inseparable from its renderer, and which constraints block direct integration with an editable Markdown document. Do not implement the feature.