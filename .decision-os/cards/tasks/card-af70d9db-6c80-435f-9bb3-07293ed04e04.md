## A. Global Context and Objective

1. **Objective:** make cross-node task collaboration live and reliable: an open Mobile thread must receive converged notes without a page refresh, and Workstation-assigned execution must accept Mobile requests without requester-local Codex artifacts.

---

## B. Verified Current State

1. **Thread hydration:** federated resource-head installation emitted only a global invalidation, while the responsive thread requires a project-, ledger-, and thread-scoped content event.
2. **Execution routing:** thread starts and continuations inspected requester-local card, thread, JSONL, and output files before assignment routing; the reported missing run-output error occurred before the Workstation executor could receive the request.
3. **Image notes:** pasted images bypassed the durable pending-message receipt and replaced structured rejection evidence with a generic confirmation error.
4. **Canary:** Dev now contains the correction, the registered Canary was restarted from the pushed Dev state, and its project route is available.

---

## C. Strategic Constraints and Choice

1. **Execution authority:** task assignment selects the executor; replica URL provenance does not.
2. **Artifact boundary:** mutable Codex run files remain executor-local and are not bulk-replicated.
3. **Content authority:** notes remain causal multi-writer contributions, with Markdown converging through resource heads and materializing on demand.
4. **Selected path:** resolve assignment before executor-local validation, publish the existing scoped content event when replicated heads change, and reuse the existing pending-message receipt for image markdown.

---

## D. Current Decision or Blocker

1. **Operator gate:** keep the task open on Mobile while another node appends a note and confirm the note appears without refresh. Submit a follow-up that continues the Workstation-assigned run. Paste an image and confirm durable convergence. If the commit is rejected, confirm that the exact retryable rejection remains visible.
2. **Lifecycle:** keep this master task active until that Mobile interaction proves the complete event-to-request-to-durable-thread transition.
