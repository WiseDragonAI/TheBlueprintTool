## A. Implementation result

1. Added a testable controller separating durable local acceptance from network settlement.
2. Routed mobile `RUN` and `PIPELINE` through durable handoff without changing recorder, upload, retry, or backend orchestration.
3. Preserved `SEND` and added delayed-upload coverage.
