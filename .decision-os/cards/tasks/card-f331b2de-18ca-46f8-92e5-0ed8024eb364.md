## A. Scope

1. **Drawer content:** Remove `.project-nav-section`, `#project-links`, project-row rendering, and inline color editing from the burger drawer.
2. **Destination order:** Render `Control room`, `Projects`, `Ledgers`, `Pipelines`, and `Skill library` in that exact order.
3. **Destination icons:** Replace the shared empty-circle pseudo-icon with five distinct inline SVG icons that inherit `currentColor`.

---

## B. Icon Contract

1. **Control room:** Use a dashboard icon.
2. **Projects:** Use a folder icon.
3. **Ledgers:** Use a book icon.
4. **Pipelines:** Use a connected-flow icon.
5. **Skill library:** Use a library icon.
6. **Accessibility:** Mark decorative SVGs `aria-hidden="true"`, keep visible labels, and preserve the existing minimum `44px` control target and focus-visible treatment.

---

## C. Acceptance Criteria

1. **No project rows:** Drawer markup and runtime contain no project list or project color input.
2. **Dedicated icons:** Every destination has one distinct SVG path contract and no `.ledger-link::before` empty-circle rule remains.
3. **Active state:** `/` activates `Control room`, both project routes activate `Projects`, and `/ledgers` activates `Ledgers`.
4. **Existing actions:** `Pipelines`, `Skill library`, and `Restart server` retain their current behavior.
5. **Verification:** Automated mobile tests cover order, labels, distinct icons, active route state, and removal of the old project controls; the served mobile drawer is inspected at `http://127.0.0.1:50150/projects` without restarting the server.

---

## D. Implementation Evidence

1. **Drawer:** Commit `041b51a` removes project rows and inline color inputs, then renders only the five application destinations in the required order.
2. **Icons:** The destinations use distinct dashboard, folder, book, connected-flow, and library SVGs; the repeated pseudo-circle rule is removed.
3. **Browser evidence:** Chromium at `390x844` observed labels `Control room`, `Projects`, `Ledgers`, `Pipelines`, and `Skill library`, icon keys `dashboard`, `folder`, `book`, `flow`, and `library`, plus zero legacy project rows.
4. **Verification:** Mobile tests passed `51/51`; frontend TypeScript compilation passed.
