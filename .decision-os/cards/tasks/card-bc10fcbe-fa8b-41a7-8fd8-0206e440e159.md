## A. Regression evidence

1. Added a served Chromium regression that proves direct `/done` HTTP `200`, vertical card geometry, visible completion dates, newest-first default, oldest-first switching, project-first disclosure, and label filtering.
2. The focused browser scenario passes with no page errors.
3. The live operator frontend confirms one column, hidden labels before project selection, `41` labels after selecting `decision-os`, and default `desc`.

---

## B. Remaining live gate

1. The running operator backend still reports `control-room-v13-task-labels`.
2. An authorized restart is required to load `control-room-v14-completion-time` and complete the final live check with populated dates.
