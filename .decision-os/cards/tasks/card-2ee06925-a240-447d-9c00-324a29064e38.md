## A. Scope

1. **Objective** `keeps` every runtime project disclosure collapsed on initial render and `shows` its rolling `24-hour` failure count in the summary card.
2. **Expanded content** `shows` one error-type group per `code` with dated occurrence events inside the group.

---

## B. Completion

1. **Rendered status** `preserves` project availability, severity, interruption, and affected-scope signals while adding the requested hierarchy.
2. **Browser proof** `confirms` initial collapse, manual expansion, grouped error types, event dates, and responsive readability on the served `/status` route.
