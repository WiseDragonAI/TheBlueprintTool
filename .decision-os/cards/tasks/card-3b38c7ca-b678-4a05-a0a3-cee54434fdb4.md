## A. Objective

1. **Propagate canonical subtask Codex execution** to the related master task's Control Room status.

---

## B. Result

1. Queued, transcribing, and running subtask executions now project the master as `task-execution`.
2. Exact leases remain on the launching card; the projection exposes `executionOwnerCardId` and `executionOwnerKind`.
3. Projector `control-room-v12-subtask-execution-ownership` invalidates old cache slices.
4. Merged as `4b44b693`.

---

## C. Verification

1. Backend typecheck passed.
2. Focused checks passed `21/21`.
3. Complete backend suite passed `243/243`.

---
