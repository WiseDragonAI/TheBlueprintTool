## A. Implementation

1. **Added `voicePipelineId` to the existing local `.decision-os/.settings.json` settings contract.**
2. **The Settings API now returns the available server pipelines and validates the selected pipeline when saving.**
3. **The Settings screen hydrates and saves a `Voice pipeline` selector.**
4. **Federation synchronization was not added.**

---

## B. Verification Evidence

1. **Focused settings tests passed.**
2. **Frontend and backend typechecks passed.**
3. **The full frontend, backend, and browser suites passed.**
4. **`http://127.0.0.1:50151/settings` returned HTTP `200` and served the new selector.**

---

## C. Remaining Runtime Check

1. **The running backend process predates commit `bbcaddd7`; its settings endpoint still has the old response shape.**
2. **Restart the server through the normal operator-controlled lifecycle, then save and reload one pipeline selection on the Settings screen.**
