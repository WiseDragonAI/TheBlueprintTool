## A. Verified failure chain

1. The server sets an absolute primary-checkout `TSX_TSCONFIG_PATH`.
2. Codex workers inherit it.
3. The verification launcher forwards it.
4. Backend scripts do not replace it.
5. `tsx` therefore resolves through the primary checkout from a worktree.

---

## B. Result

1. **Status is verified.** The first incorrect boundary is forwarding parent environment instead of constructing worktree-owned environment.
