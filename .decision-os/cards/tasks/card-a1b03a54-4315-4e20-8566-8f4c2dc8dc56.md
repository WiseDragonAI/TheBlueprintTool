## A. Scope

1. **Objective:** Replace the active Codex session widget's compact `Cancel` button with the requested large square `STOP` control.
2. **Frontend files:** Update `frontend/src/runtime/codex/component/render-card-skill-run-widget.ts`, `frontend/src/runtime/codex/effect/poll-card-skill-run.ts`, and the relevant terminal-button styles.
3. **Backend reuse:** Keep the existing `requestCardSkillRunCancel` request and card-run cancellation controller as the authoritative process termination path.

---

## B. Implementation Contract

1. **Markup:** Render a solid square stop icon and a visible `STOP` label with an accessible name of `Stop Codex run`.
2. **Sizing:** Use a dedicated Codex stop-button class that produces the prominent control; remove `terminal-button--compact` from this action.
3. **State:** Rename the UI state from cancel semantics to stop semantics: `STOP`, `STOPPING`, `Stopping run`, `Stop failed`, and terminal `CANCELLED` status.
4. **Safety:** Preserve the in-flight guard, disabled pending state, click propagation guards, visibility rules, and server-error reconciliation.

---

## C. Acceptance Criteria

1. **Rendering regression:** Tests assert the solid square icon, `STOP` label, accessible name, dedicated sizing class, and absence of `terminal-button--compact` and `Cancel` on the active-run action.
2. **State regression:** Tests assert one cancellation request per activation, disabled `STOPPING` state, hidden control after terminal settlement, and restored `STOP` state after rejection.
3. **Backend regression:** The focused backend process-controller cancellation test passes unchanged and proves the active child process is terminated.
4. **Repository gate:** Implement and test in an isolated worktree, create a focused feature commit, merge it into the primary checkout with a merge commit, then remove the worktree and branch.

---

## D. Implementation Evidence

1. **Root cause repaired:** Persist project runtime registries across HTTP requests so the stop endpoint can resolve the child process registered by the start endpoint.
2. **Control implemented:** Render the solid `■` icon, `STOP` label, `STOPPING` pending state, dedicated `58px` sizing, and accessible name without `terminal-button--compact`.
3. **Failure recovery:** Preserve the one-request in-flight guard and restore the enabled `STOP` label plus server error after rejection.
4. **Verification:** Frontend and backend TypeScript checks pass; focused frontend stop tests pass; the backend cancellation test terminates the fake Codex child and reads terminal `cancelled`; project isolation coverage passes.
5. **Repository:** Feature commit `9fad486` is merged by `de000dd`; the isolated worktree and branch were removed.
6. **Open gate:** The running server must load merge `de000dd`, then the operator-facing pointer sequence must be observed before this interaction card can be marked `done`.

---

## E. Contradicted-Claim Rework

1. **Reopened boundary:** The previous implementation changed `renderCardSkillRunWidget`, which is not the mobile `Codex Log` widget shown by the operator.
2. **Correct renderer:** `renderThreadCodexLogStatus` now renders the running-session `64px` square `STOP` action inside the mobile status widget.
3. **Correct routing:** `frontend-mobile/src/mobile-thread.js` routes `stop-thread-codex` with the active ledger and displayed card/run identities to `stopThreadCodexRunController`.
4. **Regression:** Mobile source-contract coverage and shared controller integration coverage pass; feature commit `8f05d5e` is merged by `80ae756`.
5. **Claim gate:** `implemented; automated checks pass; mobile Brave interaction not yet verified`.
6. **Visual evidence:** The `2026-07-13T09:36:42.610Z` mobile Brave screenshot shows the large square `STOP` control mounted in the running `Codex Log` status widget.
7. **Layout rework:** Merge `98d60f9` replaces the two-row action layout with one `76px` action column plus four information columns and adds blue `RESUME` for terminal existing runs.

---

## F. Active-State Rework

1. **Reopened behavior:** The operator's failed tap proves that visual presence did not establish cancellability.
2. **Renderer boundary:** `STOP` now requires a hydrated summary with `ok: true`, `active: true`, and `status: running`; an unhydrated persisted run renders `RESUME` instead of a false stop claim.
3. **Rerender state:** Per-run pending and rejection state survives `Codex Log` polling rerenders, keeping `STOPPING` disabled and keeping a server error visible.
4. **Regression:** Focused TypeScript and `11` frontend tests pass.
5. **Repository:** Feature commit `fc1d3f4` is merged by `d22d8c6`; the isolated worktree and branch were removed.
6. **Claim gate:** `implemented; automated checks pass; mobile Brave interaction not yet verified`.
