## A. Result

1. **Status:** implemented and browser verified.
2. **Transition:** active recording cancellation has first priority; desktop draft focus is released by the first `Escape`; the next `Escape` activates `.back-to-zone-button` for a master task.
3. **Preserved input:** focused `X` remains text input.

---

## B. Files

1. `frontend/src/app/responsive/thread.js` — `handleResponsiveThreadShortcut`.
2. `frontend/src/app/responsive/application.js` — responsive global `keydown` routing.

---

## C. Evidence

1. **Served route:** HTTP `200` at the canonical master-task card URL.
2. **Browser:** first `Escape` removed focus without changing the card URL; second `Escape` navigated to `/?tab=queue`; the Control Room became visible.
