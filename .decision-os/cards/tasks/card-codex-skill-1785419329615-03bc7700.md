## A. Correction

1. **This is over-engineered. The simpler anchor is the existing same-ID voice note, `localVoiceUploadId`, and `shouldApplyVoiceServerNote()` at the shared `mergeLocalThreadNotes()` boundary.**
2. **Required behavior** `is` one live voice row replacing its stale optimistic `uploading` fields with the durable same-ID transcript as soon as `loadActiveThreadSlice()` receives the forward server state.
3. **Selected product change** `remains` one import and one constrained branch in `frontend/src/runtime/ledger/helper/merge-local-thread-notes.ts`; no new state object, persistence model, route, polling path, controller, renderer, component, schema, fixture module, dependency, and backend change is justified.

---

## B. Existing Authority

1. **Stable identity** `already exists` in the shared note `id`; the optimistic row and durable transcript do not need a relationship object.
2. **Browser ownership** `already exists` in `localVoiceUploadId`; it identifies the voice-specific local overlay and preserves cleanup ownership without becoming lifecycle authority.
3. **Durable lifecycle** `already exists` in the incoming thread note through `status`, `revision`, `message`, `voiceFileRef`, `error`, and lifecycle timestamps.
4. **Transition authority** `already exists` in `shouldApplyVoiceServerNote()`, which rejects a backward voice status even when the incoming replica revision is larger.
5. **Shared installation** `already exists` in `mergeLocalThreadNotes()`, called by both `loadActiveThreadSlice()` and `reconcileActiveLedgerState()`.
6. **Visible presentation** `already derives` from the installed note in `renderThreadNotes()`; accepting the durable note removes `is-busy` and the stale upload spinner without a rendering change.

---

## C. Remove

1. **Remove three terminal variants from `T02`:** retain only `transcribed`, the reported incident state. `transcribed`, `transcription failed`, and `execution launch failed` all have rank `4` and enter the same acceptance branch; repeating the caller fixture three times does not prove a different merge decision.
2. **Remove the forward full-ledger case from `T03`:** `T02` proves forward acceptance through the incident caller, while both callers invoke the same helper. The second caller needs only the distinct backward-transition risk.
3. **Remove exhaustive incoming-field assertions:** the accepted implementation keeps the incoming object and overwrites only `localVoiceUploadId`, merged `imageSizes`, and `optimistic`. Assert representative durable fields rather than every lifecycle timestamp.
4. **Remove `T00`, `T04`, `T05`, `T06`, `T08`, `T09`, and `T10` as independent task objects:** these rows restate mandatory workspace, verification, commit, integration, and publication controls. Keep every control as an ordered execution checklist with its existing authorization gate.
5. **Remove separate groups `G01` and `G02`:** no parallel dispatch exists, one implementation owner changes all three files, and delivery follows the feature commit sequentially. The group objects add scheduling structure without an independent workstream.
6. **Keep the prior polling-deadline removal:** time is absent from `mergeLocalThreadNotes()` and cannot exercise the defective decision.

---

## D. Use Instead

1. **Retain `T01` — constrained merge correction:** when the incoming slice contains the thread, a same-ID local note is optimistic, `localVoiceUploadId` is non-empty, and `shouldApplyVoiceServerNote()` accepts the incoming note, keep the incoming record, preserve the merged local `imageSizes` plus `localVoiceUploadId`, set `optimistic: false`, then skip the generic local spread.
2. **Reduce `T02` — one incident-path integration regression:** in `frontend/test/runtime/ledger-content-refresh.integration.test.ts`, return one forward `transcribed` voice note and one same-ID optimistic text note in the same owned thread response. Assert one stable voice ID, durable `message`, `status`, `revision`, `voiceFileRef`, and `completedAt`, preserved `localVoiceUploadId` plus `imageSizes`, `optimistic: false`, no busy presentation, and unchanged text-note optimistic precedence.
3. **Reduce `T03` — amend the existing backward regression:** in `frontend/test/runtime/voice-transcription-runtime.integration.test.ts`, add non-empty `localVoiceUploadId`, explicit local and incoming revisions, and `optimistic: true` to the existing full-ledger test. Assert that a larger-revision incoming `uploading` snapshot cannot replace the local terminal `transcription failed` state.
4. **Retain `T07` as the only separate gate:** after integration, the operator records one real microphone note and confirms the same row moves from optimistic busy state to the durable transcript without reload, then remains durable after reload.
5. **Use one execution checklist:** protect staged hunks, create the required isolated worktree, implement `T01`, complete reduced `T02` and `T03`, run focused tests, run frontend typecheck, run `test:front-back` once, create the feature commit, merge it into the primary checkout, remove only the task worktree and branch, obtain `T07`, inspect the push range, then publish only authorized history.

---

## E. Tradeoff

1. **Lost diagnostic granularity** `is` separate caller-level failures for the two terminal error labels and independent forward proof through full-ledger reconciliation.
2. **No product capability is lost:** the terminal labels share the same lifecycle rank, the accepted branch preserves the complete incoming record, the renderer is unchanged, and the second caller reaches the same helper.
3. **Proof remains sufficient:** the reduced package covers forward acceptance on the reported live-refresh path, backward rejection through the full-ledger path, non-voice exclusion, local-field retention, durable-field authority, rendered busy-state removal, and the real microphone transition.
4. **Remaining uncertainty** `is` interaction-only; static analysis cannot prove the operator-owned microphone gesture.

---

## F. Next Implementation

1. **Production boundary** `is` one changed file: `frontend/src/runtime/ledger/helper/merge-local-thread-notes.ts`.
2. **Regression boundary** `is` one new active-thread case in `frontend/test/runtime/ledger-content-refresh.integration.test.ts` plus one amendment to the existing stale full-ledger case in `frontend/test/runtime/voice-transcription-runtime.integration.test.ts`.
3. **Change footprint** `is` three existing files, zero new durable objects, zero new fixture files, and zero changed backend contracts.
4. **Execution authorization** `remains pending`; this analysis did not edit product source, run tests, control Chromium, restart the server, commit, merge, push, change task status, and close the master task.

---

## G. Revised Next Four Actions

1. **Action 1** `runs` `executor-precheck` against `T01`, reduced `T02`, reduced `T03`, the single operator gate, and the retained execution checklist.
2. **Action 2** `runs` `bloating-analysis` against the complete analysis record.
3. **Action 3** `rewrites` the master-task executive summary with the retained causal explanation and minimal proof package.
4. **Action 4** `stops` the analysis sequence and requests explicit implementation authorization.
---

Codex run completed: exit code 0
