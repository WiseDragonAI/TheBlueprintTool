## A. Implementation Status

1. **Implemented:** `New` remains enabled at `All projects`, opens a server-owned editor, uses the server skill catalog, and saves through `/api/codex/server-pipelines`.
2. **Ownership preserved:** Project-filtered creation and editing continue through project-scoped routes.
3. **Automated checks:** The responsive pipeline-library tests pass.
4. **Open verification:** The active server has not loaded the merged routes, so the served click-save-reload interaction remains unverified.
