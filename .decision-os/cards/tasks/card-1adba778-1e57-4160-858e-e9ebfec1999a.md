#master-task #task-active

Ledger: Specs
Waiting since: 2026-07-15T07:39:26.774Z
Queue rank: 3
Active since: 2026-07-15T07:40:47.933Z

## A. Verified failure

1. **Observed environment leak:** the attached Codex log records a focused test loading unchanged primary-checkout code because inherited `TSX_TSCONFIG_PATH` pointed outside the feature worktree.
2. **Observed dependency failure:** the attached log records post-merge tests failing before execution because TypeScript resolved from the worktree source tree without package-local dependencies; the agent then recreated and removed a temporary `node_modules` symlink.
3. **Existing contract gap:** `AGENTS.md` defines admission and verification order, while `ledger-cli execution-profile` returns bootstrap commands and focused-test templates. Neither provides one executable command that prepares a fresh worktree and runs the selected verification scope.

---

## B. Selected correction

1. **Canonical entry point:** add `bin/decision-os-verify.mjs` as the only agent-facing command for focused tests, package typechecks, and the full suite.
2. **Environment ownership:** the command locates the active checkout, clears inherited TypeScript loader configuration, selects absolute worktree-local configs, enforces the mobile concurrency cap, and performs the workload admission check.
3. **Dependency ownership:** when a package lock matches the primary checkout, the command creates the required package-local dependency link automatically. When the lock differs, it performs a package-local `npm ci --ignore-scripts` before verification.
4. **Failure contract:** setup failures stop before test execution and report one stable cause plus the exact corrective action. Test failures retain the executed command and failing scope.

---

## C. Acceptance

1. **Fresh-worktree proof:** one command succeeds from a newly created worktree with no `node_modules` entries and a deliberately poisoned primary-checkout `TSX_TSCONFIG_PATH`.
2. **Source proof:** a regression demonstrates that the test imports the changed worktree module, never the primary-checkout module.
3. **Contract consolidation:** `AGENTS.md`, package scripts, and `ledger-cli execution-profile` point to the canonical command; agents no longer receive raw TSX loader recipes or manual symlink steps.
4. **Efficiency evidence:** record setup, test, and total durations plus command count for the fresh-worktree verification run.

---

## D. Subtasks

1. [Implement the canonical worktree verification CLI](card:card-ca6d023e-1ff0-40d4-bb77-2ab5c76662e9) — Status: pending
2. [Make every repository verification path use the canonical CLI](card:card-e5e4a624-579c-424b-8b9e-993bc61b3146) — Status: pending
3. [Verify zero-guesswork execution in a real fresh worktree](card:card-d7adddda-c781-4bdf-99c2-de7138f5a30f) — Status: pending