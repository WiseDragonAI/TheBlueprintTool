## A. Integrated Execution

1. **Merged:** `aaa8f07a` on `main` contains the complete federated project-sync implementation.
2. **Desktop:** `/projects` is the node-aware non-canvas list; project activation opens settings with `Synchronize project`; ledger zoom-out retains the separate projects canvas.
3. **Service:** fixed Git snapshots, durable runs and locks, canonical Codex roles, source-first reconciliation, federation routes, retry, SSE progress, and notifications are implemented.
4. **Verification:** both typechecks, focused sync tests, full backend `180/180`, full frontend, full browser `172/172`, and final served desktop scenarios `2/2` passed.
5. **Live route:** the existing server returns HTTP `200` at `http://127.0.0.1:50151/projects`; it was not restarted because restart authority was not granted.

---
