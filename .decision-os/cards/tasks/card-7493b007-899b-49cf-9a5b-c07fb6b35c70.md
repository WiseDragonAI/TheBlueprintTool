## A. Diagnosis Summary

1. **Fourteen assertions fail reproducibly:** ten in the front-end suite, one in the back-end suite, and three in the browser suite.
2. **Front-end and back-end typechecks pass.** The normal fail-fast workflow stops at the front-end suite, so the back-end and browser failures were run separately.
3. **Thirteen failures are stale test assumptions introduced by commit `6e012cb1` (`fix: isolate responsive and canvas surfaces`).** The production files moved to surface-specific boot and shared-asset boundaries, while the tests still inspect the former locations.
4. **One browser failure is a data-dependent thread fixture mismatch:** the test opens card `10000001`, whose `thread-10000001` resource does not exist, once on desktop and once on mobile.

---

## B. Operator Decision

1. Treat this as **test-suite repair**, not fourteen product regressions.
2. Update assertions to inspect `boot-application.ts`, `surface-runtime.ts`, and `assets/shared/*`.
3. Install the canvas surface effects in the ledger-refresh test harness before exercising resize persistence.
4. Make the responsive browser test use a card with a canonical existing thread resource.

---

## C. Subtasks

1. [Run and classify the failing tests](card:card-c767c97c-a988-4ba1-a324-271ed87376e5)
