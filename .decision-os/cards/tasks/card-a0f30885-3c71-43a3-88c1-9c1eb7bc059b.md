## A. Lifecycle Regression Coverage

1. **Open coverage:** The responsive regression proves all desktop cards use `openCardDetail()` and mobile cards explicitly keep the thread closed.
2. **Close coverage:** The regression proves navigation, Backlog, completion, deletion, Back, history, voice submission, and final view changes converge on `closeCardDetail()`.
3. **Checks:** `18` focused responsive and voice tests pass; frontend typecheck passes. The full frontend suite reports `415/426` passing with the same `11` unrelated pinned-asset assertions.
