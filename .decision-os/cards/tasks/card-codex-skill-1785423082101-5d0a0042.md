## A. Correction

1. **This is over-engineered. The simpler anchor is** `state.editor`, which already owns the open responsive pipeline draft.
2. **Required behavior:** unsaved pipeline `name` and `purpose` must remain unchanged when the operator adds a step and removes a step.
3. **Existing derivation path:** `clonePipeline()` seeds `state.editor`; `renderEditor()` hydrates both metadata controls from that draft; step mutations update `state.editor.steps` and call `renderEditor()`; `saveEditor()` trims the live draft before constructing the existing request.
4. **First incorrect transition:** the `pipeline-name` input and `pipeline-purpose` textarea do not update `state.editor`, so the next step mutation renders stale metadata.

---

## B. Remove

1. **Remove task `T03` as written:** source-pattern assertions duplicate the listener implementation shape and cannot prove the browser interaction.
2. **Remove task `T04` as written:** seven collection transitions, skill-picker coverage, project and server `POST` and `PUT` matrices, exact collection-order payloads, and fresh-load persistence exceed the reported add-step and remove-step failure.
3. **Remove the canvas-test target:** `tests/browser/codex/reusable-step-pipelines.spec.ts` exercises the canvas editor, while the defect is in `frontend/src/app/responsive/codex.js` on the responsive `/pipelines` route.
4. **Remove new persistence work:** no model, manifest, cache, index, route, backend mutation, request contract, recovery mechanism, or durable state is required.

---

## C. Use Instead

1. **Production correction:** add one `input` listener for each existing metadata control in `initializeMobileCodex()`. Store the raw name in `state.editor.name` and the raw purpose in `state.editor.purpose`.
2. **Preserve the save boundary:** keep the current `.trim()` calls in `saveEditor()` and leave the request body, backend handlers, storage files, and pipeline relationships unchanged.
3. **Behavioral regression:** add one focused case to `tests/browser/application/the-application-is-one-responsive-frontend.spec.ts`. Open `/pipelines`, create an unsaved draft, fill both metadata controls, add a step, remove that step, and assert both controls after each render.
4. **Authority:** keep `state.editor` as the single draft source of truth; the DOM controls remain its editing surface.

---

## D. Tradeoff

1. **Explicit proof narrows** to the two operator-reported transitions. Step reordering, skill mutations, and skill-picker return are not independently exercised by this regression.
2. **Persistence proof is not repeated** because the verified defect occurs before the request boundary and the correction does not change `saveEditor()` or backend persistence.
3. **Claim boundary:** completion may state that add-step and remove-step metadata retention is verified. It must not claim independent browser proof for the removed transition matrix.

---

## E. Minimal Execution

1. **Isolation:** create the required feature worktree from the current primary commit, confirm the primary index has no staged hunks, and preserve every unrelated dirty path.
2. **Implementation:** change only `frontend/src/app/responsive/codex.js` to connect the two existing controls to `state.editor`.
3. **Regression:** change only `tests/browser/application/the-application-is-one-responsive-frontend.spec.ts` to prove name and purpose retention through step addition and removal on `/pipelines`.
4. **Repository verification:** run the focused browser file through `node bin/decision-os-verify.mjs`, run `npm run typecheck:frontend` through the same lease, then run `npm run test:front-back` once after focused checks pass.
5. **Integration and target proof:** create the required feature commit, merge it into the primary checkout with the required merge message, then use separate headless Linux Chromium against the already running `http://127.0.0.1:50150/pipelines` without restarting the server and without saving test data.
6. **Documentation and publication:** record exact source, test, route, commit, and verification evidence on the source task through the scoped ledger boundary; commit only the intended documentation; push the current branch with the Wise SSH key; remove the merged worktree and local feature branch.

---

## F. Acceptance and Next Gate

1. **Draft retention:** typed `name` and `purpose` remain byte-for-byte unchanged after adding a step and after removing that step.
2. **State contract:** both controls update `state.editor` on `input`; `saveEditor()` remains the sole trimming boundary.
3. **Browser evidence:** the focused responsive browser regression exits `0`, observes no page error, and exercises the real controls on `/pipelines`.
4. **Repository evidence:** focused browser verification, frontend typecheck, and `test:front-back` each exit `0` with captured completion output.
5. **Delivery evidence:** the intended source and regression diff is merged, the served route returns `200`, the target interaction is observed, required documentation is committed, and the current branch is pushed.
6. **Next execution:** select `simply-execute` to implement this reviewed boundary. Keep the master task active until the later completion gate authorizes closure.
---

Codex run completed: exit code 0
