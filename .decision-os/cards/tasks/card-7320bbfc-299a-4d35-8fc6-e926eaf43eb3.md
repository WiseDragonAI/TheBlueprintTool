## A. Scope

1. Detect the terminal JSONL boundary during stream ingestion.
2. Give the wrapper a bounded close grace period.
3. Terminate a wrapper that remains alive and reuse the existing one-shot close settlement path.

---

## B. Verification

1. Add a regression with a fake Codex process that emits `turn.completed` and stays alive.
2. Prove terminal status and queue cleanup occur without duplicate settlement.
