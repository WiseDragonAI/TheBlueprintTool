## A. Implementation

1. Remote state, ledger, card, and thread GET routes now read the local replica.
2. Missing resources return structured `202` synchronization state and move the exact project resource to selected priority. Mutations remain owner-routed.

---

## B. Verification

1. Integration coverage proves local offline card reads and selected queue precedence.
