## A. Result

1. `federatedProjectCatalog()` returns one project per persisted ID with merged ledgers and replicas.
2. Offline cached replicas remain descriptors.

---

## B. Proof

1. Unit and federation endpoint tests prove one row before and after replica loss.
