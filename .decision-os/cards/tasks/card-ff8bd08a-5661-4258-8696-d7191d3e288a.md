## A. Implementation Delivery

1. **Hash guard:** Hunk staging validates the displayed aggregate patch hash and rejects stale or foreign patches.
2. **Index authority:** Git remains the staging authority; the Markdown stores only repository and target declarations.
3. **Voice context:** Existing voice uploads persist the review context in thread-note metadata.
4. **Agent protection:** The current staged patch is injected into every thread agent prompt and `AGENTS.md` forbids changing or unstaging staged lines.
5. **Tests:** Nested-repository resolution, hunk splitting, staging, workspace escape rejection, parser tests, frontend typecheck, backend typecheck, and all 481 frontend tests pass.
