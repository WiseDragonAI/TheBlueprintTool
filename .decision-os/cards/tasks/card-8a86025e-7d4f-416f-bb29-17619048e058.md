## A. Scope

1. **Build:** Tail the rollout backward to the latest valid `token_count`.
2. **Calculate:** Derive used, window, remaining, and percentage values with the `12000`-token baseline.

---

## B. Acceptance

1. **Proof:** Unit fixtures reproduce the native sample and cover malformed, partial, empty, and clamped cases.
