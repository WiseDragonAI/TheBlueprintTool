#master-task #skill

Ledger: Specs

## A. Objective

1. **Make `retrospect-and-close-task` extract only durable lessons, persist them through the centralized memory CLI, and atomically complete the source master and canonical subtasks.**

---

## B. Approval State

1. **Readable skill artifact:** the proposed `SKILL.md` is a dedicated sub-card directly linked from this master.
2. **Current boundary:** operator validation precedes the installed skill update and Git commit.

---
