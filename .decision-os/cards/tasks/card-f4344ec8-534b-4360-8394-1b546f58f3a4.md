## A. Delivered correction

1. Replaced the partial `submitVoiceRecording()` wrapper with one canonical `executeVoiceAction()` controller that owns stop timing, local-durability handoff, background settlement, and pre-handoff rejection.
2. Routed responsive keyboard, responsive quick actions, canvas keyboard, canvas voice toggle, and canvas action-dock buttons through that controller.
3. Centralized `VoiceLaunchMode`, modifier precedence, and dataset parsing in one helper.
4. Removed `queueCodex` as a frontend action selector. IndexedDB schema version `2` migrates retained legacy records to canonical `launchMode` so an old retry keeps its original RUN intent.
5. Commits: `a2569e6c` and merge `670017d1`.

---

## B. Verification evidence

1. Focused cross-entry frontend contract suite passed `36/36`.
2. Frontend typecheck passed and the complete frontend suite passed `498/498`.
3. Backend voice lifecycle and orchestration passed `11/11`.
4. The existing served mobile card route returned `200`; RUN reached `/?tab=exec` in `350 ms` while the intercepted upload was unresolved.
5. The background multipart request carried `launchMode=run` and completed after handoff.
6. The server was not restarted.

---

## C. Subtasks

1. [Trace mobile and desktop run-with-recording history](card:card-b057538c-c885-4292-998b-c101e0ceb691)
2. [Implement durable background voice delivery](card:card-8355a474-a340-43da-84d4-aa21158c3454)
3. [Verify mobile immediacy, retries, and persistence](card:card-d75aa814-5d04-44ca-9941-681300a8e6b0)
