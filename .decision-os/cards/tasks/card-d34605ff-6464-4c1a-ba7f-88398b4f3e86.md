## A. Verified Result

1. **Single writer:** `applyThreadViewportState()` owns matched entry pinning, continuation follow, paused restoration, observer synchronization, and jump-state synchronization.
2. **DOM-only log renderer:** `renderThreadCodexLog()` no longer writes or persists viewport position.
3. **Polling isolation:** Live Codex updates use `renderThreadCodexLogUpdate()` and never repaint the conversation surface.
4. **Delayed growth:** The active followed surface uses one coalesced `ResizeObserver` write. Served `1400px` growth remained at `0px` bottom distance.
5. **Continuation proof:** A real-wheel paused conversation remained `700px` from bottom after an ordinary render on both viewports.
