## A. Implemented Context Boundary

1. **Initial launch:** the server injects one normalized Decision OS context; the short developer prompt contains no mandatory intake query.
2. **Ordinary resume:** only newer thread notes are sent through `codex exec resume`.
3. **Replacement session:** one current normalized context reconstructs state when the previous Codex session is unavailable.
4. **Explicit refresh:** `session-context` remains available on demand and returns compact card state, relationships, linked statuses, referenced-card run metadata, validation, and action schemas.

---

## B. Verification

1. **Automated:** prompt, initial launch, ordinary resume, replacement session, and interrupted-resume regressions pass.
2. **Scope:** canonical card URLs and `card:<id>` references are deduplicated from direct ledger relationships.
3. **Merge:** implementation is included in `5adc394`.

---

## C. Remaining Gate

1. **Production evidence:** keep this card `todo` until new post-restart tasks show no mandatory initial `session-context`, no duplicated target read, and no context reinjection on ordinary resumes.
