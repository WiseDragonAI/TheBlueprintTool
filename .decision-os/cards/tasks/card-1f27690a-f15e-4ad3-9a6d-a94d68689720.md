## A. Scope

1. Define the command and resource lanes that commit image intent, local original capture, preview capture, and synchronized availability metadata.
2. Specify `canvas-preview-v1` identity from the original SHA-256 plus the pinned derivation version.
3. Exclude raw voice audio and browser retry blobs from replicated resource heads.

---

## B. Verification

1. Prove journal recovery, idempotent retry, concurrent-head conflict visibility, and local acknowledgement while the relay is unavailable.
