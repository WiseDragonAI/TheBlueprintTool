## A. Implementation

1. Replace the Control Room `Done` tab and route with `Delayed`.
2. Derive delayed master tasks from persisted card status.
3. Add `Move to backlog` and `Restore to queue` controls that use the existing ledger mutation endpoint.

---

## B. Verification

1. Cover labels, routes, counts, grouping, and mutation payloads in focused frontend tests.