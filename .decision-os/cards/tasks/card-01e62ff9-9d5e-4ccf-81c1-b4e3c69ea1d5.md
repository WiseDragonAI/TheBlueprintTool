## A. Verified corpus

1. **Database:** `../.decision-os/memories.sqlite3`; `PRAGMA quick_check` returned `ok`.
2. **Coverage:** `30` of `30` rows belong to project `ZGVjaXNpb24tb3M`; every row was read with no CLI result limit.
3. **Record range:** first creation `2026-07-12T10:44:22.401Z`; latest update `2026-07-15T08:02:08.759Z`.
4. **Types:** `26` code lessons and `4` copywriting lessons.
5. **Tags:** `decision-os` 12, `ui` 4, `engineering` 3, `verification` 3, `theming` 2, and six tags with one record each: `cli`, `codex`, `control-room`, `ledger`, `routing`, `skills`.
6. **Metadata quality:** all `30` records have a populated source, representing `26` distinct source strings; all `30` titles are unique.

---

## B. Coverage reconciliation

1. **Tag detail:** the corpus has `27` distinct tag/subtag pairs. Only `decision-os/closeout`, `decision-os/codex`, and `decision-os/lifecycle` repeat, with two records each.
2. **Analytical coverage:** the trend classification assigns all `30` memory IDs exactly once: `30` assigned, `30` unique, `0` missing, `0` duplicated.
3. **Result:** the complete stored project corpus is represented in the synthesis.