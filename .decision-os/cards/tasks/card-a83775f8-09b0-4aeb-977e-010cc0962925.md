## A. Scope

1. **Objective:** Prove the repaired voice lifecycle across backend persistence, browser reconciliation, and the served mobile Brave surface.
2. **Route:** `http://127.0.0.1:50150/specs/zone/zone-6785c01e-6b6b-4573-b410-7fa49ebcddf9/card/card-7074d33a-39da-42a1-b470-673cbe221def`.

---

## B. Automated Verification

1. **Lifecycle:** Exercise upload acceptance, phase ordering, successful completion, provider rejection, deadline expiry, and retry preservation.
2. **Synchronization:** Drop the terminal SSE, disconnect the stream through completion, restore visibility after completion, and prove the same note converges without reload.
3. **Persistence:** Prove success and failure survive a fresh ledger load and that a stale intermediate revision cannot replace either terminal state.
4. **Cleanup:** Prove polls, clocks, EventSource recovery hooks, and note timers stop when the note reaches a terminal state or leaves the active thread.
5. **Repository gate:** Run focused frontend and backend tests plus TypeScript checks from an isolated feature worktree, then commit and merge the verified change with a merge commit.

---

## C. Served Mobile Verification

1. **Optimistic moment:** On mobile Brave, record a note and observe `Uploading audio` before `POST /api/voice-upload` resolves.
2. **Progress moment:** Observe server-owned phase labels and elapsed time while the note remains pending; confirm another recording can start concurrently.
3. **Success moment:** Let transcription finish, reload the route, and confirm the transcript plus `Transcribed` state persist.
4. **Recovery moment:** Background Brave before completion, return after completion, and confirm reconciliation occurs without manual reload.
5. **Failure moment:** Exercise a controlled rejected transcription, confirm reconciliation to `Transcription failed`, reload, and confirm retry remains available.
6. **Evidence:** Record the route, HTTP result, browser and device, lifecycle timestamps, request sequence, pre-response UI state, post-completion UI state, and post-reload state.

---

## D. Completion Gate

1. **Behavioral evidence:** Do not mark this card `done` from source inspection, syntax checks, or unit tests alone.
2. **Server ownership:** Do not restart the current server unless the operator explicitly requests a restart.
3. **Claim:** Until the mobile sequence is observed, report `implemented; automated checks pass; device interaction not yet verified`.

---

## E. Current Verification State

1. **Contradiction captured:** Mobile Brave on the target thread still showed stale transcription state until reload at `2026-07-13T07:59:04.481Z`.
2. **Persisted state:** Note `note-1783929544409-7364f375b415c` was durably `transcribed` with `transcriptionStartedAt` `2026-07-13T07:59:04.463Z`.
3. **Corrected request evidence:** The earlier generic status response came from a request that omitted `x-decision-os-project: ZGVjaXNpb24tb3M` and therefore resolved to the home project. It is caller error, not evidence of server misconfiguration or stale deployment.
4. **Repository state:** The repaired route, reconciliation client, and local display clock are merged into `main` through `dd07e71`.
5. **Served state:** Project-scoped requests return `200` with `cache-control: no-store` for the exact mobile route, `/canvas-src/runtime/thread/effect/render-thread-notes.js`, and `/canvas-src/runtime/voice/effect/run-voice-progress-clock.js`. The served clock uses the authoritative timestamp and a local `1_000ms` interval.
6. **Open gate:** Execute one mobile Brave transcription and observe the elapsed label advance locally before completion, then perform the background-return recovery sequence. This card remains `todo` until that behavioral evidence exists.
