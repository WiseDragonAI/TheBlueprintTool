## A. Task Decision

1. **The master task is a product verification task:** prove what Decision OS injects into a returning `GateTest` prompt after the gate dynamically queues one worker skill.
2. **The verified workflow is:** the gate queues `product-analysis`; the worker completes; Decision OS starts a distinct returning-gate execution with the worker Markdown as `PREVIOUS_SKILL_RESULT` and refreshes the current master-card and thread context.
3. **The execution invariant is:** the returning gate keeps the admitted prompt snapshot, revision, commit, model, and effort while receiving a new execution identity and runtime context.
4. **The repository acceptance evidence is:** `backend/test/codex/start-codex-pipeline-run-controller.test.ts` asserts the master objective, latest operator note, task thread ID, `WORKER_RESULT`, and distinct returning execution ID in the generated returning input.
5. **The evidence limit is:** repository source and generated-input tests do not prove the operator-facing rendered execution surface shows the same complete prompt.

---

## B. Required Master-Task Update

1. **Rename the master task and mandatory zone** to `Verify Dynamic Gate Re-entry Prompt`.
2. **Replace the intake placeholder** with the verified goal, actor, dynamic worker-to-returning-gate workflow, immutable admission boundary, refreshed runtime inputs, and acceptance evidence.
3. **Create actionable subtasks** for:
   1. capturing the complete returning prompt on the operator-facing execution surface;
   2. specifying and testing missing, empty, stale, malformed, and failed worker-result behavior;
   3. defining operator-visible pending, completion, failure, and recovery behavior for dynamic re-entry.
4. **Keep the master task open:** live rendered-prompt acceptance and failure-path contracts remain unverified.
5. **Use the Decision OS master-task apply boundary:** preserve the mandatory zone and generated task relationships; do not edit ledger JSON directly.

---

## C. Exec-Summary Instruction

1. Update `card-e28ccdf8-3e29-4571-b714-c728926e131e` from the verified product-analysis evidence above.
2. Write a terse, causal, decision-first English summary using the task-specific lettered-section and numbered-item format.
3. Record only verified facts and explicit remaining work.
---

Codex run completed: exit code 0
