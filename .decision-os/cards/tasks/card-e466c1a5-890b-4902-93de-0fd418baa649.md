## A. Verified result

1. **Working commit:** `3baeb597` navigated from `onPersisted` after IndexedDB persistence and before network settlement.
2. **First regression:** `91c8664f` removed `onPersisted`, awaited the full voice submission, and changed tests to require the card to remain open.
3. **Current symptom:** `17b1a96c` added navigation after the awaited result; `bbcaddd7` preserved this timing while resolving the `launchMode` merge conflict.
4. **Recorded identity:** `jb windows <jbbriant@windows>` authored and committed the relevant changes. No repository evidence identifies a different human or automation identity.
5. **First incorrect transition:** `handleResponsiveThreadShortcut` waits for the terminal upload-success boolean instead of navigating from durable local persistence.
6. **Test gap:** two source-regex tests require the wrong awaited timing; no deferred-upload behavioral test protects the required order.
