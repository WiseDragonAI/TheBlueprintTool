## A. Verification Matrix

1. Add registry coverage tests under `frontend/test/runtime` and pure transition tests under `frontend/test/unit/navigation`.
2. Add browser scenarios under the existing `tests/browser` surface directories for every registered command family.
3. Exercise click, advertised keyboard shortcut, pending state, success, rejection, browser Back, reload persistence, and stale asynchronous completion where each behavior applies.
4. Use the canonical project route and the Linux Chromium procedure from `BROWSER_RUNBOOK.md`.

---

## B. Required Navigation Scenarios

1. Project to projects, zone to ledger, ordinary card to zone, master task to Control Room, and subtask to parent master task.
2. Thread Close with browser-history ownership and active-recording guard.
3. Process detail to process library, process library Close, pipelines Close, pipeline editor to pipelines, and skill picker to pipeline editor.
4. Browser Back and `Escape` produce the same owned destination when the control advertises that shortcut.

---

## C. Completion Checks

1. The command registry reports zero uncovered controls across all static and dynamic buttons.
2. Every browser scenario records the route, visible owning surface, request result, and reconciled state.
3. Mutation controls prove optimistic state before resolution, persistence after reload, and reconciliation after rejection when the command uses optimistic UI.
4. Full frontend typecheck and repository test suite pass through `node bin/decision-os-verify.mjs -- <command>`.

---

## D. Dependencies

1. Depends on the other four subtasks.
