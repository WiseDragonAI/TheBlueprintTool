## A. Verified Result

1. **Regression replacement:** The former test that protected pause-across-entry now requires persisted pause to be overridden by a scoped thread-switch request.
2. **Focused checks:** Thread runtime, panel rendering, responsive routing, and input routing checks pass.
3. **Static verification:** Frontend TypeScript passes with no emitted output.
4. **Full verification:** `npm test` passes `492/492`.
5. **Served verification:** HTTP `200`; Chromium mobile and desktop runs verified initial bottom, real-wheel pause, same-card reopen, pause-on-continuation, log-tab activation, and delayed-growth follow.
6. **Visual evidence:** `/tmp/thread-open-bottom-reopened-mobile.png` and `/tmp/thread-open-bottom-reopened-desktop.png` capture the served reopened bottom state.
