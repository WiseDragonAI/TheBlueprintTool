## A. Implementation Status

1. **Merged implementation:** Commit `564a82c` adds a server-owned pipeline catalog, server-first resolution, global create and edit routes, requesting-project execution, and ownership-aware global library behavior.
2. **Completed migration:** `Post-Implementation` and `Full Exec` moved into `/home/jbb/.decision-os/codex-pipelines.json`; their project stores retain historical runs and contain no copied pipeline definitions. Migration reported no collisions.
3. **Automated evidence:** Backend and frontend typechecks pass. Focused pipeline store, route, cross-project runner, and responsive library tests pass.
4. **Baseline evidence:** The full backend suite has `156/157` passing and the full frontend suite has `403/414` passing; the same one backend failure and eleven frontend failures reproduce on the unchanged pre-feature main revision.
5. **Remaining gate:** The active server still runs the pre-merge backend: `/` returns `200`, while `/api/codex/server-pipelines` returns `400` with `Project id is required in the URL.` Served browser verification requires an operator-authorized server restart.

---

## B. Subtasks

1. [Resolve server-owned pipelines across managed projects](card:card-fff8809f-bbac-409f-83f5-7eedaeb3bd3a)
2. [Enable server pipeline creation in the global library](card:card-d169d6ee-f916-4ee9-a50e-b39d28c686b1)
3. [Add server-pipeline catalog and UI regression coverage](card:card-adbcc9f0-9838-45ad-8e48-c127b8d3c26f)
4. [Verify the served server-wide pipeline workflow](card:card-3c921548-1f55-4331-be45-823b4e5aa3ab)
