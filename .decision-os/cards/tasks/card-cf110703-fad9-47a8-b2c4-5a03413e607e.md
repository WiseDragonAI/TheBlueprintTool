## A. Implementation

1. **Explicit mobile height:** Set `.thread-draft` to `height: 64px` and `min-height: 64px` at `max-width: 760px` so the `rows="5"` intrinsic size cannot keep it tall.
2. **Idle status removal:** Hide `.voice-terminal-status` on mobile, removing the `Thread input / idle` row.
3. **Button layout:** Preserve the existing `FILE`, `REC`, and `SEND` alignment requested by the operator.

---

## B. Verification

1. **Focused test:** `node --test --import tsx test/runtime/mobile-thread-composer.integration.test.ts` passes from `frontend/`.
2. **Type safety:** `frontend/node_modules/.bin/tsc -p frontend/tsconfig.json --noEmit` passes.
3. **Commit:** Implementation is merged in commit `1f1f4d1` through feature commit `19f591f`.
