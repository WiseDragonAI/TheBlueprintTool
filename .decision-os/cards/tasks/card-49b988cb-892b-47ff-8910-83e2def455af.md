## A. Scope

1. **Type:** `code`, `test`, `ops`.
2. **Objective:** Correct the publication transition that leaves locally authored skills in a persistent failed synchronization scope after save, without coupling the local authoring response to peer availability.

---

## B. Code Boundary

1. **Target files:** `backend/src/business/federation/runtime/federated-library-runtime.ts`, `backend/src/business/codex/http/skill-library-routes.ts`, and `backend/test/codex/codex-skill-library-routes.test.ts`.
2. **Target symbols:** `publishAuthoredSkill`, `synchronize`, publication retry scheduling, and publication-scope resolution.
3. **Action:** Preserve immediate local saves, give publication a bounded retry and cancellation lifecycle, distinguish unavailable peers from failed convergence, and resolve each skill publication scope only after its authored manifest is observed synchronized.

---

## C. Completion

1. **Done when:** Regressions cover disconnected save, later relay recovery, repeated save coalescing, shutdown cancellation, and durable incident resolution after synchronized publication; the affected authored skills are then revalidated through diagnostics.
2. **Depends on:** Correct canary listener ownership and startup settlement.
