## A. Implementation

1. Added relationship-backed runtime aggregation, deterministic selection, owner fields, federation transport, and cache-version invalidation.

---

## B. Verification

1. Typecheck passed; focused checks passed `21/21`; backend suite passed `243/243`.
2. Feature `a9b3af6d`; merge `4b44b693`.

---

## C. Runtime Note

1. The server was not restarted because no restart was requested; it will load projector `v12` on its next authorized restart.
