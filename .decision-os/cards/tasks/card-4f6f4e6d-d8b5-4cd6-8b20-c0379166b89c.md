## A. Scope

Trace the pinned CodeMirror editor and Markdown-language internals that govern document bytes, parsing, transactions, history, selection, decorations, view updates, virtualization, wrapping, search, focus, read-only behavior, and disposal. Correlate upstream library contracts with Decision OS adapters and persistent-session invariants.

## B. Deliverable

Produce a source-backed internal model and identify the precise extension points capable of carrying Git change metadata inside one editable Markdown surface. Do not implement the feature.