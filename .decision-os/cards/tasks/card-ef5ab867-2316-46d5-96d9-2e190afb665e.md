## A. Verified result

1. Added server-wide `masterTaskCompletionPipelineId` persistence beside `voicePipelineId`.
2. Settings GET and PATCH expose the value and the server pipeline catalog.
3. Non-empty stale IDs return `400`; rejection preserves the previously saved setting.

---

## B. Evidence

1. Helper persistence and HTTP integration regressions pass.
2. Valid values round-trip through `.decision-os/.settings.json`; empty values remain `Not configured`.
