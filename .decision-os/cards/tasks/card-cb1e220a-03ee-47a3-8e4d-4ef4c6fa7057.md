## A. Scope

1. Type: `docs`.
2. Convert the verified inventory into a concise description of how Decision OS buttons look, feel, communicate hierarchy, and respond to interaction.

---

## B. Codebase Targets

1. Use the evidence recorded by `Inventory Button Style Evidence` and verify every statement against `frontend/assets/shared/terminal-button.css`, `frontend/assets/application.css`, `frontend/assets/canvas/objects.css`, and `frontend/index.html`.
2. Preserve the distinction between the shared terminal baseline and named semantic, responsive, creation, and icon variants.

---

## C. Completion

1. Depends on: `Inventory Button Style Evidence`.
2. Done when this card contains one source-backed style description covering surface, shape, typography, spacing, color, depth, iconography, hierarchy, hover, pressed, focus, selected, disabled, busy, and reduced-motion behavior.
