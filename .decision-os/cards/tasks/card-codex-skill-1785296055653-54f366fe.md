## A. Scope

1. **Goal need:** inspect the dynamic pipeline re-entry that follows a gate’s queued skill and determine the information delivered to the returning gate.
2. **Affected product area:** pipeline skill execution, dynamic successor creation, dependency scheduling, and pipeline-prompt runtime rendering.
3. **Affected workflow and actor:** a running `pipeline-prompt` gate queues one selected skill; the pipeline runner executes that skill; the same gate skill runs again in a **fresh execution context** after its predecessor succeeds.
4. **Context fact:** the active stage is `product-analysis`, whose direct input is the preceding gate result. The source card content and thread were not inspected because this analysis is constrained to code evidence.

---

## B. Contract

1. **Current behavior:** `queueCodexSkillAfterExecutionController` permits one successor only from a running pipeline skill, rejects a different second request, and creates an immutable two-step run: the selected skill followed by the original calling gate.
2. **Current behavior:** the selected skill receives the calling gate’s output as its direct previous skill result. The returning gate receives the selected skill’s output through `PREVIOUS_SKILL_RESULT`.
3. **Current behavior:** the scheduler admits the returning gate only when the selected skill’s execution reaches `succeeded`; failed and cancelled pipeline skills stop later pending work.
4. **Current behavior:** the returning gate retains the admitted prompt snapshot, revision, commit, model, and effort of the original gate, while its `EXECUTION_CONTEXT` is rebuilt with a new execution ID and the returning step’s input and output card IDs.
5. **Expected behavior supported by code:** the returning gate has the complete current task-thread content, the canonical master-task content, and the selected skill output rendered into its pipeline-prompt runtime variables at execution time.

---

## C. Linked Specs

1. **No persisted spec IDs were inspected.** The code-level contract is implemented in `backend/src/business/codex/controller/queue-codex-skill-after-execution-controller.ts`, `backend/src/business/codex/helper/codex-process-scheduler.ts`, and `backend/src/business/codex/helper/build-pipeline-skill-prompt.ts`.
2. **Support:** `backend/test/codex/start-codex-pipeline-run-controller.test.ts` verifies that the worker receives the first gate result, the returning gate receives `WORKER_RESULT` in `PREVIOUS_SKILL_RESULT`, the task thread is refreshed, and the original gate prompt snapshot remains in use.
3. **Constraint:** `PREVIOUS_SKILL_RESULT` is selected by card identity. When a pipeline prompt starts from its canonical output parent, it receives the fixed no-predecessor message; otherwise it receives the complete input-card Markdown.

---

## D. Acceptance Criteria

1. **Dynamic re-entry:** after a running gate queues one skill, exactly one dynamic work-then-returning-gate run is admitted for that caller execution.
2. **Dependency:** the returning gate remains queued until the selected skill succeeds.
3. **Result propagation:** the returning gate prompt contains the selected skill output in `PREVIOUS_SKILL_RESULT`.
4. **Fresh context:** the returning gate prompt contains current master-task and thread Markdown plus a distinct execution ID in `EXECUTION_CONTEXT`.
5. **Prompt fidelity:** the returning gate uses the original admitted gate prompt snapshot, revision, commit, model, and effort.

---

## E. Missing Specs And Gaps

1. **Unknown product acceptance signal:** source code and tests establish prompt content and scheduling, but contain no operator-facing success metric for the quality of the returning gate’s decision.
2. **Unknown UX specification:** no code-level specification defines how the interface should expose that the returning gate is waiting on the selected skill beyond execution-state transitions.
3. **Unverified live fact:** this analysis did not execute the active pipeline; the active gate’s rendered prompt content is not proven from runtime evidence in this stage.
---

Codex run completed: exit code 0
