Completed at: 2026-07-20T18:27:25.497Z

## A. Objective

1. Render the center of the desktop **master-task view as a flat surface without a gradient or shadow cast from the thread panel**.
2. Preserve the master-task structure, thread interior, regular card views, navigation, controls, content, and interactions.

---

## B. Verified Current State

1. The latest operator screenshots show a dark gradient extending left from the thread boundary across the master-task center.
2. The master task body already computes `background-image: none` and `box-shadow: none`; it is not the remaining source.
3. Before correction, the desktop `.mobile-thread-inspector` computed `box-shadow: -18px 0 42px rgba(0,0,0,.34)`, which projects outside the fixed panel and onto the task area.

---

## C. Root Cause and Constraint

1. **The second success claim stopped at the task body and missed an overlapping sibling paint layer.** Opening the thread exposes `.mobile-thread-inspector`; its negative horizontal shadow is the first transition that paints onto the center.
2. The thread panel's own shadow is inset and remains contained inside the thread surface.
3. The correction applies only when `#card-view` identifies a master task; regular card split views retain their existing inspector elevation.

---

## D. Decision and Blocker

1. **Expose master-task identity as `data-master-task` and suppress the inspector's outward `box-shadow` only for the open desktop master-task view.**
2. No implementation blocker remains. The master task remains open for direct operator closeout.

---

## E. Applied Path

1. `renderCard` now projects the parsed master-task identity onto `#card-view`.
2. The desktop split-view rule sets `.mobile-thread-inspector` to `box-shadow: none` only when the visible card is a master task and the thread is open.
3. The task body remains a solid `var(--panel)` fill. This removes both mechanisms visible in the operator evidence while preserving adjacent and regular-card surfaces.

---

## F. Outcome

1. **The master-task center and the gap before the thread panel now remain flat and uniformly dark.**
2. The thread boundary remains visible through its solid border, with no shadow projected onto the task.
3. A served master-task split view computes `box-shadow: none` for the inspector, while a served regular-card split view retains its prior shadow.

---

## G. Open Operator Questions

1. None. The remaining artifact is traced to the exact overlapping paint layer and corrected against the latest screenshots.

---

## H. Subtasks

1. [Remove thread shadow spill from the master task view](card:card-4d540bf7-6947-4c51-84e8-b5af3266d38d)
