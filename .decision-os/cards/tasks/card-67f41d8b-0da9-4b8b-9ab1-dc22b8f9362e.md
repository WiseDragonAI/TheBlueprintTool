## A. Corrected flow trace

1. Execute calls `startSkill()` in `frontend/src/app/responsive/codex.js`.
2. Acceptance calls `finishProcessLaunch()`, which closes the Process modal and emits `decision-os:codex-run-enqueued`.
3. The application listener previously called `loadRoute()` without changing the card route.
4. The listener now calls `navigateVoiceSubmission()`, which delegates to `navigate(controlRoomPath('exec'), true)`.
