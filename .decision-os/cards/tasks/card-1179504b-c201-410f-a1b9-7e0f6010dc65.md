## A. Scope

1. Reuse the existing visible-card detail mounting boundary to request `canvas-preview-v1` only for visible canvas cards.
2. Request the original only from detail and fullscreen surfaces.
3. Preserve project scoping, lazy decode, carousel state, aspect ratios, persisted `imageSizes`, and rejection reconciliation.

---

## B. Verification

1. Prove viewport entry and exit demand, preview fallback state, original promotion, cache reuse, and zero original fetches during canvas-only navigation.
