## A. Task Inventory

| id | type | title | target_files | target_symbols | action | done_when | depends_on |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `T01` | `ops` | Create the protected implementation worktree | `AGENTS.md`<br>`.worktrees/` | Commit Hygiene<br>Staged Code Protection | Create one feature branch in an isolated worktree under `.worktrees/`. Confirm that the primary checkout's staged set remains empty and preserve every unrelated dirty path before source edits and test launches. | The feature worktree points at the current `main` commit, the implementation branch is named, and the primary checkout has no changed staged hunk. | None |
| `T02` | `code` | Keep pipeline metadata in the live editor draft | `frontend/src/app/responsive/codex.js` | `state.editor`<br>`renderEditor()`<br>`initializeMobileCodex()`<br>`pipeline-name` input<br>`pipeline-purpose` textarea | Bind each existing pipeline metadata control's `input` event to its matching `state.editor` field. Retain raw input in the live draft so every existing `renderEditor()` call rehydrates the current value. Keep trimming in `saveEditor()`. Preserve the current form structure, styling, collection handlers, picker flow, request shape, backend boundary, and persistence model. | Code review shows both metadata controls update `state.editor` before any step action, skill action, and picker return can call `renderEditor()`. `saveEditor()` still trims both fields exactly once before building the request. | `T01` |
| `T03` | `test` | Lock the responsive metadata draft contract | `frontend/test-responsive/mobile-codex.test.mjs` | `mobile pipeline editor supports ordered steps, ordered skills, inheritance, and persistence` | Extend the responsive source contract with assertions for the Name-to-`state.editor.name` and Purpose-to-`state.editor.purpose` input transitions. Retain assertions that collection actions render from the draft and save uses the existing `POST`/`PUT` boundary. | The focused test fails when either metadata listener is absent and passes when both controls own the live draft transition. | `T02` |
| `T04` | `test` | Prove metadata retention through collection edits and persistence | `tests/browser/codex/reusable-step-pipelines.spec.ts` | `createFixture()`<br>`startDecisionOsServer()`<br>`launchBrowser()`<br>new responsive `/pipelines` regression cases | Reuse the isolated pipeline fixture and Linux Chromium launcher. On `/pipelines`, cover a new pipeline through step addition, removal, and movement; cover an existing pipeline through skill-picker return, skill addition, removal, and movement. Assert Name and Purpose after every re-render. Capture project `POST`/`PUT` and server `POST`/`PUT` requests, asserting retained trimmed metadata and edited collection order. After successful saves, reload `/pipelines`, reopen each saved pipeline, and assert the durable metadata plus step order. | Browser cases prove all seven collection transitions, both save scopes, exact request metadata, exact step ordering, and fresh-load persistence without page errors. | `T02` |
| `T05` | `ops` | Run the focused responsive regressions | `bin/decision-os-verify.mjs`<br>`frontend/test-responsive/mobile-codex.test.mjs`<br>`tests/browser/codex/reusable-step-pipelines.spec.ts` | Repository verification lease<br>Node test runner<br>Playwright Chromium fixture | Run the responsive contract test and the named browser regression through `node bin/decision-os-verify.mjs -- <direct-command>`. Use root `@playwright/test`, `/snap/bin/chromium`, and the Linux flags already pinned by the browser fixture. | Both lease-wrapped commands exit `0`; the captured browser result names the responsive metadata regression and reports no page error. | `T03`, `T04` |
| `T06` | `ops` | Typecheck the changed frontend boundary | `bin/decision-os-verify.mjs`<br>`frontend/tsconfig.json` | `typecheck:frontend` | Run `node bin/decision-os-verify.mjs -- npm run typecheck:frontend`. | The lease-wrapped frontend typecheck exits `0`. | `T02` |
| `T07` | `ops` | Run the repository verification suite | `bin/decision-os-verify.mjs`<br>`package.json`<br>`frontend/package.json` | `test:front-back` | After focused checks and frontend typecheck pass, run `node bin/decision-os-verify.mjs -- npm run test:front-back` once. | The lease-wrapped repository suite exits `0` with captured completion output. | `T05`, `T06` |
| `T08` | `ops` | Integrate the verified source change into main | `frontend/src/app/responsive/codex.js`<br>`frontend/test-responsive/mobile-codex.test.mjs`<br>`tests/browser/codex/reusable-step-pipelines.spec.ts` | Feature commit<br>merge commit<br>`main` | Commit only the production and regression files on the feature branch with a concise subject plus non-empty `WHAT:` and `WHY:` paragraphs. Verify the full message, merge the feature branch into the primary checkout with a merge commit carrying the same required paragraphs, then verify the merged tree contains the intended diff while unrelated dirty files remain unchanged. | `main` contains the focused source and test diff in verified commits; both complete commit messages satisfy the repository contract; no unrelated path is staged. | `T07` |
| `T09` | `scenario` | Confirm draft retention on the served responsive route | `BROWSER_RUNBOOK.md`<br>`frontend/src/app/responsive/codex.js` | `/pipelines`<br>`renderEditor()`<br>step controls<br>skill-picker controls | Without restarting port `50150`, follow the Linux Chromium procedure on `http://127.0.0.1:50150/pipelines`. Confirm HTTP `200`, enter unsaved Name and Purpose values, exercise the step transitions and skill-picker transitions, and observe both live controls after each action. Close the editor without saving test data. | The served route uses the merged frontend; both controls remain unchanged through every exercised re-render; no save request is sent; browser errors remain empty. | `T08` |
| `T10` | `docs` | Record implementation and proof on the source task | `.decision-os/cards/tasks/card-27ec1923-e426-423e-af78-459051fc4ee2.md`<br>`.decision-os/threads/tasks/thread-card-27ec1923-e426-423e-af78-459051fc4ee2.md` | `Global Progress`<br>implementation evidence thread reply | Through the scoped ledger command boundary, record the exact changed symbols, focused checks, typecheck, full-suite result, `/pipelines` observation, feature commit, and merge commit. Keep the master task active unless a later gate explicitly authorizes closure. | The source task contains a concise evidence-backed implementation record and one truthful English agent reply; no runtime JSON is edited manually. | `T08`, `T09` |
| `T11` | `ops` | Commit documentation, push main, and remove the worktree | `.decision-os/cards/tasks/card-27ec1923-e426-423e-af78-459051fc4ee2.md`<br>`.decision-os/threads/tasks/thread-card-27ec1923-e426-423e-af78-459051fc4ee2.md`<br>`.worktrees/` | Documentation commit<br>`origin/main`<br>feature worktree<br>feature branch | Commit only the intended versioned task documentation with a concise subject plus `WHAT:` and `WHY:` paragraphs, verify the full message, push `main` to `origin` with `GIT_SSH_COMMAND='ssh -i ~/.ssh/id_jb_wise -o IdentitiesOnly=yes'`, then remove the merged worktree and delete its local feature branch. | The verified documentation commit and merge are present on `origin/main`; the temporary worktree and merged local branch are absent; unrelated workspace changes remain intact. | `T10` |

---

## B. Open Questions

1. **Blocking questions:** None.

---

## C. Readiness

1. **Status:** `READY_FOR_TASK_DEPENDENCY`
---

Codex run completed: exit code 0
