## A. Implementation Result

1. **Implemented:** persisted execution state controls `exec` membership even before runtime queue metadata is available.
2. **Cache correction:** `codex-process-queue.json` and `codex-pipelines.json`, including their later creation, participate in projection invalidation.
3. **Federation:** new `exec` projections merge normally; legacy remote `active` projections are upgraded at the federation boundary.
