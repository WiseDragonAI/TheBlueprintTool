## A. Delivery Decision

1. **Implementation target** `is` `dev`; the iteration `must use` `feature/project-failure-history-20260731` inside `.worktrees/project-failure-history-20260731` from verified baseline `712de13ccc6688ce93e9ae10bb6be8d5819fd6d7`.
2. **Runtime project disclosures** `must render` collapsed on first load, while each summary `shows` its exact or explicitly partial rolling `24-hour` occurrence total.
3. **Expanded diagnostics** `must group` by verified project owner plus incident `code` and `must preserve` every dated event's distinct message and source evidence.

---

## B. Durable History Decision

1. **Versioned occurrence history** `must append` one actual timestamp per failure and `must upgrade` readable version-1 state only on a valid subsequent write.
2. **Bounded retention** `must mark` observation-cap and whole-incident loss with a time-bounded truncation watermark, so incomplete totals `become exact` only after all discarded evidence leaves the window.
3. **Failure totals** `include` retained paused and resolved observations inside the preceding `24 hours`; expired observations `are excluded`.
4. **Invalid durable bytes** `must remain` preserved while the existing diagnostics endpoint `continues serving` the single incident source.

---

## C. Projection and Interface Decision

1. **Evaluation time** `comes from` the diagnostics `observedAt` response in normal, backend-emergency, and launcher-emergency modes.
2. **Error identity** `is` verified owner plus `code`; different messages `remain` evidence inside one group.
3. **System history** `must remain` visible for recent unowned resolved evidence without presenting a current interruption.
4. **Availability state** `remains` governed only by active incidents while rolling totals `include` retained resolved evidence.
5. **Served-route proof** `must confirm` initial closure, pointer and keyboard expansion, exact and partial totals, hierarchy, distinct messages, dates, and responsive containment.

---

## D. Execution Decision

1. **Dispatch readiness** `is` established because the audited inventory contains no blocking question and defines every dependency, collision boundary, and acceptance gate.
2. **Five sequential groups** `remain`: isolated durable backend contract, frontend projection, disclosure rendering, served-route proof, then verification and integration.
3. **Implementation orchestration** `must initialize` the complete group registry before dispatch and `must advance` only after each upstream worker result is persisted.
4. **Stage decision** `queues` `implementation-orchestrator` at `high` effort because implementation and verification evidence are the next missing delivery facts.

---

## E. Closure Boundary

1. **Master lifecycle gate** `is not ready` because every linked implementation and verification card `remains` `todo`; closure `is not authorized`.
2. **Integration** `remains blocked` until focused checks, affected typechecks, the full repository suite, served Linux Chromium verification, complete diff review, compliant commits, merge, and push all `pass`.
3. **Dirty registered worktrees** `must remain` unchanged; only the named iteration worktree and branch `may be removed` after verified integration.
