## A. Requirement

1. **Launch transition:** A successful Codex launch must replace `#task-waiting` with `#task-active` immediately.
2. **Timestamp source:** Write the backend run's exact `startedAt` value to `Active since`.
3. **Active display:** Move the master task to the `Active` tab and derive its elapsed timer from `Active since`.

---

## B. Completion Evidence

1. **Status:** `complete`.
2. **Commit:** `f1ef665` (`fix(mobile): wire active master task runs`).
3. **Verification:** The source thread records `11` passing mobile tests plus frontend and backend TypeScript and syntax checks.
