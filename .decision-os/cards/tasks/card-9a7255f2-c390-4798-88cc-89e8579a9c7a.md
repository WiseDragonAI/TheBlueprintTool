## A. Task

1. **Type:** `config` and `docs`.
2. Create server skills for source publishing, initiator reconciliation, and source finalization under `.skills/project-sync-source-publisher/SKILL.md`, `.skills/project-sync-initiator-reconciler/SKILL.md`, and `.skills/project-sync-source-finalizer/SKILL.md`.
3. Encode the existing non-destructive Git rules, required SHA evidence, worktree checks, output JSON contract, and role authority from `buildProjectSyncPrompt` in those skills.
4. Add a canonical server-owned `Project synchronization` pipeline definition with the three skills in phase order through `backend/src/business/codex/helper/server-pipeline-catalog.ts` and a dedicated project-sync pipeline definition helper.

---

## B. Completion

1. Skill discovery returns all three named skills as server skills.
2. The server pipeline catalog returns one stable synchronization pipeline whose ordered steps reference those skills.
3. Repeated startup preserves operator-edited pipeline data and does not duplicate the built-in definition.

---

## C. Dependencies

1. **Depends on:** none.
