## A. Execution report

1. **Root cause:** render-time scroll tracking used a two-frame suppression window. An upward user scroll inside that window did not clear follow-bottom, so the delayed-layout `ResizeObserver` retained authority and wrote the viewport back to `scrollHeight`.
2. **Correction:** only the viewport `scroll` event now mutates reader-owned position and follow state. Render, restore, pin, and delayed-layout visibility synchronization no longer share that mutation path.
3. **Race correction:** the queued second entry pin now checks thread identity and follow-bottom state before writing, so a newer upward user scroll takes precedence.
4. **Merged commits:** `e6ae4c70` and merge commit `b3107975`.

---

## B. Verification

1. Focused thread runtime suite: **25 passed, 0 failed**.
2. Full frontend suite: **504 passed, 0 failed**.
3. Operator-facing route `http://127.0.0.1:50151/p/ZGV2L0VkaXRvckJQL2RlY2lzaW9uLW9z/ledgers/specs`: **HTTP 200**. Initial thread entry had `bottomDistance: 0`; a Chromium wheel gesture changed `scrollTop` from `1566` to `1066`; delayed content growth left `scrollTop` at `1066`.
4. Frontend typecheck reaches the pre-existing untouched error `frontend/src/runtime/ledger/component/render-ledger-card-questions.ts:263` (`startVoiceRecording` expects zero arguments but receives one).

---
