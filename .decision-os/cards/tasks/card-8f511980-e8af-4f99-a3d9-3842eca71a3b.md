## A. Scope

1. **Desktop preservation:** Keep the desktop `A` shortcut and `focusThreadDraft()` behavior unchanged.
2. **Mobile correction:** Remove the explicit `.thread-draft` focus from `openMobileThread()`.
3. **Coverage:** Add a regression assertion scoped to the mobile thread-opening function.

---

## B. Acceptance Criteria

1. **Desktop behavior:** `focusThreadDraft()` remains available to the desktop keyboard router.
2. **Mobile behavior:** `openMobileThread()` renders the inspector without calling `.focus()`.
3. **Verification:** All mobile thread tests and frontend TypeScript checking pass.
