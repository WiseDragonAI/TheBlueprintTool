## A. Contradicted Result and Root Cause

1. **Operator evidence:** after launching a new turn, the Control Room still displayed approximately `51m`, contradicting the prior session-boundary fix.
2. **Exact run evidence:** run `codex-skill-1784262780355-9907b20e` persisted its latest Decision OS continuation at `2026-07-17T05:24:39.283Z`; JSONL lines `344` and `345` then recorded `thread.started` and `turn.started`.
3. **Codex session evidence:** the durable Codex rollout recorded the corresponding latest `task_started` at `2026-07-17T05:24:43.638Z`.
4. **First incorrect transition:** the Control Room projection intentionally ignored continuation boundaries and anchored the timer to the original session start at `2026-07-17T04:33:00.367Z`. The required clock is the latest Codex turn, not the whole resumed session.

---

## B. Corrected Implementation

1. The event ingestor now records a durable `decision-os:codex-turn-start` marker when `turn.started` is observed.
2. Direct Control Room projections use the latest persisted turn marker; legacy runs reconstruct the latest completed boundary from the saved segment and JSONL records.
3. A launched process without `turn.started` displays `Running` without a stopwatch, preventing the previous turn clock from leaking into a pending turn.
4. The server invalidates and republishes the Control Room projection immediately on `codex-turn-started`.
5. Internal turn markers are excluded from operator-facing diagnostics.
6. Implementation commit `964c0668` is merged into `main` by merge commit `787c062f`.

---

## C. Verification Evidence

1. **Timer regressions:** `4/4` passed for persisted latest-turn selection, live fallback, and the pre-`turn.started` pending state.
2. **Turn-transition integration:** passed and verified the `codex-turn-started` publication plus unchanged diagnostics.
3. **Backend typecheck:** passed.
4. **Full backend suite run:** initially reported `160/162`; the change-related diagnostics regression was corrected and its isolated integration now passes. The sole remaining failure is the pre-existing project-catalog assertion expecting `canvas-runtime.ts` while current HTML serves `surface-runtime.ts`.

---

## D. Required Served Validation

1. Restart the Decision OS server so it loads merge commit `787c062f`.
2. Launch a new turn and confirm the row initially shows `Running`, then starts its stopwatch from the new turn's persisted `turn.started` time.
3. Restart the server once more and confirm the same turn timer survives without reverting to the original session start.
4. The master task and subtask remain open until this served interaction is recorded.

---
