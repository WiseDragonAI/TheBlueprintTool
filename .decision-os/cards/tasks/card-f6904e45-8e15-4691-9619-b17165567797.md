## A. Verified causes

1. **Subtask data:** the gate resolves all four canonical links, but `/navigation` omitted `relationships` and card `labels`, so the card rendered `0 of 0`.

2. **Section titles:** the structured writer added a letter to titles that already had one, producing `A. H.`.

3. **Stopwatch data:** the row and timer were intact, but the Control Room projection ignored a direct run's `startedAt` when canonical Markdown had no legacy `Active since`, so it displayed `Running`.

---

## B. Implemented corrections

1. **Commit `e105f9d`:** preserve relationships and task labels in the navigation projection.

2. **Commit `e9fc540`:** put the linked-subtask summary before the narrative and normalize section titles before lettering.

3. **Commit `e24ac10`:** use the live direct-run start when no persisted logical start exists and retain the persisted start when it does.

4. **Merged:** `2211745`, `a13e314`, and `f800fe3`; all temporary branches and worktrees were removed.

---

## C. Verification and remaining gate

1. **Passed bounded checks:** change-specific ledger CLI, responsive, navigation, and stopwatch regressions plus ledger CLI, frontend, and backend typechecks passed through the lease with one-way test concurrency.

2. **Served state:** HTTP returned `200`; Chromium confirmed the summary is first. The running backend still has pre-fix modules loaded, so it still shows `0 of 0` and `Running`.

3. **Next action:** restart the existing server, then confirm four subtask rows appear above the narrative and the Active row has an incrementing stopwatch. No restart was performed because none was requested.

4. **Status:** this master stays open; the separate Codex lifecycle merge gate is unchanged.

---
