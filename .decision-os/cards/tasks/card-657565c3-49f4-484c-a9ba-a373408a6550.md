## A. Implemented behavior

1. New card-run developer instructions now define the master task as a **living strategic summary for a CTO**.
2. The contract requires the **context, objective, verified state, constraints, choices, blocker, path, outcome, and open questions** on every run.
3. It enforces **inline bold for strategic meaning**, selective `backticks` for supporting context, and exclusion of routine implementation inventory.
4. It makes the thread reply a **few very short numbered bullets with no heading or section**.
5. Replies are restricted to material iteration outcomes and exclude `analysis`, `rationale`, `process narration`, full-task summaries, and implicit workflow actions.

---

## B. Regression coverage

1. Prompt-builder assertions protect the **CTO purpose**, complete strategic fields, visual emphasis rules, and output separation.
2. Launch-route assertions verify that the complete contract reaches the actual `developer_instructions` argument.
3. The compact prompt-size guard was updated to cover the explicit contract without admitting unbounded context.

---

## C. Verification

1. The focused prompt-builder test passes: `2/2`.
2. The focused launch-route test passes: `6/6`.
3. Backend typechecking passes.
4. The full backend run identified one relevant size-guard assertion, which was corrected and verified.
5. An unrelated `federation connector` test still times out when rerun alone; the changed prompt path is not involved.

---

## D. Result

1. The contract is merged on `main` in `02b25a7e`.
2. The requested prompt behavior is **implemented and protected by focused regression coverage**.
