## A. Current Result

1. The voice correction is merged and published.
2. Its focused runtime checks passed.
3. The later repository repair is separately merged.
4. Only real microphone proof remains for this master.

---

## B. First Scope Deviation

1. The first deviation occurred after the voice patch was delivered.
2. The full suite then failed in unchanged backend scopes.
3. No evidence connected those failures to the voice patch.
4. The gate still made repository-green a voice completion condition.
5. It then selected `run-test-and-fix` under the voice master.
6. That selection expanded the authorized scope.

---

## C. Why Test Runs Repeated

1. `run-test-and-fix` is designed to loop internally until the full suite is green.
2. The first execution performed repairs and reached repository green.
3. Its thirty-minute execution deadline arrived before the final report was written.
4. The output card therefore contained only the exit line.
5. The detailed JSONL execution stream remained available.
6. The gate read the empty card and launched a new execution.
7. The second execution also reached the thirty-minute deadline.
8. The gate inspected retained evidence only after both empty handoffs.

---

## D. Why Failures Appeared Sequentially

1. The initial backend baseline was broadly red: `643/661`, with `11` failures and `7` cancellations.
2. Early summaries exposed only the first visible failures.
3. Cancellations prevented later tests from completing.
4. Backend failure prevented browser admission.
5. Repairs allowed more backend coverage to finish.
6. Backend green then admitted browser coverage.
7. Most later failures were previously hidden, not newly created.

---

## E. Required Controller Correction

1. Keep `run-test-and-fix` strict once admitted.
2. Before selection, require evidence that failures come from the authorized iteration.
3. Record unrelated failures under a separate repository task.
4. Stop the current feature at its proven boundary.
5. Treat a missing final report as a failed pipeline handoff.
6. Inspect retained JSONL before scheduling another execution.
7. Keep one post-review verification when review changes code.

---

## F. Remaining Voice Decision

1. Do not commit the stale voice repair worktree.
2. Validate one real voice note.
3. Confirm the same row updates without reload.
4. Reload and confirm transcript durability.
5. Keep closure pending explicit authorization.
