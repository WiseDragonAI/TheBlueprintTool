## A. Scope

1. **Type:** `test`.
2. **Targets:** `backend/test/server/control-room-projection.integration.test.ts`, `backend/test/unit/server/helper/federated-control-room-projection.test.ts`, and `backend/test/server/federation-node-connector.integration.test.ts`.

---

## B. Coverage

1. Add waiting tasks with distinct timestamps across two local projects.
2. Add local and remote waiting tasks whose timestamps require node interleaving.
3. Cover explicit ranks and equal-timestamp deterministic ties.
4. Run every focused check through `node bin/decision-os-verify.mjs -- <command>`.

---

## C. Completion

1. Tests fail if timestamp order becomes oldest-first.
2. Tests fail if federation output is local-first or node-grouped.
3. Tests fail if explicit rank precedence or deterministic ties regress.
4. Focused backend checks pass.

---

## D. Verification Results

1. Added comparator, multi-project projection, and federation merge regressions.
2. Focused unit checks pass `8/8`.
3. Focused HTTP projection and two-node federation integration checks pass `5/5`.
4. Backend typechecking passes.
5. Complete backend suite passes `188/188` with the worktree `TSX_TSCONFIG_PATH` pinned.
