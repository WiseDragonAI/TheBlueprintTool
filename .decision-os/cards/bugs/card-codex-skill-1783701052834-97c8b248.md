## A. Outcome

1. **Completed:** Codex run model and effort preferences now persist immediately as one validated pair on the active card through `codexRunModel` and `codexRunEffort`.
2. **Synchronized:** The card run widget and active thread controls reconcile from the returned ledger after every successful selector mutation.
3. **Safe failure behavior:** A rejected mutation restores both mounted surfaces to the last durable card pair and records the existing ledger mutation failure telemetry.
4. **Committed implementation:** `e2d1160` (`fix(codex): persist card run preferences`).

---

## B. Implementation

1. **Canonical preference resolver:** Added `frontend/src/runtime/codex/helper/card-codex-run-preference.ts` to validate card fields and return the complete default pair `gpt-5.6-sol` / `high` when either durable field is absent or invalid.
2. **Single persistence effect:** Added `frontend/src/runtime/codex/effect/persist-card-codex-run-preference.ts` to submit the complete pair through `patch-card`, reconcile the server ledger, and synchronize every mounted widget and thread control for the card.
3. **Widget writes:** Bound both selectors in `render-card-skill-run-widget.ts` to immediate pair persistence and tagged each widget with its owning card ID.
4. **Thread writes:** Replaced `threadCodexPreferencesByThreadId` with card-derived preferences in `render-thread-panel.ts`. Thread command datasets remain on durable values until mutation success.
5. **Continuation inheritance:** Updated `poll-card-skill-run.ts` so `Continue` and `New session` read the reconciled card preference rather than transient selector DOM state. Historical polling metadata no longer overwrites a valid card-derived selection.
6. **Ledger contract:** Extended the frontend `patch-card` type and backend mutation handler with `codexRunModel` and `codexRunEffort`. The backend rejects partial pairs and values outside the shared `codexModelOptions` and `codexEffortOptions` catalogs with HTTP `400`.
7. **Backend default:** Updated `resolve-codex-command.ts` to use `gpt-5.6-sol` / `high` when no valid explicit, environment, or workspace setting is present.
8. **Removed duplicate authority:** Deleted `ThreadCodexPreference` and `threadCodexPreferencesByThreadId` from frontend runtime state.

---

## C. Acceptance Evidence

1. **Thread persistence and remount:** The thread selector test persists the complete default pair, reconciles the returned ledger, rerenders the same thread, and retains the persisted values without launching Codex.
2. **Bidirectional synchronization:** The same test verifies a thread selector change updates a mounted widget and the widget persistence path updates the open thread controls.
3. **Refresh durability:** The server integration test verifies the complete pair is written to the ledger file and remains unchanged after rejected partial and invalid mutations.
4. **Failure rollback:** The frontend failure test returns HTTP `500`, verifies both surfaces restore `gpt-5.4` / `medium`, confirms the command dataset remains durable, and confirms `commit-ledger-edit-failed` plus reconciliation failure telemetry.
5. **Default and command inheritance:** Backend command tests verify `gpt-5.6-sol` / `high`; continuation tests verify commands use the card-derived pair. Existing thread launch, `Shift+X`, initial launch persistence, continuation persistence, and voice orchestration tests remain green.

---

## D. Verification

1. **Frontend typecheck:** `npm run typecheck:frontend` passed.
2. **Backend typecheck:** `npm run typecheck:backend` passed.
3. **Frontend suite:** `npm test --prefix frontend` passed `280/280` tests.
4. **Backend suite:** `npm test --prefix backend` passed `94/94` tests.
5. **Patch hygiene:** The committed feature diff passes `git diff --cached --check`; unrelated operator changes were excluded from the commit.

---

## E. Handoff

1. **Status:** Complete with no known implementation blocker.
2. **Next pipeline input:** Use commit `e2d1160` and the acceptance evidence above for subsequent review or release work.
---

Codex run completed: exit code 0
