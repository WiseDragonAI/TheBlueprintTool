/**
 * WHAT: Shared timing constants for low-detail branch mounting and teardown.
 * WHY: Low-detail presentation must use the same transition window consistently across mode edges.
 */
export const LOW_DETAIL_MOUNT_OPACITY_MS = 160;
